# Bevezetés

Sok operátor használható összevont értékadásként is, ami egy rövidített jelölés, ahol az `x = op y` helyett `x op= y` írható:

```csharp
var features = PhoneFeatures.Call;
features |= PhoneFeatures.Text;
features &= ~PhoneFeatures.Call;
```
