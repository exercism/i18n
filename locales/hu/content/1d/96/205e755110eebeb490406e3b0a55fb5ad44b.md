# nincs visszatérési típus

Minden nyilvános tagnál add meg a `return` típust. A visszatérési típus megadásával a fordító ellenőrizni tudja a várt `return` típust. Az is biztosítja, hogy a Scala típuskövetkeztetésének változásai ne törjék meg a bináris kompatibilitást.
