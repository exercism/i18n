# bezárás az after blokkban

Az `after` blokk segítségével akkor is szabályosan tudsz kilépni, ha a `try` blokkban lévő kód sikeres volt, és akkor is, ha nem. A `resource` lezárásához használd az `after` blokkot.
