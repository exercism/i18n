# Bevezetés

## Bools

A Gleam az igaz és hamis értékeket a `Bool` típussal ábrázolja. Mindössze két érték létezik: `True` és `False`. Ezeket az értékeket egy változóhoz is kötheted:

```gleam
let true_variable = True
let false_variable = False
```

A `&&`, `||` és `!` operátorok segítségével Boolean értékekkel végezhetsz műveleteket:

```gleam
True && True  // -> True
True && False // -> False

False || True  // -> True
False || False // -> False

!False // -> True
!True  // -> False
```

A `&&` és `||` operátor rövidzár-kiértékelést használ, ami azt jelenti, hogy az operátor jobb oldalán szereplő kifejezés csak akkor értékelődik ki, ha szükséges.

Az egyes operátorok precedenciája eltérő: a `!` a `&&` és `||` előtt értékelődik ki. A kapcsos zárójelekkel elérheted, hogy a kifejezés egy része a többiek előtt értékelődjön ki:

```gleam
!True && False   // -> False
!{True && False} // -> True
```
