# Inline exportálás

A `function`, `class` és `const` kulcsszóval létrehozott függvények, osztályok és konstansok mind közvetlenül inline is exportálhatók az `export` kulcsszóval.

```javascript
export %{method.signature}
```
