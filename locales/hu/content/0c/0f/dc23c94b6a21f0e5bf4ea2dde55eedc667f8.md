# SQLite-specifikus utasítások

Ehhez a feladathoz a `kindergarten-garden` tábla `result` oszlopát kell beállítanod a helyes értékre a `diagram` és `student` oszlopok alapján.

## Tábla sémája

```sql
CREATE TABLE "kindergarten-garden" ("diagram" TEXT, "student" TEXT, "result" TEXT);
```
