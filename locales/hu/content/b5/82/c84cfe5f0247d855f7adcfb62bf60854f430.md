# Névjegy

A `foreach` vezérlőszerkezet kényelmes módot kínál arra, hogy _tömbökön_ és _objektumokon_ iterálj. A `foreach`-nek két alapvető formája van:

```php
foreach (iterable as $value) {
  // statements
}

foreach (iterable as $key => $value) {
  // statements
}
```
