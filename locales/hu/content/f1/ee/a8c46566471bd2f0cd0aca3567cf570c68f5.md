# Kiegészítés az utasításokhoz

## Kurzusspecifikus megjegyzések

A Scheme-ben az egyik módja az opcionális argumentumok megadásának, hogy listaként adod meg őket. Erre két lehetőség van: `(define (two-fer . args) ...)` vagy `(define two-fer (lambda args ...))`.
