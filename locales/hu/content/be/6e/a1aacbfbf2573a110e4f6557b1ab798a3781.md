# Bevezetés

A Booleanokat az F#-ban a `bool` típus képviseli, amelynek értéke `true` vagy `false` lehet.

Az F# három Boolean-operátort támogat: a `not` (NOT), a `&&` (AND) és a `||` (OR) operátort.
