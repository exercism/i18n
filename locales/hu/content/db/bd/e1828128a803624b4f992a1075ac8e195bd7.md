# A `send_newsletter` implicit visszatérése

A `send_newsletter/3` függvény ne explicit módon adja vissza az `:ok` értéket, hanem implicit módon azt adja vissza, amit a `close_log/1` visszaad (ami akkor épp `:ok`, ha minden rendben megy).
