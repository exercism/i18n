# export egy sorban

A függvényeket, például a `value`-t, az osztályokat, valamint a konstansokat, például a `COLORS`-t `export` kulcsszóval egy sorban is megjelölheted.

```javascript
export %{constant.signature}

export %{method.signature}
```
