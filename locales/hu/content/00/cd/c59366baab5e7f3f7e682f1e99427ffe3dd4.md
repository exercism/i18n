# string nélkül

Ennek a feladatnak a célja, hogy megtanítsa neked a charlisteket. Oldd meg stringek használata nélkül: ne használd a `to_string` vagy a `to_charlist` függvényt, a `String` modult, illetve a bitstring-specifikus `<<>>` formát.
