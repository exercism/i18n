# SQLite-ra vonatkozó utasítások

Ennél a feladatnál a `color` oszlopban található szín alapján kell a `color_code` tábla `result` oszlopát a helyes értékre állítanod.

## Táblaséma

```sql
CREATE TABLE "color_code" ("color" TEXT, "result" INT);
```
