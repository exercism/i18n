# ne használd a filtert

Ahogy a feladatleírás is említi, a `keep` és a `discard` függvényt a `List.filter` és a `List.filterMap` használata nélkül kell megvalósítanod. Ehelyett rekurziót vagy más `List`-függvényeket használhatsz.
