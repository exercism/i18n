# A `strings.Join` használata összefűzésre

Ne használd a `strings.Join`-t, ha az adat nem `[]string` típusú.
Helyette a `fmt.Sprintf`-et használd. Ez egy nagyon hatékony módja annak, hogy több darabból építs fel stringeket.
