# Bevezetés

A függvények a Kotlinban első osztályú állampolgárok.
Ez azt jelenti, hogy változókban tárolhatók, argumentumként átadhatók, sőt más függvények vissza is adhatják őket.
Például egy függvény hozzárendelése az `addOne` változóhoz:

```kotlin
val addOne = fun (num : Int) : Int {
    return num + 1
}
```

Egy függvény visszaadhat egy másik függvényt is:

```kotlin
fun makeAddOne() : (Int) -> Int {
    return fun(num : Int) : Int {
        return num + 1
    }
}

val addOne = makeAddOne()
```

Miután hozzárendeltük, a függvényt a változón keresztül ugyanúgy meghívhatjuk, mintha csak egy másik függvény lenne:
```kotlin
addOne(3)
// => 4
```

## Függvénytípusok deklarálása

Egy változó vagy paraméter típusa a `(parameter types) -> return type` szintaxissal deklarálható.
Például egy változó deklarálása:

```kotlin
val addOne : (Int) -> Int = fun (num : Int) : Int {
    return num + 1
}
```

És egy függvényparaméter esetén:

```kotlin
fun perform(operation : (Int) -> Int) {
    operation(3)
}
```

## Lambda kifejezések

A lambda kifejezés egy rövidebb módja egy függvény definiálásának.
A következő formában írjuk le őket: `{ parameters -> body }`.
A kifejezés a törzs utolsó utasításának értékét adja vissza.
Például:

```kotlin
// Assigning to a variable
val addOne = { num : Int -> num +  1}

// In a function call
perform({ num : Int -> num + 1 })
```

A Kotlin akkor tudja kikövetkeztetni a típusokat, ha elég információ áll rendelkezésre.
Például a fenti példák így is írhatók:

```kotlin
// Assigning to a variable
val addOne : (Int) -> Int = { num -> num + 1 }

// Or ...
val addOne  = { num : Int -> num + 1 }

// Using in a function
perform({ num -> num + 1 })
```

## `it` egyparaméteres lambdákhoz

Ha a kifejezésnek csak egy paramétere van, a paraméter elhagyható.
A Kotlin ilyenkor elérhetővé tesz egy `it` nevű paramétert a törzsben.
Például:

```kotlin
val addOne : (Int) -> Int = { it +  1 }
```

## Záró lambdák átadása

Amikor egy függvény utolsó argumentuma egy lambda, a lambda kifejezést közvetlenül a zárójelek _után_ írjuk.
Például:

```kotlin
// Function that takes a function as last argument
fun calculateFrom(from : Int, operation : (Int) -> Int) : Int {
    return operation(from)
}

// Call the calculateFrom function
calculateFrom(2) { it * 3 }
// => 6
```
