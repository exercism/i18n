# nem optimális szignatúra

Ha megnézed a teszteket, a `gigasecond(...)` függvény csak egy paramétert kap. A `gigasecond` definíciójából távolítsd el a plusz paramétereket, mert ezek mindig `undefined` értéket kapnak, vagy azt az alapértelmezett értéket, ami jelenleg a szignatúrában szerepel.
