# A stream `filter` és `count` metódusának használata javasolt

Amikor streamekkel számítod ki a Hamming-távolságot, használd a stream `filter` és `count` metódusát, hogy átláthatóbb legyen a számítás.
