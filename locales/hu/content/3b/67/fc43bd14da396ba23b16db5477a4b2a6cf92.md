# A run más függvényeket használ

A kódduplikációt úgy kerülheted el, hogy a nemrég definiált `welcome/0`, `ask_name/0`, `ask_class/0` és `ask_level/0` függvényeket használod a `run/0` függvényen belül.
