# Bevezetés

Ahol a típusokat nem lehet implicit módon átalakítani, ott explicit típuskonverziós operátort kell használnod.
A konverziós operátor nem más, mint a cél típus zárójelben, a konvertálni kívánt érték előtt:

```csharp

int i = 7;
byte b = (byte)i; // Explicit cast from int to byte
```
