# Névjegy

A függvénydeklarációk és a függvénykifejezések mellett a JavaScriptnek van egy másik, nagyon tömör szintaxisa is függvények definiálására.
Ezeket a függvényeket _nyílfüggvényeknek_ nevezzük.

Ebben a fogalomban a nyílfüggvények írásához használt szintaxisra összpontosítunk.
A nyílfüggvények működésében vannak eltérések, például a _this_ kötése, amelyeket más fogalmakban tárgyalunk majd.

Az alábbi példa egy függvénydeklarációt és egy nyílfüggvényt hasonlít össze.

```javascript
function addUpTwoNumbers(num1, num2) {
  return num1 + num2;
}

// function keyword removed and => added
const addUpTwoNumbers = (num1, num2) => {
  return num1 + num2;
};
```

A fenti példában láthatod, hogy a nyílfüggvény szintaxisa:

1. eltávolítja a `function` kulcsszót
2. az `addUpTwoNumbers` azonosítót `const`-ként deklarálja
3. hozzáad egy vastag nyilat (`=>`)

Ha a függvény törzse csak egy `return` utasítást tartalmaz, mint a fenti példában, akkor a `{}` és a `return` kulcsszó elhagyható.

<!-- prettier-ignore-start -->
```javascript
const addUpTwoNumbers = (num1, num2) => { return num1 + num2 };

// can be shortened to
const addUpTwoNumbers = (num1, num2) => num1 + num2;
// braces {} and return removed
```
<!-- prettier-ignore-end -->

Abban a különleges esetben, amikor egy nyílfüggvény csak egy objektumot ad vissza, zárójelek kellenek az objektum köré, hogy el lehessen hagyni a `return` utasítást.

```javascript
// explicit return of object
const addUpTwoNumbers = (num1, num2) => {
  return { num1, num2 };
};

// implicit return of object
const addUpTwoNumbers = (num1, num2) => ({ num1, num2 });
```

A paraméterek körüli zárójelek használata a paraméterek számától függ.

<!-- prettier-ignore-start -->
```javascript
// one parameter does not need parenthesis
const square = num => num * num;

// two or more parameters need to be wrapped in parenthesis
const addUpTwoNumbers = (num1, num2) => num1 + num2;
```
<!-- prettier-ignore-end -->

Más fogalmak, például a [Rest paraméterek][concept-rest] és a [Destrukturálás][concept-destructure] szintén használhatók nyílfüggvényekkel.

[concept-rest]: /tracks/javascript/concepts/rest-and-spread
[concept-destructure]: /tracks/javascript/concepts/array-destructuring
