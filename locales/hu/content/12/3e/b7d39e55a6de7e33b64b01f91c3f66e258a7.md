# Használd a `MapSet.intersection` és az `Enum.sort` függvényeket

A `boring_cards`-ban a `MapSet.intersection`-t használd. A függvény definiálására más idiomatikus megoldás is van, de ez illik a legjobban ahhoz, hogy teljesen megtanuld a feladat fogalmát.