# Bevezetés

A C# elsődleges objektumorientált építőeleme az _osztály_, amely adatok (_mezők_) és viselkedés (_metódusok_) kombinációja. Az osztály mezőit és metódusait _tagoknak_ nevezzük.

A tagokhoz való hozzáférést hozzáférés-módosítókkal korlátozhatod; a két leggyakoribb ezek közül:

- `public`: a tagot bármely kód elérheti (nincs megkötés).
- `private`: a tagot csak az ugyanabban az osztályban lévő kód érheti el.

Gondolhatsz úgy egy osztályra, mint egy sablonra, amellyel az adott osztály példányait létrehozod. Egy osztály példányának (más néven _objektumnak_) a létrehozásához a `new` kulcsszót használjuk:

```csharp
class Car
{
}

// Create two car instances
var myCar = new Car();
var yourCar = new Car();
```

A mezőknek típusuk van, és az osztály bármely pontján definiálhatók.
A nyilvános mezőket PascalCase írásmóddal definiáljuk, a privát mezőket pedig camelCase írásmóddal, egy alulvonás `_` előtaggal:

```csharp
class Car
{
    // Accessible by anyone
    public int Weight;

    // Only accessible by code in this class
    private string _color;
}
```

Egy mezőnek opcionálisan kezdőértéket adhatsz. Ha egy mező _nem_ ad meg kezdőértéket, akkor a típusa alapértelmezett értékét kapja. Egy példány mezőértékei pontjelöléssel érhetők el és frissíthetők.

```csharp
class Car
{
    // Will be set to specified value
    public int Weight = 2500;

    // Will be set to default value (0)
    public int Year;
}

var newCar = new Car();
newCar.Weight; // => 2500
newCar.Year;   // => 0

// Update value of the field
newCar.Year = 2018;
```

A privát mezőket általában egy metódus hívásának mellékhatásaként frissítjük. Az ilyen metódusok általában nem adnak vissza értéket, ilyenkor a visszatérési típus `void` legyen:

```csharp
class CarImporter
{
    private int _carsImported;

    public void ImportCars(int numberOfCars)
    {
        // Update private field from public method
        _carsImported = _carsImported + numberOfCars;
    }
}
```
