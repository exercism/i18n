# használj rekurziót

Ahogy a leírásban is szerepel, a `keep/2` és a `discard/2` függvényt az `Enum.filter/2` és az `Enum.reject/2` használata nélkül kell megvalósítanod. A `Stream`-megfelelőiket és a listaértelmezéseket is kerüld.
