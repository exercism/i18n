# Bevezetés

A `throw` kifejezések alternatívái a `throw` utasításoknak, és különösen növelhetik a ternáris és más összetett kifejezések erejét.

```csharp
string trimmed = str == null ? throw new ArgumentException() : str.Trim();
```
