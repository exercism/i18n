# Utasítások kiegészítése

## Megvalósítás

A gráfot a DSL-ben a `Graph` osztály képviseli.
A csomópontok és az élek megvalósítása (amelyeket a `Node`, illetve az `Edge` osztály képvisel) adott.

Ha többet szeretnél megtudni a DSL elvárt felépítéséről, vess egy pillantást a `GraphTest.java` teszteseteire.
