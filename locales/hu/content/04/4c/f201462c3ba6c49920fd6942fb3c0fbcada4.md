# Az ANSI importálása :except használatával

A feladat leírása megemlíti, hogy „más autószínek támogatását tervezed, ezért a teljes modult importálni szeretnéd”, kivéve az ütköző `color` függvényt. Ehhez a `:except` opciót használd, így:

```elixir
import IO.ANSI, except: [color: 1]
```
