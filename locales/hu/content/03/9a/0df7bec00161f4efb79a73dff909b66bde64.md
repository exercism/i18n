# Bevezetés

Az Elmben a [`List`][lists] egy módosíthatatlan gyűjtemény, amely nulla vagy több azonos típusú értéket tartalmaz.

A listákat a következőképpen hozhatod létre:

```elm
empty = []
singleValue = [ 5 ]
threeValues = [ "a", "b", "c" ]
```

A leggyakrabban a `::` (cons) operátorral adhatsz hozzá elemet egy listához:

```elm
twoToFour = [ 2, 3, 4 ]
oneToFour = 1 :: twoToFour --> [ 1, 2, 3, 4 ]
```

A listákat a [`List` modulban][list-module] definiált függvényekkel és operátorokkal, vagy [mintaillesztéssel][list-destructuring] kezelheted

```elm
describe : List String -> String
describe list =
    case list of
        [] ->
            "Empty list"

        [ "hello" ] ->
            "Singleton list with: hello"

        x :: xs ->
            "Non-empty list with head: " ++ x

describe []                   --> "Empty list"
describe [ "hello" ]          --> "Singleton list with: hello"
describe [ "hello", "world" ] --> "Non-empty list with head: hello"
```

[lists]: https://elmprogramming.com/list.html
[list-module]: https://package.elm-lang.org/packages/elm/core/latest/List
[list-destructuring]: https://www.bekk.christmas/post/2020/8/peeking-inside-lists
