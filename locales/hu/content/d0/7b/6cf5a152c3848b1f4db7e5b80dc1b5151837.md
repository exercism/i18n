# Az `assignTicketTo` argumentumának destrukturálása

Ahogy a feladatleírásban is szerepel, destrukturáld az `assignTicketTo` argumentumát, hogy kinyerd a `status` rekordmezőt a `Ticket` típusból, és nevezd el a rekordot az `as` szintaxissal.
