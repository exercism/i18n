# Bevezetés

## Boolean

A F#-ban a Boolean értékeket a `bool` típus képviseli, melynek értéke `true` vagy `false` lehet.

A F# három Boolean-operátort támogat: a `not` (NOT), a `&&` (AND) és a `||` (OR) operátort.
