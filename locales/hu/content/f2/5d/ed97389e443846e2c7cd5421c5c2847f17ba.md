# Bevezetés

Ha a ciklusban lévő kódot mindig legalább egyszer végre kell hajtani, akkor `do`/`while` ciklust használhatunk:

```csharp
int x = 23;

do
{
    // Always executes at least once, then repeats as long as x > 10
    x = x - 2;
} while (x > 10)
```
