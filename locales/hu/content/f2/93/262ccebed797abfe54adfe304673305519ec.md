# beágyazott export

A függvényeket, például a `twoFer`-t, az osztályokat és a konstansokat is elláthatod inline `export`-tal.

```javascript
// Not inlined
export { twoFer }

// Inlined
export function twoFer() {}
```
