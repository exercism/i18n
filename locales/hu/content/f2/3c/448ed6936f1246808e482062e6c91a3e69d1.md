# Figyelmen kívül hagyás

## További jq parancssori kapcsolók

A tesztekben további kapcsolókat láthatsz majd a `jq`-hoz.

```sh
run jq -s -R -c -f minesweeper.jq << 'END_INPUT'
```

Mik ezek?

- `-s`

  Ez a „slurp” mód.
  Ahelyett, hogy a bemenetet JSON-objektumok folyamaként kezelné,
  a teljes bemenet egyetlen tömbbe gyűlik.

  ```sh
  $ seq 5 | jq -c '.'
  1
  2
  3
  4
  5
  $ seq 5 | jq -s -c '.'
  [1,2,3,4,5]
  ```

- `-R`

  A `-r` kapcsolót talán már láttad: „raw” stringeket ír ki, idézőjelek nélkül.
  Ez ennek az ellentéte.
  A bemenet minden sorát stringként kezeli.

  Két példa:

  ```sh
  $ seq 5 | jq -R -c .
  "1"
  "2"
  "3"
  "4"
  "5"

  $ echo 42 | jq '. * 2'      # input is a JSON number
  84
  $ echo 42 | jq -R '. * 2'   # input is a JSON string
  "4242"
  ```

Ha ezt a két kapcsolót együtt használod, _nem_ stringek tömbjét kapod:
a teljes bemenetet egyetlen stringként kapod meg, a sorvégi újsorokat is beleértve.

```sh
$ seq 5 | jq -s -R -c .
"1\n2\n3\n4\n5\n"
```

Ennek a kezelése is a feladat kihívásának a része.
