# Bővebben

Egy struct vagy class **tárolt tulajdonságokat** definiálhat, hogy az adatokat az adott példány részeként tárolja. A tárolt tulajdonságokat deklarálhatod változóként (a `var` használatával), ha az értékük változhat, vagy konstansként (a `let` használatával), ha az értékük az inicializálás után változatlan marad:

```swift
class GameCharacter {
    var health = 10
    let id = makeRandomID()
}

let character1 = GameCharacter()
let character2 = GameCharacter()

print(character1.health) // Prints 10
print(character2.health) // Prints 10

character1.health = 25

print(character1.health) // Prints 25
print(character2.health) // Prints 10

// The following line triggers a compiler error because 'id' is a constant:
// character2.id = 2
```
