# Bevezetés

## Összehasonlító operátorok

Az [összehasonlító operátorok][comparison-operators] két azonos típusú értéket hasonlítanak össze, és egy `Bool` értéket (`true` vagy `false`) adnak vissza.
Ha különböző típusú értékeket próbálsz összehasonlítani, a fordító hibát dob.

A Swift szabványos összehasonlító operátorai:

| Operátor | Leírás                   | Példa           |
| -------- | ------------------------ | --------------- |
| `==`     | Egyenlő                  | `1 == 1` (igaz) |
| `!=`     | Nem egyenlő              | `1 != 2` (igaz) |
| `<`      | Kisebb, mint             | `4 < 5`  (igaz) |
| `<=`     | Kisebb vagy egyenlő      | `4 <= 4` (igaz) |
| `>`      | Nagyobb, mint            | `3 > 1`  (igaz) |
| `>=`     | Nagyobb vagy egyenlő     | `2 >= 2` (igaz) |

## `if` és `else` elágazások

A [feltételes utasítás][conditional-statement] attól függően futtat kódot, hogy egy feltétel `true` vagy `false` értékre értékelődik-e ki.
A leggyakoribb feltételes utasítás az [`if` elágazás][if], amely csak akkor futtat egy kódblokkot, ha a feltétele `true`.
Egy `if` elágazáshoz az `if` kulcsszót írod le, utána a feltételt, végül a kapcsos zárójelbe zárt kódblokkot:

```swift
let wheelCount = 4
if wheelCount == 4 {
    print("Your vehicle is a car")
}
// Prints "Your vehicle is a car"
```

Az `if`-et összekapcsolhatod egy `else` ággal, hogy más kódot futtass, amikor a feltétel `false`:

```swift
let precipitationInMM = 0.5
if precipitationInMM > 0.5 {
    print("You will need an umbrella")
} else {
    print("You will not need an umbrella")
}
// Prints "You will not need an umbrella"
```

Több feltétel egymás utáni ellenőrzéséhez láncba fűzheted az elágazásokat `else if` használatával.
Egy `if` elágazáshoz nem kötelező `else` ág, de hozzáadhatsz egyet azokra az esetekre, amikor egyik feltétel sem teljesül:

```swift
let favoriteFruit = "Apple"

if favoriteFruit == "Banana" {
    print("Your favorite fruit is a banana")
} else if favoriteFruit == "Orange" {
    print("Your favorite fruit is an orange")
} else if favoriteFruit == "Apple" {
    print("Your favorite fruit is an apple")
} else {
    print("Your favorite fruit is not a banana, orange, or apple")
}
// Prints "Your favorite fruit is an apple"
```

## `if` kifejezés

A modern Swiftben az `if` elágazások értéket előállító kifejezésként is használhatók.
Minden ág egy értékre értékelődik ki, így könnyen rendelhetsz hozzá értéket feltételesen:

```swift
let cpuBits = 32
let architecture = if cpuBits == 64 {
    "x86_64"
} else {
    "x86_32"
}

print(architecture)
// Prints "x86_32"
```

[comparison-operators]: https://docs.swift.org/swift-book/documentation/the-swift-programming-language/basicoperators#Comparison-Operators
[conditional-statement]: https://docs.swift.org/swift-book/documentation/the-swift-programming-language/controlflow/#Conditional-Statements
[if]: https://docs.swift.org/swift-book/documentation/the-swift-programming-language/controlflow/#If
