# Tippek

## Általános

- A 8 bites egész számokat az `al`-ban kell visszaadni.
- A 16 bites egész számokat az `ax`-ban kell visszaadni, a bemenő argumentumot pedig a `di`-ben kapod.
- Ha egy meghívott függvény módosít egy regisztert, akkor minden olyan értéket ments el belőle, amire később szükséged lesz.

## 1. A maszk kinyerése

- A 8 bites egész számokat az `al`-ban kell visszaadni.
- A 16 bites egész számokat az `ax`-ban kell visszaadni, a bemenő argumentumot pedig a `di`-ben kapod.
- Ha egy meghívott függvény módosít egy regisztert, akkor minden olyan értéket ments el belőle, amire később szükséged lesz.
- Van egy utasítás, amivel a biteket egy megadott számú pozícióval balra tolhatod.

## 2. Az üzenet kinyerése

- A 8 bites egész számokat az `al`-ban kell visszaadni.
- A 16 bites egész számokat az `ax`-ban kell visszaadni, a bemenő argumentumot pedig a `di`-ben kapod.
- Ha egy meghívott függvény módosít egy regisztert, akkor minden olyan értéket ments el belőle, amire később szükséged lesz.
- A 8 bites egész számokat az `al`-ban kell visszaadni.

## 3. A redundáns bitek kinyerése

- A 8 bites egész számokat az `al`-ban kell visszaadni.
- A 16 bites egész számokat az `ax`-ban kell visszaadni, a bemenő argumentumot pedig a `di`-ben kapod.
- Ha egy meghívott függvény módosít egy regisztert, akkor minden olyan értéket ments el belőle, amire később szükséged lesz.
- Használhatod a korábban definiált `extract_higher_bits` és `extract_lower_bits` függvényt.
- Van egy utasítás, ami azokat a biteket állítja be, amelyek mindkét értékben be vannak állítva.

## 4. Az összes üzenetbit beállítása

- A 8 bites egész számokat az `al`-ban kell visszaadni.
- A 16 bites egész számokat az `ax`-ban kell visszaadni, a bemenő argumentumot pedig a `di`-ben kapod.
- Ha egy meghívott függvény módosít egy regisztert, akkor minden olyan értéket ments el belőle, amire később szükséged lesz.
- Használhatod a korábban definiált `extract_higher_bits` és `extract_lower_bits` függvényt.
- Van egy utasítás, ami azokat a biteket állítja be, amelyek a két érték közül legalább az egyikben be vannak állítva.

## 5. A privát kulcs elforgatása

- A 8 bites egész számokat az `al`-ban kell visszaadni.
- A 16 bites egész számokat az `ax`-ban kell visszaadni, a bemenő argumentumot pedig a `di`-ben kapod.
- Ha egy meghívott függvény módosít egy regisztert, akkor minden olyan értéket ments el belőle, amire később szükséged lesz.
- Használhatod a korábban definiált `extract_higher_bits` és `extract_lower_bits` függvényt.
- A fájl elején az `equ` utasítással definiálhatsz egy konstans egész számot.
- Van egy utasítás, amivel megtudhatod, hány bit van beállítva egy 16 bites számban.
- A [movzx][movzx] utasítással egy 8 bites számot 16 bites számmá alakíthatsz úgy, hogy közben minden felesleges bit törlődik.
- Van egy utasítás, amivel a biteket annyi pozícióval forgathatod balra, amennyit a `cl` regiszterben lévő 8 bites érték megad.

## 6. A privát kulcs formázása

- A 8 bites egész számokat az `al`-ban kell visszaadni.
- A 16 bites egész számokat az `ax`-ban kell visszaadni, a bemenő argumentumot pedig a `di`-ben kapod.
- Ha egy meghívott függvény módosít egy regisztert, akkor minden olyan értéket ments el belőle, amire később szükséged lesz.
- Használhatod a korábban definiált `rotate_private_key`, `extract_higher_bits` és `extract_lower_bits` függvényt.
- Van egy utasítás, ami akkor állít be egy bitet, ha az a két érték közül csak az egyikben van beállítva, nem mindkettőben.
- Van egy utasítás, ami egy szám összes bitjét átfordítja.

## 7. A visszafejtés befejezése

- A 8 bites egész számokat az `al`-ban kell visszaadni.
- A 16 bites egész számokat az `ax`-ban kell visszaadni, a bemenő argumentumot pedig a `di`-ben kapod.
- Ha egy meghívott függvény módosít egy regisztert, akkor minden olyan értéket ments el belőle, amire később szükséged lesz.
- Használhatod a korábban definiált `set_bits` és `format_private_key` függvényt.
- A [movzx][movzx] utasítással egy 8 bites számot 16 bites számmá alakíthatsz úgy, hogy közben minden felesleges bit törlődik.
- Van egy utasítás, amivel a biteket egy megadott számú pozícióval balra tolhatod.
- Van egy utasítás, ami azokat a biteket állítja be, amelyek a két érték közül legalább az egyikben be vannak állítva.

[movzx]: https://www.felixcloutier.com/x86/movzx
