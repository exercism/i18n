# Használd az AddSeconds-t

Próbáld meg a `DateTime` beépített `AddSeconds` metódusával egyszerűsíteni a kódot. Figyeld meg, hogy az `AddSeconds` egy új `DateTime` példányt ad vissza, és nem módosítja azt a példányt, amelyen meghívták.
