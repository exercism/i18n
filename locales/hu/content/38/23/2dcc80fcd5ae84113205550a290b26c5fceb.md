# SQLite-specifikus utasítások

Ennél a feladatnál a `roman-numerals` tábla `result` oszlopát kell a `number` oszlopban szereplő szám alapján a helyes értékre állítanod.

## A tábla sémája

```sql
CREATE TABLE "roman-numerals" ("number" INT, "result" TEXT);
```
