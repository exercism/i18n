# Használd a `rand.uniform`-ot az `Enum.random` helyett

A `random_stardate` függvényben az `Enum.random` helyett a `:rand.uniform`-ot használd. A `:rand.uniform` lehetővé teszi, hogy közvetlenül tizedes törtet generálj, szemben az `Enum.random`-mal, ami csak egész számokat tud generálni.
