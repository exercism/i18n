# Bevezetés

## Boolean-ok

A C#-ban a Boolean értékeket a `bool` típus reprezentálja, amelynek értékei `true` vagy `false` lehetnek.

A C# három Boolean operátort támogat: `!` (NEM), `&&` (ÉS) és `||` (VAGY).
