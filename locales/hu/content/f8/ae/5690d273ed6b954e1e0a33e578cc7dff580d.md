# Bevezetés

A `foreach` vezérlési szerkezet kényelmes módot ad arra, hogy végigmenj _tömbökön_ és _objektumokon_. A `foreach`-nek két alapvető formája van:

```php
foreach (iterable as $value) {
  // statements
}

foreach (iterable as $key => value) {
  // statements
}
```
