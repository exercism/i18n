# Debug

测试失败时，会显示一条消息，说明哪里出了问题，以及是哪个输入导致的。你可以把程序中的任意值传给 `debug` 方法（由 `exercism/test_runner` 提供）来查看它们。它会捕获这些值，并显示输出。


```gleam
import exercism/test_runner.{ debug }

let value: String = "test"
debug("The value is " <> value)
```