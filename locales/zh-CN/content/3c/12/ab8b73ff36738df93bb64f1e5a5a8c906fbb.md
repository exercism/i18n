# rune 或 byte 转换为 string

为了保持一致，请比较 `runes` 或 `bytes`，但不要两者都比较。
遍历 `string` 得到的是 `rune` 值，而按下标访问 `string` 得到的是 `byte` 值。
两者的区别在 [Strings, bytes, runes and characters in Go](https://blog.golang.org/strings) 中有详细说明。
