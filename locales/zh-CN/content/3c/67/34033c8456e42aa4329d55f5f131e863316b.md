# 关于

字符串缓冲区是一种高效构建大字符串的方式：把一个个小片段依次追加到一起。与 String 不同，StringBuf 的实例不是不可变的，因为它可以作为实参传给函数，由这些函数通过追加更多值来修改它。

```haxe
var a = new StringBuf();
a.add("Hello");
trace(a.toString()); // Hello
a.add("Haxe!");
trace(a.toString()); // HelloHaxe
```
