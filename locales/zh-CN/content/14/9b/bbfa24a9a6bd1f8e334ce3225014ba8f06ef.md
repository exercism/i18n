# 简介

字符串缓冲区是一种高效构建大字符串的方式：把一个个小片段不断追加到一起。与 String 不同，StringBuf 的实例并非不可变，也就是说，它可以作为实参传给函数，而函数可以通过追加更多值来修改它。

```haxe
var a = new StringBuf();
a.add("Hello");
trace(a.toString()); // Hello
a.add("Haxe!");
trace(a.toString()); // HelloHaxe
```
