# 使用空合并运算符，而不是 is null or white space

与其用`string.IsNullOrWhiteSpace`来判断是否应该使用默认名字，不如试试[空合并运算符](https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/operators/null-coalescing-operator)，这样既可以简化代码，也可以体会一下两者在行为上的差异。
