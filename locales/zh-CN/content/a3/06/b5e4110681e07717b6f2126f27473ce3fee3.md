# 关于

PureScript 使用`true`和`false`来表示逻辑中的两个真值。

在 PureScript 中，三种逻辑运算（与、或、非）各有一个对应的运算符：`&&`、`||`和`not`。一般来说，这些运算有先后顺序的规则：这里会先应用`not`（取反），然后是`&&`（与），最后是`||`（或）。

你随时都可以“绕过”这些规则：用一个优先级更高的运算符就行，也就是`( )`，它被称为“分组运算符”，或者直接叫“括号”。
