# 简介

Factor 在单个操作系统进程中运行协作式线程。这些线程会在 I/O 操作和显式的`yield`调用时让出控制权，因此同步起来比抢占式调度的语言更简单。不过，竞态仍然可能出现，因为任何线程都可能在共享状态的两个非原子操作之间运行。Factor 的并发词汇库提供了标准工具集：用`spawn`启动线程，用 promise 做一次性结果交接，用`parallel-map`对序列做 fork/join，用锁实现互斥。
