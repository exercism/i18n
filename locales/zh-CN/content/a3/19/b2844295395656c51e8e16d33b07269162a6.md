# 说明

在这个练习中，你需要把经典游戏 Pac-Man 里的一些规则翻译成 Unison 函数。

你需要翻译四条规则，它们都与游戏状态有关。

> 不用纠结这些实参是怎么来的，只要专注于把它们组合起来，返回预期的结果。

## 1. 定义 Pac-Man 是否吃掉幽灵

定义`pacmanRules.eatGhost`函数，它接收两个实参（_Pac-Man 是否激活了能量豆_ 和 _Pac-Man 是否碰到幽灵_），并返回一个布尔值，表示 Pac-Man 能否吃掉幽灵。只有当 Pac-Man 激活了能量豆并且正碰到幽灵时，函数才返回 true。

```unison
pacmanRules.eatGhost: Boolean -> Boolean -> Boolean
pacmanRules.eatGhost powerPelletActive touchingGhost =
  todo "implement eatGhost"
```

## 2. 定义 Pac-Man 是否得分

定义`pacmanRules.score`函数，它接收两个实参（_Pac-Man 是否碰到能量豆_ 和 _Pac-Man 是否碰到豆子_），并返回一个布尔值，表示 Pac-Man 是否得分。如果 Pac-Man 碰到能量豆或豆子，函数应返回 true。

```haskell
pacmanRules.score: Boolean -> Boolean -> Boolean
pacmanRules.score powerPelletActive touchingDot =
  todo "implement score"
```

## 3. 定义 Pac-Man 是否失败

定义`pacmanRules.lose`函数，它接收两个实参（_Pac-Man 是否激活了能量豆_ 和 _Pac-Man 是否碰到幽灵_），并返回一个布尔值，表示 Pac-Man 是否失败。如果 Pac-Man 碰到幽灵且没有激活能量豆，函数应返回 true。

```unison
pacmanRules.lose : Boolean -> Boolean -> Boolean
pacmanRules.lose powerPelletActive touchingGhost =
  todo "implement lose"
```

## 4. 定义 Pac-Man 是否获胜

定义`pacmanRules.win`函数，它接收三个实参（_Pac-Man 是否吃掉了所有豆子_、_Pac-Man 是否激活了能量豆_ 和 _Pac-Man 是否碰到幽灵_），并返回一个布尔值，表示 Pac-Man 是否获胜。如果 Pac-Man 吃掉了所有豆子，并且根据第 3 部分中定义的实参没有失败，函数应返回 true。

```unison
pacmanRules.win : Boolean -> Boolean -> Boolean -> Boolean
pacmanRules.win eatenAll powerPelletActive touchingGhost
  todo "implement Rules.win"
```
