# 介绍

## 泛型

集合的定义通常会在尖括号中放入一个占位符，按照惯例通常写作`T`。这样的集合称为泛型。这样，集合的使用者就可以指定要在集合中存储什么类型的元素。在下面的示例代码中，我们实例化了一个字符串数组。

## 数组

C# 中的数组是基元值或结构体、类实例的集合。它们在基类库中实现为`List<T>`，其中`T`是数组中元素的类型。API 提供了丰富的方法来创建和操作数组。

元素可以添加到数组中，也可以从数组中移除。数组会根据需要增长和缩小。

```csharp
var listOfStrings = new List<string>();
```
