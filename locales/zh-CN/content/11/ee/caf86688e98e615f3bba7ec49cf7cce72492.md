# 简介

在 Cairo 中，所有权这一概念适用于_变量_，它规定了谁可以使用这些变量。
每个声明的变量只能使用一次。也就是说，变量一旦被使用，就会根据使用方式被销毁或移动。

_销毁_可能以多种方式发生：

- 变量离开作用域（无论是否被使用）。
- 结构体被解构。
- 使用`destruct()`显式销毁。

_移动_一个值，就是指把这个值传给另一个函数。

Cairo 语言使用线性类型系统，并借助它来确保所有代码都是可证明（且可验证）的，同时将 Cairo VM 的不可变内存抽象掉。
