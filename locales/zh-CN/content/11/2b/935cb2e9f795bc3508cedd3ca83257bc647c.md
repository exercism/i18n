# 提示

## 1. 定义一个函数，检查编程语言列表是否为空

- 可以用 List 类型上的某个[方法][list]来检查列表是否为空。

## 2. 定义一个函数，向列表中添加一门编程语言

- 可以用 List 类型上的某个[方法][list]来添加元素。
- 提醒：返回`void`的方法不需要任何`return`语句。

## 3. 定义一个函数，从列表中删除一门编程语言

- 可以用 List 类型上的某个[方法][list]来删除元素。
- 提醒：返回`void`的方法不需要任何`return`语句。

## 4. 定义一个函数，返回列表中的第一个元素

- 可以用 List 类型上的某个[方法][list]来获取指定下标处的元素。

## 5. 定义一个函数，返回列表中有多少门编程语言

- 可以用 List 类型上的某个[方法][list]来获取列表的大小。

## 6. 定义一个函数，判断某个编程语言是否在列表中

- 可以用 List 类型上的某个[方法][list]来检查列表中是否包含某个元素。

## 7. 定义一个函数，判断列表是否令人兴奋

- 试试使用类中已经定义好的方法。

[list]: https://docs.oracle.com/en/java/javase/11/docs/api/java.base/java/util/List.html
