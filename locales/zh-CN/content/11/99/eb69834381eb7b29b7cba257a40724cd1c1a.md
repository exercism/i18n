# 简介

C# 的 `enum` 类型表示一组固定的命名常量（枚举）。它的主要用途是提供一种类型安全的方式来处理数字常量，把可用值限制在一个预先定义的集合内。一个简单的枚举可以这样定义：

```csharp
enum Season
{
    Spring,
    Summer,
    Autumn,
    Winter
}
```

如果没有显式定义，枚举成员会自动被赋予递增的整数值，第一个值为 0。也可以显式赋值：

```csharp
enum Answer
{
    Maybe = 1,
    Yes = 3,
    No = 5
}
```
