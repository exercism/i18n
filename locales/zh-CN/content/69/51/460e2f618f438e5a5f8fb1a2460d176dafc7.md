# 补充说明

## 实现

在 Emacs Lisp 赛道中，除了用于清理电话号码的函数 _numbers_，你还需要再写两个函数：_area-code_ 和 _pprint_。

这两个额外的函数都只接收一个形参，也就是电话号码。

第一个函数 _area-code_ 从输入中提取区号，第二个函数 _pprint_ 则把输入转换成下面这种格式的字符串：

```
(NXX) NXX-XXXX
```

其中`N`和`X`的含义与上文相同。
