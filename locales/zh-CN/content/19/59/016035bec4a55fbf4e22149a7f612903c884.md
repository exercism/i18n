# 简介

递归函数是调用自身的函数。

递归函数至少需要一个*基本情况*和至少一个*递归情况*。

*基本情况*不再次调用函数，直接返回一个值。*递归情况*会再次调用函数，并修改输入，使它最终会匹配基本情况。

很多时候，每种情况都写在各自的函数子句中。

```elixir
# base case
def count([]), do: 0

# recursive case
def count([_head | tail]), do: 1 + count(tail)
```
