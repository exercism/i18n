# 字符串插值

除了字符串插值，在 Ruby 中创建字符串的另一种常见方式是使用 [String#%%](https://www.rubyguides.com/2012/01/ruby-string-formatting/)（也可以读作“String format”）。
例如：

```ruby
"One for %%s, one for me." %% %{name_variable}
```
