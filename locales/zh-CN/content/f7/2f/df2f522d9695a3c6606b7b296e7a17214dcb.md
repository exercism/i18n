# 简介

## JSON

JSON（JavaScript Object Notation）是一种人类可读的数据和文件格式，常用于交换数据，尤其是在 Web 应用中。因此，它在 Elm 语言中占有特殊的地位也就不足为奇了。

Elm 提供了核心模块`Json.Decode`和`Json.Encode`，用来解析和写入 JSON 数据。JSON 解码器是一种_声明_的方式：声明期望的数据类型，以及数据应该映射到哪种数据结构。

我们从这些模块中导入一些别名和类型：

```elm
import Json.Decode as Decode exposing (Decoder, Error)
import Json.Encode as Encode exposing (Value)
```

把`Decode.decodeString : Decoder a -> String -> Result Error a`和一个解码器以及一段 JSON 字符串一起使用：如果字符串符合解码器的规格，就会成功；否则就会失败。定义解码器时无法检查原始的 JSON 数据，对于来自命令式语言的人来说，这可能会感觉不太寻常。

## 基本数据类型解码器

JSON 有少量核心数据类型：

- 字符串：用双引号编码，例如`"hello world"`
- 布尔值：`true`或`false`
- 数字：例如`34`或`0.12`
- 空值：没有数据，即`null`

每一种都对应一个`Decode`函数：

```elm
Decode.decodeString Decode.string """\"hello\""""
    --> Ok "hello"

Decode.decodeString Decode.string """null"""
    --> Err ...

Decode.decodeString Decode.bool """true"""
    --> Ok True

Decode.decodeString Decode.bool """0"""
    --> Err ...

Decode.decodeString Decode.int """12"""
    --> Ok 12

Decode.decodeString Decode.float """3.14"""
    --> Ok 3.14

Decode.decodeString (Decode.null AnyValue) """null"""
    --> Ok AnyValue

Decode.decodeString (Decode.null AnyValue) """true"""
    --> Err ...
```

注意，`Decode.null`要求你传入一个任意值作为实参（可以是`()`、`Nothing`，或任何适合你程序的值），让你自行决定如何在程序中表示`null`。

## 组合解码器

JSON 还定义了两种用来汇集核心数据类型的数据结构：

- 数组：其他数据类型的集合`[0, null, "none", []]`
- 对象：键值对的集合`{"id": 1, "is_admin": false, "more_info": {}}`

`Decode`也提供了用于解码它们的函数：

```elm
Decode.decodeString (Decode.list Decode.int) """[1, 2, 3]"""
    --> Ok [1, 2, 3]

Decode.decodeString (Decode.list Decode.int) """[1, 2, 3, "not an int"]"""
    --> Err ...

Decode.decodeString (Decode.dict Decode.int) """{ "key1": 17, "key2": 71 }"""
    --> Ok (Dict.fromList [("key1", 17), ("key2", 71)])

Decode.decodeString (Decode.dict Decode.int) """{ "key1": 17, "key2": "seventy-one" }"""
    --> Err ...
```

这些函数`Decode.list : Decoder a -> Decoder (List a)`和`Decode.dict : Decoder a -> Decoder (Dict String a)`都需要一个解码器作为实参，这个解码器要能解码数据结构中的每一个元素。如果作为实参的解码器解码数组或对象值中的任何一个元素时失败，整个解码器就会失败，这是一种常见的模式。

把简单的解码器组合成更复杂的解码器，是这项技术的核心思想。我们会通过一个例子，介绍更多可以用来构建任意复杂的真实数据解码器的函数。

## GeoJSON

为了练习组合基本解码器来解析复杂的 JSON 数据，我们来构建用于解析（一部分）GeoJSON 数据的解码器。GeoJSON 是一种基于 JSON 的专用数据格式，用来表示地理要素，例如位置、路径或区域，它们由 2D 或 3D 地理坐标以及其他属性描述。

### 几何对象

GeoJSON 几何对象是一个包含`type`字段和`coordinates`字段的对象，`type`字段的值是字符串（共有 7 种可能的类型值），`coordinates`字段存放地理坐标（坐标的组织结构取决于类型）。例如，

```json
{
  "type": "Point",
  "coordinates": [127.831, 26.461]
}
```

或

```json
{
  "type": "LineString",
  "coordinates": [
    [127.831, 26.461],
    [127.829, 26.465],
    [127.829, 26.469],
    [127.83, 26.47]
  ]
}
```

在 Elm 中，几何对象可以用下面这个类型来表示：

```elm
type Geometry
  = Point (List Float)
  | LineString (List (List Float))
  | ...
```

要取出某个字段的值，我们可以用`Decode.field : String -> Decoder a -> Decoder a`，它会解码对象中某个特定键的值。

```elm
decodePointCoordinates : Decoder (List Float)
decodePointCoordinates =
    Decode.field "coordinates" (Decode.list Decode.float)

decodeLineStringCoordinates : Decoder (List (List Float))
decodeLineStringCoordinates =
    Decode.field "coordinates" (Decode.list (Decode.list Decode.float))
```

要转换解码器成功解析出来的值，我们可以用`Decode.map : (a -> value) -> Decoder a -> Decoder value`，或者它的任何一个同类函数，从`Decode.map2  (a -> b -> value) -> Decoder a -> Decoder b -> Decoder value`一直到`Decode.map8`。

在这个例子里，我们想根据`type`字段的值来决定使用哪个坐标解码器，这正是`Decode.andThen : (a -> Decoder b) -> Decoder a -> Decoder b`以及它的伙伴`Decode.succeed : a -> Decoder a`和`Decode.fail : String -> Decoder a`的用武之地。

```elm
decodeGeometry : Decoder Geometry
decodeGeometry =
    Decode.field "type" Decode.string
        |> Decode.andThen
            (\value ->
                case value of
                    "Point" ->
                        decodePointCoordinates |> Decode.map Point

                    "LineString" ->
                        decodeLineStringCoordinates |> Decode.map LineString

                    _ ->
                        Decode.fail "Geometry not implemented yet, or invalid type value"
            )
```

这里要注意，我们也可以用`Decode.andThen`来校验坐标（检查坐标是否成对或成三出现，以及坐标值在地理上是否说得通）。

```elm
Decode.decodeString decodeGeometry """{"type": "Point", "coordinates": [127.831, 26.461]}"""
    --> Ok (Point [127.831, 26.461])

Decode.decodeString decodeGeometry """{"type": "LineString", "coordinates": [127.831, 26.461]}"""
    --> Err ...
```

### 要素对象

GeoJSON 要素对象表示某个有空间范围的东西：它有一个`type`字段，值为`"Feature"`；一个`geometry`字段，值为几何对象或`null`；一个`properties`字段，值为任意 JSON 对象或`null`；以及一个可选的`id`字段，值为 JSON 字符串或数字。

```json
{
  "type": "Feature",
  "geometry": null,
  "properties": null
}
```

或

```json
{
  "type": "Feature",
  "id": "0157",
  "geometry": {
    "type": "Point",
    "coordinates": [127.831, 26.461]
  },
  "properties": {
    "country": "Japan"
  }
}
```

我们先来处理`id`字段。如果它存在，就必须能容纳字符串或数字，所以我们可以为这个值引入一个自定义类型，或者始终把它保存为字符串。

要在两个可能的解码器之间做选择，我们可以用`Decode.oneOf : List (Decoder a) -> Decoder a`。

```elm
decodeFeatureId : Decoder String
decodeFeatureId =
    Decode.oneOf
        [ Decode.string
        , Decode.int |> Decode.map String.fromInt
        , Decode.float |> Decode.map String.fromFloat
        ]
        |> Decode.field "id"

Decode.decodeString decodeFeatureId """{"id": "seventeen"}"""
    --> Ok "seventeen"

Decode.decodeString decodeFeatureId """{"id": 17}"""
    --> Ok "17"

Decode.decodeString decodeFeatureId """{"type": "Point"}"""
    --> Er ...
```

要表示这个字段是可选的，我们可以用同样的技巧：

```elm
decodeMaybeFeatureId : Decoder (Maybe String)
decodeMaybeFeatureId =
    Decode.oneOf
        [ decodeFeatureId |> Decode.map Just
        , Decode.succeed Nothing
        ]
```

不过我们也可以改用`Decode.maybe : Decoder a -> Decoder (Maybe a)`：

```elm
decodeMaybeFeatureId : Decoder (Maybe String)
decodeMaybeFeatureId =
    Decode.maybe decodeFeatureId

Decode.decodeString decodeMaybeFeatureId """{"id": 17}"""
    --> Ok (Just "17")

Decode.decodeString decodeMaybeFeatureId """{"type": "Point"}"""
    --> Ok Nothing
```

注意，`decodeMaybeFeatureId`（或任何用`Decode.maybe`包装的解码器）总会成功，因为它总可以回退到`Ok Nothing`，不过使用它的解码器仍然可能失败：

```elm
Decode.decodeString (Decode.list decodeMaybeFeatureId) """{"id": 17}"""
    --> Err ...
```

接下来看`geometry`字段，它要么是几何对象，要么是`null`。我们可以再次使用`Decode.oneOf`，但有一个更好的选择：`Decode.nullable : Decoder a -> Decoder (Maybe a)`。

```elm
decodeFeatureGeometry : Decoder (Maybe Geometry)
decodeFeatureGeometry =
    Decode.nullable decodeGeometry
        |> Decode.field "geometry"

Decode.decodeString decodeFeatureGeometry """{"geometry": {"type": "Point", "coordinates": [127.831, 26.461]}}"""
    --> Ok (Just (Point [127.831, 26.461]))

Decode.decodeString decodeFeatureGeometry """{"geometry": null}"""
    --> Ok Nothing

Decode.decodeString decodeFeatureGeometry """{"geometry": {}}"""
    --> Err ...
```

`Decode.maybe`和`Decode.nullable`的签名相同，但行为不同，因为`Decode.nullable`的`OK Nothing`结果只可能来自特定的 JSON 值`null`。

最后，`properties`字段可以包含任意 JSON 对象或`null`，这正适合交给`Decode.value : Decoder Value`。

```elm
decodeProperties : Decoder (Maybe (Dict String Value))
decodeProperties =
    Decode.nullable (Decode.dict Decode.value)
        |> Decode.field "properties"


Decode.decodeString decodeProperties """{"properties": null}"""
    --> Ok Nothing

Decode.decodeString decodeProperties """{"properties": {"country": "Japan"}}"""
    --> Ok (Just (Dict.fromList [("country", <internals>)]))

Decode.decodeString decodeProperties """{"properties": 17}"""
    --> Err ...
```

注意，`Value`是一种不透明类型，这意味着你无法轻易查看它的内容。要使用它，你可以为它写一个解码器，再用`Decode.decodeValue : Decoder a -> Value -> Result Error a`运行，或者通过 port 直接发送给 JavaScript。

现在可以解析要素对象了：

```elm
type alias Feature =
    { id : Maybe String
    , geometry : Maybe Geometry
    , properties : Maybe (Dict String Value)
    }

decodeFeature : Decoder Feature
decodeFeature =
    Decode.field "type" Decode.string
        |> Decode.andThen
            (\value ->
                if value /= "Feature" then
                    Decode.fail "not a Feature"

                else
                    Decode.map3 Feature
                        decodeMaybeFeatureId
                        decodeFeatureGeometry
                        decodeProperties
            )

Decode.decodeString decodeFeature """{"type": "Feature", "geometry": null, "properties": null}"""
    --> Ok { geometry = Nothing, id = Nothing, properties = Nothing }

"""
{
  "type": "Feature",
  "id": "0157",
  "geometry": {
    "type": "Point",
    "coordinates": [127.831, 26.461]
  },
  "properties": {
    "country": "Japan"
  }
}
"""
  |> Decode.decodeString decodeFeature
    --> Ok { geometry = Just (Point [127.831,26.461]), id = Just "0157", properties = Just (Dict.fromList [("country",<internals>)]) }
```

## JSON 编码器

编码器让你可以用`Encode.encode : Int -> Value -> String`函数，把 Elm 值写出为合法的 JSON。第一个实参指定最终结果中的缩进量，第二个实参是要写出的 JSON 值。

`Value`可以从`Decode.value`获得，也可以由某个编码器生成：

```elm
Encode.encode 0 (Encode.string "hello")
    --> "hello"

Encode.encode 0 (Encode.bool True)
    --> "true"

Encode.encode 0 (Encode.int 12)
    --> "12"

Encode.encode 0 (Encode.float 3.14)
    --> "3.14"

Encode.encode 0 Encode.null
    --> "null"

Encode.encode 0 (Encode.list Encode.int [1, 2, 3])
    --> "[1,2,3]"

Encode.encode 4 (Encode.list Encode.int [1, 2, 3])
    --> "[\n    1,\n    2,\n    3\n]"

Encode.encode 0 (Encode.dict String.toLower Encode.int (Dict.fromList [("KEY1", 17), ("KEY2", 71)]))
    --> "{\"key1\":17,\"key2\":71}"

Encode.encode 4 (Encode.dict String.toLower Encode.int (Dict.fromList [("KEY1", 17), ("KEY2", 71)]))
    --> "{\n    \"key1\": 17,\n    \"key2\": 71\n}"
```

也可以用`Encode.object : List ( String, Value ) -> Value`：

```elm
Encode.object
    [ ( "key1", Encode.int 17 )
    , ( "key2", Encode.int 71 )
    ]
    |> Encode.encode 0
    --> "{\"key1\":17,\"key2\":71}"
```

下面我们来为前面定义的 GeoJSON 解码器编写编码器：

```elm
encodeGeometry : Geometry -> Value
encodeGeometry geometry =
    case geometry of
        Point coord ->
            Encode.object
                [ ( "type", Encode.string "Point" )
                , ( "coordinates", Encode.list Encode.float coord )
                ]

        LineString coord ->
            Encode.object
                [ ( "type", Encode.string "LineString" )
                , ( "coordinates", Encode.list (Encode.list Encode.float) coord )
                ]

encodeFeature : Feature -> Value
encodeFeature feature =
    let
        maybeId =
            case feature.id of
                Nothing ->
                    []

                Just id ->
                    [ ( "id", Encode.string id ) ]
    in
    Encode.object
        (maybeId
            ++ [ ( "type", Encode.string "Feature" )
               , ( "geometry"
                 , case feature.geometry of
                    Nothing ->
                        Encode.null

                    Just geometry ->
                        encodeGeometry geometry
                 )
               , ( "properties"
                 , case feature.properties of
                    Nothing ->
                        Encode.null

                    Just dict ->
                        Encode.dict identity identity dict
                 )
               ]
        )
```

一般来说，检验解码器-编码器配对的好办法，是检查`elmValue |> encoder |> decoder == elmValue`是否成立。
