# 说明

实现 `accumulate` 操作：给定一个集合以及一个要对集合中每个元素执行的操作，它返回一个新的集合，其中包含把该操作应用到输入集合的每个元素后得到的结果。

给定下面这组字符串：

- "cat", "Dog", "b4t", "gO"

以及这样一个操作：

- 把一个字符串转换为大写

你的代码应该能生成由这些字符串组成的集合：

- "CAT", "DOG", "B4T, "GO"

查看测试套件，了解预期的函数签名。

## 限制

不要动标准库提供的那套 collect/map/fmap 之类的功能！
请改用其他基础工具，自己解决这个问题。
