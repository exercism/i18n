# 简介

元组是一种轻量级的方式，可以把一组数量固定、类型任意的数据组合在一起。
元组可以有任意数量的元素。
你可以通过解构来访问其中的元素。

```rust
fn main() {
    let tup: (u32, u64, bool) = (10, 20, true);
}

fn main() {
    let (x, y, z) = (500, 6, true);

    if y == 6 {
        println!("y is 6!");
    }
}
