# avoid init 函数

避免使用 `init` 函数：
1) 它会拖慢启动速度，而使用这个包的开发者对此也无能为力，除非把 `init` 函数删掉。
2) 使用 `init` 函数大多是为了初始化`global variables`。

在可能的情况下，应[避免使用全局状态](https://dave.cheney.net/practical-go/presentations/qcon-china.html#_avoid_package_level_state)。
