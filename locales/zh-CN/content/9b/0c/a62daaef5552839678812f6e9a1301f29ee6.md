# 简介

Go 中有一些基本数字类型，可以表示整数或浮点数的取值集合。

本概念将重点介绍两种数字类型：

- `int`：例如 `0`、`255`、`2147483647`。
  一种有符号整数，大小至少为 32 位（取值范围：-2147483648 到 2147483647）。
  但这取决于系统的架构。
  大多数现代计算机都是 64 位的，因此 `int` 的大小为 64 位（取值范围：-9223372036854775808 到 9223372036854775807）。

- `float64`：例如 `0.0`、`3.14`。
  包含所有 64 位浮点数的集合。

Go 支持标准的算术运算符 `+`、`-`、`*`、`/` 和 `%`（是求余，不是求模）。

在 Go 中，在不同类型之间赋值需要进行显式转换。
例如，要把 `int` 转换成 `float64`，需要这样做：

```go
var x int = 42
f := float64(x)

fmt.Printf("x is of type: %s\n", reflect.TypeOf(x))
// Output: x is of type: int

fmt.Printf("f is of type: %s\n", reflect.TypeOf(f))
// Output: f is of type: float64
```
