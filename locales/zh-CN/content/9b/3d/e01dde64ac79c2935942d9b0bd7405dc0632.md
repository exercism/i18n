# 补充说明

## 提示

要完成这道练习，你需要实现`wordCount`函数，它接收一段*文本*，返回每个*单词*出现的次数。

如果这是你第一次解这道练习，建议你沿用给出的签名：

```haskell
wordCount :: String -> [(String, Int)]
```

之后，你可以回头再看看这道题，试试其他的数据类型和库：

- `Text`，来自*text*包。
- `Map`，来自*containers*包。
- `MultiSet`，来自*multiset*包。

测试套件有意设计成能接受几乎所有说得通的类型签名，所以鼓励你去找出自己认为最合适的那一种。
