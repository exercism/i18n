# 简介

## 三元运算符

_三元运算符_是一种轻量、紧凑的写法，可以替代简单的 _if/else_ 语句。
它通常用在返回语句中（但不限于此），只需一行就能完成判断：如果表达式为 `true`，就返回左边的值；如果为 `false`，就返回右边的值，如下所示：

```java
boolean expr = 0 != 200;

// Ternary statement
int value = expr ? 22 : 33;
// => 22
```

很多简单的 _if/else_ 表达式都可以用_三元运算符_来简化。
