# 关于

`null`值用来表示值的_缺失_。**可空**类型是一种允许变量取`null`值的类型。要让任意类型变得可空，可以用`Null<T>`把它包起来：

```haxe
var a:Int = null; // Error
var b:Null<Int> = null; // Allowed
```

要检查某个值当前是否为`null`，直接用`if`表达式即可：

```haxe
var a:Null<Int> = 10;

if (a != null) {
  trace("a is not null"); // a is not null
}
```
