# 简介

本练习以你在 [Cargo Shuffle][cargo-shuffle] 中接触过的数据栈和栈效应记法为基础。在这里，你将编写用于组合布尔值的单词。

## 重排单词

最常见的重排单词位于[`kernel`][kernel]中：

```
dup    ( x -- x x )
dupd   ( x y -- x x y )
drop   ( x -- )
swap   ( x y -- y x )
swapd  ( x y z -- y x z )
over   ( x y -- x y x )
pick   ( x y z -- x y z x )
rot    ( x y z -- y z x )
-rot   ( x y z -- z x y )
rotd   ( w x y z -- w y z x )
spin   ( x y z -- z y x )
nip    ( x y -- y )
tuck   ( x y -- y x y )

2dup   ( x y -- x y x y )
3dup   ( x y z -- x y z x y z )
4dup   ( w x y z -- w x y z w x y z )
2drop  ( x y -- )
3drop  ( x y z -- )
4drop  ( w x y z -- )
2nip   ( x y z -- z )
2swap  ( x y z w -- z w x y )
```

## 布尔值

两个布尔字面量是`t`和`f`。`f`是 Factor 中唯一为假的值，其他一切，包括`0`和空集合，都为真。

```factor
t .    ! => t
f .    ! => f
```

## 布尔单词

以下三个布尔单词来自`kernel`词汇表：

```
and ( x y -- ? )    ! true when both inputs are truthy
or  ( x y -- ? )    ! true when either input is truthy
not ( x   -- ? )    ! inverts its argument
```

```factor
t t and .    ! => t
t f and .    ! => f
f t or .     ! => t
t not .      ! => f
```

每个输出中的`?`是栈效应记法中表示“布尔值”的约定。

下面有一个任务会用到`swap`。

[cargo-shuffle]: https://exercism.org/tracks/factor/exercises/cargo-shuffle
[kernel]: https://docs.factorcode.org/content/vocab-kernel.html
