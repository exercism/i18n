# 关于

静态扩展允许在不修改现有类型源代码的情况下，对它们进行伪扩展。这是通过使用`using`语句实现的。静态扩展是一种强大的工具，它允许增强类型而无需实际改变它们。

```haxe
using Main.IntExtender;

class Main {
  public static function main() {
    trace(2.triple()); // 6
  }
}

class IntExtender {
  public static function triple(i:Int):Int {
    return i * 3;
  }
}
```
