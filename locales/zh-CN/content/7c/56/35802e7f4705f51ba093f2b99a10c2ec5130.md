# 简介

所有 Common Lisp 对象要么是“原子”（单一、不可分割的值），要么是“cons 单元”（由构造函数“cons”生成）。
一个 cons 单元由两部分组成：第一个元素和其余元素。
由于历史原因，这两部分被称为`car`和`cdr`。
当这些 cons 单元作为代码求值时，第一个元素（`car`）表示被调用的函数，而其余元素（`cdr`）表示传给该函数的实参：

```lisp
(<function> <arg1> <arg2> ... <argN>)
; ^ car ^  |        ^ cdr ^
```
