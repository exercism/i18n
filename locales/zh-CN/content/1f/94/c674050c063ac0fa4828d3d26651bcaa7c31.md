# 说明

调用 API 是编写网络代码的重要一环。在这个练习中，你需要从某个端点获取数据，并确认它已成功取回。

## 目标

- 修改提供的代码。
- 调用 HTTP 客户端，尝试获取一条引文。结果应该是一个由`string`和`error`组成的联合类型。
- 如果成功收到引文，就把它打印到控制台（使用`io:println()`）。
- 如果发生了`error`，就把错误信息打印到控制台。
