在 JavaScript 中，透镜是函数式编程中的一个概念，它让你能以模块化、不可变的方式访问和修改数据。本质上，透镜是一对可组合的纯函数，一个负责取值，一个负责设值，都聚焦于对象内部的某个特定字段。

用透镜可以简化代码、提高代码的复用性，并避免常见的编程错误。例如，可以用透镜来：

- 访问和修改嵌套的数据结构，而不必关心数据的具体结构。
- 以纯函数的方式更新数据，而不改动原对象。
- 把多个透镜组合起来，构建出更复杂的透镜，从而以多种方式访问和修改数据。