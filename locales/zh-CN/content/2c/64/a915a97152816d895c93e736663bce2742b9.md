# 介绍

静态扩展允许在不修改现有类型源代码的前提下，对这些类型进行伪扩展。这是通过`using`语句实现的。静态扩展是一个强大的工具，它让你能增强类型，而不必真正修改它们。

```haxe
using Main.IntExtender;

class Main {
  public static function main() {
    trace(2.triple()); // 6
  }
}

class IntExtender {
  public static function triple(i:Int):Int {
    return i * 3;
  }
}
```
