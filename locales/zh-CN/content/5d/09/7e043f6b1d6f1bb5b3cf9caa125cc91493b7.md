# 简介

## 资源清理

如果一个类实现了`IDisposable`接口，那么当不再需要某个实例时，就必须调用它的`Dispose()`方法。这通常是在`catch`或`finally`子句中完成的，或者在某个调用方的`Dispose()`例程中完成。`Dispose()`为非托管资源（例如操作系统对象，它们不由 .NET 运行时管理）提供了被释放的机会，同时也让托管资源的内部状态得以重置。
