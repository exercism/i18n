# 简介

JavaScript 的数组解构语法，可以简洁地从数组中提取值，并把它们赋值给不同的变量。

在这个例子中，`numberOfMoons`数组里的每个值都被赋值给了对应的行星：

```javascript
const numberOfMoons = [0, 2, 14];
const [venus, mars, neptune] = numberOfMoons;

neptune;
// => 14
```
