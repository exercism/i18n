# 补充说明

## 函数与测试

这里提供了一个`SayHi`函数的初始定义。

```delphi
function SayHi : string;
begin
  result := 'Goodbye, Mars!';
end;
```

测试运行器会拿这个函数来运行，并将它[返回][returns]的值与它期望的值进行比较。
请以这个初始函数定义为基础，编写你提交的解答。

[returns]: https://www.freepascal.org/docs-html/3.2.2/ref/refse94.html
