# 关于

PHP 内置了多种循环控制结构，例如`while`循环。`while`语句的基本形式是：

```php
while (expr) {
  // statements
}
```

只要表达式（_expr_）的求值结果为_真值_，循环就会反复迭代其中的语句。如果表达式（_expr_）在第一次迭代前求值为 false，这些语句就永远不会运行。

你也可以使用`do-while`循环，它会在对表达式（_expr_）求值之前，先运行一次循环中的语句。

```php
do {
  // statements
} while (expr);
```
