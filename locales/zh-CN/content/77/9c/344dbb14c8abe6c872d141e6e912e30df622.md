# 补充说明

## 提示

完成这个练习，你需要创建数据类型`ForthState`，并实现下面这些函数：

- `emptyState`返回一个空的`ForthState`。
- `evalText`对输入的 Text 求值，返回新的状态。
- `toList`返回当前栈的数组，其中栈顶元素排在最右边（也就是最后一个）。

代码里已经放好了示例的数据声明和类型签名，但函数的具体定义，以及一个有实际意义的数据类型、newtype 或类型同义词，都要由你来完成。
