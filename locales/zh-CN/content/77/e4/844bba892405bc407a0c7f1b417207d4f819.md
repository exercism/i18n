# 简介

在 Gleam 中，常量是一种给值起名字的方式。常量用`const`关键字定义。

```gleam
pub const pi: Float = 3.14159

pub fn circle_area(radius: Float) -> Float {
  radius *. radius *. pi
}
```

用`pub`关键字定义时，模块常量可以从其他模块访问，否则只能在定义它的模块内部访问。
