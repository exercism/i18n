# 简介

_三元运算符_是简单 _if/else_ 语句的一种轻量、简洁的替代写法。
它通常用在返回语句中（但不限于此），只需一行就能完成判断：表达式为 `true` 时返回左边的值，为 `false` 时返回右边的值，如下所示：

```java
boolean expr = 0 != 200;

// Ternary statement
int value = expr ? 22 : 33;
// => 22
```

很多简单的 _if/else_ 表达式都可以用_三元运算符_来简化。
