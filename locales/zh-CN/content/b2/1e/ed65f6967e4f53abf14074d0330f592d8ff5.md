# 提示

## 通用

- 虽然你收到的是`ElectionResult`的`reference`，但你仍然可以用点号`.`来访问它的成员，就好像它不是`reference`一样！
- 你可以用`&`运算符来获取变量的`reference`，例如`int&`。
- 如果你不确定`references`是如何工作的，可以读一读 [Learn C++: References][learncpp-references]。

## 1. 获取`ElectionResult`的票数

- 你需要创建一个函数，它接收一个指向`ElectionResult`的`reference`，也就是`ElectionResult&`。
- - 虽然你收到的是`ElectionResult`的`reference`，但你仍然可以用点号`.`来访问它的成员，就好像它不是`reference`一样！

## 2. 增加`ElectionResult`的票数

- 你需要创建一个函数，它接收一个指向`ElectionResult`的`reference`，也就是`ElectionResult&`。
- - 虽然你收到的是`ElectionResult`的`reference`，但你仍然可以用点号`.`来修改它的成员变量，就好像它不是`reference`一样！

## 3. 计票与总统职位

- 你可以用循环像遍历普通 vector 那样遍历`vector`的`reference`。
- 你可以用`myvector.size()`获取 vector 的大小。


[learncpp-references]: https://www.learncpp.com/cpp-tutorial/lvalue-references/
