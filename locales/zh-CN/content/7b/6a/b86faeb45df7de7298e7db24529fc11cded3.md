# 必须调用计算备餐时间的函数

修改 `%{target}`，让它调用 `%{function}`。

根据题目说明，`%{target}`可以通过取以分钟计的备餐时间，再加上在烤箱里花费的时间来实现。
备餐时间（分钟）就是调用 `%{function}` 的结果。

在 JavaScript 中，可以使用 `()` 来执行一个函数，实参写在左括号 `(` 和右括号 `)` 之间，用逗号分隔。
