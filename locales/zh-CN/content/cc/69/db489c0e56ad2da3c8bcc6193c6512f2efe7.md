# 说明

开发一种用于掷骰子的简单领域特定语言（DSL）。像《龙与地下城》这类游戏的玩家对这样的 DSL 并不陌生。例如下面这个表达式：`2 D20 + 1 D6`，它的意思是掷两个 20 面骰子和一个 6 面骰子。

表达式`2 d20 + 3 d10`可以使用 Pharo 语法实现。`+`和任何其他方法一样，也可以定义在数字以外的类上。像`d20`这样的方法可以通过类扩展在现有的`Integer`类上实现；类扩展是定义在类所在包以外的另一个包中的方法。
