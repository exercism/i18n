# 调试

测试失败时，会显示一条消息，说明哪里出了问题，以及是哪个输入导致的。你还可以利用[控制台输出][fmt-println]也会一并显示这一点。可以使用下面的方式向控制台输出：

```go
import "fmt"
fmt.Println("Debug message")
```

[fmt-println]: https://pkg.go.dev/fmt#Println

注意：在浏览器编辑器中调试时，使用`fmt.Print`之类的方法不会在输出后换行，这可能会触发`go test --json`里的一个 bug，导致测试输出错乱。
