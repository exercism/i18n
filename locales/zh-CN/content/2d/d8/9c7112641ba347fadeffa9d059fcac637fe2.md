# 指令补充

## 实现

定义
`type Classification`，
用来容纳自然数的分类值。
这个类型的具体表示形式由你自行选择。

定义三个 Classification 常量，名称分别为

```text
	ClassificationDeficient
	ClassificationPerfect
	ClassificationAbundant
```

实现一个名为 Classify 的函数，它接受一个 int64 输入，并返回一个 Classification 和一个错误值。

创建一个名为 ErrOnlyPositive 的错误，当输入不是正整数时返回它。
