# 简介

## 错误

当程序或某段代码出错，或出现意外情况时，把错误报告出来是很有用的。

错误是 JavaScript 对象。

这个对象的主要属性是`message`：

```javascript
const error = new Error('Oops, something went wrong');

console.log(error.message);
// => "Oops, something went wrong"
```

使用`throw`语法，你可以抛出一个错误。

```javascript
throw new Error('Oops');
```

当错误被抛出时，当前的执行会停止，然后在调用栈的第一个 catch 代码块中继续。

```javascript
try {
  throw new Error('Oops');
} catch (error) {
  console.log(error.message);
  // => "Oops"
}
```

## 继承

继承是一种在类之间建立父子关系的方式。
子类（有时称为_派生类_）可以访问父类（有时称为_基类_）定义的行为和数据。

```javascript
class Pet {
  constructor(name) {
    this.name = name;
  }

  introduce() {
    console.log(`This is my pet, ${this.name}.`);
  }
}

class Dog extends Pet {}

const dog = new Dog('Otis');
dog.introduce();
// => This is my pet, Otis.
```

子类声明中的`extends`关键字通过[原型链][prototype-chain]与父类建立关系。

由子类构造函数创建的对象，其原型链中会包含父类的原型，从而可以访问父类定义的所有方法或数据。

```javascript
const dog = new Dog('Otis');

Dog.prototype.isPrototypeOf(dog); // => true
Pet.prototype.isPrototypeOf(dog); // => true
Pet.prototype.isPrototypeOf(Dog.prototype); // => true

Pet.prototype.hasOwnProperty('introduce'); // => true
Dog.prototype.hasOwnProperty('introduce'); // => false
dog.hasOwnProperty('introduce'); // => false
```

和 JavaScript 中的任何类一样，派生类可以使用`extends`关键字继承`Error`，从而创建自定义错误。
`instanceof`语法会检查捕获到的错误是否是`Error`某个特定派生类的实例。

```javascript
class CustomError extends Error {}

try {
  // ... Code that may throw an error
} catch (error) {
  if (error instanceof CustomError) {
    console.log('The error thrown is an instance of the CustomError');
  }
}
```

[prototype-chain]: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Inheritance_and_the_prototype_chain
