# 说明补充

## Prolog 中的可变数据

默认情况下，Prolog 中的值是_不可变_的，也就是说值永远不会改变（每产生一个新值，都必须引入一个新变量）。
虽然这对绝大多数使用场景来说都很合适，但这个练习_确实_需要修改值。
要在 Prolog 中修改数据，可以使用 [setarg] 谓词。

[setarg]: https://www.swi-prolog.org/pldoc/doc_for?object=setarg/3
