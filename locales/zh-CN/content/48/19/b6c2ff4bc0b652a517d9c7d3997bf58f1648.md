# 说明

重构一个 Markdown 解析器。

Markdown 练习是一个重构练习。有一段代码可以解析给定的字符串，使用 [Markdown 语法](https://guides.github.com/features/mastering-markdown/) 并返回该字符串对应的 HTML。尽管这段代码写得让人困惑，难以理解，但它居然能正常运行，而且所有测试都通过了！你的挑战是重写这段代码，让它更易于阅读和维护，同时确保所有测试仍然通过。

如果你在重构过程中在注释里记录下你所做的修改，让审查者可以看到，那会很有帮助，但这并不是严格必须的。最重要的是让代码变得更好！
