# 优化显式的默认值

把 `%{parameter}` 的[默认值](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Default_parameters)设为 `'you'`，而不要依赖“没有传入值时 `%{maybe_undefined_expression}` 就是 `undefined`”这一点。
