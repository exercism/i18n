# 不要将布尔值与 true 或 false 比较

将布尔值与 true 或 false 比较是多余的。
使用`==`会生成一个布尔值，因此没有必要再把它与另一个硬编码的布尔值比较。
如果要翻转布尔值，请使用`!`运算符。

```crystal
# Rather than:
some_bool == true
some_bool == false

# Consider:
some_bool
!some_bool
```
