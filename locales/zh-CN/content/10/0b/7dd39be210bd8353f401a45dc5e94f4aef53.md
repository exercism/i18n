# 测试

## 从命令行

只需输入 `p test`。前提是 phix 可执行文件的路径已声明在 PATH 环境变量中。

常用的测试框架详情请参见[单元测试][unit-testing]。

## 编辑 test.exw 文件

刚开始学习时，我们鼓励你这样做。除开头的一两个测试外，在其余每个测试前都插入 `--`。保存测试文件。
先编写代码通过第一个或前两个测试。然后逐步启用更多测试，直到全部通过。

[unit-testing]: http://phix.x10.mx/docs/html/unit_test.htm
