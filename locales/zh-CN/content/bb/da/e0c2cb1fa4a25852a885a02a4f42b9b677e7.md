# 说明

你的任务是给一块已完成的花田中的空格填上花的数量。花园本身是一块矩形棋盘，棋盘上的格子要么是空格（`' '`），要么是一朵花（`'*'`）。

对每个空格，数出与它相邻的花的数量（横向、纵向、斜向）。如果空格周围没有相邻的花，就保持为空。否则，用相邻花的数量替换它。

例如，你可能会拿到下面这样一块 5 x 4 的棋盘（为了在屏幕上显示，这里的空白位置用“·”表示）：

```text
·*·*·
··*··
··*··
·····
```

你的代码应该把它转换成这样：

```text
1*3*1
13*31
·2*2·
·111·
```
