# 测试

要运行测试，需要为给定的练习组装并运行测试运行器文件，可以通过 MARS 模拟器的图形界面，也可以在命令行中完成。要在命令行中运行，请先指定运行器文件，再指定你的实现，如下所示：

    java -jar /path/to/mars.jar nc runner.mips impl.mips

要在图形界面中运行，请取消最后一行的注释，把实现 `include` 到 "runner.mips" 的末尾。

测试运行器要么以 “success” 消息正常结束，要么在某个测试失败时中止，并给出消息，说明是哪份输入导致该测试失败。
