# 提示

这个练习测试的是可以被多个线程安全访问的可变状态。Scala 提供了多种保护可变状态的方式。对于熟悉 Java 并发的开发者来说，Scala 可以利用 Java 的并发支持，比如 Java 的 synchronized 块。

## 常见的坑

在 Scala 中，实现可变状态有两种方式：使用 “var”，或者使用可变对象。这里有两个常见错误：

- 不要使用既是可变对象的 “var”。二者选其一就够了，不要同时使用。
- 不要把 “var” 或可变对象暴露给外部世界。要把它们设为 “private”，并在把可变对象作为值返回之前，先把它改成不可变的。
