# 简介

在表意直接、地道的 Elixir 代码里，模式匹配的使用占主导地位。你可能还记得，`=/2` 被描述为匹配运算符，而不是赋值运算符。使用匹配运算符时，如果左边的模式与右边匹配，左边所有的变量都会被绑定，并返回右边的值。如果没有匹配，就会抛出`MatchError`。

```elixir
2 = 2
# => 2
# Literals can be matched if they are the same

2 = 3
# => ** (MatchError) no match of right hand side value: 3

{_, denominator} = Float.ratio(0.25)
# => {1, 4}
# the variable `denominator` is bound to the value 4
```

记住，匹配是从右边向左边发生的。

在最后一个例子里，如果模式匹配中不需要某个变量，可以通过引用`_`把它丢掉。任何以`_`开头的变量都不会被运行时跟踪。

## 命名函数中的模式匹配

在定义多个函数子句时，模式匹配也是一个很有用的工具。模式匹配可以用在函数的形参上，由此决定调用哪个函数子句：从文件顶部开始向下，直到第一个匹配的子句。变量可以在函数头中绑定，并在函数体中使用。

```elixir
defmodule Example do
  def named_function(:a = variable_a) do
    {variable_a, 1}
  end

  def named_function(:b = variable_b) do
    {variable_b, 2}
  end
end

Example.named_function(:a)
# => {:a, 1}

Example.named_function(:b)
# => {:b, 2}

Example.named_function(:c)
# => ** (FunctionClauseError) no function clause matching in Example.named_function/1
```
