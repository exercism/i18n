# 补充说明

~~~~exercism/note

Python 由于[全局解释器锁][GIL]，并不支持“真正的”并发。
虽然为 [Python 自由线程][free-threading] 开发支持的工作正在进行中，但它目前仍处于实验阶段。
而 [multiprocessing][multiprocessing-module] 和 [threading][threading-module] 这类标准库方案，用当前的 track 工具很难实现。


因此，本练习暂时搁置了并发要求。
账户操作在单线程上依次进行，也不会运行并发或“竞态条件”相关的测试。


如果你想尝试自由线程或并发测试，请在[本地][testing-locally]进行。
如需了解如何在本地下载和完成练习，我们推荐使用 [Exercism CLI][exercism-cli]。

[exercism-cli]: https://exercism.org/cli-walkthrough
[free-threading]: https://docs.python.org/3/howto/free-threading-python.html
[GIL]: https://realpython.com/python-gil/
[multiprocessing-module]: https://docs.python.org/3/library/multiprocessing.html#sharing-state-between-processes
[testing-locally]: https://exercism.org/docs/tracks/python/tests
[threading-module]: https://docs.python.org/3/library/threading.html#module-threading
~~~~

<br>

## 异常消息

有时需要[抛出异常](https://docs.python.org/3/tutorial/errors.html#raising-exceptions)。这样做时，应始终包含一条**有意义的错误消息**，说明错误的来源。这能让代码更易读，也对调试大有帮助。如果你知道错误来源属于某种特定类型，可以选择抛出某个[内置错误类型](https://docs.python.org/3/library/exceptions.html#base-classes)，但仍应包含一条有意义的说明。

本练习要求你使用 [raise 语句](https://docs.python.org/3/reference/simple_stmts.html#the-raise-statement)，在账户已打开或未打开，或者取款或存款金额不正确时“抛出” `ValueError`。只有同时 `raise` 该 `exception` 并附上消息，测试才会通过。

要为 `ValueError` 附上消息，请把消息作为实参传给 `exception` 类型：


```python
# account is not open
raise ValueError('account not open')

# account is already open
raise ValueError('account already open')

# incorrect withdrawal/deposit amount
raise ValueError('amount must be greater than 0')

# withdrawal is too big
raise ValueError('amount must be less than balance')
```
