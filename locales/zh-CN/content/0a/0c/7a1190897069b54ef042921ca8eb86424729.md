# 补充说明

## 一些提示

对于`filelike_objects_are_closed_on_exception`函数，`filelike_object`会是测试套件中定义的自定义`FileLike`类的实例。
这个类实现了以下方法：

- `open`和`close`，用于显式打开和关闭。
- `__enter__`和`__exit__`，用于隐式打开和关闭。
- `do_something`，可能会抛出`Exception`，也可能不会。
