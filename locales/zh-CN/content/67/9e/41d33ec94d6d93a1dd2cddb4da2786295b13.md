# 关于

在 Haxe 中，可以用*继承*来定义类的层次结构，它允许派生类（`Car`）继承父类（`Vehicle`）的行为和数据。

在父类中，你可以：

- 可以被重写的方法
- 必须由派生类实现的抽象方法

```haxe
abstract class Vehicle {
  public function new() {}

  // Can be overridden
  public function drive() {
    // ...
  }

  // Must be implemented
  public abstract function getSpeed():Int;
}

class Car extends Vehicle {
  public function new() {
    super(); // Runs the parent class' constructor
  }

  public override function drive() {
    // ...
  }

  public function getSpeed():Int {
    // ...
  }
}
```

如果父类有构造函数，可以使用`super()`调用它：

```haxe
abstract class Vehicle {
  var wheels:Int;

  public function new(wheels:Int) {
    this.wheels = wheels
  }
}

class Car extends Vehicle {
  var color:String;

  public function new(wheels:Int, color:String) {
    super(wheels);
    this.color = color;
  }
}

```
