# 关于

Clojure 中的数字包括：

- 整数：小数点后面没有数字的数（即整数）。例如 `-6`、`0`、`1`、`25`、`976` 和 `500000`。
- 浮点数：小数点后面有零个或多个数字的数。例如 `-2.4`、`0.1`、`3.14`、`16.984025` 和 `1024.0`。

两种常见的数值类型是`int`和`float`。`int` 是 32 位整数，`float` 是 64 位浮点数。

算术运算通过标准的算术运算符进行。可以使用标准的数值比较运算符，以及相等（`=`）和不等（`<>`）运算符来比较数字。
