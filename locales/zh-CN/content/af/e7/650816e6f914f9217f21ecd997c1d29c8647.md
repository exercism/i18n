## 通用

* 双向链表没有清晰的归属层级，因此要么使用 unsafe，要么就得借助像 `Rc` 这样的共享所有权抽象。后者的开销在本例中并不必要。

* 关于如何正确使用`unsafe {}`，请参阅 [Rustonomicon](https://doc.rust-lang.org/nomicon/)。

* 有几个函数需要在不同方向（朝前或朝后）上实现相似的行为。尽量不要重复共用的代码路径。
