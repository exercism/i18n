# 用`grep`还是不用`grep`，这是一个问题

## AWK 轨道对要求的调整

你不需要实现完整的命令行解析：标志会以空格分隔的字符串形式提供，拆开很容易。
```sh
gawk -f grep.awk -v flags="l v" -v pattern="hello" file1.txt file2.txt
```

和真正的 grep 一样，当_没有找到任何匹配_时，你的程序会返回**非零退出状态**。
