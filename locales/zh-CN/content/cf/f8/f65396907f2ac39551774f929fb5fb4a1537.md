# 简介

在这个练习中，你需要按条件执行逻辑。在 C# 中，最常见的做法是使用 `if/else` 语句：

```csharp
int x = 6;

if (x == 5)
{
    // Execute logic if x equals 5
}
else if (x > 7)
{
    // Execute logic if x greater than 7
}
else
{
    // Execute logic in all other cases
}
```

`if` 语句的条件必须是 `bool` 类型。C# 里不存在_真值_的概念。
