# 提示

## 1. 从日志行中获取消息

- 提取_第一个_冒号后面的文本。
  - 可以使用`index/1`和切片语法。
  - 也可以尝试使用正则表达式函数。
- 使用提供的`trim`函数。

## 2. 从日志行中获取日志级别

- 提取第一对括号内的文本。
- 将结果以小写形式输出。

## 3. 重新格式化日志行

- 复用已有的函数。
- 有两种做法：使用字符串插值或字符串拼接。
