移除所有不必要的字面量。

在 JavaScript 中，返回布尔值时不需要 `return` 字面量 `true` 或 `false`。表达式的结果可以直接返回。

```javascript
// instead of
if (some_expression) {
  return true;
} else {
  return false;
}

// ...in JavaScript, these can be returned directly
return some_expression;
```
