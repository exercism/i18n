# 形参

[comment]: # (当形参没有被使用时，就会报这个错误)
[comment]: # (有些形参，比如`_ as x`，是无法使用的)
[comment]: # (需要 1 个参数：)
[comment]: # (- definition：带有未使用值的代码片段)

下面标出的形参没有被用到。
你可以把它删除，也可以在它能派上用场的地方使用它。

```
%{definition}
```
