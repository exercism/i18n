# 简介

如果一个类实现了 `IDisposable` 接口，那么当不再需要某个实例时，就必须调用它的 `Dispose()` 方法。这通常是在 `catch` 或 `finally` 子句中完成的，或者由某个调用者的 `Dispose()` 例程完成。`Dispose()` 提供了一个机会，让非托管资源（例如不受 .NET 运行时管理的操作系统对象）得以释放，并让托管资源的内部状态得以重置。
