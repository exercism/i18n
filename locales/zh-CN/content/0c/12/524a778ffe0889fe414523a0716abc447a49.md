# 说明补充

## 单词

为每颗行星各声明一个符号：
`mercury`、`venus`、`earth`、`mars`、`jupiter`、`saturn`、
`uranus`、`neptune`。

- `on-planet ( seconds planet -- years )`，把出生以来的秒数换算成在给定行星上的年数，换算时使用 `earth-year-seconds`（`31557600.0`）和每颗行星的公转周期作为换算比例。
