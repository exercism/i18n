# 简介

Elixir 的`atom`类型表示一个固定常量。atom 的值就是它自己的名字。这让我们能以类型安全的方式与数据交互。atom 可以这样定义：

```elixir
# All atoms are preceded with a ':' then follow with alphanumeric snake-cased characters
variable = :an_atom
```

_atom_ 在内部由查找表中的一个整数表示，这些整数是自动设置的。无法更改这个内部值。
