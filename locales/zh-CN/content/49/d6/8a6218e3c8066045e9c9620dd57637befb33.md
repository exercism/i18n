# 提示

## 通用

- 生成随机数时，Erlang 的`:rand.uniform`或`Enum.random`都是不错的起点。
- 可以用`Integer.pow`求一个数的幂，用`rem`求模。
- 这两种方法在处理超大整数时都不太好用（也说不上快）。密码学通常使用的整数比 1024 位还大。Erlang 的`:crypto`模块里有一个很好用的函数，可以求幂运算的模，尤其适合处理特大的整数，不过你可能需要用`:binary`来解码结果。
