# 说明补充

## 实现

测试要求有一个接收数组的构造函数。你的自定义集合实现内部可以使用其他数据结构，但为了让测试通过，你可能需要实现一个外层构造函数，它恰好接收一个数组。

某些方法有等价的 unicode 运算符写法。例如，`intersect(CustomSet([1, 2, 3, 4]), CustomSet([]))`等同于`CustomSet([1, 2, 3, 4]) ∩ CustomSet([])`。
