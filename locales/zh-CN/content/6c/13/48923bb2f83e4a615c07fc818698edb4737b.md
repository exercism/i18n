# 使用字符串内插，而不是 string.Format

在 C# 中，更常见的做法是使用[字符串内插](https://csharp.net-tutorials.com/operators/the-string-interpolation-operator/)，而不是用`string.Format`动态构建字符串。

注意，字符串内插只是一种编译器技巧，又称语法糖。也就是说，当字符串内插表达式被编译时，编译器实际上会把它转换成一次`string.Format`调用。因此，使用字符串内插带来的好处纯属视觉上的。
