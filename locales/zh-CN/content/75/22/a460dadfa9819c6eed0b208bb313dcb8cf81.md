# 补充说明

## 实现

在 Racket 中，本练习使用已排序的[向量][vectors]而不是数组，因为向量支持常数时间访问其中的元素。

和其他几个 Lisp 语言一样，当某个元素不在向量中时，预期返回的是[布尔字面量 `#f`][booleans]，而不是抛出异常。

[vectors]: https://docs.racket-lang.org/guide/vectors.html
[booleans]: https://docs.racket-lang.org/reference/booleans.html
