# 提示

## 1. 定义`with_retry`函数

- 可以用`case`表达式对结果进行模式匹配。

## 2. 定义`record_timing`函数

- 即使`experiment`函数返回`Error`值，也应该调用`time_logger`函数。

## 3. 定义`run_experiment`函数

- 可以在`use`表达式中使用 [`result.try` 函数][result-try]，一旦结果是`Error`值就停止。

[result-try]: https://hexdocs.pm/gleam_stdlib/gleam/result.html#try
