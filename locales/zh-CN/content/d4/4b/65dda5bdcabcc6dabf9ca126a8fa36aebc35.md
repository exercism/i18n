# 说明补充

## 实现

本练习中的函数接受一个数组，其中包含一个或多个数字，每个数字代表一次“游戏得分”。

玩家的游戏得分可以从以下位置读取：

| `scores`   |
|---|
| `scores[0]` |
| `scores[1]` |
| ... |
| `scores[scores_len - 1]` |

`personal_top_three()` 应当把玩家的最高得分写入以下位置：

| `output`   |
|---|
| `output[0]` |
| ... |
