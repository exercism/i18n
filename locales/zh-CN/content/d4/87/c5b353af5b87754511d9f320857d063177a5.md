# 提示

## 1. 确定调制一杯果汁需要多长时间

- Julia 没有 `switch` 语句，这与其他许多语言不同。
- `if`……`elsefif`……`else` 语法已在条件语句概念中讲过。

## 2. 补充青柠角供应

- `while` 循环可以一直运行，直到你有足够的青柠角。
- `popfirst!()` 函数已在向量概念中讲过。

## 3. 列出队列中调制每个订单所需的时间

- 使用 `for`……`in` 可以轻松遍历向量。
- 构建新向量可以使用 `push!()`，这已在向量概念中讲过。
- 在教学大纲的后面，我们会探索在 Julia 中实现这一点的各种其他方式。

## 4. 完成轮班

- 参见任务 2。
