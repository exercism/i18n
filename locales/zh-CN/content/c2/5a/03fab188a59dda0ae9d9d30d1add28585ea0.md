# 说明

实现`accumulate`操作。它接收一个集合，以及一个要对集合中每个元素执行的操作，返回一个新集合，新集合中的每个元素都是把该操作应用到输入集合中对应元素后的结果。

给定下面这个数字集合：

- 1, 2, 3, 4, 5

以及这个操作：

- 求一个数的平方（`x => x * x`）

你的代码应该能够生成由平方值组成的集合：

- 1, 4, 9, 16, 25

查看测试套件，了解预期的函数签名。

## 限制

别去碰标准库提供的那些 collect/map/fmap/whatchamacallit 功能！请改用其他基本工具，自己把这道题解决掉。
