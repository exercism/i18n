# 显式 return

去掉显式的 `return`，让方法更简洁。

在 Ruby 中，方法会返回最后一个被求值的语句的结果。因此，显式使用 `return` 关键字是不必要的，除非需要在方法中提前返回。

例如，下面的代码会打印 `16`，因为 `multiply` 方法返回了最后一个表达式 `a * b` 的结果。

```ruby
def multiple(a, b)
  a * b
end

print multiple(2, 8)
```
