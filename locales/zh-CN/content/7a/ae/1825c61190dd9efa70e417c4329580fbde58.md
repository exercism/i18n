# 测试

要运行测试套件：
```bash
tclsh exercise.test
```

## 跳过的测试

完成一个练习，意味着让它所有的测试都通过。默认情况下，运行测试时只会执行一个测试（第一个）。这是有意为之，这样你可以专注于让这一个测试通过。它通过之后，你可以注释掉或删除它前面的`skip`命令，来启用下一个测试。

或者，如果你想不管测试是否处于“跳过”状态，运行全部测试：
```bash
RUN_ALL=1 tclsh exercise.test
```
