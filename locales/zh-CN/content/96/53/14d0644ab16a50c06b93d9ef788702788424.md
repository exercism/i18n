# 提示

## 通用

- 对于每个任务，每个函数都应使用提供的实参返回一个函数闭包。

## 1. 平移坐标

- 提供的实参指定了坐标对沿 _x_ 轴和 _y_ 轴的平移量。

## 2. 缩放坐标

- 提供的实参指定了坐标对在 _x_ 轴和 _y_ 轴上的缩放量。

## 3. 组合变换函数

- 第一次变换的结果是一个数组，但变换函数接受两个数字实参。你需要从数组中获取这些值。
- 记住，函数执行的顺序很重要。

## 4. 保存函数的结果

- 对于这个函数，你只需记忆化最后一次变换的结果。
- 为了返回最后一次变换的结果，你需要检查输入的实参是否相同。
