# 说明

实现一个基本的响应式系统。

响应式编程是一种编程范式，关注值之间如何相互计算，让一个值的变化能自动传播到其他值，就像电子表格那样。

实现一个基本的响应式系统，其中既有值可设置的单元格（“输入”单元格），也有值由其他单元格计算得出的单元格（“计算”单元格）。
实现相应的更新机制：输入值发生变化时，各值会传播开来，最终达到新的稳定系统状态。

此外，计算单元格还应支持注册变更通知回调。
当单元格在新的稳定状态下的值不同于上一个稳定状态时，调用该单元格的回调。
