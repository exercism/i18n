# 使用`rand.uniform`而不是`rand.uniform_real`

在`random_stardate`函数中，使用`:rand.uniform`而不是`:rand.uniform_real`。这两个函数有个细微的区别：`:rand.uniform`返回的值大于等于 0 且小于 1，而`:rand.uniform_real`始终返回大于 0 且小于 1 的值。`random_stardate`的要求是返回一个介于 41000.0（含）和 42000.0（不含）之间的值，这一点用`:rand.uniform_real`无法做到。
