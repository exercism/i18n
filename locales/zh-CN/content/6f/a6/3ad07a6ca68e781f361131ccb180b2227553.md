# 简介

## 字符串

Java 中的`String`是一个对象，它把不可变的文本表示为一串 Unicode 字符（字母、数字、标点符号等）。
双引号用于定义`String`实例：

```java
String fruit = "Apple";
```

通过调用字符串的方法即可对字符串进行操作。
字符串一旦构造出来，它的值就永远不会改变。
任何看似会修改字符串的方法，实际上都会返回一个新的字符串。
`String`类提供了一些_静态_方法，用于转换字符串。
