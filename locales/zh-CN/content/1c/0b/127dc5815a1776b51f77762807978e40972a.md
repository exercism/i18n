# 简介

Clojure 的顺序解构语法是一种简洁的方式，可以从向量中提取值，并将其分别赋值给不同的变量。

在这个例子中，`number-of-moons`向量中的每个值都被赋值给对应的行星：

```clojure
(def number-of-moons [0 2 14])

(let [[venus mars neptune] number-of-moons]
  neptune)

;=> 14
```
