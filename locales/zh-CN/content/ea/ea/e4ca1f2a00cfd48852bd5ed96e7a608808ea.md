# 简介

`switch`语句会对一个表达式求值，然后运行第一个与其值匹配的`case`。相比冗长的`if ... else if`链，它通常是更简洁的写法。
例如：

```go
operatingSystem := "windows"

if operatingSystem == "windows" {
    // do something if the operating system is windows
} else if operatingSystem == "linux" {
    // do something if the operating system is linux
} else if operatingSystem == "macos" {
    // do something if the operating system is macos
} else {
    // do something if the operating system is none of the above
}
```

可以改写成：

```go
operatingSystem := "windows"

switch operatingSystem {
case "windows":
    // do something if the operating system is windows
case "linux":
    // do something if the operating system is linux
case "macos":
    // do something if the operating system is macos
default:
    // do something if the operating system is none of the above
}
```

可以用逗号把多个值归进同一个`case`。只要表达式的值等于其中任意一个，这个`case`就会匹配：

```go
switch operatingSystem {
case "windows", "linux":
    // do something if the operating system is windows or linux
case "macos":
    // do something if the operating system is macos
default:
    // do something if the operating system is none of the above
}
```

`switch`后面的表达式也可以整个省略。这时每个`case`都包含一个布尔表达式，第一个为 true 的会被运行。

```go
age := 21

switch {
case age > 20 && age < 30:
    // do something if age is between 20 and 30
case age == 10:
    // do something if age is equal to 10
default:
    // do something else for every other case
}
```
