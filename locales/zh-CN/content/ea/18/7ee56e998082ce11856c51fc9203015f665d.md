# 调试

为了帮助调试，你可以使用[`print`或类似的函数][cl-print]将内容写入标准输出，这些内容会显示在测试输出中。

例如，在你正在编写的函数里加入`(print "hello there")`，运行测试时就能看到它（前提是测试会执行这个函数）。

一个小“技巧”：如果你有好几样东西要打印，可以把它们收集到一个数组里再打印出来：`(print (list :this "that" 'the-other))`。

[cl-print]: http://www.lispworks.com/documentation/HyperSpec/Body/f_wr_pr.htm
