# 在 `log_sent_email` 中优先使用 IO.puts

向文件中写入一行时，优先使用`IO.puts/2`而不是`IO.write/2`，因为`IO.puts/2`会自动在末尾加上一个换行符。

```elixir
# preferred
IO.puts(pid, email)

# not preferred
IO.write(pid, email)
IO.write(pid, "\n")
```
