# 简介

Haxe 有两种数字类型：整数和浮点数。

- `Int`：表示整数。
- `Float`：表示双精度 64 位浮点数。

```haxe
var integer:Int = 10;
var floating:Float = 5.4;
```

虽然每个`Int`都可以用在需要`Float`的地方（也就是说，`Int`**可赋值给**`Float`，或**可与**`Float`**统一**），反过来则不成立：把`Float`赋值给`Int`可能会造成精度损失，因此不允许隐式进行。
