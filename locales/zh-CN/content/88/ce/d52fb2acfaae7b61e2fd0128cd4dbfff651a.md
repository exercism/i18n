# 简介

有时候，某段代码需要多次使用。如果是这样，把这段代码放进一个函数里会很方便。函数通常只执行一个特定的操作。在 Rust 中，使用```fn```关键字来定义函数。属于函数的代码总是在大括号之间（即`{}`）。名为`main`的函数很特殊，因为它是程序的入口点。从那个函数里可以调用其他函数。

```rust
fn say_my_name(name: &str) -> &str {
    name
}
```

函数也可以接收形参，比如```say_my_name()```里的```name: &str```。函数也可以返回值，比如上面的`-> &str`。
