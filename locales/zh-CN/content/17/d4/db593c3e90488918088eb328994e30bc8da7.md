# 优先使用显式返回类型

为了防止意外改动，请始终为公开 API 定义 `return` 类型，例如用 `export` 导出的函数：

\`\`\`typescript
  ${'signature'}: <return-type> {}
\`\`\`

TypeScript 非常擅长类型推断，会自动为所有函数推断出 `return` 类型。但意外改动可能会改变推断出的类型，而这是否会在改动发生的位置引发编译错误，却并不确定。加上显式的返回类型，就能让编译在改动发生的位置直接失败。
