# 简介

和大多数编程语言一样，Gleam 也有“副作用”：函数除了返回一个值，还能读取并改变世界的状态。

Gleam 标准库中的`gleam/io`模块提供了一些函数，用于把字符串打印到控制台。

```gleam
io.println("Hello, Joe!")
// Hello, Joe!
// -> Nil
```

其他包可能会提供其他的 IO 函数，比如`simplifile`，这个包提供了读写文件的函数。

```gleam
simplifile.read("favourite-colour.txt")
// -> Ok("Pink\n")
```
