# 说明补充

## 实现

可以使用核心语言特性，通过解构来构建和拆分数组，也可以使用数组字面量`[]`，但不应该使用`Array.prototype`中的任何函数。

为了能测试你提交的解答，请确保实现`forEach`。

```typescript
const list = List.create(1, 2)
list.forEach((item) => console.log(item))
// =>
//    1
//    2
```
