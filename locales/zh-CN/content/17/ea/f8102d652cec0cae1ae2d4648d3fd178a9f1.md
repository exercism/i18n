# 通用

## 1. 定义`TreasureChest`泛型自定义类型

- `TreasureChest`自定义类型只有一个变体。

## 2. 定义`UnlockResult`泛型自定义类型

- `UnlockResult`自定义类型有 2 个变体，分别是`Unlocked`和`WrongPassword`。

## 3. 定义`get_treasure`函数

- 自定义类型同样可以进行模式匹配，从而访问其中的值。
