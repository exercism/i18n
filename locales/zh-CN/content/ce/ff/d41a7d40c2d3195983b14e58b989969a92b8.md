# 关于

Futhark 是一门纯函数式编程语言，专为高性能的数据并行计算而设计。
它的语法和结构与 Haskell、Standard ML 等函数式语言有几分相似，但关注点却不在这里。
Futhark 并不强调丰富的类型系统或极致的表现力，它的目标是生成高效的并行代码。
Futhark 的核心是被称为二阶数组组合子（SOAC）的批量数组操作，比如 `map`、`reduce` 和 `filter`，它们与其他函数式语言中的高阶函数十分相似。
这些 SOAC 以顺序语义定义，却被设计为可编译成并行执行，从而兼顾清晰和性能。
