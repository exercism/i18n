# 指令补充

## 词汇

定义一个`robot`元组，包含槽位`x`、`y`和`direction`。`direction`槽位存放`north`、`east`、`south`或`west`这几个符号中的一个（在本练习的词汇表中声明）。

- `<robot> ( x y direction -- robot )`：构造一个机器人。
- `move ( robot instructions -- robot )`：接受一串由`R`、`L`和`A`组成的指令，并返回更新后的机器人。
