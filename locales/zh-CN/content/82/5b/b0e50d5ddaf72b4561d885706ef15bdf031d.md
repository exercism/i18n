# 简介

在 C# 中，能容纳零个或多个元素的数据结构称为_集合_。**数组**是一种集合，它的大小或长度固定，其中的元素必须都属于同一类型。可以使用下标把元素赋值给数组，也可以用它从数组中取出元素。C# 数组的下标从零开始，也就是说第一个元素的下标始终是零：

```csharp
// Declare array with explicit size (size is 2)
int[] twoInts = new int[2];

// Assign second element by index
twoInts[1] = 8;

// Retrieve the second element by index
twoInts[1] == 8; // => true
```

也可以使用简写方式定义数组，这样既能创建数组，又能设置它的值。由于编译器此时能判断出数组会有多少个元素，长度可以省略：

```csharp
// Three equivalent ways to declare and initialize an array (size is 3)
int[] threeIntsV1 = new int[] { 4, 9, 7 };
int[] threeIntsV2 = new[] { 4, 9, 7 };
int[] threeIntsV3 = { 4, 9, 7 };
```

要操作数组，既可以调用数组实例的方法或属性，也可以使用 `Array` 类中定义的静态方法。
