# 补充说明

## 实现

假设：

1. 零在输出中始终表示为 [0]，而不是 []。
2. 其他任何情况下，输出中都不会出现前导零。
3. 输入中可以包含前导零。
4. 空的输入数字序列视为零，而不是错误。

处理问题情况时，返回一个错误，其 Error() 方法返回以下消息之一：

- input base must be >= 2
- output base must be >= 2
- all digits must satisfy 0 <= d < input base
