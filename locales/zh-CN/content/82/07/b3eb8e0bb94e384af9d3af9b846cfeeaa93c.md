# 练习说明附录

## 输出格式

`solve()`方法应返回一个对象，包含以下属性：

- `moves`：达到目标所需的操作次数（包括装满起始桶），
- `goalBucket`：达到目标水量的那个桶的名称，
- `otherBucket`：另一个桶中的水量。

示例：

```json
{
  "moves": 5,
  "goalBucket": "one",
  "otherBucket": 2
}
```
