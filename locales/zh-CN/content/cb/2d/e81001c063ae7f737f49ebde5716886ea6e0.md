# 附加说明

## 实现自己的数组操作

Rust 有丰富的迭代器系统，用它们来解决这道练习轻而易举。
请尽量在不使用`map()`、`flatten()`、`filter()`、`count()`、`fold()`、`chain()`、`rev()`这类迭代器方法的情况下完成这道练习。
`next()`和`next_back()`这两个方法不算。

## 避免内存分配

这道练习可以在不为解答引入任何额外内存分配的情况下完成。
看看你能不能创建自己的迭代器类型，而不是从集合中创建一个迭代器。
