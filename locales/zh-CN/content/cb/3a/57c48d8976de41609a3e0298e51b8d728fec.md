# 避免返回布尔字面量

与其在条件判断之后返回布尔字面量，不如直接返回这个条件。

示例：

```java
// instead of
if (someExpression) {
  return true;
} else {
  return false;
}

// ... return the expression directly
return someExpression;
```
