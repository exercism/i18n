# 使用 null 合并运算符，而不是 IsNullOrEmpty

与其使用 `string.IsNullOrEmpty` 判断是否应该使用默认名字，不如试试 [null 合并运算符](https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/operators/null-coalescing-operator)，既可以简化代码，也能留意到两者在行为上的差异。
