# 简介

在 Python 中，函数是一等对象，这意味着函数也可以作为实参传给其他函数。
装饰器是[高阶函数][hofunc]：它接受另一个函数作为实参，在扩展或修改其行为之后再将其返回。
装饰器的定义方式与其他函数相同，但使用时写在被装饰函数的上一行，并在名字前加上 `@` 符号（`@my_decorator`）。装饰器虽然可以接受多个实参，但至少要把它正在装饰的那个函数作为实参传入。

[hofunc]:https://en.wikipedia.org/wiki/Higher-order_function
