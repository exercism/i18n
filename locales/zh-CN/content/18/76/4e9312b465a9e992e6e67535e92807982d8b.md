# 补充说明

## 实现说明

按照说明第 1 步的描述，定义一个 recognizeDigit 函数，只不过要让它能识别 0 到 9 这全部十个数字。
形参和返回值随你定，但要让它能当作第 2 步的子程序来用。

第 2 步要定义

func Recognize(string) []string

并用 recognizeDigit 实现它。

这里测试的输入字符串，每一行的开头都有一个 \n，而最后一行的末尾没有 \n。（这样便于写出易读的原始字符串字面量。）

加分项：优雅地处理格式有误的数据。
不完整的单元格该怎么办？
丢掉它？
用空格补齐？
还是用 `"?"` 字符报告出来？
如果第一个字符不是 `\n`，又该怎么办？
