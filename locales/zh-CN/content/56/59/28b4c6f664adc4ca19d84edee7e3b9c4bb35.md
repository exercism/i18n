# 可变参数

Swift 中的[_可变参数_][variadic-parameters]允许把 0 个或多个同类型的值传入函数中的一个形参。要表示这一点，只需在形参的类型标注后面加上`...`。

这些值会自动组合成一个数组，其中每个元素的类型都与可变形参的类型相同。

```swift
func geometricMean(_ numbers: Double...) -> Double {
  var total = 1.0
  for number in numbers {
      total *= number
  }
  return pow(total, 1.0 / Double(numbers.count))
}

geometricMean(1, 2, 3, 4, 5)
// => 2.605171084697352
```

注意，使用可变参数时，Swift 有一个限制。如果函数定义中可变参数之后还有形参，那么紧随可变参数之后的第一个形参_必须_带有实参标签。

[variadic-parameters]: https://docs.swift.org/swift-book/documentation/the-swift-programming-language/functions/#Variadic-Parameters
