# 提示

## 概述

## 1. 记录扑克轮次

- Python 中的数组可以通过多种方式[构造][constructed]。
- 这个函数应该[返回][return]一个`list`。

## 2. 将所有轮次保存在同一个地方

- 像`list`这样的序列类型支持[常见操作][common sequence operations]。
- 这个函数应该[返回][return]一个`list`。

## 3. 查找之前的轮次

- 像`list`这样的序列类型支持一些[常见操作][common sequence operations]。
- 这个函数应该[返回][return]一个`bool`。

## 4. 计算牌面值的平均值

- 要得到平均值，这个函数应该先数出`list`中有多少个元素，再把它们的值相加。然后返回总和除以元素个数。

## 5. 另一种求平均值的方法

- 像`list`这样的序列类型支持一些[常见操作][common sequence operations]。
- 要访问某个元素，可以使用方括号（`<list>[]`）这种写法。
- 记住，从**左侧**数起，`list`的第一个元素的下标是 0。
- 在 Python 中，负下标从**右侧**数起，起始值为 -1。这意味着，你可以用`<list>[-1]`找到`list`的最后一个元素。
- 想一想，你可以如何复用已经实现的那些函数里的代码。

## 6. 更多求平均值的技巧

- 像`list`这样的序列类型已经支持一些[常见操作][common sequence operations]。
- 想一想如何复用你刚刚实现的那些函数里的代码。
- 切片语法支持一个_步长_（`<list>[<start>:<stop>:<step>]`）。

## 7. 奖励轮规则

- 数组是_可变的_。一旦创建了`list`，你就可以随意修改、删除或添加任何类型的元素。
- Python 提供了多种[修改`lists`的方式][ways to modify `lists`]。


[common sequence operations]: https://docs.python.org/3/library/stdtypes.html#sequence-types-list-tuple-range
[constructed]: https://docs.python.org/3/library/stdtypes.html#list
[return]: https://www.w3schools.com/python/ref_keyword_return.asp
[ways to modify `lists`]: https://realpython.com/python-lists-tuples/#lists-are-mutable
