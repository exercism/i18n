# 简介

Common Lisp 允许程序员编写被计算机忽略的“注释”。

单行注释以一个或多个分号（`;`）开头，偶尔你还会看到下面这种写法：

```lisp
(code...) ; => value
```

这里的注释用来指出 Common Lisp 运行完这一行代码后返回的值。

惯用做法是：行末的简短注释用一个分号，代码段上方的较长注释用两个分号，描述函数之类内容的注释用三个分号，而源文件顶部那种当作文件头的注释用四个分号。
