# 优先提取为顶层常量

可以考虑把这个常量提取到顶层作用域：

```javascript
const %{name} = %{value}

// the rest of your code below it
export %{method.signature}
```

只有被`export`导出的函数、类和常量，才能从外部看到和访问。这些常量可以放在同一个文件里，而不会暴露给其他代码。另外，把常量提取出来还能节省内存：当同一个常量被反复使用时，可以避免在函数中重复分配。
