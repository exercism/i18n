# 调试

为了方便调试，你可以利用一点：所有控制台输出都会被捕获，并显示在测试结果窗口中。只要把任意值传给`IO.inspect`，就能把它输出。

`IO.inspect`会原样返回传入的值，因此特别适合用来查看函数调用链中的中间值。

```elixir
"hello"
|> String.upcase()
|> IO.inspect()
|> String.split("", trim: true)
```

注意，出于安全考虑，每个测试的输出在 500 个字符之后会被截断。
