# 提示

## 1. 定义烤箱的预期时间（分钟）

- 你需要把正确的值赋给 `$ExpectedMinutesInOven` 变量。

## 2. 计算剩余的烤箱时间（分钟）

- 你可以用 `-` 运算符把数字相减。

## 3. 计算准备时间（分钟）

- 你可以用 `*` 运算符把数字相乘。

## 4. 计算总工作时间（分钟）

- 你可以调用其他已定义且可能有用的函数，例如`function($arg)`。
- 你可以用 `+` 运算符把数字相加。

## 5. 创建千层面已做好的通知

- 你可以用单引号`'`或双引号`"`把文本括起来，创建一个字符串。
