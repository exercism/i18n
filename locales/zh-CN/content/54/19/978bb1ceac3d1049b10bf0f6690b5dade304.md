# 补充说明

## 词

`smallest`和`largest`各自都返回**两**个栈值：回文值（如果范围内不存在，则为 `f`）和一个因数对数组。

- `smallest ( mn mx -- value factors )`
- `largest  ( mn mx -- value factors )`

`factors`中的每一对都是一个包含 2 个元素的数组 `{ a b }`。如果 `mn > mx`，就抛出字符串 `"min must be <= max"`。
