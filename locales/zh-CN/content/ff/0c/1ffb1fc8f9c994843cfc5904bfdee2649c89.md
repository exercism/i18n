# 补充说明

## SQLite 专用说明

要填充`results`表，有四个“属性”需要考虑：

* **scores**：你需要汇总每个`game_id`的所有分数，并把它们存成一个以逗号分隔的字符串。
  分数的顺序必须与插入`scores`表时的顺序一致。
* **latest**：你需要找出最后插入`scores`表的那条分数。
* **personalBest**：找出每个`game_id`的最高分。
* **personalTopThree**：找出每个`game_id`最高的三个分数，把它们汇总成一个以逗号分隔的字符串，并按降序排列。
