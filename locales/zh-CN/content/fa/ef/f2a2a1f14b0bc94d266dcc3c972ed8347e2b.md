# 简介

守卫用于阻止 Elixir 调用函数，判断依据是守卫函数对参数的求值结果。守卫以 `when` 关键字开头，后面跟一个布尔表达式。守卫函数是一类特殊的函数，它们必须满足以下要求：

- 是纯函数，不能改变任何全局状态。
- 返回严格的`true`或`false`值。

常见的守卫列表可以在 [Elixir 文档][kernel-guards] 中找到，其中包括类型检查、基本算术运算、比较，以及严格布尔运算符。

[kernel-guards]: https://hexdocs.pm/elixir/Kernel.html#guards
