# 简介

PHP 允许在其他函数内部声明函数。这些函数不需要命名，但可以赋值给变量，甚至可以直接传给另一个需要函数作为形参的函数。

```php
function onlyOdds(array $numbers): array
{
    return array_filter($numbers, function($num) {
        return $num % 2 === 1;
    });
}
```
