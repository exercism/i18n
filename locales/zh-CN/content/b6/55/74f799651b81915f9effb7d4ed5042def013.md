# 提示

## 1. 自我介绍

- 使用[`option.unwrap`函数][unwrap]从 Option 中获取值，如果没有值，则返回默认值。

## 2. 实现复活机制

- 可以使用`gleam/option`模块中的`Some`和`None`构造器来创建 Option 值。

## 3. 实现施法机制

- 使用[`int.max`函数][max]来确保生命值不会低于 0。

[max]: https://hexdocs.pm/gleam_stdlib/gleam/int.html#max
