# 介绍

## 类型别名

在 Gleam 中，可以用类型别名给某个写起来很麻烦的已有类型取一个方便的名字。

```gleam
pub type Headers =
  List(#(String, String))

pub fn html_headers() -> Headers {
  [
    #("content-type", "text/html"),
    #("x-frame-options", "DENY"),
  ]
}
```

用`pub type`写出的别名可以在定义它的模块之外使用。如果省略`pub`，别名就是私有的，无法在其他模块中引用。
