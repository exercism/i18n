# 提示

## 1. 将位右移回去

- 右移有两种运算符，但只有一种总是会补入 0。

## 2. 设置一些位

- 有一种按位运算符会在两个值对应的位都为 1 时，把结果中的这一位设为 1。

## 3. 翻转特定的位

- 有一种按位运算会翻转掩码中为 1 的那些位。

## 4. 清除特定的位

- 有一种按位运算会清除掩码中为 0 的那些位。
- 但你可能需要把它和另一个运算符组合使用，才能清除掩码中为 1 的那些位。
