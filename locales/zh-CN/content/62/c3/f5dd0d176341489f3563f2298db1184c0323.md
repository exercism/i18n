# 说明附录

## 实现

在 Pharo 中，类是对象，可以有实例方法和类方法。不过，与 HelloWorld 不同，TwoFer 的测试调用的是实例方法。
类方法通常用于构造新对象（就像其他语言中的构造函数）。

谈到方法时，有一点值得了解：没有形参的方法称为一元方法；而接受多个形参、每个形参由一个以“:”结尾的单词标示的方法，称为关键字方法。

还有第三种方法，即二元方法，它只接受 1 个形参，并用符号作为名称（通常是数学符号，如 +、-、& 等）。
