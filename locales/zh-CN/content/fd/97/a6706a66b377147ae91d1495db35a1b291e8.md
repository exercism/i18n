# 说明

给定一个数字字符串，按它们出现的顺序输出该字符串中所有长度为 `n` 的连续子串。

例如，字符串“49142”包含以下 3 位数的序列：

- “491”
- “914”
- “142”

以及以下 4 位数的序列：

- “4914”
- “9142”

如果你从一个 5 位数的字符串里要一个 6 位数的序列，那不管得到什么，都是你自找的。

注意，这些序列只要求在输入中占据_相邻的位置_，数字本身不必_在数值上连续_。
