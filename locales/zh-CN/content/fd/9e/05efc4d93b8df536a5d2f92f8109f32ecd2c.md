# 简介

如果你想用 Raspberry Pi 做点什么，多半会用到_电阻器_。
和之前的 `Resistor Color Duo`、`Resistor Color Trio` 练习一样，你需要把电阻的色环转换成人类可读的标签。

- 每个电阻都有一个阻值。
- 电阻很小，小到如果把阻值印在上面，会很难看清。
  为了解决这个问题，制造商会把用颜色编码的色环印到电阻上，来表示它们的阻值。
- 每个色环都相当于数字的一位。
  例如，如果印上一个棕色色环（值为 1），后面跟着一个绿色色环（值为 5），表示的数字就是 15。
