# 有损的 rune 到 byte 转换

这里把`rune`转换成`byte`是安全的，因为输入字符串只包含 ASCII 字符。
不过，Go 的`runes`可能由多个`bytes`组成，处理非 ASCII 字符时就可能出问题。

想了解更多，请阅读 Go 博客上的 [Strings, bytes, runes and characters in Go](https://blog.golang.org/strings)。

想实际验证一下，可以添加一个包含非 ASCII 字符的测试。
