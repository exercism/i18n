# 提示

## 1. 将数字分类

* 函数`evenp`或`oddp`可用于判断一个数字是偶数还是奇数。
* 函数`append`可用于在数组开头添加一个元素。
* 函数`car`和`cdr`可用于获取一对中的第一个或第二个元素。

## 2. 将大量数字分类

* `reduce`的第一个形参是一个函数，该函数接受两个实参：当前累积值和要处理的元素。
* `reduce`接受一个`:initial-value`形参，其值将作为传给该函数的第一个累积值。
