# 指令补充

覆盖传入的电话号码字符串。
如果电话号码无效，就把它替换为空字符串。

## 寄存器

| 寄存器 | 用途 | 类型 | 说明 |
| -------- | ------------ | ------- | -------------------------------------- |
| `$a0`    | 输入/输出 | 地址 | 以空字符结尾的字符串，内容为电话号码 |
| `$t0-9`  | 临时 | 任意 | 用于临时存储 |
