# 简介

Haxe 支持字符串插值：用单引号（`'`）把字符串括起来，再用美元符号（`$`）触发插值。

还可以用`${expr}`嵌入完整的表达式，其中`expr`是任意合法的 Haxe 表达式。

```haxe
var x = 10;
trace('They have $x apples, of which ${x - 4} are red.');
// They have 10 apples of which 6 are red.
```
