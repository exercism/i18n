# 提示

## 1. 生成编号

- 这个函数应该返回另一个函数。
- 返回的函数需要访问`day`形参。

## 2. 用密码保护找回的机密计划

- 返回的函数只有一个形参。
- 字符串可以使用[相等性][equality]来比较。

## 3. 找出密码

- 调用小兵的那个函数，并传入另一个函数，由它来完成算法中我们负责的那部分。

[equality]: https://kotlinlang.org/docs/equality.html#structural-equality