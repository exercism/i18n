# 简介

## 扩展方法

扩展方法让你能向现有类型添加方法，而无需创建新的派生类型、重新编译或以其他方式修改原始类型。它们定义为静态方法，但使用实例方法语法调用。它们的第一个形参前面带有`this`修饰符，用于指定该方法所作用的类型。扩展方法在命名空间级别进入作用域。

```csharp
public static int WordCount(this string str)
{
    return str.Split().Length;
}

"Hello World".WordCount();
// => 2
```
