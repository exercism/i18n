# 说明补充

## 输出格式

`treeFromTraversals`函数应当返回一个包含 3 个属性的对象：

- `value`当前节点的值
- `left` 一个包含该节点左子节点的对象
- `right` 一个包含该节点右子节点的对象

```javascript
{
    value: 'a',
    left: { value: 'i', left: {}, right: {} },
    right: { value: 'x', left: {}, right: {} },
}
```

如果树为空，应返回一个空对象。
