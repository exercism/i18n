# 提示

## 概述

- 点`(x, y)`到靶心`(0, 0)`的距离可以用`sqrt(x * x + y * y)`算出来。
- Elixir 没有内置的平方根函数，不过可以使用 Erlang 提供的`:math.sqrt/1`函数。
- 另一种做法是，把`x`提高到`1/2`次幂来求它的平方根。也就是说，`def sqrt(x), do: Float.pow(x, 0.5)`。注意`Float.pow/2`只接受浮点数实参，整数需要显式转换，例如写成`1.0 * x`。
