# 简介

在 Gleam 中，`Nil` 是一种只有一个值的类型，这个值也叫 `Nil`。它与其他语言中的 `void` 类似，当函数没有更合适的值可以返回时，就会用到它。

```gleam
io.println("Hello, Joe!")
// -> Nil
```

Gleam 中的值不像某些其他语言那样是“可 nil”或“可空”的。只有当值的类型是 `Nil` 时，这个值才能是 `Nil`；而任何其他类型的值都永远不可能是 `Nil`。
