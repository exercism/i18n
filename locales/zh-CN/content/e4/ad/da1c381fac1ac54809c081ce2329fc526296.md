# 意外的装箱类型

使用`${'literal.type'}`，而不是装箱类型`${'boxed.type'}`。在 TypeScript 中，`Object`、`Number`、`Boolean` 和 `String`这些类型指的是非原始的装箱对象，但它们在 TypeScript 或 JavaScript 代码中几乎从不被恰当使用。阅读更多关于装箱的内容，[点这里](https://en.wikipedia.org/wiki/Object_type_(object-oriented_programming)#Boxing)。
