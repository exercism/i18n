# 简介

## 字符

**字符编码**是一种具体的约定，用来把自然语言中的文本元素表示成一串字节。
这里的“文本元素”可以是自然语言中有意义的任何单位，例如字母、空格、标点、表情符号、表意文字等。

NASM（The Netwide Assembler，本 track 使用的汇编器）支持使用**ASCII**定义数据。

NASM 中的字符常量最长可达 8 个字节，可以用单引号（`'`）、双引号（`"`）或反引号（`` ` ``）定义：

```x86asm
mov rax, '0' ; this copies the ASCII character '0' (decimal value 48)
mov rcx, "z" ; this copies the ASCII character 'z' (decimal value 122)
mov rdx, ` ` ; this copies the ASCII character ' ' (decimal value 32)
```

特殊字符必须写在反引号里，并用`\`转义：

```x86asm
mov rax, `\n` ; this copies the ASCII character newline (decimal value 10)
mov rcx, `\t` ; this copies the ASCII character tab (decimal value 9)
```

注意，在 x86-64 中，任何字符编码都没有作为独立类型的内置支持。
汇编中的数据就是一串字节，把这些字节解释为字符、数字还是别的什么，都由程序员自己决定。

例如，ASCII 字符 '0' 是一个值为 48 的单字节。
因此，往一个字节里存入 48，和存入 ASCII 字符 '0' 完全是同一回事：

```x86asm
mov al, '0' ; al has the value 48
mov cl, 48  ; cl has the value '0'
; al and cl now have the same value
```

出于同样的原因，操作字符就是直接变换这些字节中的值：

```x86asm
mov al, 'A' ; al now has the value 'A', which is 65 in ASCII
add al, 32 ; al now has the value 97, which is 'a' in ASCII
; so, summing 32 to an uppercase ASCII letter makes it lowercase

mov al, 5 ; al now has the value 5
add al, 48 ; al now has the value 53, which is '5' in ASCII
; so, summing 48 (equivalently, '0') transforms a number between 0 and 9 into an ASCII digit
```

## 字符串

**字符串**是一串字符。
在汇编中，它通常用数组来实现。

在 NASM 中，可以像声明任何数组那样声明字符串：

```x86asm
section .data
    string1 db "h", "e", "l", "l", "o"
```

字符串与其他数组的一个区别是，NASM 支持在声明字符串时不用逗号（`,`）分隔各个值。
也可以在值之间穿插使用逗号：

```x86asm
section .data
    string2 db "hello"
    string3 db "h", "ello"
```

注意，正如前面提到的，字符就是字节（在 Unicode 编码中则是一串字节）：

```x86asm
section .data
    string1 db "hi"
    string2 db "h", "i"
    string3 db 104, 105
; string1, string2 and string3 are all equivalent
```

由于每个 ASCII 字符只占一个字节，所以在更宽的尺寸中，后面的字节都是`0`。
例如，把`'a'`声明为 qword（8 字节）时，第一个字节是`'a'`（`97`），其余七个字节全是`0`。

因此，下面这几种写法完全等价：

```x86asm
section .data
    ; in all of the following, "hello" takes 5 bytes
    string1 dw 'hello'         ; 3 words (2-byte) - 5 bytes for "hello" + 0
    string2 dw 'he', "ll", `o` ; 3 words (2-byte) - 5 bytes for "hello" + 0
    string3 db `hello`, 0      ; 6 bytes - 5 bytes for "hello" + 0
```

~~~~exercism/caution
在汇编中，字符串不会自动以 ASCII 的 NUL 字符（值为`0`）结尾。

在与 C 之类的高级语言交互时，程序员需要自己负责在字符串末尾加上正确的终止符（如果有的话）。
~~~~

## 字符串指令

既然字符串就是数组，你就可以像操作任何数组那样操作它们，也就是显式地解引用内存：

```x86asm
section .data
    hello_msg db "hello", 0

section .text
fn:
    lea rdi, [rel hello_msg]
    mov al, byte [rdi]       ; al == 'h'
    mov cl, byte [rdi + 4]   ; cl == 'o'
    ret
```

除此之外，还有一些指令可以在内存中的字节序列里加载、移动和存储值。
虽然它们被称为_字符串指令_，但其实可以用于内存中的任何字节序列，也就是任何数组。

最重要的三条字符串指令是`lods`、`stos`和`movs`：

| 指令 | 作用                                                                                                      |
| ----------- | ----------------------------------------------------------------------------------------------------------------- |
| `lods`      | 从`rsi`指示的内存位置加载一个值到`rax`                                                |
| `stos`      | 把`rax`中的值存入`rdi`指示的内存位置                                               |
| `movs`      | 从`rsi`指示的内存位置复制一个值，存入`rdi`指示的内存位置    |

注意，这些指令_不_接受任何显式操作数。
它们的操作数是隐式的。

一条经验法则是：

1. 当源操作数是内存位置时，它的地址应存放在`rsi`中（`s`代表 source）。
2. 当目标操作数是内存位置时，它的地址则应存放在`rdi`中（`d`代表 destination）。
3. 如果其中一个操作数不是内存位置，那它就是`rax`寄存器。

注意，这些指令会根据自己所执行的具体操作，自动解引用内存地址。

它们都可以在不同的尺寸下工作。
在末尾添加一个后缀来表明尺寸：

| 后缀 | 含义 | 尺寸    |
| :----: | ------- | :------ |
|  `b`   | 字节    | 1 字节  |
|  `w`   | 字    | 2 字节 |
|  `d`   | 双字   | 4 字节 |
|  `q`   | 四字   | 8 字节 |

例如：

```x86asm
movsb ; copies one byte from the memory location whose address is in `rsi` and stores it into the memory location whose address is in `rdi`
lodsq ; copies one qword (8 bytes) from the memory location whose address is in `rsi` and stores it in `rax`
stosw ; stores one word (2 bytes) from `ax` into the memory location whose address is in `rdi`
```

它们还会按操作的大小更新所用到的地址。
这种更新取决于**方向标志（DF）**的值：

- 如果`DF`被清除，地址会按操作的大小_增加_。
- 如果`DF`被置位，地址则会_减少_相同的量。

System V ABI 规定，方向标志在函数入口处必须被清除。
所以，_默认行为是按照地址递增的顺序读写内存_。

`cld`指令可以用来清除方向标志，而`std`指令用于置位：

```x86asm
lea r8, [rdi]
lea r9, [rsi]

movsq ; both rdi and rsi increase (because `DF` is cleared by default) by 8 (`q` for qword)
; so now:
; rdi == r8 + 8
; rsi == r9 + 8

std ; sets `DF`
lodsw ; rsi decreases (because `DF` is set) by 2 (`w` for word)
; so now:
; rdi == r8 + 8
; rsi == r9 + 8 - 2 == r9 + 6

cld ; clears `DF`
stosd ; rdi increases (because `DF` is cleared) by 4 (`d` for dword)
; so now:
; rdi == r8 + 8 + 4 == r8 + 12
; rsi == r9 + 6
```

~~~~exercism/caution
按照 System V ABI，调用方期望被调用的函数返回时`DF`已被清除。
如果你设置了`DF`，一定要在`ret`之前把它清除。
~~~~

### Rep

除了上面这些，**rep**指令并不直接与内存交互。
相反，它可以作为前缀加在前面提到的其他指令上，让这些指令_重复_执行。

加上`rep`前缀后，字符串指令会重复执行，次数等于`rcx`中的值。
每执行一次，`rcx`中的值就减`1`。
注意，`rcx`中的值必须在指令使用之前设置好：

```x86asm
mov rcx, 4
rep movsq ; this repeats `movsq` 4 times
          ; it copies 4 * 8 == 32 bytes from a memory location indicated by `rsi` to a memory location indicated by `rdi`
```

~~~~exercism/advanced
在现代 CPU 上，搬移大块数据时，`rep movs`和`rep stos`通常是很快的选择。
不过，其他字符串指令，甚至是不带`rep`的`movs`和`stos`，通常都比普通的循环慢。

关于这个话题更深入的讲解，可以在[这本参考书的第 16.9 章“String instructions”][agner-optimization-guide]中找到。

[agner-optimization-guide]: https://www.agner.org/optimize/optimizing_assembly.pdf#%5B%7B%22num%22%3A261%2C%22gen%22%3A0%7D%2C%7B%22name%22%3A%22XYZ%22%7D%2C87%2C785%2C0%5D
~~~~
