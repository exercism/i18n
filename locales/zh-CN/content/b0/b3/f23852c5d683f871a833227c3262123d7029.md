# 忽略

## CoffeeScript 专属说明

BinarySearchTree 实例必须有 3 个属性：`data`、`left`和`right`。

实例必须有以下方法：

* `insert`，用于向树中新增一条数据。这会在树的某个位置新增一个“叶子”节点。
* `each`，接受一个“回调”实参。也就是一个函数，它会被_对树中的每一项数据_调用，而且**按顺序**调用。

这个练习是练习**递归**的好地方。
