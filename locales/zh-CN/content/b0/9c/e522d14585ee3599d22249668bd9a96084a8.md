# 意外的展开参数

定义一个名为 `%{splat-arg.name}`、类型为 `%{parameter.type}` 的形参，而不是使用 `...%{splat-arg.name}: %{parameter.type}[]`。

[剩余参数 / 展开参数](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/rest_parameters)在函数不限定要接收多少个值的时候很好用，但在这个例子里，函数会被传入多少个值已经明确规定。
