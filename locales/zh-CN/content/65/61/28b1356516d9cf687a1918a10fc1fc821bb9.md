# 将字段转换为 const

考虑把 `%{name}` 字段转换为 `const`，因为这个值本来就不应该改变。使用常量不仅更常见，`const` 和 `field` 之间还有一些细微的差别，[这篇 StackOverflow 帖子](https://stackoverflow.com/questions/755685/static-readonly-vs-const#755693)里有详细说明。
