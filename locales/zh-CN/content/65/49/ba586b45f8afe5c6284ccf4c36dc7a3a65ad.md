# 说明附录

## 词

定义一个`triangle`元组，包含槽位`a`、`b`和`c`。

- `<triangle> ( a b c -- triangle )`：根据三条边长构造一个三角形。
- `equilateral? ( triangle -- ? )`
- `isosceles? ( triangle -- ? )`
- `scalene? ( triangle -- ? )`

对于违反三角不等式或边长非正的三角形，每个谓词都返回`f`。
