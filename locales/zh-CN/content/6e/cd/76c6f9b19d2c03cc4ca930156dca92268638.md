# 简介

我们经常需要编写能够根据条件分支的代码。在 Elixir 中实现这一点有很多种方式，其中最简单的一种就是使用`cond/1`。

最简单的用法是：`cond`会执行第一个求值为`true`的分支，分支可以有一个或多个：

```elixir
cond do
  x > 10 -> :this_might_be_the_way
  y < 7 -> :or_that_might_be_the_way
  true -> :this_is_the_default_way
end
```

如果没有任何分支求值为`true`，运行时会抛出错误。
