移除所有对`Boolean(...)`的显式调用。

在 JavaScript 中，表达式的结果不需要_强制转换_为特定类型。比如，二元表达式的结果可以直接返回，不需要再调用一次把它变成`boolean`。

```javascript
// instead of
return Boolean(some_expression);

// ...In JavaScript, in general, values don't need to be explicitly changed
return some_expression;
```
