# 调试

测试失败时，会显示一条消息，说明哪里出了问题，以及是哪个输入导致的。你还可以利用控制台输出也会一并显示这一点。你可以用下面的方式写入控制台输出：

```crystal
puts "Debug message"
```

或

```crystal
p "Debug message"
```

`puts`会把值转换成字符串，再打印到控制台输出。
`p`会把值打印到控制台输出，而不转换成字符串。
