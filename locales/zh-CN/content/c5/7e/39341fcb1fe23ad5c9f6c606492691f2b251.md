# 简介

`for`循环是 PHP 中最复杂的一种循环。它的行为与`C`、`Java`和`JavaScript`中的`for`循环一样。它的基本形式是：

```php
for (expr1; expr2; expr3) {
  // statements
}
```

第一个表达式（_expr1_）在循环开始前求值。第二个表达式（_expr2_）在每次迭代前求值，如果它的值为 _truthy_，就执行这些语句。这些语句执行完后，再对第三个表达式（_expr3_）求值。
