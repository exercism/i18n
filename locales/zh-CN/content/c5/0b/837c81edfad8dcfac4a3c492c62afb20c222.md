# 使用函数捕获

[comment]: # (需要 2 个参数：)
[comment]: # (- expected：修正后的函数)
[comment]: # (- actual：学生使用的不正确的函数)

把一个已有的函数作为实参传递时，要用捕获记法来引用它，而不是新建一个匿名函数。

```elixir
# preferred: using capture notation

%{expected}

# discouraged: creating new anonymous functions

%{actual}
```
