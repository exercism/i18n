# 提示

## 通用

- 处理布尔值时，有三个逻辑运算符 '!`(logical NOT), '||`（逻辑或）和`&&`（逻辑与）。
- 一个表达式里可以组合多个运算符。

## 1. 检查能否发起快速攻击

- 逻辑非运算符（`!`）可以放在表达式前面，用来把表达式的值取反。

## 2. 检查能否侦查这个小组

- 逻辑运算符通常用于判断两个或多个表达式是 true 还是非 true。

## 3. 检查能否向囚犯发出信号

- 逻辑运算符按优先级顺序执行（从高到低）：`!`、`&&`、`||`。
