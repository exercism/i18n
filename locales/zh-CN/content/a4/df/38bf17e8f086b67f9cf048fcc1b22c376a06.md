# 介绍

特殊形式`import`让你无需使用模块名，就能调用外部模块中的函数。

导入整个外部模块可能会与现有的本地函数产生冲突。为了避免这种情况，有两个选项可用：`:except`和`:only`。它们都接受一个关键字列表，其中键是函数名，值是函数的元数。

通常，导入会添加在模块定义的开头。

```elixir
defmodule Square do
  import Integer, only: [pow: 2]

  def area(a), do: pow(a, 2)
end
```
