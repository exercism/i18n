# SQLite 专用说明

- **matrix** 列包含输入，即一个 JSON 编码的整数数组组成的数组。
- **result** 列应该包含输出，即鞍点列表。
  输出应该是一个 JSON 编码的对象数组，每个对象有两个键：row 和 column。
  例如，`[{"row": 2, "column": 1}]`。

## JSON 文档

SQLite 的 JSON 函数参见 [JSON 函数与运算符][json-docs]。

[json-docs]: https://www.sqlite.org/json1.html
