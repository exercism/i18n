# 关于

Clojure 的顺序解构语法可以简洁地从向量中提取值，并赋值给不同的变量。

在这个例子中，`number-of-moons`向量里的每个值都会被赋值给对应的行星：

```clojure
(def number-of-moons [0 2 14])

(let [[venus mars neptune] number-of-moons]
  neptune)

;=> 14
```
