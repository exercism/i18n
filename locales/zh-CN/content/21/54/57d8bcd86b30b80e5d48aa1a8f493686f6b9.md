# 指令附录

## 单词

`convert ( rows -- str )` 接收以字符串数组形式给出的网格，每行一个字符串，并把识别出的数字作为单个字符串返回。输出中，多行单元格用逗号（`,`）连接。

## 错误

对于格式错误的输入，请抛出以下字符串之一（必须原样）：

- `"Number of input lines is not a multiple of four"`
- `"Number of input columns is not a multiple of three"`
