# 关于

Gleam 中的常量用于给值命名。使用`const`关键字定义。

```gleam
pub const pi: Float = 3.14159

pub fn circle_area(radius: Float) -> Float {
  radius *. radius *. pi
}
```

用`pub`关键字定义后，模块常量就可以从其他模块访问；否则只能在定义它的那个模块内部访问。
