# 简介

Swift 有很多不同的类型，不过每个类型都归入两大类之一：_值类型_和_引用类型_。在 Swift 中，结构体是值类型，类是引用类型。

值类型和引用类型最主要的不同在于：把值类型赋给变量或常量，或者传给函数时，实际赋值或传入的是该值的一份副本，而不是那个值本身。修改结构体的一份副本，不会影响其他副本。而对于引用类型，赋值或传入的是对该值的引用，因此两个不同的名字可以指向同一个类的实例。这样一来，用另一个名字访问时，就可能改动原来的实例。
