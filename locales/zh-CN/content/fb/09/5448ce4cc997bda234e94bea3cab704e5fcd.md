# 附加说明

## 提示

要解决这道练习，你需要实现下面两个函数：

- `largestPalindrome`
- `smallestPalindrome`

这两个函数接收因数的下限和上限，返回一个数对 `(value, [(factor1, factor2)])`，其中包含回文以及它可能的因数对。

如果不确定类型，可以使用给出的签名，但别让它们限制你的创造力。

因数列表中可以有重复项，因数的顺序也不重要。

寻找较小或较大的回文时，可以考虑换一种稍有差别的算法。
