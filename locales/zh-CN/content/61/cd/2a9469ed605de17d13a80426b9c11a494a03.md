# 简介

[Nil][nil-dictionary] 是一个英文单词，意思是“无”或“零”。
在 Ruby 中，`nil`用于表示对象的_缺失_。
在其他编程语言中，`null`或`none`值可能起到类似的作用。

```ruby
# I do not have a favorite color
favorite_color = nil
```

在首次遇到任何实例变量时，Ruby 都会把它的默认值设为 `nil`，直到它被另行赋值。

[nil-dictionary]: https://www.merriam-webster.com/dictionary/nil
