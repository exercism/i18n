# 关于

Gleam 中的`Nil`是一种只有一个值的类型，这个值也叫`Nil`。它类似于其他语言中的`void`，用于函数没有更合适的值可返回时。

```gleam
io.println("Hello, Joe!")
// -> Nil
```

Gleam 中的值不像某些其他语言那样可以为“nil”或“可空”。一个值只有在它的类型是`Nil`时才能是`Nil`，而任何其他类型的值都永远不能是`Nil`。
