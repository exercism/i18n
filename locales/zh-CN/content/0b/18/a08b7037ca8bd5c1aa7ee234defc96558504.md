# 关于

如果函数执行的_最后_一件事是调用自身，那它就是[尾递归][recursion-tc]。

每次调用函数时，一个包含局部变量、实参等内容的_栈帧_会被压入[函数调用栈][call-stack]的顶部。函数返回时，这个栈帧会从栈中移除。

尾递归函数可以启用[尾调用优化][tail-call-optimization]。这种优化允许下一个函数调用复用上一个栈帧，因为可以保证前一个函数不再需要它了。这缓解了函数调用栈溢出的问题：当函数调用栈上的帧太多，没有内存再创建新帧时，就会发生溢出。

- 在 Elixir 中实现尾递归时，由于数据不可变的规则，通常需要一个累加器。

  - 累加器是一个随递归调用一起传递的变量，它保存递归函数_目前为止_的状态，直到基本情况返回最终结果。

    ```elixir
    # Count the length of a list without an accumulator
    def count([]), do: 0
    def count([_head | tail]), do: 1 + count(tail)

    # Count the length of a list with an accumulator
    def count(list), do: do_count(list, 0)

    defp do_count([], count), do: count
    defp do_count([_head | tail], count), do: do_count(tail, count + 1)
    ```

- 处理列表时，请记住它们是用单链表实现的：头部插入操作非常快，但追加操作非常慢。

  ```elixir
  # Reverse a list with an accumulator
  def reverse(list), do: do_reverse(list, [])

  defp do_reverse([], acc), do: acc

  defp do_reverse([head | tail], acc) do
    do_reverse(tail, [head | acc])
  end
  ```

[call-stack]: https://en.wikipedia.org/wiki/Call_stack
[recursion-tc]: https://en.wikipedia.org/wiki/Tail_call
[tail-call-optimization]: https://dino.codes/posts/tail-call-optimization-in-elixir/
