# 提示

## 1. 定义认可

- 可以使用`pub type Name { Variant1 }`语法来定义自定义类型。

## 4. 定义活动

- 可以使用`pub type Name { Variant1(SomeType) }`语法来定义包含数据的自定义类型。

## 5. 给活动评分

- 可以使用 case 表达式对自定义类型的变体进行模式匹配。
- 如果想给模式添加一个额外的条件，可以使用`if`关键字为模式加上守卫。
