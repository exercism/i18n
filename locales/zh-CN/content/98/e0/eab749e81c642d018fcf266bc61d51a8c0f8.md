# 说明补充

## 返回的字符串

`classify ( n -- str )`返回以下字符串之一：`"perfect"`、`"abundant"`或`"deficient"`。

## 错误

当输入为零或负数时，抛出以下字符串：

```
"Classification is only possible for positive integers."
```

（逐字保留，包括末尾的句点）。
