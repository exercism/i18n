# 关于

自 `PHP 8.0.0` 起，**联合类型声明**可以接受多个不同类型的值，而不再局限于单一类型。联合类型的定义使用竖线（`|`）分隔各个类型。

```php
function add(int|float $a, int|float $b): int|float
{
    return $a + $b;
}
```

## 细节

- `null` 类型可以通过 `?Type` 或 `Type1|Type2|null` 声明为_可空_类型。
- 重复声明或冗余的类型会导致编译时错误。
- `void` 不能用在联合类型声明中。
