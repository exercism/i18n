# 对字符串使用 fmt 占位符

在 Go 中，人们倾向于使用类型专属的占位符，而不是通用的 `%%v`。这样做有两个原因。首先，更具体的占位符通常有额外选项。例如，用于 `float` 值的格式 `%%f` 还可以指定精度：`%%.2f`。第二个原因是 `%%v` 接受任何值类型。如果变量类型在重构期间发生变化，`%%v` 会打印该类型的默认值，这可能不是期望的行为。类型专属的占位符会让 linter 报错，从而在错误被部署到生产环境之前将其捕获。
