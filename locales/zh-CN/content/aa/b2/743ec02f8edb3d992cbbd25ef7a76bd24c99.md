# 提示

## 通用

- 使用`enum-iterator` crate 就可以遍历枚举。查看[文档](https://docs.rs/enum-iterator/1.2.0/enum_iterator/)了解具体做法。

- 使用`int-enum` crate 可以把整数转换成枚举。详情见[文档](https://docs.rs/int-enum/0.5.0/int_enum/)。

- 提供`Debug` trait 是因为你多半会用到它。

- 你需要扩展颜色枚举上由`derive`派生的 trait 列表。
