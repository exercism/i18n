# 简介

映射是指把一个函数应用到序列中的每个元素上，从而生成一个新的序列。

借助`mapcar`函数，可以轻松地对数组完成这一操作。
假设有一个函数`double`，它把实参乘以`2`：

```lisp
(mapcar #'double '(1 2 3 4)) ; => (2 4 6 8)
```

注意，第一个实参是要调用的函数。`#'`是一种特殊的引用语法，用来指定函数。

`double`先被第一个元素调用，然后被第二个元素调用，以此类推。
这些结果被收集到一个数组中，而这个数组就是该表达式的求值结果。
