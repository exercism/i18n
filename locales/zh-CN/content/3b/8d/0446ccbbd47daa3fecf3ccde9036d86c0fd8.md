# 说明附录

## 实现

实现`RestAPI`类的`get`和`post`方法。

你只需要编写处理函数，不需要实现真正的 HTTP 服务器。你可以用一个内存中的对象来模拟数据库，其中保存着所有已存储的用户。`RestAPI`类的构造函数应当接受这个数据库的实例作为实参（如果没有传入实参，则为其设置一个默认值）。

在这份实现中，如果是`GET`请求，载荷应当作为 URL 的一部分，并像查询参数一样处理，例如`/users?users=Adam,Bob`。
