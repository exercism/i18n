实现`list_t`类型和以下过程：

- `new`返回一个空链表。
- `push`在链表末尾插入一个具有指定值的元素。
- `pop`从链表末尾移除一个元素，并返回该元素的值。
- `unshift`在链表开头插入一个具有指定值的元素。
- `shift`从链表开头移除一个元素，并返回该元素的值。
- `length`返回链表中的元素个数。
- `delete`从链表中移除第一个具有指定值的元素。
- `destroy`清空链表。

当动态分配的内存不再需要时，记得释放它。
