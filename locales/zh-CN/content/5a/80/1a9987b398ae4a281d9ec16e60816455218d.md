# 简介

Elixir 在 _Bitwise 模块_ 中提供了许多用于处理位的函数。

- `band/2`：按位与
- `bsl/2`：按位左移
- `bsr/2`：按位右移
- `bxor/2`：按位异或
- `bor/2`：按位或
- `bnot/1`：按位取反

下面是一个使用位运算函数的例子：

```elixir
Bitwise.bsl(1, 3)
# => 8
```

所有位运算函数都只能处理整数。

如果你使用的是 Elixir 1.9 或更低的版本，就需要在模块定义的开头调用 `require Bitwise`，才能使用 _Bitwise_ 模块。
