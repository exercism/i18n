## 通用

- 定义字符串时，可以使用双引号`"`。
- 拼接字符串时，可以使用`+`运算符。
- 可以使用`#{}`来插值字符串。

## 1. 欢迎新客人参加派对

- 使用`+`运算符，可以把一个字符串和另一个字符串拼接起来。

## 2. 欢迎今天过生日的新客人参加派对

- 可以使用`to_s`方法把数字转换为字符串。

## 3. 指路

- 要对数字取整，可以使用`round`方法。
- 要获取字符串中的某个字符，可以使用`[]`方法，它接受一个整数作为下标。
