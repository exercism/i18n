# 附加说明

## 性能提示

所有输入和输出都是 ASCII。
Rust 的`String`和`&str`是 utf8，所以，虽然你可能以为`"Hello".chars()`很简单，实际上它必须逐个检查每个字符，看看它的长度是 1、2、3 还是 4 个`u8`。
如果我们知道一个`&str`是 ASCII，就可以调用`.as_bytes()`，并把底层数据当作`&[u8]`（字节切片）来引用。
对一个 ASCII 字节切片进行迭代要快得多，因为不涉及码点，每个 ASCII 字节的长度都是 1 个`u8`。

你能在不克隆输入的情况下完成这个挑战吗？
