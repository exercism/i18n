# 优先使用 const，而不是 let 和 var

与其写成`${'kind'} ${'name'}`，不如考虑使用`const`。

`const`声明能防止变量被意外重新赋值，这既带来了安全性，也减轻了阅读代码的人的认知负担。[这篇文章](https://medium.com/javascript-scene/javascript-es6-var-let-or-const-ba58b8dcde75)解释了这三者的区别。
