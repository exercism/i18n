# 简介

三元运算符允许在表达式中定义`if`条件，而不是在语句块中。这呼应了函数式编程的做法，通常能让代码更具表达力，也更不容易出错。

三元运算符组合了 3 个表达式：一个条件，后面是在条件为真时求值并返回的表达式（即`if`部分，由`?`引入），以及一个在条件为假时求值并返回的表达式（即`else`部分，由`:`引入）。

```haxe
trace(true ? "Haxe" : "Not Haxe"); // Haxe
trace(1 == 2 ? "Something is wrong" : "That's obvious"); // That's obvious
```
