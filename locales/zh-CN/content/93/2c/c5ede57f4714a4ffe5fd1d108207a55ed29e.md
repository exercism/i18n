# 简介

`null`值用于表示值的_缺失_。**可空**类型是一种允许变量取`null`值的类型。要让任何类型可空，可以用`Null<T>`把它包起来：

```haxe
var a:Int = null; // Error
var b:Null<Int> = null; // Allowed
```

要检查一个值当前是否为`null`，只需使用一个`if`表达式：

```haxe
var a:Null<Int> = 10;

if (a != null) {
  trace("a is not null"); // a is not null
}
```
