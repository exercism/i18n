# 附加说明

## Zipper 的行为

Zipper 提供了一种以函数式方式在`Node`树中移动的方法。

所有 Zipper 调用都应返回一个_新的_ Zipper 对象。
如果 Zipper 没有改变值（即没有调用任何`Set*`方法），就应沿用同一棵底层树。
调用`Set*`方法后，新的 Zipper 应该有一棵新的树，而原来的树保持不变。

把焦点切换到无效`Node`的 Zipper 调用应返回错误。
