# 说明补充

## 提示

[`Ordering`](https://hackage.haskell.org/package/base/docs/Data-Ord.html#t:Ordering) 类型有三个构造子：`LT`（“小于”）、`EQ`（“等于”）和 `GT`（“大于”）。它们可以用来表示子列表的排序关系：`Just LT` 表示“子列表”，依此类推；而 `Nothing` 则表示既不是子列表、超列表，也不与之相等。
