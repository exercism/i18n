# 关于

**用户定义**函数使用`function`关键字创建。关键字后面必须跟一个函数名和一列实参。

```php
function alarm($hour, $minute, $second)
{
    return 'Alarm!';
}
```

函数可以在文件中定义之前就被调用，因为它们会被提升到作用域的顶部。

## 命名

PHP 中的函数名遵循其他标识符的规则：必须以字母或下划线开头，之后可以跟任意数量的字母、数字或下划线。
