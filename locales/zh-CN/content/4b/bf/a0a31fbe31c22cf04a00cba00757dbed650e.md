# 补充说明

## 实现

实现 `ZebraPuzzle` 类中的 `waterDrinker` 和 `zebraOwner` 方法。这两个方法都必须返回一个字符串，其值就是斑马谜题中“谁喝水？”和“谁养斑马？”这两个问题的答案。每个答案都是某位居民的国籍之一：Englishman、Spaniard、Ukrainian、Norwegian 或 Japanese。

当然，只要偷看一下测试程序里的预期解答，你完全可以随手写出两个只有一条语句的函数。但我们的目标是设计一个算法，利用谜题给出的事实和约束，自己推导出这两个正确答案。
