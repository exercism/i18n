# 测试

PureScript 练习的测试套件可以在练习目录下用 spago 工具运行：

    cd <exercism-workspace>/purescript/<exercise name>
    spago test

其中 <exercism-workspace> 是 CLI 的工作区目录。

例如：

    cd ~/exercism/purescript/accumulate
    spago test
