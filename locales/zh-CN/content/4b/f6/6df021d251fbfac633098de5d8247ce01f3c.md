# 可变参数

Swift 中的[_可变参数_][variadic-parameters]允许把零个或多个相同类型的值传入函数中的同一个形参。
具体写法是在形参的类型标注后面加上`...`。

这些值会被自动归入一个数组，数组中元素的类型与可变参数的类型相同。

```swift
func geometricMean(_ numbers: Double...) -> Double {
  var total = 1.0
  for number in numbers {
      total *= number
  }
  return pow(total, 1.0 / Double(numbers.count))
}

geometricMean(1, 2, 3, 4, 5)
// Returns 2.605171084697352
```

使用可变参数时，Swift 有两条限制：

1. Swift 只允许函数有一个可变参数。Swift 已经决定在未来的版本中取消这一限制，但目前它仍然有效。
2. 如果函数定义中在可变参数后面还有形参，那么紧跟在可变参数后面的第一个形参_必须_带有实参标签。

[variadic-parameters]: https://docs.swift.org/swift-book/documentation/the-swift-programming-language/functions/#Variadic-Parameters
