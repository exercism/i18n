# 关于

PureScript 是一门纯函数式、静态类型的编程语言，具备全局类型推断。

**函数式**意味着函数是一等值。
函数是进行抽象的重要而强大的工具。
在 PureScript 中，计算被建模为对由函数应用构成的表达式求值，而不是执行一串指令。

**纯函数式**意味着可以判断一个计算是否有副作用，也就是说，如果某个计算修改了全局变量、从输入读取数据、向套接字写入数据等，都会体现在这个计算的类型中。

**静态类型**意味着所有计算都有类型，且这个类型在运行程序之前就已知。

**全局类型推断**意味着编译器无需程序员指定类型，就能推断出整个程序中计算的类型。
