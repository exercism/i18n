# 避免打印和记录日志

避免通过`stdout`和`stderr`打印和记录日志。

通常，在底层包中向`stdout`或`stderr`输出内容并不是个好主意。这应该在上层或`main`包中完成，因为它们拥有必要的上下文，知道该如何处理错误。在底层包中使用`log.Println`或`fmt.Println`无法从外部覆盖。打印和记录日志对调试很有用，但在提交练习或发布包之前应该移除。

有时看起来应该修改函数签名以返回错误。与现实世界不同，这些练习中的函数签名是预先确定的。有讨论空间：返回错误可能终究没有必要，或者练习可能需要调整。
