# 关于

基本的`try .. rescue`概念可以扩展为支持`else`和`after`子句：

- `else`块：
  - 用于对`try`块成功返回的结果进行模式匹配。
- `after`块：
  - 只要程序仍在运行，无论`try`块成功还是出错并被捕获，其中的函数都会被调用。
  - `after`块的结果不会返回给调用方的作用域。

```elixir
try do
  :a
rescue
  _ -> :error
else
  :a -> :success
after
  :some_action
end

# => :success
```
