# trait

trait 规定了一组方法，类型可以实现这组方法。
trait 很适合用来定义应当在多个类型之间共享的方法。
当 trait 与泛型类型一起使用时，定义的方法既能在特定类型上调用，也能在其他数据类型上调用。
抽象地说，借助 _trait 约束_，我们可以规定泛型类型必须共享某些行为。

> **注意：** trait 与其他编程语言中所谓的接口有相似之处，但也有一些差别。

## trait 定义

定义 trait 有助于明确函数签名，而这些签名构成了类型为实现某种结果所能具备的一组行为。
这些方法构成了类型的行为。
如果能在不同类型上调用同样的方法，就说明这些类型具有相同的行为。

```rust
#[derive(Drop)]
pub struct NewsArticle {
    pub headline: ByteArray,
    pub location: ByteArray,
    pub author: ByteArray,
    pub content: ByteArray,
}

// A trait for NewsArticle struct type
pub trait Summary {
    fn summarize(self: @NewsArticle) -> ByteArray;
}
```

根据上面 `Summary` trait 的定义，`summarize` 方法是一种只能由 `NewsArticle` 结构体类型调用的行为。
如果在其他类型的实例上调用，它会抛出编译错误。
用 `pub` 关键字声明 trait，会把这个 crate 中的该 trait 暴露给其他也想使用它的 crate。
还要注意，在 trait 里声明方法签名后要加分号。

trait 可以带一个泛型参数来定义，这样函数就能接收任意数据类型的参数。
这为函数签名带来了一种灵活性，使它们能够处理各种数据类型。
就下面的 `Summary` trait 来说，它被重新设计成适用于任意数据类型，这样当在它们上面调用 `summarize` 方法时，会返回一个 ByteArray。

```rust
// A trait for a generic type
pub trait Summary<T> {
    fn summarize(self: @T) -> ByteArray;
}
```

一个 trait 的体内可以有多个方法；每个声明的方法签名都是一行，并以分号结尾。

## 为类型实现 trait

定义好构成 trait 的函数签名之后，编译器会强制要求采用该 trait 的类型也定义相应的实现。
下面我们写了一个媒体库 crate `aggregator`，它从 `NewsArticle` 和 `Tweet` 的实例中汇总一些数据。
对于 `NewsArticle` 结构体，我们使用 headline、author 和 location 来返回 `Summary` trait 中 `summarize` 方法的值；而对于 `Tweet` 结构体，我们使用 username 和 content，并假设推文内容有 280 个字符的限制。

```rust
mod aggregator {
    pub trait Summary<T> {
        fn summarize(self: @T) -> ByteArray;
    }

    #[derive(Drop)]
    pub struct NewsArticle {
        pub headline: ByteArray,
        pub location: ByteArray,
        pub author: ByteArray,
        pub content: ByteArray,
    }

    impl NewsArticleSummary of Summary<NewsArticle> {
        fn summarize(self: @NewsArticle) -> ByteArray {
            format!("{} by {} ({})", self.headline, self.author, self.location)
        }
    }

    #[derive(Drop)]
    pub struct Tweet {
        pub username: ByteArray,
        pub content: ByteArray,
        pub reply: bool,
        pub retweet: bool,
    }

    impl TweetSummary of Summary<Tweet> {
        fn summarize(self: @Tweet) -> ByteArray {
            format!("{}: {}", self.username, self.content)
        }
    }
}

use aggregator::{Summary, NewsArticle, Tweet};

fn main() {
    let news = NewsArticle {
        headline: "Cairo has become the most popular language for developers",
        location: "Worldwide",
        author: "Cairo Digger",
        content: "Cairo is a new programming language for zero-knowledge proofs",
    };

    let tweet = Tweet {
        username: "EliBenSasson",
        content: "Crypto is full of short-term maximizing projects. \n @Starknet and @StarkWareLtd are about long-term vision maximization.",
        reply: false,
        retweet: false
    };

    println!("New article available! {}", news.summarize());
    println!("New tweet! {}", tweet.summarize());
}
```

## 默认实现

有时，可以为 trait 的一些方法或全部方法定义默认实现。
这样做既能避免要求每个类型都实现所有方法，也能让代码更易读。
如果某个类型需要特别实现 trait 的某个方法，它可以覆盖默认实现。

```rust
mod aggregator {
    pub trait Summary<T> {
        fn summarize(self: @T) -> ByteArray {
            "(Read more...)"
        }
    }

    #[derive(Drop)]
    pub struct NewsArticle {
        pub headline: ByteArray,
        pub location: ByteArray,
        pub author: ByteArray,
        pub content: ByteArray,
    }

    impl NewsArticleSummary of Summary<NewsArticle> {}

    #[derive(Drop)]
    pub struct Tweet {
        pub username: ByteArray,
        pub content: ByteArray,
        pub reply: bool,
        pub retweet: bool,
    }

    impl TweetSummary of Summary<Tweet> {
        fn summarize(self: @Tweet) -> ByteArray {
            format!("{}: {}", self.username, self.content)
        }
    }
}

use aggregator::{Summary, NewsArticle, Tweet};

fn main() {
    let news = NewsArticle {
        headline: "Cairo has become the most popular language for developers",
        location: "Worldwide",
        author: "Cairo Digger",
        content: "Cairo is a new programming language for zero-knowledge proofs",
    };

    println!("New article available! {}", news.summarize()); // prints "New article available! (Read more...)"


    let tweet = Tweet {
        username: "Alice",
        content: "Cairo is fun and engaging.",
        reply: true,
        retweet: true,
    };
    println!("New tweet from {}", tweet.summarize()); // prints "New tweet from Alice: Cairo is fun and engaging."
}
```

## 如何使用和管理外部 trait

trait 和实现可以定义在外部模块中，并在其他模块里使用，只要正确导入即可。
假设我们有一个 `ShapeGeometry` trait，其中的 `boundary` 方法必须为 `Circle` 和 `Rectangle` 两个结构体分别实现。
下面是一个示例，展示 trait 和实现如何跨模块导入、管理和使用。

```rust
// Here T is an alias type which will be provided during implementation
pub trait ShapeGeometry<T> {
    fn boundary(self: T) -> u64;
    fn area(self: T) -> u64;
}

mod rectangle {
    // Importing ShapeGeometry is required to implement this trait for Rectangle
    use super::ShapeGeometry;

    #[derive(Copy, Drop)]
    pub struct Rectangle {
        pub height: u64,
        pub width: u64,
    }

    // Implementation RectangleGeometry passes in <Rectangle>
    // to implement the trait for that type
    impl RectangleGeometry of ShapeGeometry<Rectangle> {
        fn boundary(self: Rectangle) -> u64 {
            2 * (self.height + self.width)
        }
        fn area(self: Rectangle) -> u64 {
            self.height * self.width
        }
    }
}

mod circle {
    // Importing ShapeGeometry is required to implement this trait for Circle
    use super::ShapeGeometry;

    #[derive(Copy, Drop)]
    pub struct Circle {
        pub radius: u64
    }

    // Implementation CircleGeometry passes in <Circle>
    // to implement the imported trait for that type
    impl CircleGeometry of ShapeGeometry<Circle> {
        fn boundary(self: Circle) -> u64 {
            (2 * 314 * self.radius) / 100
        }
        fn area(self: Circle) -> u64 {
            (314 * self.radius * self.radius) / 100
        }
    }
}

use rectangle::Rectangle;
use circle::Circle;

fn main() {
    let rect = Rectangle { height: 5, width: 7 };
    println!("Rectangle area: {}", ShapeGeometry::area(rect)); //35
    println!("Rectangle boundary: {}", ShapeGeometry::boundary(rect)); //24

    let circ = Circle { radius: 5 };
    println!("Circle area: {}", ShapeGeometry::area(circ)); //78
    println!("Circle boundary: {}", ShapeGeometry::boundary(circ)); //31
}
```

`CircleGeometry` 和 `RectangleGeometry` 的实现可见性不需要是公开的，因为 `ShapeGeometry` trait 已被设为公开，编译器应该能轻松找到它。

## impl 别名

实现可以在导入时用 `impl` 关键字起别名。
在为具体类型实例化泛型实现时，这会很有帮助。
我们来为所有实现了 `One` trait 的类型定义一个简单的 `Two` trait 实现：把 `One` 的值加两次，然后返回求和结果。
之后，我们可以暴露只对 `u8` 和 `u128` 类型有效的 `Two` 实现。

```rust
trait Two<T> {
    fn two() -> T;
}

mod one_based {
    pub impl TwoImpl<
        T, +Copy<T>, +Drop<T>, +Add<T>, impl One: core::num::traits::One<T>
    > of super::Two<T> {
        fn two() -> T {
            One::one() + One::one()
        }
    }
}

pub impl U8Two = one_based::TwoImpl<u8>;
pub impl U128Two = one_based::TwoImpl<u128>;
```

泛型实现可以写在私有模块中，而借助 impl 别名，我们可以为这两个具体类型实例化该泛型实现。
使用私有、不对外暴露的泛型实现，好处是可以避免代码在多个文件之间重复。
这也能让公开的 API 保持简单整洁。
