# 关于

在 Go 中，函数是一等值。
这意味着，你能对函数做的事情，和你能对其他任何值做的事情完全一样：把函数赋值给变量、把它们作为实参传给其他函数，甚至从其他函数中返回函数。

下面我们创建两个函数`engGreeting`和`espGreeting`，并把它们赋值给变量`greeting`：

```go
import "fmt"

func engGreeting(name string) string {
	return fmt.Sprintf("Hello %s, nice to meet you!", name)
}

func espGreeting(name string) string {
	return fmt.Sprintf("¡Hola %s, mucho gusto!", name)
}

greeting := engGreeting			// greeting is a variable of type func(string) string
fmt.Println(greeting("Alice"))	// Hello Alice, nice to meet you!

greeting = espGreeting
fmt.Println(greeting("Alice")) 	// ¡Hola Alice, mucho gusto!
```

函数值让我们不仅可以按数据来参数化函数，还可以按行为来参数化。
在下面的例子中，我们通过形参`greetingFunc`把一个行为传给了函数`dialog`：

```go
func dialog(name string, greetingFunc func(string) string) {
	fmt.Println(greetingFunc(name))
	fmt.Println("I'm a dialog bot.")
}

func espGreeting(name string) string {
	return fmt.Sprintf("¡Hola %s, mucho gusto!", name)
}

greeting := espGreeting
dialog("Alice", greeting)
// Output:
// ¡Hola Alice, mucho gusto!
// I'm a dialog bot.
```

未初始化的函数类型变量，其值是`nil`。
因此，调用一个值为`nil`的函数会导致 panic。

```go
var dutchGreeting func(string) string
dutchGreeting("Alice") // panic: call of nil function
```

函数值可以和`nil`比较。
这对于避免程序出现不必要的 panic 很有用。

```go
var dutchGreeting func(string) string
if dutchGreeting != nil {
	dutchGreeting("Alice") // safe to call dutchGreeting
}
```

## 函数类型

之所以能使用函数值，要归功于 Go 中的函数类型。
函数类型表示具有相同形参类型序列和相同返回值类型序列的所有函数的集合。
可以在函数类型之上声明用户定义的类型。
例如，前面例子中的函数`dialog`可以这样改写：

```go
type greetingFunc func(string) string

func dialog(name string, f greetingFunc) {
	fmt.Println(f(name))
	fmt.Println("I'm a dialog bot.")
}
```

## 匿名函数

得益于对一等函数的支持，我们还能使用另一个强大的工具：匿名函数。
匿名函数在使用的地方就地定义，`func`关键字后面不带名字。
这类函数可以访问所在外层函数中的变量。

例如：

```go
func fib() func() int {
	var n1, n2 int

	return func() int {
		if n1 == 0 && n2 == 0 {
			n1 = 1
		} else {
			n1, n2 = n2, n1 + n2
		}
		return n2
	}
}

next := fib()
for i := 0; i < N; i++ {
  fmt.Printf("F%d\t= %4d\n", i, next())
}
```

调用`fib`会声明变量`n1`和`n2`，并返回一个匿名函数；这个匿名函数每次被调用时，都会改变这些变量的值。
匿名函数第 N 次被调用时，返回的是从 0 开始的斐波那契数列的第 N 个数。这个匿名内部函数可以访问外层函数`fib`的局部变量（`n1`和`n2`）。
这是让函数值在多次调用之间保持状态的好办法。
我们说这个匿名函数是变量`n1`和`n2`的闭包。
[闭包][closure]在编程中应用广泛，你可能也会看到其他语言支持闭包。

[closure]: https://en.wikipedia.org/wiki/Closure_(computer_programming)
