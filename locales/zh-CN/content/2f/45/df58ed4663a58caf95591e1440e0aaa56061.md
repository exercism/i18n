# 复用 Describe two params 中的代码

`describe(Character, Destination)` 方法应当复用 `describe(Character, Destination, TravelMethod)` 中实现的逻辑，或者复用 `describe(Character)`、`describe(Destination)`、`describe(TravelMethod)` 这几个单独方法中的逻辑。复用已有的方法有助于让代码更易于维护。