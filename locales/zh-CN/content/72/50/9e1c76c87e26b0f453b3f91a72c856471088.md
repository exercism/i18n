# 说明补充

## 注意

完成这个练习时，Pharo 的字典可能会对你有帮助。
Pharo 有多种创建字典的方式，包括 `Dictionary new: at:put:`，以及使用 `Association` 的字面量语法。
一个 `Association` 看起来像 `key -> value`，可以像这样转换成字典：`{ 'key' -> 'value' } asDictionary`。
请记住，在 Smalltalk 中，所有中缀运算符都是右结合的，而且你可以实时浏览上面提到的任何类。
