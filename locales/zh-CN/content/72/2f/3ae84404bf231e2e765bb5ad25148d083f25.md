# 补充说明

## 实现说明

你将实现一个双向链表。

实现一个 Node，用它保存一个值，以及指向下一个节点和上一个节点的指针。

然后实现一个 LinkedList，它保存对第一个和最后一个节点的引用，并提供类似数组的接口来添加和删除元素：

- `push`（*在尾部插入值*）；
- `pop`（*从尾部移除值*）；
- `shift`（*从头部移除值*）；
- `unshift`（*在头部插入值*）；
- `delete`（*移除第一个匹配的节点*）。

为了让实现保持简单，测试不会覆盖错误情况。具体来说：不会对空链表调用 `pop` 或 `shift`。
