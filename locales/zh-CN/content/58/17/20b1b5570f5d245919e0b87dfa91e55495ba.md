# 意外的必填形参

测试中调用形参`${'parameter.name'}`时，有时会传值，有时不传值，但它_没有标记为可选_，所以测试无法通过。TypeScript 的形参可以用`?`后缀显式标记为可选：

```typescript
function(${'parameter.name'}?: ${'parameter.type'}) {
  //
}
```

或者，也可以为它赋一个默认值：

```typescript
function(${'parameter.name'}: ${'parameter.type'} = <default>) {
  //
}
```
