# 简介

Cairo 中的控制流通过 `if` 表达式、循环等结构，让开发者可以根据条件逻辑或重复性任务来控制程序的执行。
`if` 表达式根据条件的求值结果是 `true` 还是 `false` 来分支，而循环（`loop`、`while`、`for`）则让代码块重复执行，直到满足特定条件。
这些结构对于实现逻辑、迭代，以及简洁地处理多种条件都至关重要，是编写高效、动态的 Cairo 代码必不可少的工具。
