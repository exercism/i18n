# 运行测试

要编译并运行测试，只需在练习目录中运行以下命令：
```bash
$ ponyc
$ ./exercise-name
```

（把`exercise-name`换成练习目录的名字）

编译得到的二进制文件会在它的 Main actor 里运行测试，所以只要运行这个二进制文件，看看测试有没有通过就行了。Pony 会在编译时检查所有代码，所以你可能会发现，在写出必需的代码之前，测试根本编译不过。对于那些编译不过的测试，你可以在每一行开头加上 `//` 把它们注释掉。之后，等你准备着手那个测试时，再取消注释即可。
