# 说明

在这个练习中，你将为一个本地新闻台生成天气报告。

## 生成天气报告

你会从一些待实现的函数和下面这个枚举开始：

```rust
#[derive(Drop, Clone, PartialEq, Debug)]
pub enum WeatherCondition {
    Sunny,
    Rainy,
    Cloudy,
    Stormy
}
```

你需要把天气报告格式化成类似这样：`"Today is <CONDITION> with a temperature of <TEMPERATURE> degrees Celsius."`。
你还需要实现那个输出正确天气状况的函数。

例如，下面的代码片段演示了`get_weather_report`函数在 "rainy" 天气下的预期输出。

```rust
get_weather_report(WeatherCondition::Rainy, "25")
// Returns: "Today is RAINY with a temperature of 25 degrees Celsius."
```

再来看一个 "sunny" 天气的例子：

```rust
get_weather_report(WeatherCondition::Sunny, "30")
// Returns: "Today is SUNNY with a temperature of 30 degrees Celsius."
```
