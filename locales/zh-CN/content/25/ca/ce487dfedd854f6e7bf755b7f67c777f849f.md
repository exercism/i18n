# 关于

剩余参数在 lambda 列表里由 `&rest` 这个 lambda 列表关键字来标记。这个参数会被绑定到一个值上。这个值是一个列表，包含调用方传入、但没有被其他参数消耗掉的所有实参。

虽然多种类型的参数可以与其他类型的参数（可选参数和关键字参数）组合使用，但这可能会带来问题，应当谨慎处理。
参见 Practical Common Lisp 中的[“混合不同类型的参数”][pcl-function]一节。

[pcl-function]: http://www.gigamonkeys.com/book/functions.html
