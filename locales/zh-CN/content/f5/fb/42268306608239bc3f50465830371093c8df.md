# 简介

## 递归

某个事物能够用自身来定义，这种能力称为递归。在 Gleam 中，递归最常见于递归函数，也就是调用自身的函数。

递归函数至少需要一个*基准情况*和一个*递归情况*。*基准情况*不再调用函数，直接返回一个值。*递归情况*会再次调用函数，并修改输入，使其最终能够匹配基准情况。

```gleam
pub fn factorial(x: Int) -> Int {
  case x {
    // Base case
    1 -> 1

    // Recursive case
    _ -> x * factorial(x - 1)
  }
}
```

Gleam 没有专门的循环语法，因此所有循环都通过递归实现。

```gleam
pub fn list_length(list: List(String)) -> Int {
  case list {
    [] -> 0
    [_, ..rest] -> 1 + list_length(rest)
  }
}
```

Gleam 还支持递归自定义类型。递归自定义类型中，有一个或多个变体会在自己包含的数据中引用自身。

```gleam
pub type RussianDoll {
  Child               // Base case
  Mother(RussianDoll) // Recursive case
}
```
```gleam
let very_big_doll = Mother(Mother(Mother(Child)))
let small_doll = Mother(Child)
```
