# 优先使用模板字符串

使用[字符串模板字面量](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Template_literals)来插入动态值，而不是用字符串拼接手动构造字符串：

```javascript
"Hello there \${firstName}, I will give you \${calculateInventory()} apples."
```
