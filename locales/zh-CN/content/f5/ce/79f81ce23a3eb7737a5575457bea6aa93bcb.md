# 介绍

C# 的`char`类型是一个 16 位的量，用来表示文本中最小可寻址的组成部分。多个`char`可以组成一个字符串，比如`"word"`；每个`char`也可以单独处理。它们的字面量使用单引号，例如`'A'`。

C# 的`char`支持 UTF-16 Unicode 编码，因此除了拉丁字符集，世界上使用的几乎所有书写系统都能表示出来，例如希腊字母`'β'`。

有许多内置的库方法可以检查和操作`char`。它们都是`System.Char`类的静态方法。
