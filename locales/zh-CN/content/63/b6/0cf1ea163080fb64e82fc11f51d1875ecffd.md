# 介绍

`Result` 类型让函数可以返回一个值，同时表明以下几件事：
- 操作是成功还是失败
- 成功时，操作得到的结果值
- 失败时，这次失败的原因

## 用法

`Result` 类型是一种泛型类型，包含两种底层类型：
- 操作成功时结果值的类型
- 失败时错误的类型

`Result` 类型也是一种[可辨识联合][discriminated-union]，可能有以下几种情况：
* `Ok <value>`，表示成功的结果
* `Error <reason>`，表示失败

下面的函数演示了如何创建一个 `Result` 值：

```fsharp
let validateName (name: string) : Result<string, string> = 
    match name with
    | null -> Error "Name not found."
    | "" -> Error "Name is empty."
    | _ -> Ok name
```

在这个例子中，`Ok` 的值是一个字符串（给定的名字），`Error` 的值也是一个字符串（错误原因，以人类可读的形式给出）。

## 读取 `Result` 值的内容

考虑下面的类型定义和函数签名：

```
type FileOpenError = 
| NotFound
| AccessDenied
| FileLocked

let openFile (filename: string) : Result<int, FileOpenError> =
```

调用 `openFile` 函数的代码可以用模式匹配来处理成功和失败的情况，如下例所示：

```fsharp
match openFile(filename) with
| Ok handle -> doSomethingWithFile(handle)
| Error NotFound -> printfn $"Error: file {filename} was not found."
| Error AccessDenied -> printfn $"Error: you do not have permission to open the file {filename}."
| Error FileLocked -> printfn $"Error: file {filename} is already in use."
```
