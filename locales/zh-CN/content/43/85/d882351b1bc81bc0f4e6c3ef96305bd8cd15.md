# 简介

扩展方法允许为现有类型添加方法，而无需创建新的派生类型、重新编译或以其他方式修改原始类型。它们被定义为静态方法，但通过实例方法的语法来调用。它们的第一个形参前面带有 `this` 修饰符，用于指明该方法作用于哪种类型，并且这些方法会在命名空间级别进入作用域。

```csharp
public static int WordCount(this string str)
{
    return str.Split().Length;
}

"Hello World".WordCount();
// => 2
```
