# 关于

批处理脚本是一种脚本语言，主要用于在 Windows 命令提示符（cmd.exe）中运行命令。
借助它，用户可以自动执行任务、管理文件，并通过一系列命令行指令控制系统环境。

虽然批处理脚本简单直接，却能处理各种各样的任务，从简单的文件操作到更复杂的自动化流程。

批处理文件使用 `.bat` 或 `.cmd` 扩展名，可以直接由 Windows 命令解释器执行。

批处理脚本的灵活性使它成为强大的工具，适合需要自动执行日常任务或创建自定义命令行工具的系统管理员、开发者和高级用户。
