# 用 rune 的索引访问 bytes

在这个例子中，遍历 `runes` 并用索引来获取单个 `bytes` 是安全的，因为输入全部是 ASCII 字符。
想看看使用非 ASCII 字符时会发生什么，请添加这个测试用例：

```go
{
	s1:          "aüa",
	s2:          "aÃa",
	want:        1,
	expectError: false,
}
```
