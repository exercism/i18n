# 说明

接收一个任意深度的嵌套数组，返回一个完全扁平化后的数组。

注意，某些语言轨道可能会在输入数组中包含类似 null 的值，而这些值的表示方式因轨道而异。
这些值应当从扁平化后的数组中排除。

此外，输入数据的类型可能不同，所包含的类型也可能不同，具体取决于轨道。

详情请查阅测试套件。

## 示例

输入：`[1, [2, 6, null], [[null, [4]], 5]]`

输出：`[1, 2, 6, 4, 5]`
