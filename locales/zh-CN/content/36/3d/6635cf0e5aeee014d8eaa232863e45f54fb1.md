# 说明

在这个练习中，你将编写一些代码，帮助你照着最喜欢的烹饪书做出美味的千层面。

你有四个任务，它们都与烹饪千层面所花的时间有关。

## 1. 定义预期的烤箱时间（分钟）

用 `OvenTime` 常量定义千层面应该在烤箱里烤多少分钟。
根据这本烹饪书，预期的烤箱时间是 40 分钟：

```go
OvenTime
// => 40
```

## 2. 计算剩余的烤箱时间（分钟）

定义 `RemainingOvenTime()` 函数，它接受千层面已经在烤箱里烤过的实际分钟数作为形参，并根据上一个任务中预期的烤箱时间，返回千层面还需要在烤箱里待多少分钟。

```go
func RemainingOvenTime(actual int) int {
    // TODO
}

RemainingOvenTime(30)
// => 10
```

## 3. 计算准备时间（分钟）

定义 `PreparationTime` 函数，它接受你给千层面添加的层数作为形参，并返回你准备千层面花了多少分钟，假设每一层需要 2 分钟准备。

```go
func PreparationTime(numberOfLayers int) int {
    // TODO
}

PreparationTime(2)
// => 4
```

## 4. 计算已用的工作时间（分钟）

定义 `ElapsedTime` 函数，它接受两个形参：第一个形参是你给千层面添加的层数，第二个形参是千层面已经在烤箱里烤了多少分钟。
这个函数应该返回你总共花了多少分钟来烹饪千层面，也就是准备时间的分钟数加上此刻千层面在烤箱里已经历的分钟数。

```go
func ElapsedTime(numberOfLayers, actualMinutesInOven int) int {
    // TODO
}

ElapsedTime(3, 20)
// => 26
```
