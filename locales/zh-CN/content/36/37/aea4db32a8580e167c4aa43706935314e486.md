# 简介

如果形参的类型可以被推断出来，Swift 还允许在闭包定义中省略形参名和关键字`in`。取而代之，可以使用一种特殊的简写语法来引用形参：`$0`指第一个形参，`$1`指第二个形参，依此类推。

```swift
let mean: (Double, Double) -> Double = { $0 + $1 / 2.0 }

["apple", "ball", "carrot"].sorted(by: { $0.count < $1.count })
```
