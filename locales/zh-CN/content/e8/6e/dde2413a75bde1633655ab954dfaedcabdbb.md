# 说明

你的任务是判断一袋糖里是否存在某种颜色，其数量占了整袋的多数。🍬 多数元素指的是在集合中占据超过一半空间的元素。给定一个表示各种颜色的元素数组，返回找到的多数颜色，否则就表示没有找到多数。

对于一个简单的数组`[Red, Red, Blue, Red, Green]`，返回值应该是 Red，因为 Red 在总共 5 个元素中出现了 3 次。但对于数组`[Red, Red, Blue, Green]`，尽管 Red 是最常见的元素，却不存在多数，因为 2/4 并不大于元素总数的一半。
