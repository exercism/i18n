# 介绍

注释让你能在代码中留下说明，而不影响代码的运行。
单行注释以`//`开头，多行注释则用`/*`和`*/`包裹：

```go
// This is a single-line comment.

/*
This is a
multiline comment.
*/
```

注释的用途取决于它们出现的位置。

## 代码注释

代码注释解释的是为什么这样做，而不是做了什么。
当某个决定背后的原因无法从代码中一眼看出时，就可以使用它们：

```go
results = results[1:] // skipping the header row
```

## 文档注释

文档注释紧挨在声明之前，注释与声明之间不留空行。
[`godoc`][godoc]会解析它们来生成包文档。
文档注释应当是一个完整的句子，以标识符的名称开头，以句号结尾。

包注释以包名开头：

```go
// Package kelvin provides tools to convert temperatures to and from Kelvin.
package kelvin
```

函数注释以函数名开头：

```go
// CelsiusFreezingTemp returns an integer value equal to the temperature at which water freezes in degrees Celsius.
func CelsiusFreezingTemp() int {
	return 0
}
```

[godoc]: https://go.dev/blog/godoc
