# 测试

每个练习都提供了单元测试和一个 Make 构建配方，你只需要提供实现。

每个测试文件都会与你的实现链接，生成一个运行测试的控制台可执行文件。运行测试可执行文件时，每个失败的测试都会打印一条消息；只要有测试失败，程序就会返回非零的退出状态。

完成一个练习的步骤：
* 用 Make 创建初始构建
* 对每个单元测试：
  * 删除 `TEST_IGNORE()` 这一行。
  * 解决编译错误，让测试能够失败。
  * 只实现刚好让测试通过的那部分。
  * 重构你的实现，提高可读性、减少重复等。
