# 说明补充

## 单词

`find-fewest-coins ( coins target -- result )`返回一个由硬币面额组成的数组，其和为`target`。`coins`是一个哈希集合（例如`HS{ 1 5 10 25 }`），用`sets`词汇表中的单词（`members`、`in?`等）来操作它。

当`coins`的任何组合都无法凑出`target`时，抛出`cannot-make-change`错误。
