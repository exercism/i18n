# 说明补充

## 提示

你可以把这种数据结构看作 [`Rose Tree`](https://en.wikipedia.org/wiki/Rose_tree)。题目会给你一个表示这种数据结构的数据类型 `'a tree`。

**注意**

- 在本练习中，`Empty` 表示 `null`。
- 嵌套列表用 `List [...]` 表示。
- 输入示例表示为 `List [Elem 1, List [Elem 2, Elem 3, Empty, Elem 4], List [Empty], Elem 5]`。
