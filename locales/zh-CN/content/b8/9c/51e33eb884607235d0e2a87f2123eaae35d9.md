# 提示

- 本练习会用到`format`函数。

## 1. 格式化季度值

- 除非 stream 形参为`nil`，否则`format`总是返回`nil`。
- 可以用`~A`格式化一个值。

## 2. 双季度报告

- 你之前的函数非常适合创建所需的两行内容。
- 可以用`~%`和`~&`创建换行符。

## 3. 人类可读 vs. Lisp 外星人可读

- 你之前的季度格式化函数非常适合创建所需的字符串。
- 可以用`~S`将值格式化为可读形式。
