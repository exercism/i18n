# 简介

向量是一维数组。
它们可以包含不同类型的元素。

## 创建向量

向量可以通过`vector`函数创建，也可以使用它的读取器形式`#()`创建。

```lisp
(vector 1 t "foo" 'b) ; => #(1 t "foo" B)
#(2 nil "bar" a)      ; => #(2 nil "bar" A)
```

## 访问元素

要访问向量中的元素，可以使用`aref`，下标从 0 开始：

```lisp
(aref #(1 2 3) 1) ; => 2
(aref #(a b c) 0) ; => A
```
