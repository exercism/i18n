# 提示

## 1. 提取错误

- 可以使用 `Error(value)` 模式语法对错误进行模式匹配。

## 2. 移除团队前缀

- 可以使用 `"prefix" <> rest` 模式语法对字符串前缀进行匹配。

## 3. 拆分区域和团队

- 可以使用 [`string.split` 函数][split] 按字符拆分字符串。
- 可以使用 `[a, b, c]` 模式语法对数组进行匹配。

[split]: https://hexdocs.pm/gleam_stdlib/gleam/string.html#split
