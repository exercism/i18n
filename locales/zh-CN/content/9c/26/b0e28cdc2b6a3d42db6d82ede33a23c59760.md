# 附加说明

要理解为什么在 Cairo 中操作数组不像在其他编程语言中那样直截了当，需要记住 Cairo 采用的是不可变内存模型：一旦某个内存单元被写入，就永远无法被覆盖，只能从中读取。

该语言在一定程度上对这一特性做了抽象，让开发者可以声明和使用自己的变量，就好像这些变量是可变的。

尽管这让编写代码变得稍微更具挑战性，尤其是使用`Array`类型的代码（包括`ByteArray`，它在底层是一个“字节数组”），但它让编写 _可证明的_ 程序成为可能，这些程序即使在不受信任的机器上也能被放心地执行，因此这一取舍非常值得。
