# 简介

`for`循环可以重复执行一段代码，直到满足某个条件为止。

```csharp
for (int i = 0; i < 5; i++)
{
    System.Console.Write(i);
}

// => 01234
```

`for`循环由四个部分组成：

1. 初始化部分：在进入循环之前执行一次。通常用于定义循环中要用到的变量。
2. 条件：在每次循环迭代之前执行。只要它的求值结果为`true`，循环就会继续执行。
3. 迭代器：在每次循环迭代之后执行。通常用于修改循环变量（常常是自增或自减）。
4. 循环体：每次循环迭代时执行的代码。
