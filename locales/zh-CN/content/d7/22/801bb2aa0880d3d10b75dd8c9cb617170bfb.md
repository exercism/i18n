# 补充说明

## 预留内存

线性内存的第 1024 到 1279 字节保留给输入字符串。

如果需要，可以原地修改输入字符串。

## 输入格式

密码子会拼接成一个字符串传入，不加任何填充。

## 输出格式

把蛋白质序列输出为以空字符结尾的字符串，每个蛋白质后面跟一个换行符。

一个输出示例是 `"Methionine\nPhenylalanine\nSerine\n"`

如果输入无效，输出一个空字符串。
