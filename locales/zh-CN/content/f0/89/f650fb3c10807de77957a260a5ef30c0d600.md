# 提示

## 1. 创建一个预约

- 你需要创建一个新的日期。简介里详细介绍了各种不同的创建方式。
- `Date.now()`返回当前时间的毫秒数
- `Date`有多个 getter 方法，简介里都列了出来，可以用来获取日期的各个组成部分。你能用上其中的一个吗？
- 同样，`Date`也有对应的 setter 方法，可以设置这些组成部分，需要的话还会向“更高”的组成部分进位。

## 2. 把日期转换成时间戳

- 简介里列出了把日期转换成不同类型的不同方法。你能用上其中的一个吗？

## 3. 获取预约的详细信息

- 从日期里提取信息所需的全部内容，简介里都有。

## 4. 用给定的选项更新预约

- 从日期里提取信息所需的全部内容，简介里都有。
- 你可以复用`getAppointmentDetails`

## 5. 获取两个预约之间的可用时间

- 对两个日期做普通的减法，就能得到这两个日期之间的时间戳。
- 你最好先转换成数字，不要依赖类型强制转换。

## 6. 检查预约现在是否有效

- 条件运算符可以帮你判断两个日期之间哪个更大、哪个更小。
