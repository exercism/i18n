# 说明补充

## 实现

Go 源代码字符串（以及单元测试中的字符串）采用 UTF-8 编码。
字符串保存的是字节，而字节表示 rune。
在 Go 中，大多数情况下，一个“代码单元”是一个`byte`，一个“代码点”是一个`rune`。

更多细节请参阅 [Go 中的字符串、字节、rune 和字符][strings]。

[strings]: https://go.dev/blog/strings
