# 提示

## 通用

- 每个例程接收整数，返回的也是整数，所以实参和结果都是`INT`。
- 类型相同的两个实参可以共用一条声明：`ladder_points(wins, draws : INT) : INT`。

## 1. 排名积分

- 把每个结果乘上它对应的分值，然后相加。

## 2. 净胜球

- 相减。就算结果是负数，Sather 也很满意。

## 3. 打完的整节数

- 两个`INT`之间的`/`本来就会丢弃余数，所以不需要再做舍入。

## 4. 本节已进行的分钟数

- `%`给出的正是`/`丢弃的部分。
