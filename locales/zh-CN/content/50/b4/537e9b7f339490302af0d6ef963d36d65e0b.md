# 遍历 map

在这种情况下，使用 map 是非常自然的想法。不过要小心：map 底层是用哈希表实现的，所以键的顺序没有保证。
实际上，每次遍历 map 的键时，顺序都不一样，而且是随机的。
参见 [Go maps in action](https://blog.golang.org/go-maps-in-action) 的 `Iteration order` 一节。
如果遍历顺序很重要，用切片更合适。
