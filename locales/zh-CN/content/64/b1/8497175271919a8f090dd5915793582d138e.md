# 简介

## 时间

在 .NET 中，_时间_这一概念通过 `DateTime` 结构体来处理。有一些例程可以在本地时间与 UTC 之间相互转换。借助 `TimeSpan` 还可以执行算术运算。

## 时区

`TimeZoneInfo` 类提供了处理不同时区之间差异的例程。`TimeZoneInfo` 类还包含一些便于处理夏令时的方法。

`CultureInfo` 类支持依赖区域设置的日期时间格式。

为了支持跨平台编程，`RuntimeInformation` 类让你可以检测代码正在哪个操作系统上运行：Linux、Windows 或 OSX。
