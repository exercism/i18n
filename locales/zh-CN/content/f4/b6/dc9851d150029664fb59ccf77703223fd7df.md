# 内核 format

除了 `format` 方法，在 Ruby 中创建字符串还有一种常见方式：[字符串插值](http://ruby-for-beginners.rubymonstas.org/bonus/string_interpolation.html)。例如：

```ruby
# Rather than:
format("One for %%s, one for me.", %{name_variable})

# Consider:
"One for #{%{name_variable}}, one for me."
```
