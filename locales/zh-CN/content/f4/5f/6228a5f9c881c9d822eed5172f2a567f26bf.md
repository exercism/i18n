# 简介

Factor 的关联数组（哈希表以及类似的结构）共用一个来自 [`assocs`][assocs] 的[协议][assoc-protocol]。哈希表字面量的写法是`H{ }`：

```factor
H{ { "coal" 1 } { "wood" 2 } } .

"coal" H{ { "coal" 1 } { "wood" 2 } } at .   ! => 1
"gold" H{ { "coal" 1 } { "wood" 2 } } at .   ! => f
```

常用操作：

```
at        ( key assoc -- value/f )
key?      ( key assoc -- ? )
set-at    ( value key assoc -- )
delete-at ( key assoc -- )
inc-at    ( key assoc -- )
change-at ( key assoc quot -- )
keys      ( assoc -- keys )
values    ( assoc -- values )
>alist    ( assoc -- alist )
```

哈希表是*可变的*。修改之前先`clone`一份`H{ }`字面量，这样字面量就不会在多次调用之间共享。

[assocs]: https://docs.factorcode.org/content/vocab-assocs.html
[assoc-protocol]: https://docs.factorcode.org/content/article-assocs-protocol.html
