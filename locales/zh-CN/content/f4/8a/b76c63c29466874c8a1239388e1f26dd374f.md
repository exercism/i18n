# 关于

块是由语句组成的小型组合，可以被多次执行。你可以把它们理解为闭包或匿名函数。块用 `do...end` 语法定义（见上），也可以用 `{}` 定义（见下）。这两种写法可以互换使用，至于各自该在什么时候用，则众说纷纭。

## 简写语法

如果块只调用一个方法，你可以用 `&:` 加上方法名来替代整个块。例如，下面 2 行是等价的：

```ruby
people.sum { |person| person.age }
people.sum(&:age)
```
