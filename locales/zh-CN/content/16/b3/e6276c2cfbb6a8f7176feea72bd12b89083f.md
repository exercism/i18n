# 关于

`Result` 类型是 Elm 语言中处理错误报告和错误恢复的方案。它与 `Maybe` 类型有些相似，但 `Maybe` 的 `Nothing` 虽然表示了某种失败，却不会告诉你失败的原因。`Result` 则包含了关于失败原因的上下文。

## 定义与类型变量

`Result` 类型的定义如下：

```elm
type Result error value
  = Ok value
  | Err error
```

简而言之，这个类型表示一次操作的结果：要么成功并返回一个`value`，要么失败并返回对失败的解释（`error`）。

在 Elm 的术语中，这被称为“自定义类型”的定义。自定义类型会在本赛道的另一个概念中详细介绍，但要专门理解 `Result`，我们并不需要它。`OK value` 和 `Error error` 中的 `error` 和 `value` 表示类型变量，也就是说它可以是任何类型，比如 `Int`、`Bool` 或 `String`。因此，`Result String Int` 是一个预期包含 `Int` 的值；如果出错，错误会以 `String` 表示。

```elm
ageFromString : String -> Result String Int
ageFromString input =
  case String.toInt input of
    Nothing ->
      Err "That is not a number!"

    Just age ->
      if age < 0 then
        Err "Please try again after you are born."

      else if age > 135 then
        Err "Are you some kind of turtle?"

      else
        Ok age
```

## 读取 Result 值的内容

读取 `Result` 值的内容是通过“模式匹配”完成的。模式匹配也会在另一个概念中详细介绍，这里我们只关注对 `Result` 进行模式匹配是怎么回事。

```elm
formatAge : Int -> String
formatAge age =
    "You are " ++ toString(age)

howOldAmI : String -> String
howOldAmI age =
    case ageFromString age of
        Err message -> message
        Ok age -> formatAge age

howOldAmI "Bananas"
    --> "That is not a number!"

howOldAmI "32"
    --> "You are 32"
```

每次使用 `Result` 值时，Elm 编译器都会检查你的代码是否处理了所有可能性，因此你不可能在对 `Result` 进行模式匹配时只处理 `Ok value` 这一种情况。起初这可能让人觉得麻烦，但这正是 Elm 语言最大的优势之一。它能避免数百个 bug，也让由编译器引导的重构成为一种大胆而富有成就感的体验。

## 操作 Result 值的内容

有很多函数可以用来处理 `Result` 类型，它们通常比模式匹配更可取。可以用 `Result.map` 来映射值，用 `Result.andThen` 把可能失败的函数串联起来。

Result.map 用于改变 Ok 值，适合把函数串联起来，而不必操心 Result 中是否包含错误。如果值是 Ok，Result.map 就会调用被串联的函数（下面的 `formatAge`）；但如果出现错误，Result.map 什么也不做，直接返回该错误。

```elm
howOldAmI : String -> Result String String
howOldAmI age =
    Result.map formatAge (ageFromString age)

howOldAmI "Bananas"
    --> Err "That is not a number!" (ageFromString returns Err, so Result.map just returns the Err)

howOldAmI "32"
    --> Ok "You are 32" (ageFromString returns Ok 32, Result.map passses this to formatAge)
```

Result.andThen 用于改变 Ok 值，也适合把函数串联起来，而不必操心 Result 中是否包含错误。它与 Result.map 类似，但 Result.andThen 处理的被串联函数（下面的 `formatSpecialBirthday`）本身也返回一个 Result。`Result.map` 也能做到这一点，但结果会多出一层嵌套，而 `andThen` 巧妙地避免了这一点。

```elm
formatSpecialBirthday : Int -> Result String String
formatSpecialBirthday age =
    if age == 18 then
        Ok "Welcome to adulthood!"

    else if age == 65 then
        Ok "Welcome to retirement!"

    else
        Err "Not a special birthday"

specialBirthdayMessage : String -> Result String String
specialBirthdayMessage age =
    Result.andThen formatSpecialBirthday (ageFromString age)

specialBirthdayMessage "Bananas"
    --> Err "That is not a number!" (ageFromString returns Err, so Result.andThen just returns the Err)

specialBirthdayMessage "32"
    --> Err "Not a special birthday" (ageFromString returns Ok 32, Result.map passses this to formatSpecialBirthday, which returns Err)

specialBirthdayMessage "18"
    --> Ok "Welcome to adulthood" (ageFromString returns Ok 32, Result.map passses this to formatSpecialBirthday, which returns Ok)
    --> If we used Result.map, it would be OK (Ok "Welcome to audulthood"), which is annoying
```
