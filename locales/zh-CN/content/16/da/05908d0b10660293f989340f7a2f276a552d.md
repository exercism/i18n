# 字符串格式化

除了 `String#%%` 方法，在 Ruby 中创建字符串还有一种常见方式，就是使用[字符串插值](http://ruby-for-beginners.rubymonstas.org/bonus/string_interpolation.html)。例如：

```ruby
# Rather than:
"One for %%s, one for me." %% %{name_variable}

# Consider:
"One for #{%{name_variable}}, one for me."
```
