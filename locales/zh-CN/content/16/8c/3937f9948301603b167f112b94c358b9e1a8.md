# 提示

## 1. 生成一个随机星球

- Java 不提供从集合中随机选择某个元素的函数。
- 记住，你可以通过下标从数组中取出元素，下标是整数。

## 2. 生成一个随机的星舰注册号

- `java.util.Random`类提供了一个方法，可以生成 0 到给定最大值之间的随机`int`。

## 3. 生成一个随机星历

- `java.util.Random`类中生成随机`double`的方法始终返回`0.0`到`1.0`之间的数字。
