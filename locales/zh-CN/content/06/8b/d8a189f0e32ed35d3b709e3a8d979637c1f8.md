# 补充说明

## 实现

在`grep`包中，定义一个 Go 函数`Search`，它接受一个模式字符串、一个由字符串组成的标志切片，以及一个文件名切片。`Search`应返回一个字符串切片，内容为给定标志和文件名对应的输出。

函数`Search`请使用以下签名：

```go
func Search(pattern string, flags, files []string) []string {
```
