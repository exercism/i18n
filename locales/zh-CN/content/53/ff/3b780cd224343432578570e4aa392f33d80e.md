# 关于

和大多数编程语言一样，Gleam 也有“副作用”：函数除了能返回一个值，还能读取和改变外部世界的状态。

Gleam 标准库中的 `gleam/io` 模块提供了把字符串打印到控制台的函数。

```gleam
io.println("Hello, Joe!")
// Hello, Joe!
// -> Nil
```

其他包也可能提供别的 IO 函数，比如 `simplifile`，这个包提供了读写文件的函数。

```gleam
simplifile.read("favourite-colour.txt")
// -> Ok("Pink\n")
```
