# 说明补充

## 实现

在本练习中，你将实现一个过程，它需要为返回的字符串分配内存。释放该字符串由这个过程的调用方负责。

```odin
// The procedure will be called like that:
converted := convert(3)
// ...
// and eventually the caller will delete the string
delete(converted)
```
