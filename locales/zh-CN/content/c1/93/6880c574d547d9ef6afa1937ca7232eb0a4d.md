# 关于

[高阶函数](https://docs.microsoft.com/en-us/dotnet/fsharp/introduction-to-functional-programming/first-class-functions)是指满足以下任一条件的函数：

- 接收一个函数作为实参
- 返回一个函数

函数实参就是一种普通的实参，只不过它是可以被调用的。

```fsharp
let double = fun x -> x * 2

let apply f x = f x

apply double 3
// => 6
```
