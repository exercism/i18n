# 占位符

## ASCII 值

这个练习需要在 ASCII 字符和对应的数值之间来回转换。GNU awk 的 [ordchr 扩展][ordchr] 提供了这个功能。要添加`ord()`和`chr()`函数，请在文件顶部添加一条`@load`指令来加载`ordchr`扩展：

```
@load "ordchr"
```

[ordchr]: https://www.gnu.org/software/gawk/manual/gawk.html#Extension-Sample-Ord
