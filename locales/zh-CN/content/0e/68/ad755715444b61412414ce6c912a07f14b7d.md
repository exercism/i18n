# 简介

三元运算符让你可以在表达式里定义 if 条件，而不必写成语句块。这也呼应了函数式编程的思路，往往能让代码更有表现力，也更不容易出错。

三元运算符把 3 个表达式组合在一起：一个条件，一个在条件为真时求值并返回的表达式（也就是`if`部分，由`?`引出），以及一个在条件为假时求值并返回的表达式（也就是`else`部分，由`:`引出）。

```csharp
int a = 3, b = 4;
int max = a > b ? a : b;
// => 4
```
