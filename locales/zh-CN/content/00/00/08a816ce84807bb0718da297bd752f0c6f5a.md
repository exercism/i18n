# 简介

*正则表达式*（regex）是一小段模式，用来描述某段文本应该长什么样。Factor 的正则表达式工具都在[`regexp`][regexp]词库里，模式则写在`R/ `和`/`之间。

模式里的大部分字符就代表它们自己，只有少数几个特殊片段表示字符的*种类*，或者*期望出现多少个*：

```
\d        ! any digit 0–9
\s        ! any whitespace; \S means any non-whitespace
[A-Z]     ! any one capital letter
.         ! any single character

{4}       ! exactly 4 times       +   ! one or more
{2,5}     ! 2 to 5 times          *   ! zero or more
?         ! optional (0 or 1)
```

所以`R/ [A-Z]{2}-\d{4}/`的意思是“两个大写字母、一个连字符、四个数字”。

`matches?`测试*整个*字符串是否符合模式：

```
matches? ( string regexp -- ? )
```

```factor
USING: regexp ;

"NS-1024" R/ [A-Z]{2}-\d{4}/ matches? .   ! => t
"NS-10"   R/ [A-Z]{2}-\d{4}/ matches? .   ! => f
```

另一些词则在字符串*内部*查找：`all-matching-subseqs`返回每个匹配项，`count-matches`统计匹配数量，`re-contains?`报告是否存在匹配，`re-replace`把每个匹配项替换成新文本：

```
all-matching-subseqs ( string regexp -- seq )
count-matches        ( string regexp -- n )
re-contains?         ( string regexp -- ? )
re-replace           ( string regexp replacement -- result )
```

```factor
USING: regexp ;

"seen NS-1024 then AB-0007" R/ [A-Z]{2}-\d{4}/ all-matching-subseqs .
! => { "NS-1024" "AB-0007" }

"user=alice pass=hunter2" R/ pass=\S+/ "pass=****" re-replace .
! => "user=alice pass=****"
```

[regexp]: https://docs.factorcode.org/content/vocab-regexp.html
