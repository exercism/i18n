# 提示

你的代码中应包含一个函数，它的类型签名是 frequency :: Int -> [Text] -> Map Char Int。这个函数接收并行处理时使用的工作进程数量和一组文本，返回文本中每个字母的总频率。

## 基准测试

通过运行基准测试，看看改变工作进程数量会如何影响你解答的性能。用`stack bench`来运行它。你可以随意修改`bench/Benchmark.hs`，探索你的解答在不同输入下的性能表现。
