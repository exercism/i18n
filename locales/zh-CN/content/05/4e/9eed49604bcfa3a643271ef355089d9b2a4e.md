# 简介

[三元条件运算符][ternary-operator]是`if-else`表达式的一种简洁写法。它会先求出一个条件，再根据该条件为`true`还是`false`，返回两个值中的一个。

这种运算符由三部分组成：`condition ? trueExpression : falseExpression`。
如果`condition`求值为`true`，就求值并返回`trueExpression`；否则求值并返回`falseExpression`。

三元运算符非常适合简单的一行选择：

```swift
let showFahrenheit = true
let temperatureInCelsius = 30

let temperature = showFahrenheit ? temperatureInCelsius * 9 / 5 + 32 : temperatureInCelsius
print(temperature)
// Prints 86
```

上面的例子等价于下面这段代码：

```swift
let showFahrenheit = true
let temperatureInCelsius = 30

let temperature = if showFahrenheit {
    temperatureInCelsius * 9 / 5 + 32
} else {
    temperatureInCelsius
}
```

[ternary-operator]: https://docs.swift.org/swift-book/LanguageGuide/BasicOperators.html#ID71
