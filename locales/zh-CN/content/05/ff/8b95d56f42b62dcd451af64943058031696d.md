# 提示

## 1. 定义`TreasureChest`不透明类型。

- 不透明类型使用`opaque`关键字定义。

## 2. 定义`create`函数。

- 可以使用[`string.length`函数][length]获取字符串的长度。

## 3. 定义`open`函数。

- 可以使用`==`运算符比较两个字符串是否相等。

[length]: https://hexdocs.pm/gleam_stdlib/gleam/string.html#length
