# 补充说明

## 处理边界情况

在 Exercism 上，不同的语言对子字符串长度为 0 时结果应该是什么，有着不同的预期。
在 Rust 赛道上，我们没有针对这种情况的测试，所以你可以自由选择自己认为最合适的做法。

想一想以下几种可能性各自的优缺点：

- 用`panic!`让程序崩溃。
- 返回`Result::Err`。（这里做不到，因为函数签名是给定的）
- 返回一个空向量。
- 返回一个向量，其中包含的空字符串数量等于字符串 "digits" 的长度**加一**。（这有一些不错的数学性质！）
