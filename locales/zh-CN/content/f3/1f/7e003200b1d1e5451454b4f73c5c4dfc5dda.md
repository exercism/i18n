# 说明补充

## 性能提示

所有输入和输出都是 ASCII。
Rust 的`String`和`&str`都是 utf8，所以你可能以为`"Hello".chars()`很简单，实际上它必须逐个检查每个字符，看它究竟占 1 个、2 个、3 个还是 4 个`u8`那么长。
如果我们确定某个`&str`是 ASCII，就可以调用`.as_bytes()`，把底层数据当作`&[u8]`（字节切片）来处理。
遍历字节切片比遍历 ASCII 字节快得多，因为不涉及码点，每个 ASCII 字节都只占一个`u8`。

你能在不克隆输入的情况下完成这道挑战吗？
