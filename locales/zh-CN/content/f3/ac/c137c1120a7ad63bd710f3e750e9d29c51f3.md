# 说明

你要写一些代码，帮你用最喜欢的食谱书做出一份美味的千层面。

一共有五个任务，都和做这道菜有关。

<br>

~~~~exercism/note
我们已经在骨架文件里帮你写好了第一个函数定义，但其余的函数定义需要你自己来写。
你还需要自己定义所有常量。
请仔细阅读骨架文件里的 #TODO 注释行。
完成一个任务后，就把 TODO 注释删掉。
~~~~

<br>

## 1. 用常量定义预期的烘焙时间（分钟）

定义 `EXPECTED_BAKE_TIME`[常量][constants]，它表示千层面需要在烤箱里烘焙多少分钟。
根据你的食谱书，千层面应该在烤箱里烘焙 40 分钟：

```python
>>> print(EXPECTED_BAKE_TIME)
40
```

## 2. 计算剩余的烘焙时间（分钟）

补全`bake_time_remaining()`函数，它接收千层面已经在烤箱里待了多少分钟作为实参，并基于`EXPECTED_BAKE_TIME`常量返回千层面还需要烘焙多少分钟。

```python
>>> bake_time_remaining(30)
10
```


## 3. 计算准备时间（分钟）

定义`preparation_time_in_minutes()`[函数][functions]，它接收你想加进千层面里的`number_of_layers`作为实参，返回制作这些层需要花费多少分钟。
假设每层需要 2 分钟来准备。

```python
>>> def preparation_time_in_minutes(number_of_layers):
        ...
        ...
        
>>> preparation_time_in_minutes(2)
4
```


## 4. 计算总共已用时间（准备 + 烘焙）的分钟数

定义`elapsed_time_in_minutes()`函数，它接收两个形参作为实参：

- `number_of_layers`（_加进千层面里的层数_）
- `elapsed_bake_time`（_千层面已经在烤箱里烘焙的分钟数_）。

这个函数应该返回你在厨房里烹饪的总分钟数，也就是你分层准备的时间加上千层面在烤箱里烘焙的时间。

```python
>>> def elapsed_time_in_minutes(number_of_layers, elapsed_bake_time):
        ...
        ...
        
>>> elapsed_time_in_minutes(3, 20)
26
```


## 5. 为食谱补充注释

回头再看一遍这份食谱，用[函数文档字符串][function-docstrings]的形式添加“注释”。

```python
def elapsed_time_in_minutes(number_of_layers, elapsed_bake_time):
    """Calculate the elapsed cooking time.
    
    Parameters:
        number_of_layers (int): The number of layers in the lasagna.
        elapsed_bake_time (int): Time the lasagna has been baking in the oven.
    
    Returns:
        int: The total time elapsed (in minutes) preparing and baking.

    This function takes two integers representing the number of lasagna 
    layers and the time already spent baking the lasagna. It calculates 
    the total elapsed minutes spent cooking (preparing + baking).
    
    """
```

[constants]: https://stackoverflow.com/a/2682752
[functions]: https://docs.python.org/3/tutorial/controlflow.html#defining-functions
[function-docstrings]: https://docs.python.org/3/tutorial/controlflow.html#documentation-strings
