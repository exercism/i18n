# 占位符

## `--argjson`选项

测试使用了`jq`选项`--argjson n 1`。这会创建一个名为`$n`的 jq 变量，其值是_**数字**_`1`。

对比一下选项`--arg n 1`，它创建一个名为`$n`的 jq 变量，值是_**字符串**_`"1"`。

在你的解答中，用`$n`作为输入来求第 n 个质数。

参见手册中的[调用 jq][man-invoke]。

[man-invoke]: https://jqlang.org/manual/#invoking-jq
