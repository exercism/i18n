# 简介

默认情况下，变量只在其被定义的作用域内有效。如果变量定义在函数或类之外的文件中，它的作用域就限于这个文件；如果定义在函数或类中，它的作用域就限于这个函数或类。

```php
<?php

$file_scoped = "I am file scoped";

function example(): void
{
  $function_scoped = "I am function scoped!";
}
```

变量还可以专门声明为`global`或`static`作用域。
