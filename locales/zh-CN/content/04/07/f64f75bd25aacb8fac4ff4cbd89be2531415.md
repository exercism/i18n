# 忽略我

## 输入

你的脚本将通过标准输入（stdin）接收到一个字符串形式的字母算术谜题。谜题字符串的格式为 `WORD1 + WORD2 + ... == RESULT`。

例如：

```text
SEND + MORE == MONEY
```

## 输出

如果谜题有解，打印一行，内容为以空格分隔的 `LETTER=DIGIT` 对，按字母顺序排序。

对于上面的例子，输出应该是：

```text
D=7 E=5 M=1 N=6 O=0 R=8 S=9 Y=2
```

如果谜题有多个有效解，打印其中任意一个即可。

如果谜题无解，则什么也不打印（输出一个空行）。
