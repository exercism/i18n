# 描述

实现 `accumulate` 操作：给定一个集合，以及一个要对集合中每个元素执行的操作，返回一个新集合，其中包含把该操作应用到输入集合中每个元素后得到的结果。

给定这个数字集合：

- 1, 2, 3, 4, 5

以及一段执行以下操作的代码引用：

- 求一个数字的平方（`[ sq ]`）

你的代码应该能够生成由这些平方数组成的集合：

- 1, 4, 9, 16, 25

## 限制

别去碰标准库提供的 collect/map/fmap 之类的功能！

请改用其他基础工具自己解决这道题。
