# 补充说明

## 实现

`diagram`实参的每一行都以`\n`开头。
这样，Go 的原始字符串字面量就能在源代码中把示意图美观地呈现为靠左对齐的两行。
例如，测试中可能包含如下内容。

```go
        diagram := `
VVCCGG
VVCCGG`
```

如果`children`实参为`nil`，就使用上面说明中定义的 children 列表。
如果不为`nil`，就使用给定的值。
