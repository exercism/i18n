# 运行测试

要运行测试套件，请执行以下命令：

```bash
stack test
```

#### 如果你遇到这样的错误信息……

```
No .cabal file found in directory
```

或者

```
RedownloadInvalidResponse Request {
...
}
 "/home/username/.stack/build-plan/lts-xx.yy.yaml" (Response {responseStatus = Status {statusCode = 404, statusMessage = "Not Found"},
```

那你很可能在用旧版的 stack，需要升级。试试运行：

```bash
stack upgrade
```

或者到 [Stack 文档](https://docs.haskellstack.org/en/stable/install_and_upgrade/#upgrade) 里看看其他升级方式。

#### 否则，如果你遇到这样的错误信息……

```
No compiler found, expected minor version match with...
Try running "stack setup" to install the correct GHC...
```

照它说的做就行，它会下载并安装正确的编译器版本：

```bash
stack setup
```

# 运行 *GHCi*

如果你想在 GHCi 里试验你的解答，直接运行这个命令：

```bash
stack ghci
```
