# 简介

文件本质上就是磁盘上带名字的流。[`io.files`][io.files]词汇表提供两种处理文件的方式：整文件便捷词，一次调用就能读取或写入全部内容；以及流打开器，用于需要增量读写的时候。

每个与文件打交道的词都接受一个**编码**，也就是把字节转换成字符、再转换回来的方案。处理文本时，用的几乎总是 `io.encodings.utf8` 中的 [`utf8`][utf8]。

## 读取整个文件

```
file-contents   ( path encoding -- str )
file-lines      ( path encoding -- seq )
```

`file-contents` 把整个文件作为一个字符串返回；`file-lines` 返回由各行组成的数组，并去掉换行符。

```factor
USING: io.encodings.utf8 io.files prettyprint ;

"sunny\ncloudy\n" "forecast.txt" utf8 set-file-contents

"forecast.txt" utf8 file-contents .
! => "sunny\ncloudy\n"

"forecast.txt" utf8 file-lines .
! => { "sunny" "cloudy" }
```

## 写入整个文件

```
set-file-contents   ( str path encoding -- )
set-file-lines      ( seq path encoding -- )
```

两者都会**替换**文件的内容（如有需要则创建文件）。`set-file-contents` 原样写入一个字符串；`set-file-lines` 把序列中的每个元素各写一行，并自动为你加上换行符。

```factor
USING: io.encodings.utf8 io.files prettyprint ;

{ "sunny" "cloudy" } "forecast.txt" utf8 set-file-lines
"forecast.txt" utf8 file-contents .
! => "sunny\ncloudy\n"
```

## 向文件追加内容

要在不清除已有内容的前提下向文件添加内容，可以用 `with-…` 组合子打开文件，并在引用内部写入环境流：

```
with-file-appender   ( path encoding quot -- )
with-file-reader     ( path encoding quot -- )
with-file-writer     ( path encoding quot -- )
```

```factor
USING: io io.encodings.utf8 io.files prettyprint ;

"sunny\ncloudy\n" "forecast.txt" utf8 set-file-contents
"forecast.txt" utf8 [ "rainy" print ] with-file-appender
"forecast.txt" utf8 file-contents .
! => "sunny\ncloudy\nrainy\n"
```

与所有 `with-…` 组合子一样，它们都是析构作用域：引用执行完毕时，文件句柄就会关闭，即使其中抛出了异常。

[io.files]: https://docs.factorcode.org/content/vocab-io.files.html
[utf8]: https://docs.factorcode.org/content/vocab-io.encodings.utf8.html
