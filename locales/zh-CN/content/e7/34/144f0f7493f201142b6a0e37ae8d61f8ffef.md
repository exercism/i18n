# to_i{bit}

用`to_i{bit}`方法把值转换成某个进制是可行的。
但在定义[数字字面量][int]时，更地道的做法是使用`_i{bit}`后缀。

```crystal
# Rather than:
500034214414.to_i64

# Consider:
500034214414_i64
```

[int]: https://crystal-lang.org/reference/latest/syntax_and_semantics/literals/integers.html
