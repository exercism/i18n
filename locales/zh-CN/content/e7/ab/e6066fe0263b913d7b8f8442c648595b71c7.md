# 说明补充

## 提示

要让测试通过，你需要为代码创建一些可用的类型：你需要一个对象，供`School`保存一串`Student`类型的元素，而`Student`是一个由`string`和`int`组成的元组，用来保存学生的名字和年级。

下面这些类型的代码在刚开始时可能会有帮助

```nim
type
  Student* = tuple[name: string, grade: int]
  School* = object
    students*: seq[Student]
```
