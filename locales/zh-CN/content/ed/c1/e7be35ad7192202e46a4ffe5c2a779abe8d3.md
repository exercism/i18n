# 简介

所有 Common Lisp 代码都由 S-表达式（S-Expression，符号表达式）构成，简称 sexpr。每个 sexpr 要么是原子，要么是一个 cons。S-表达式在求值时，会自动返回一个值，这个值会取代该表达式。在编写自己的函数时（使用`defun`），`defun`函数体里的最后一个值会被自动返回：

```lisp
;; Defining a new function
(defun gimme-foo () 'foo)
;; Calling the function as an S-Expression
(gimme-foo) ; => FOO
```
