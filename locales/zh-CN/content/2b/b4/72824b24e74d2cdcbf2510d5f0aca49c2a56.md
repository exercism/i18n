# 说明附录

这道练习的一种解法是自己实现一个模幂运算函数。想了解更多，可以参考[这个页面](https://en.wikipedia.org/wiki/Modular_exponentiation)。

## 加分项

很多实现在输入的全部取值范围内并不能正常工作。
我们还定义了一些额外的可选测试，用来帮助确保所有合法的输入都能得到正确的结果。

要运行这些加分测试，请去掉 `#[ignore]` 标记，并使用 `big-primes` feature 来执行测试，像这样：

```bash
$ cargo test --features big-primes
```
