# 字符串拼接

在 Ruby 中，字符串拼接完全可行，但更常见的字符串组合方式是使用[字符串插值](http://ruby-for-beginners.rubymonstas.org/bonus/string_interpolation.html)。例如：

```ruby
# Rather than:
"One for " + %{name_variable} + ", one for me"

# Consider:
"One for #{%{name_variable}}, one for me."
```
