# 使用`||`代替 if-else

当想在值为`nil`或其他假值时替换它，请使用`||`运算符，而不是 if-else 语句。
这样更符合惯用法，也更简洁，性能也更好。

`||`运算符会在左侧的值为真时返回左侧的值，否则返回右侧的值。

例如：

```crystal
# Rather than:
value.nil? ? "default" : value

# Consider:
value || "default"
```
