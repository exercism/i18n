# 提示

## 通用

- 这道练习的运行时间很大程度上取决于所用的算法，所以要提前为速度做打算。
- 测试中特意使用了一个很大的质数，设计不佳的解法会超时。
- 如果把经常用到的代码行拆分成单独的函数，Julia 代码往往能运行得更快（也可以选择把这个函数嵌套在主函数里）。
  - 与其他一些语言不同，在 Julia 里预编译比任何函数调用开销都更重要。
- _在断定一个数是质数之前，必须测试哪些值以确保它们不是它的因数？_ 把这组值控制在最少！
