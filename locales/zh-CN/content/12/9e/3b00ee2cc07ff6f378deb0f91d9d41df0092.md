# 简介

在 Java 中，`String`是一个对象，它将不可变文本表示为 Unicode 字符序列（字母、数字、标点等）。
双引号用来定义`String`实例：

```java
String fruit = "Apple";
```

通过调用字符串的方法来操作字符串。
字符串一旦创建，其值就永远不能改变。
任何看起来会修改字符串的方法，实际上都会返回一个新字符串。
`String`类提供了一些 _static_ 方法来转换字符串。
