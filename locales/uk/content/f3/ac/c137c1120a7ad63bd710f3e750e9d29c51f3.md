# Інструкції

Ми напишемо трохи коду, щоб допомогти приготувати розкішну лазанью з нашої улюбленої кулінарної книги.

На нас чекає пʼять завдань, усі повʼязані з приготуванням нашої страви.

<br>

~~~~exercism/note
У файлі-заготовці ми вже почали перше визначення функції, але решту визначень функцій доведеться написати самостійно.
Також самостійно доведеться визначити потрібні константи.
Уважно прочитайте рядки коментарів #TODO у файлі-заготовці.
Коли завершите завдання, видаліть коментар TODO.
~~~~

<br>

## 1. Визначте очікуваний час випікання в хвилинах як константу

Визначте [константу][constants] `EXPECTED_BAKE_TIME`, яка задає, скільки хвилин лазанья має пектися в духовці.
Згідно з нашою кулінарною книгою, лазанью слід тримати в духовці 40 хвилин:

```python
>>> print(EXPECTED_BAKE_TIME)
40
```

## 2. Обчисліть залишок часу випікання в хвилинах

Доповніть функцію `bake_time_remaining()`, яка приймає як аргумент фактичну кількість хвилин, які лазанья вже провела в духовці, і повертає, скільки хвилин їй ще залишилося пектися, виходячи з константи `EXPECTED_BAKE_TIME`.

```python
>>> bake_time_remaining(30)
10
```


## 3. Обчисліть час підготовки в хвилинах

Визначте [функцію][functions] `preparation_time_in_minutes()`, яка приймає як аргумент `number_of_layers` — кількість шарів, які потрібно додати до лазаньї, — і повертає, скільки хвилин піде на їх приготування.
Вважайте, що на приготування кожного шару потрібно 2 хвилини.

```python
>>> def preparation_time_in_minutes(number_of_layers):
        ...
        ...
        
>>> preparation_time_in_minutes(2)
4
```


## 4. Обчисліть загальний витрачений час (підготовка + випікання) в хвилинах

Визначте функцію `elapsed_time_in_minutes()`, яка приймає як аргументи два параметри:

- `number_of_layers` (_кількість шарів, доданих до лазаньї_)
- `elapsed_bake_time` (_кількість хвилин, які лазанья вже провела в духовці_).

Ця функція має повернути загальну кількість хвилин, які ми провели на кухні за приготуванням: час на викладання шарів плюс час, який лазанья провела в духовці.


```python
>>> def elapsed_time_in_minutes(number_of_layers, elapsed_bake_time):
        ...
        ...
        
>>> elapsed_time_in_minutes(3, 20)
26
```


## 5. Доповніть рецепт примітками

Поверніться до рецепта й додайте «примітки» у вигляді [рядків документації функцій][function-docstrings].

```python
def elapsed_time_in_minutes(number_of_layers, elapsed_bake_time):
    """Calculate the elapsed cooking time.
    
    Parameters:
        number_of_layers (int): The number of layers in the lasagna.
        elapsed_bake_time (int): Time the lasagna has been baking in the oven.
    
    Returns:
        int: The total time elapsed (in minutes) preparing and baking.

    This function takes two integers representing the number of lasagna 
    layers and the time already spent baking the lasagna. It calculates 
    the total elapsed minutes spent cooking (preparing + baking).
    
    """
```

[constants]: https://stackoverflow.com/a/2682752
[functions]: https://docs.python.org/3/tutorial/controlflow.html#defining-functions
[function-docstrings]: https://docs.python.org/3/tutorial/controlflow.html#documentation-strings
