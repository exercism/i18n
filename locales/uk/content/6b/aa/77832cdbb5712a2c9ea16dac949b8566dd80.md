# Вступ

Щоб перевірити, чи рівні дві змінні простих типів, як-от рядки тексту (англ. string), цілі числа тощо, ми використовуємо оператори рівності `==` і `!=`:

```haxe
var a = 5;
var b = 5;

a == b; // true
a != b; // false
```
