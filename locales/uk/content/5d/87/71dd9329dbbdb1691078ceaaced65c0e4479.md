# Додаток до інструкцій

## Помилки

Викиньте дослівно один із цих рядків тексту (англ. string), якщо вхідні дані недійсні:

- `"input base must be >= 2"`
- `"output base must be >= 2"`
- `"all digits must satisfy 0 <= d < input base"`

Відʼємні числа не підтримуються.
