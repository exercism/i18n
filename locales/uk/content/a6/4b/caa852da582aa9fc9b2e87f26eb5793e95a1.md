# Найкращий вибір функції для `increase_quantity`

Використайте або функцію `Map.new/2`, або функцію `Enum.into/3` у функції `increase_quantity`. Вони найкраще підходять для цього завдання і не потребують використання функції `Enum.map/2`.
