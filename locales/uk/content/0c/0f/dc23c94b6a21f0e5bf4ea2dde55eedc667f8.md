# Інструкції для SQLite

У цій вправі потрібно присвоїти стовпцю `result` таблиці `kindergarten-garden` правильне значення на основі стовпців `diagram` і `student`.

## Схема таблиці

```sql
CREATE TABLE "kindergarten-garden" ("diagram" TEXT, "student" TEXT, "result" TEXT);
```
