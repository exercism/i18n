# Доповнення до інструкцій

## Реалізація

Якщо під час трансляції трапляється недійсний символ або кодон, потрібно `throw` помилку з повідомленням `Invalid codon`.

```javascript
translate('AAA');
// => Error: Invalid codon
```
