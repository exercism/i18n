# Вступ

Коментарі дають змогу залишати в коді нотатки, не впливаючи на те, як він виконується.
Однорядкові коментарі починаються з `//`, а багаторядкові беруть у `/*` і `*/`:

```go
// This is a single-line comment.

/*
This is a
multiline comment.
*/
```

Коментарі виконують різні ролі залежно від того, де вони розташовані.

## Коментарі в коді

Коментарі в коді пояснюють, чому щось зроблено, а не що саме.
Використовуймо їх, коли причина рішення не очевидна з коду:

```go
results = results[1:] // skipping the header row
```

## Документаційні коментарі

Документаційні коментарі стоять безпосередньо перед оголошенням, і між коментарем та оголошенням немає порожнього рядка.
[`godoc`][godoc] аналізує їх, щоб згенерувати документацію пакета.
Документаційний коментар має бути повним реченням, яке починається з назви ідентифікатора й закінчується крапкою.

Коментарі до пакета починаються з назви пакета:

```go
// Package kelvin provides tools to convert temperatures to and from Kelvin.
package kelvin
```

Коментарі до функції починаються з назви функції:

```go
// CelsiusFreezingTemp returns an integer value equal to the temperature at which water freezes in degrees Celsius.
func CelsiusFreezingTemp() int {
	return 0
}
```

[godoc]: https://go.dev/blog/godoc
