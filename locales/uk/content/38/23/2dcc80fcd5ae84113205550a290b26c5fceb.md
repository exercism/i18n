# Специфічні інструкції для SQLite

У цій вправі потрібно присвоїти стовпцю `result` таблиці `roman-numerals` правильне значення на основі числа зі стовпця `number`.

## Схема таблиці

```sql
CREATE TABLE "roman-numerals" ("number" INT, "result" TEXT);
```
