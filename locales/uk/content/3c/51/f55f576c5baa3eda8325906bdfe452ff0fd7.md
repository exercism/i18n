# Оптимізація типового значення

Замість того щоб використовувати умовну конструкцію для розгалуження на випадок, коли в `twoFer` не передано жодного значення, встановіть типове значення параметра `%{parameter}` як `'you'`, щоб уникнути цієї умовної конструкції.
