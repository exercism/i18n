# Використовуйте неявний `includes`

Використання `includes` перебирає масив _двічі_. У цій вправі швидкодія не має значення, але ця логіка тут зайва.

**Зверніть увагу**: якщо кольору не існує, цілком припустимо викинути `Error`, і не варто відмовляти від цього.