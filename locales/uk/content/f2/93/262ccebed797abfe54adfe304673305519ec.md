# експорт на місці оголошення

Функції, як-от `twoFer`, класи та константи можна експортувати за допомогою `export` одразу в оголошенні.

```javascript
// Not inlined
export { twoFer }

// Inlined
export function twoFer() {}
```
