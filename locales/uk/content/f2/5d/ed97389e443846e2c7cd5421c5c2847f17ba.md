# Вступ

Якщо код у циклі має завжди виконуватися принаймні один раз, можна використати цикл `do`/`while`:

```csharp
int x = 23;

do
{
    // Always executes at least once, then repeats as long as x > 10
    x = x - 2;
} while (x > 10)
```
