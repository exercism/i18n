# Доповнення до інструкцій

## Нотатки щодо треку

Вхідні дані подано як масив рядків тексту (англ. string).
Визначення подано у вигляді `: var x ... ;`, де `var` повʼязується з тим, що йде далі.
В іншому разі рядок тексту позначає послідовність операцій зі стеком.
