# Сплат-аргументи

Визначте явну змінну `name` замість того, щоб використовувати ключове слово сплат-аргументу (`*`). Так код стає зрозумілішим для читання, і потрібно менше маніпуляцій, щоб отримати значення аргументу.
