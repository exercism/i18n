# кілька варіантів функції

[comment]: # (Ідея цього коментаря - сказати людям, щоб вони використовували кілька варіантів функції, але оскільки написати якісний аналіз для цього дуже складно, він натомість перевіряє використання `if`, `case` і `cond`)

Мета цієї вправи - навчити працювати з кількома варіантами функції. Розвʼяжіть її без використання `if`, `case` або `cond`. Натомість визначте кілька варіантів функції `compare`.
