# Вступ

Щоб згенерувати випадкове число в Haxe, можна використати метод `Std.random(x)`. Він генерує випадкове число в діапазоні від 0 включно до `x` виключно:

```haxe
public function main() {
  trace(Std.random(10)); // 0...9
}
```
