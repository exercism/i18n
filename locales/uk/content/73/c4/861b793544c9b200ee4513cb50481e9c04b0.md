# Вступ

Якщо типи не можна привести неявно, потрібно використати оператор явного приведення.
Оператор приведення - це не що інше, як цільовий тип у дужках перед значенням, яке потрібно привести:

```csharp

int i = 7;
byte b = (byte)i; // Explicit cast from int to byte
```
