# Вступ

Подібно до інших мов програмування, enum, скорочено від `enumerations`, використовуються в Cairo для визначення власних типів даних, які містять набір фіксованих іменованих значень, що називаються _варіантами_.
Кожен варіант, з яких складається enum, є окремим і має конкретне значення.
