# Імпорт ANSI з :except

В інструкціях сказано: «Ви плануєте додати підтримку інших кольорів автомобіля, тож хочете імпортувати весь модуль», за винятком конфліктної функції `color`. Щоб це зробити, використайте опцію `:except` ось так:

```elixir
import IO.ANSI, except: [color: 1]
```
