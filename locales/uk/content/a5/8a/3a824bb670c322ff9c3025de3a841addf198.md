# Уникайте порівняння з булевими літералами

Замість того щоб порівнювати булеві значення (англ. Boolean) з літеральними значеннями, можна просто використати самі змінні:

```java
// instead of
return someBoolVariable == true;

// use
return someBoolVariable;
```
