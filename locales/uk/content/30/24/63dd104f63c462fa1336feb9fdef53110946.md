# Використайте `with` з `else` у `get_new_passport`

Використайте `with` з блоком `else` у своїй реалізації `get_new_passport/3`, щоб обробити неуспішний шлях. Мета цієї вправи - навчитися використовувати спеціальну форму `with`, зокрема й те, як використовувати її блоки `else`.
