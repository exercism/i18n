# Вступ

У JavaScript є оператори інкременту та декременту.
Вони змінюють змінну на місці.
`++` додає одиницю до числа, `--` віднімає одиницю від числа.

```javascript
let i = 3;
i++;
// i is now 4

let j = 0;
j--;
// j is now -1
```
