# Вступ

В англійській мові [Nil][nil-dictionary] означає «ніщо» або «нуль». В Elixir `nil` - це особливе значення, що означає _відсутність_ значення.

```elixir
# I do not have a favorite color
favorite_color = nil
```

В інших мовах програмування подібну роль можуть відігравати значення `null` або `none`.

[nil-dictionary]: https://www.merriam-webster.com/dictionary/nil
