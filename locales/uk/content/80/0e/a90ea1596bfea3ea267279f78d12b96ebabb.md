# Тести

Щоб запустити тести, спершу встановіть пакет `z3-solver`:

```
pip install z3-solver
```

Потім запустіть тести, виконавши таку команду з каталогу вправи:

```
python <snake_case_exercise>_test.py
```

де `<snake_case_exercise>` замінюється на назву вправи.
