# Про

Керуюча конструкція `foreach` дає зручний спосіб перебирати _масиви_ та _обʼєкти_. `foreach` має дві базові форми:

```php
foreach (iterable as $value) {
  // statements
}

foreach (iterable as $key => $value) {
  // statements
}
```
