# Вступ

[Stringer][stringer-interface] - це інтерфейс для визначення формату рядка тексту (англ. string) для значень.

Інтерфейс складається з одного методу `String`:

```go
type Stringer interface {
    String() string
}
```

Типи, які хочуть реалізувати цей інтерфейс, повинні мати метод `String()`, що повертає зручне для людини представлення типу у вигляді рядка тексту.
Пакет [fmt][fmt-package] (і багато інших) будуть шукати цей метод, щоб форматувати та друкувати значення.

## Приклад: відстані

Припустімо, що ми працюємо над застосунком, який працює з географічними відстанями, вимірюваними в різних одиницях.
Ми визначили типи `DistanceUnit` і `Distance` так:

```go
type DistanceUnit int

const (
	Kilometer    DistanceUnit = 0
	Mile         DistanceUnit = 1
)

type Distance struct {
	number float64
	unit   DistanceUnit
}
```

У наведеному вище прикладі `Kilometer` і `Mile` визначено як константи типу `DistanceUnit`.

Ці типи не реалізують інтерфейс `Stringer`, оскільки не мають методу `String`.
Отже, функції `fmt` будуть друкувати значення `Distance` у типовому форматі Go:

```go
mileUnit := Mile
fmt.Sprint(mileUnit)
// => 1
// The result is '1' because that is the underlying value of the 'Mile' constant (see constant declarations above)

dist := Distance{number: 790.7, unit: Kilometer}
fmt.Sprint(dist)
// => {790.7 0}
// not a very useful output!
```

Щоб зробити вихідні дані інформативнішими, ми реалізуємо інтерфейс `Stringer` для типів `DistanceUnit` і `Distance`, додавши метод `String` до кожного з них:

```go
func (sc DistanceUnit) String() string {
	units := []string{"km", "mi"}
	return units[sc]
}

func (d Distance) String() string {
	return fmt.Sprintf("%v %v", d.number, d.unit)
}
```

Функції пакета `fmt` будуть викликати ці методи під час форматування значень `Distance`:

```go
kmUnit := Kilometer
kmUnit.String()
// => km

mileUnit := Mile
mileUnit.String()
// => mi

dist := Distance{
	number: 790.7,
	unit: Kilometer,
}
dist.String()
// => 790.7 km
```

[stringer-interface]: https://pkg.go.dev/fmt#Stringer
[fmt-package]: https://pkg.go.dev/fmt
