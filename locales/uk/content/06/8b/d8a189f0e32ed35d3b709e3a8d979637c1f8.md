# Додаток до вказівок

## Реалізація

У пакеті grep визначте одну функцію Go, Search, яка приймає шаблон у вигляді рядка тексту (англ. string), зріз прапорців, які є рядками тексту, і зріз назв файлів, які є рядками тексту. Search має повернути вихідні дані для заданих прапорців і назв файлів у вигляді зрізу рядків тексту.

Використовуйте таку сигнатуру для функції Search:

```go
func Search(pattern string, flags, files []string) []string {
```
