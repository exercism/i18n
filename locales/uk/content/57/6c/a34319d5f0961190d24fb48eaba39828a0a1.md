# Неоптимальна сигнатура

Якщо подивитися на тести, функція `value` приймає лише один параметр.
Другого параметра не потрібно. Його значення завжди буде `undefined`
(або те типове значення, яке ми призначили).
