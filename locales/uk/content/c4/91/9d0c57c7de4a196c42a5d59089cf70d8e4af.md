# Вступ

## Reduce

Припустімо, ми хочемо підсумувати масив чисел.
Існує багато способів досягти цієї мети.

- У багатьох мовах цю задачу виражають як цикл зі змінною-акумулятором.
- Цю задачу можна записати як рекурсивну функцію.
  У псевдокоді це могло б мати такий вигляд.

  ```none
  function Add(X, Sum=0):
    if X is empty then
      return Sum
    else
      return Add(rest(X), Sum + first(X))
    end
  end
  ```

  Такий метод поділу задачі на менші частини можна також описати як «зведення до базового випадку».

**Reduce** - це спосіб поєднати всі елементи структури даних в одне значення.
Цей процес перебирає структуру даних, застосовуючи функцію до кожного елемента, щоб оновити накопичений результат.

У `jq` цей процес реалізовано у [фільтрі `reduce`][jq-man-reduce].
В інших мовах його можуть називати «fold», «fold-left», «inject» або «aggregate».

Вираз `reduce` у `jq` має такий вигляд.

```jq
reduce STREAM_EXPRESSION as $var (INITIAL_VALUE; UPDATE_EXPRESSION)
```

- `STREAM_EXPRESSION` - це _потік_ елементів, кожен з яких по черзі зберігається у змінній `$var`.
  - Нагадаємо, щоб перетворити масив на потік, скористаймося [фільтром-ітератором `.[]`][jq-man-iterator]: `$myArray | .[]`.
- `INITIAL_VALUE` - це початкове значення накопиченого результату (відоме як «акумулятор»).
- `UPDATE_EXPRESSION` поєднує («згортає») поточне значення ($var) з акумулятором.
  - У контексті цього виразу `.` - це значення акумулятора.
  - Результат виразу зберігається в акумуляторі для використання в наступній ітерації.
  - Після останньої ітерації накопичений результат стає результатом `reduce`.

Розглянемо приклад: додавання чисел у масиві.
Фільтр `add` робить саме це, але ми побачимо, як його реалізувати.

Якщо взяти `[10, 20, 30, 40]` як вхідні дані та нуль як початковий стан, кожен крок матиме такий вигляд.

| \#  | стан | елемент | згортка | результат |
| :-: | :---: | :-----: | :-----: | :----: |
|  1  |   0   |   10    | 0 + 10  |   10   |
|  2  |  10   |   20    | 10 + 20 |   30   |
|  3  |  30   |   30    | 30 + 30 |   60   |
|  4  |  60   |   40    | 60 + 40 |  100   |

У синтаксисі `jq` це має такий вигляд.

```jq
0 + 10 | . + 20 | . + 30 | . + 40
```

Це можна виразити за допомогою фільтра `reduce`.

```jq
[10, 20, 30, 40] | reduce .[] as $n (0; . + $n)     # => 100
```

<!-- prettier-ignore -->
~~~~exercism/note
Вбудований `add` насправді [реалізовано через `reduce`][jq-code-add], але він використовує «null» як початковий стан (до null можна додати будь-який тип даних).

```jq
def add: reduce .[] as $x (null; . + $x);
```

[jq-code-add]: https://github.com/search?q=repo%3Ajqlang%2Fjq+path%3A**%2Fsrc%2Fbuiltin.jq+%22def+add%28f%29%22&type=code
~~~~

<!-- prettier-ignore-end -->

### Дещо, що варто памʼятати

- У виразі згортки `.` - це акумулятор.
  Якщо вхідні дані - це якийсь обʼєкт, до якого потрібно звернутися всередині функції згортки, його треба зберегти у змінній.

  ```jq
  {"apple": 10, "banana": 16, "carrot": 4}
  | . as $obj
  | reduce (keys | .[]) as $key (0; . + $obj[$key])     # => 30
  ```

- Акумулятор може бути будь-якого типу даних.
  Наприклад, може знадобитися обернути масив.

  ```jq
  ["A", "B", "C", "D"]
  | reduce .[] as $elem ([]; [$elem] + .)       # => ["D", "C", "B", "A"]
  ```

[jq-man-reduce]: https://jqlang.org/manual/#reduce
[jq-man-iterator]: https://jqlang.org/manual/#array-object-value-iterator
