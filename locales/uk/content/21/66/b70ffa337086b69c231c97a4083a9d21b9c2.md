# Докладніше

Typedef-и можна використовувати, щоб скоротити назви складних типів:

```haxe
typedef IA = Array<Int>;
```

Також typedef-и дають змогу створити структуру типу:

```haxe
typedef User = {
  var name:String;
  var age:Int;
}
```
