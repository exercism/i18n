# Використання strings.Join для конкатенації

Не використовуйте `strings.Join`, якщо дані не мають типу `[]string`.
Натомість використайте `fmt.Sprintf`. Це дуже потужний спосіб збирати рядки тексту (англ. string) з кількох частин.
