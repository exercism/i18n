# Підказки

## 1. Визначте непрозорий тип `TreasureChest`.

- Непрозорі типи визначаються за допомогою ключового слова `opaque`.

## 2. Визначте функцію `create`.

- Функцію [`string.length`][length] можна використати, щоб отримати довжину рядка тексту (англ. string).

## 3. Визначте функцію `open`.

- Оператор `==` можна використати, щоб порівняти два рядки тексту на рівність.

[length]: https://hexdocs.pm/gleam_stdlib/gleam/string.html#length
