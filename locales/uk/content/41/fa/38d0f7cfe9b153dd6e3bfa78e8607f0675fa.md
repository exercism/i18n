# Повторне використання коду

Метод `%{callingMethod}` має повторно використати логіку, реалізовану в `%{methodToCall}`. Повторне використання наявних методів може зробити код простішим для підтримки.
