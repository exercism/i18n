# Вступ

Вирази `throw` є альтернативою інструкціям `throw` і, зокрема, можуть розширювати можливості тернарних та інших складених виразів.

```csharp
string trimmed = str == null ? throw new ArgumentException() : str.Trim();
```
