# використовуйте літерал null

Працюючи з `Strings`, надавайте перевагу прямому порівнянню з `null` замість використання `Optional`.

Використання `Optional` у цьому контексті додає зайвої складності й уникає того підходу, який тут задумано: безпосередньої роботи зі значеннями `null`.
