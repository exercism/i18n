# Порівняння байтів

Порівняння `byte` безпечні, коли ми маємо справу з символами ASCII. Щойно в справу вступають символи `utf-8`, усе ускладнюється. Щоб дослідити, додайте додатковий тест зі спеціальними символами й подивіться, що станеться.
