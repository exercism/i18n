# Вступ

## Анонімні функції

У Gleam функції належать до першого класу, тобто це звичайні значення, які можна присвоювати змінним, передавати як аргументи й повертати з інших функцій.

На іменовану функцію, визначену в модулі, можна посилатися за її назвою.

```gleam
pub fn main() {
  // Assign the function to a variable
  let f = add_one
  
  // Invoke the function
  f(1) // -> 2
  f(2) // -> 3
}

fn add_one(x) {
  x + 1
}
```

Також у Gleam є анонімні функції, які визначають усередині інших функцій.

```gleam
// Define the function
let f = fn(x) { x + 1 }

// Invoke the function
f(1) // -> 2
f(2) // -> 3
```

Тип функції записують за синтаксисом, схожим на синтаксис анонімних функцій. Тип функції, яка приймає `Int` і `Float` та повертає `Int`, записують так:

```gleam
fn(Int, Float) -> Int
```

Анонімні функції можуть посилатися на змінні, які були в області видимості на момент їх визначення, і тому вони є _замиканнями_.

```gleam
let secret_number = 42

// This function always returns 42
fn() { secret_number }
```

Синтаксис _захоплення функції_ дає зручний скорочений запис для створення анонімних функцій, які передають єдиний аргумент у функцію. Ці два вирази рівнозначні:

```gleam
// Anonymous function syntax
let f = fn(x) { my_function(1, 2, x) }

// Function capture syntax
let f = my_function(1, 2, _)
```
