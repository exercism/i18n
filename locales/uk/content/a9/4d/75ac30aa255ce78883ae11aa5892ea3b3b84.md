# неоптимальна сигнатура

Якщо подивитися на тести, функція `colorCode` приймає лише один параметр. Ні більше, ні менше. У функції учня оголошено додаткові параметри, але їхнє значення завжди буде `undefined` або тим, що призначено як типове. Допоможіть учневі позбутися зайвих параметрів.