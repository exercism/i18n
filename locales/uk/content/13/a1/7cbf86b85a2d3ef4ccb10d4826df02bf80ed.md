# Конструкція `unless` із блоком `else`

Уникайте виразів `unless` із блоком `else`. Натомість використовуйте вираз `if`.

Причина цього не технічна, а людська. У цьому випадку `else` виконається тоді, коли умову виконано, а це протилежність до того, що підказує саме формулювання.
