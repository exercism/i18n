# Вступ

Винятки - це одна з форм обробки помилок.
Їх називають винятками, бо зазвичай вони виникають, коли ми маємо справу з якоюсь несподіваною подією.

У будь-якому місці нашого коду ми можемо «згенерувати» виняток.
Робимо ми це за допомогою методу `raise`, передаючи обʼєкт, зазвичай обʼєкт Exception, хоча можна використати й звичайні рядки тексту (англ. string).
Наприклад, у заготовках вправ ми побачимо, що використовуємо вбудований `RuntimeError`, щоб повідомити Ruby, що метод ще не реалізовано.
Також можна скористатися скороченим синтаксисом `raise(ExceptionObject, params)`.
Якщо клас винятку не вказано, типово використовується `RuntimeError`.
Наприклад:

```ruby
# These are equivalent
raise RuntimeError.new("Please implement this method")
raise RuntimeError, "Please implement this method"
raise "Please implement this method"
```

Коли Ruby це бачить, помилка спливає на верхній рівень програми, після чого програма завершує роботу.
Наприклад, якщо спробувати поділити щось на нуль, ми побачимо щось таке:
```ruby
5/0

#=> Traceback (most recent call last):
#=> ...
#=> ZeroDivisionError (divided by 0)
```
