# Докладніше

У Haxe, щоб згенерувати випадкове число, можна використати метод `Std.random(x)`. Він генерує випадкове число від 0 включно до `x` не включно:

```haxe
public function main() {
  trace(Std.random(10)); // 0...9
}
```
