# Використання захоплення функції

[comment]: # (потрібно 2 параметри:)
[comment]: # (- expected: виправлена функція)
[comment]: # (- actual: неправильна функція, яку використано)

Коли ми передаємо наявну функцію як аргумент, подбайте про її захоплення, а не про створення нової анонімної функції.

```elixir
# preferred: using capture notation

%{expected}

# discouraged: creating new anonymous functions

%{actual}
```
