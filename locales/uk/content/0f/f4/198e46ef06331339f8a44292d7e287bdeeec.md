# Вказівки

Переверніть заданий рядок тексту (англ. string).

Кілька прикладів:

- Перетворіть `"stressed"` на `"desserts"`.
- Перетворіть `"strops"` на `"sports"`.
- Перетворіть `"racecar"` на `"racecar"`.
