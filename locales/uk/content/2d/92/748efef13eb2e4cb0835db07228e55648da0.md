# порожній типовий параметр

Вкажіть для (типового значення параметра)[https://docs.scala-lang.org/tour/default-parameter-values.html] `name` щось інше, ніж порожній рядок тексту (англ. string). Якщо вказати непорожнє значення, умовну логіку можна буде прибрати з функції.
