# Вступ

## Рекурсія

Здатність чогось визначатися через самого себе називають рекурсією. У Gleam рекурсія найчастіше трапляється у рекурсивних функціях, тобто функціях, які викликають самі себе.

Рекурсивна функція повинна мати принаймні один _базовий випадок_ і принаймні один _рекурсивний випадок_. _Базовий випадок_ повертає значення, не викликаючи функцію знову. _Рекурсивний випадок_ викликає функцію знову, змінюючи вхідні дані так, щоб колись вони збіглися з базовим випадком.

```gleam
pub fn factorial(x: Int) -> Int {
  case x {
    // Base case
    1 -> 1

    // Recursive case
    _ -> x * factorial(x - 1)
  }
}
```

У Gleam немає спеціального синтаксису для циклів, тож усі цикли виконують через рекурсію.

```gleam
pub fn list_length(list: List(String)) -> Int {
  case list {
    [] -> 0
    [_, ..rest] -> 1 + list_length(rest)
  }
}
```

Gleam також підтримує рекурсивні власні типи. У рекурсивного власного типу один або кілька його варіантів посилаються на нього самого у своїх даних.

```gleam
pub type RussianDoll {
  Child               // Base case
  Mother(RussianDoll) // Recursive case
}
```
```gleam
let very_big_doll = Mother(Mother(Mother(Child)))
let small_doll = Mother(Child)
```
