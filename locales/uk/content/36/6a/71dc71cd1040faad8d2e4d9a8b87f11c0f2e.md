# використано розбиття рядка

Використовуйте вбудований тип `rune`, щоб працювати з окремими символами в Go.
`string` можна перетворити на `[]rune` (зріз рун) за допомогою:

```go
aRunes := []rune(a)
```

Розбиття рядка тексту (англ. string) на багато маленьких рядків працює, але це неефективно.
