# Інструкції

У цій вправі ми будемо писати трохи коду, щоб допомогти приготувати чудову лазанью з улюбленої кулінарної книги.

На нас чекають чотири завдання, усі повʼязані з часом, витраченим на приготування лазаньї.

## 1. Визначте очікуваний час у духовці у хвилинах

Визначте константу `OvenTime` зі значенням, що дорівнює кількості хвилин, які лазанья має провести в духовці.
Згідно з кулінарною книгою, очікуваний час у духовці становить 40 хвилин:

```go
OvenTime
// => 40
```

## 2. Обчисліть залишковий час у духовці у хвилинах

Визначте функцію `RemainingOvenTime()`, яка приймає як параметр фактичну кількість хвилин, які лазанья вже провела в духовці, і повертає, скільки хвилин лазанья ще має залишатися в духовці, виходячи з очікуваного часу в духовці з попереднього завдання.

```go
func RemainingOvenTime(actual int) int {
    // TODO
}

RemainingOvenTime(30)
// => 10
```

## 3. Обчисліть час підготовки у хвилинах

Визначте функцію `PreparationTime`, яка приймає як параметр кількість шарів, доданих до лазаньї, і повертає кількість хвилин, витрачених на підготовку лазаньї, якщо припустити, що на підготовку кожного шару потрібно 2 хвилини.

```go
func PreparationTime(numberOfLayers int) int {
    // TODO
}

PreparationTime(2)
// => 4
```

## 4. Обчисліть загальний робочий час у хвилинах

Визначте функцію `ElapsedTime`, яка приймає два параметри: перший - кількість шарів, доданих до лазаньї, а другий - кількість хвилин, які лазанья провела в духовці.
Функція має повернути, скільки хвилин загалом ми витратили на приготування лазаньї, тобто суму часу підготовки та часу, який лазанья провела в духовці на цей момент, у хвилинах.

```go
func ElapsedTime(numberOfLayers, actualMinutesInOven int) int {
    // TODO
}

ElapsedTime(3, 20)
// => 26
```
