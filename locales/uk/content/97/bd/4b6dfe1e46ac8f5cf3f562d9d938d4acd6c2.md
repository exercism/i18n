# Підказки

## Загальне

Документація про `IDisposable` доступна [тут][idisposable]

## 2. Записати трохи даних у базу даних

[`try`/`catch`][try-catch-finally] потрібен, щоб база даних повернулася в стан `Closed`, якщо через некоректні дані буде викинуто виняток.

## 3. Зафіксувати раніше записані дані в базі даних

[`try`/`catch`][try-catch-finally] потрібен, щоб база даних повернулася в стан `Closed`, якщо через некоректні дані буде викинуто виняток.

[idisposable]: https://docs.microsoft.com/en-us/dotnet/api/system.idisposable
[try-catch-finally]: https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/try-catch-finally
