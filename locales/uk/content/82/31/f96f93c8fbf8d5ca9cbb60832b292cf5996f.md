# Запуск тестів

Напишіть код у файлі `<exercise_name>.zig`.

Щоб запустити тести для вправи, виконайте:

```bash
zig test test_exercise_name.zig
```

у кореневій директорії вправи (замініть `exercise_name` на назву вправи).
