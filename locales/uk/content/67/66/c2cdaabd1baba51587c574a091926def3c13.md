# `vec![]`

За допомогою макросів Rust можна створювати вектори.

## Синтаксис

- `vec![]` використовує стандартний макрос Rust, щоб ініціалізувати порожній `Vec<T>`
- `vec![1; 3]` ініціалізує вектор із трьох одиниць
- `vec![1, 2, 3]` ініціалізує вектор літеральними значеннями 1, 2, 3
