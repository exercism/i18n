# Використання `strings.Builder` для конкатенації

Використовувати `strings.Builder` для такого простого випадку - надмірно.
Натомість використайте `fmt.Sprintf` або просту конкатенацію за допомогою оператора `+`.
