# Доповнення до інструкцій

## Зміни, специфічні для Elixir

У цій вправі комплексні числа подано як кортеж-пару, що містить дійсну та уявну частини.

Наприклад, дійсне число `1` - це `{1, 0}`, уявне число `i` - це `{0, 1}`, а комплексне число `4+3i` - це `{4, 3}`.
