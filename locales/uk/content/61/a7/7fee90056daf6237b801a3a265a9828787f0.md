# Додаток

## Наша програма на awk

Тести запускають нашу програму за допомогою `gawk`, передаючи значення у змінну awk, але **не вказують жодних імен файлів і не перенаправляють вхідні дані**.
Реалізуйте логіку всередині блоку `BEGIN`.
