# Вступ

Булеві значення (англ. Boolean) у C# представлені типом `bool`, який може набувати значень `true` або `false`.

C# підтримує три булеві оператори: `!` (логічне заперечення), `&&` (логічне і) та `||` (логічне або).
