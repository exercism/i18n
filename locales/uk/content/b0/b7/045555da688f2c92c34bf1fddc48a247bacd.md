# Вступ

Шаблон - це рядок тексту (англ. string) або файл, який використовується для створення будь-яких рядкових вихідних даних залежно від вхідних даних.

```haxe
var sample = "My name is ::name::";
var template = new haxe.Template(sample);
var myName = {name: "Joe"};
var output = template.execute(myName);

trace(output); // My name is Joe
```

Крім іншого, у шаблонах також можна використовувати умови для перевірки:

```haxe
var sample = "::if isAwake:: I'm awake ::else:: Zzzzz ::end::";
var template = new haxe.Template(sample);
var awake = {isAwake: false};
var output = template.execute(awake);

trace(output); // Zzzzz
```
