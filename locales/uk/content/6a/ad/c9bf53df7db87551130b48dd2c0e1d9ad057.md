# Вступ

## Булеві значення (англ. Boolean)

У F# булеві значення представлені типом `bool`, який може набувати значень `true` або `false`.

F# підтримує три булеві оператори: `not` (логічне заперечення), `&&` (логічне і) та `||` (логічне або).
