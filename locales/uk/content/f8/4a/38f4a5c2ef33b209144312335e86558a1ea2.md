# Вступ

## Булеві значення

Булеві значення (англ. Boolean) у C# представлені типом `bool`, значення якого можуть бути або `true`, або `false`.

C# підтримує три булеві оператори: `!` (логічне заперечення), `&&` (логічне і) та `||` (логічне або).
