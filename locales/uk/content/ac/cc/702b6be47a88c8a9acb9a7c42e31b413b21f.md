# значення не повертається

Схоже, функція `${export.name}` не в усіх випадках `return`-ить значення.

Чи впевнені ми, що тести проходять?

Якщо так, створіть issue тут (**Incorrect Analysis**) і додайте своє рішення:

<https://github.com/exercism/javascript-analyzer/issues/new/choose>
