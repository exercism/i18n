# Уникаймо повернення булевих літералів

Замість того щоб повертати булеві літерали після умови, можна повернути саму умову.

Приклад:

```java
// instead of
if (someExpression) {
  return true;
} else {
  return false;
}

// ... return the expression directly
return someExpression;
```
