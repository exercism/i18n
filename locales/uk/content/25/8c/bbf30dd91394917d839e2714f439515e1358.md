# Тести

## Запуск тестів

З теки вправи виконайте `exercism test` або `eutest`.
Після цього Euphoria запустить `t_<exercise>.e` для перевірки рішення.

## Читання вихідних даних

На початку вправи у файлі рішення немає поверненого значення, тож під час запуску він завершиться аварійно.

```shell
C:\path\to\leap.ex:3
<0120>:: no value returned from function
end function
           ^

FAILURE: t_leap.e EUPHORIA error with status 1

Test results summary:
    FAIL: t_leap.e
Files (run: 1) (failed: 1) (0% success)
```

Так і має бути.
Реалізуйте функцію, а потім запустіть тести знову, щоб побачити, які з них проходять, а які ні.

```shell
interpreting t_leap.e:
  passed: year not divisible by 4 in common year
  failed: year divisible by 2, not divisible by 4 in common year, expected: FALSE but got: TRUE
  failed: year divisible by 4, not divisible by 100 in leap year, expected: TRUE but got: FALSE
  ...
  9 tests run, 6 passed, 3 failed, 67% success
FAILURE: t_leap.e program died with status 1

Test results summary:
    FAIL: t_leap.e
Files (run: 1) (failed: 1) (0% success)
```

## Поступове виконання тестів

Гарний спосіб почати - закоментувати всі тести, крім першого, у `t_<exercise>.e`.

```euphoria
test_false("year not divisible by 4 in common year", leap(1800))
-- test_false("year divisible by 100 but not by 3 is still not a leap year", leap(1900))
-- test_false("year divisible by 2, not divisible by 4 in common year", leap(1970))
```

Пройдімо їх по одному, розкоментовуючи наступний тест, щойно поточний пройде.
Коли всі тести проходять, вправу виконано.
