У Chapel, якщо функція може викинути помилку, це потрібно вказати у визначенні функції за допомогою ключового слова `throws`, як показує наведений нижче шаблон.

```chapel
proc canThrow(n: int) throws {
  // function body here 
}
```
