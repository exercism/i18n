# Суміш `runes` і `bytes`

Використовувати суміш `runes` і `bytes` тут безпечно, бо вхідні дані містять лише символи ASCII.
Щоб побачити, що станеться, коли це небезпечно, спробуйте додати тест, який використовує символи, що не належать до ASCII.
