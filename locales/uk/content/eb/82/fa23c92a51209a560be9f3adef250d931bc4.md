# Додаток до інструкцій

## Обмеження

Можна припустити, що вхідні дані `isBalanced` ніколи не містять понад 10 рівнів вкладеності.

Цю вправу цілком можна розвʼязати без `allocator`, але зауважмо: функція `isBalanced` не може повертати просто `bool`, бо тести використовують `try`.
