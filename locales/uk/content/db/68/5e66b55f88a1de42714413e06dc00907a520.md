# Експорт у рядку

Функції, як-от `value`, класи та константи, як-от `COLORS`, можна експортувати одразу в рядку за допомогою `export`.

```javascript
export %{constant.signature}

export %{method.signature}
```
