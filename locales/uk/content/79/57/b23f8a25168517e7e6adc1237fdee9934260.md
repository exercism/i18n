# Вступ

Цикл `do-while` - це альтернатива циклу `while`, і єдина відмінність у тому, що цикл `do-while` виконується принаймні один раз:

```haxe
var i = 0;

do {
  trace("Hello");
} while (i < 10);
```
