# Вступ

Функція вищого порядку - це функція, яка або:

- Приймає функцію як аргумент
- Повертає функцію

Аргумент-функція - це просто звичайний аргумент, хоч і такий, який можна викликати.

```fsharp
let double = fun x -> x * 2

let apply f x = f x

apply double 3
// => 6
```
