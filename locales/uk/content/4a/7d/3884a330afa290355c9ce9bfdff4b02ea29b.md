# Вступ

У Kotlin функції є обʼєктами першого класу.
Це означає, що їх можна зберігати у змінних, передавати як аргументи і навіть повертати з інших функцій.
Наприклад, присвоєння функції змінній `addOne`:

```kotlin
val addOne = fun (num : Int) : Int {
    return num + 1
}
```

Функція також може повертати іншу функцію:

```kotlin
fun makeAddOne() : (Int) -> Int {
    return fun(num : Int) : Int {
        return num + 1
    }
}

val addOne = makeAddOne()
```

Після присвоєння функцію можна викликати через змінну так само, як будь-яку іншу функцію:
```kotlin
addOne(3)
// => 4
```

## Оголошення типів функцій

Тип змінної або параметра можна оголосити за допомогою синтаксису `(parameter types) -> return type`.
Наприклад, оголошення змінної:

```kotlin
val addOne : (Int) -> Int = fun (num : Int) : Int {
    return num + 1
}
```

А для параметра функції:

```kotlin
fun perform(operation : (Int) -> Int) {
    operation(3)
}
```

## Лямбда-вирази

Лямбда-вираз дає змогу визначити функцію коротше.
Їх записують у формі `{ parameters -> body }`.
Вираз повертає останнє значення з останньої інструкції в тілі.
Наприклад:

```kotlin
// Assigning to a variable
val addOne = { num : Int -> num +  1}

// In a function call
perform({ num : Int -> num + 1 })
```

Kotlin може виводити типи, коли є достатньо інформації.
Наприклад, наведені вище приклади можна записати й так:

```kotlin
// Assigning to a variable
val addOne : (Int) -> Int = { num -> num + 1 }

// Or ...
val addOne  = { num : Int -> num + 1 }

// Using in a function
perform({ num -> num + 1 })
```

## `it` для лямбд з одним параметром

Коли у виразі лише один параметр, його можна пропустити.
Тоді Kotlin зробить параметр із назвою `it` доступним у тілі.
Наприклад:

```kotlin
val addOne : (Int) -> Int = { it +  1 }
```

## Передавання лямбди останнім аргументом

Коли лямбда є останнім аргументом функції, лямбда-вираз записують безпосередньо _після_ дужок.
Наприклад:

```kotlin
// Function that takes a function as last argument
fun calculateFrom(from : Int, operation : (Int) -> Int) : Int {
    return operation(from)
}

// Call the calculateFrom function
calculateFrom(2) { it * 3 }
// => 6
```
