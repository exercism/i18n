# Помилку зафіксовано без джерельного файлу

Очікувався джерельний файл `%{expected}`, а знайдено: `%{available}`.

Тестовий файл робить `import` з очікуваного джерельного файлу.
Переконайтеся, що файл існує і має правильну назву.
