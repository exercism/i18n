# Вступ

Булеве значення (англ. Boolean) може набувати одного з двох значень: `true` або `false`.
Логічні оператори, як-от `!` (заперечення), `&&` (логічне і) та `||` (логічне або), зазвичай використовують із булевими значеннями. Вони повертають булеве значення.
