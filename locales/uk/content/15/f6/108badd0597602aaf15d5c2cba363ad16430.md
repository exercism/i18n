# функцію рішення не знайдено

Набір тестів для цієї вправи очікує, що у файлі `src/lib.rs` буде визначено таку функцію:
```rust
pub fn reverse(input: &str) -> String {
}
```
Потрібної функції не знайдено.
