# Вступ

## Імпортування

Хоча Swift надає багато функціональності у своїй стандартній бібліотеці, багато додаткових можливостей можна отримати, імпортуючи зовнішні бібліотеки та фреймворки.
Ці модулі можуть походити безпосередньо від проєкту Swift (як-от [Swift Argument Parser][argument-parser]), від фреймворків платформи Apple (як-от [Foundation][apple-foundation] і [Network Framework][network-framework]) або від сторонніх бібліотек з відкритим кодом (як-от [SwiftyBeaver][swifty-beaver]).

Основні модулі, як-от Foundation, одна з найуживаніших бібліотек у Swift, входять до складу інструментарію Swift.
Сторонні бібліотеки потрібно додати до конфігурації проєкту (наприклад, скориставшись Swift Package Manager), перш ніж їх можна буде імпортувати.

Імпортування модулів відбувається так: пишемо ключове слово `import`, а потім назву модуля.
Тож щоб імпортувати Foundation, достатньо додати до своєї програми таке.

```swift
import Foundation
```

Імпортування модуля робить його типи, методи й функції доступними для нашого коду.
Наприклад, імпорт Foundation розширює `String` корисними допоміжними методами, як-от `components(separatedBy:)`.

За домовленістю інструкції `import` ставлять на початку файлу Swift, щоб залежності було відразу видно кожному, хто читає код.

## Індексування рядків

Окремі символи в `String` можна отримати тим самим способом індексування, який використовують для масивів.
Однак [індекси][string-indices], які використовують для рядків тексту (англ. string), _не_ є цілими числами, і працювати з ними безпосередньо не можна.
Замість цього їх потрібно обчислити на основі якогось іншого відомого індексу, наприклад `startIndex`, який указує на позицію першого символу в непорожньому рядку тексту, скориставшись методами, доступними в бібліотеках [`String`][string-docs] та [`NSString`][nsstring-docs] (коли модуль `Foundation` імпортовано, рядки тексту у Swift мають доступ до всіх властивостей і методів NSString).

Наприклад, маємо такий рядок тексту:

```swift
let csv = "apple,pear,peach,orange,cherry,lime,goosberry"
```

Не можна написати `csv[21]`, щоб отримати "g" в "orange": замість цього потрібно обчислити значення типу `String.Index` і передати саме його.
Зауважмо, що ці індекси не призначені для безпосереднього сприйняття людиною.
Їх називають _непрозорими індексами_, бо людині не потрібно знати, що всередині них.

```swift
let index = csv.index(csv.startIndex, offsetBy: 21)
csv[index]
// "g"
print(index)
// prints Index(_rawBits: 1376513)
```

Проте зауважмо: якщо зсув не відповідає дійсному індексу, тобто якщо він указує на позицію перед `startIndex` або після `endIndex`, операція спричинить помилку й аварійно завершить програму.
Щоб запобігти цій проблемі, можна вказати обмежувальний індекс.
Тоді операція повертає необовʼязковий індекс і поверне nil для індексів, які інакше були б недійсними.

```swift
let tooFar = csv.index(csv.startIndex, offsetBy: 200)
// error: Execution was interrupted, reason: EXC_BAD_INSTRUCTION (code=EXC_I386_INVOP, subcode=0x0).
let tooFarButSafe = csv.index(csv.startIndex, offsetBy: 200, limitedBy: csv.endIndex)
// nil
```

Крім того, індекси не можна використовувати для різних рядків тексту.
Наприклад, якщо скористатися обчисленим вище `index` для "g" в "orange", щоб проіндексувати рядок `fam = "This is my family: 👨‍👩‍👦‍👦, this is our dog: 🐶, this is our cat: 🐱!"`, тобто `fam[index]`, програма аварійно завершиться.

Для всього цього є багато причин, але всі вони зводяться до того, що «з Unicode усе непросто».

Загалом, якщо потрібен довільний доступ до окремих символів, імовірно, варто використати якусь іншу структуру даних, як-от `Array<Character>`.

### Деякі корисні методи

У Swift є методи `first` і `last`, щоб отримати перший і останній символ рядка тексту.
Ці методи повертають необовʼязковий символ, тож якщо рядок тексту порожній, вони повернуть nil.

```swift
let str = "Hello, world!"
print(str.first) // Prints Optional("H")
print(str.last)  // Prints Optional("!")
```

Також можна скористатися методами `prefix` і `suffix`, щоб отримати підрядок рядка тексту.
Ці методи приймають ціле число як аргумент і повертають підрядок, що містить перші або останні n символів рядка тексту.

```swift
let str = "Hello, world!"
print(str.prefix(5)) // Prints "Hello"
print(str.suffix(6)) // Prints "world!"
```

## Методи рядків

Робота з текстом трапляється в програмуванні часто.
Swift разом із фреймворком Foundation надає багато зручних методів і властивостей для перегляду, перетворення й опрацювання рядків тексту.

## Розбиття рядків тексту за допомогою `components(separatedBy:)`

Щоб розбити рядок тексту на масив підрядків за розділювачем, можна скористатися методом `components(separatedBy:)` (потрібен імпорт Foundation):

```swift
import Foundation

let sentence = "Hello, world!"
let words = sentence.components(separatedBy: " ")
print(words) // Prints ["Hello,", "world!"]
```

## Перевірка префіксів і суфіксів за допомогою `hasPrefix(_:)` і `hasSuffix(_:)`

Щоб перевірити, чи рядок тексту починається або закінчується певною послідовністю символів, можна скористатися методами `hasPrefix(_:)` і `hasSuffix(_:)`:

```swift
let greeting = "Hello, world!"
print(greeting.hasPrefix("Hello"))  // Prints true
print(greeting.hasSuffix("world!")) // Prints true
```

## Зміна регістру

Змінити регістр рядка тексту можна за допомогою цих стандартних методів і властивостей:

- `lowercased()`: повертає копію рядка тексту в нижньому регістрі.
- `uppercased()`: повертає копію рядка тексту у верхньому регістрі.
- `capitalized`: повертає копію, у якій першу літеру кожного слова записано з великої літери (з Foundation).

```swift
let greeting = "hello, world!"
print(greeting.lowercased())  // Prints "hello, world!"
print(greeting.uppercased())  // Prints "HELLO, WORLD!"
print(greeting.capitalized)   // Prints "Hello, World!"
```

## Заміна підрядків за допомогою `replacingOccurrences(of:with:)`

Щоб замінити всі входження цільового підрядка на інший рядок тексту, можна скористатися методом `replacingOccurrences(of:with:)` (з Foundation):

```swift
let greeting = "Hello, world!"
let updated = greeting.replacingOccurrences(of: "world", with: "Swift")
print(updated) // Prints "Hello, Swift!"
```

## Зʼєднання рядків за допомогою `joined(separator:)`

Метод `joined(separator:)` належить колекціям рядків тексту, як-от `[String]`.
Він поєднує елементи в один рядок тексту за допомогою розділювача:

```swift
let words = ["Hello", "world"]
let sentence = words.joined(separator: ", ")
print(sentence) // Prints "Hello, world"
```

[components]: https://developer.apple.com/documentation/foundation/nsstring/components(separatedby:)-238fy
[hasPrefix]: https://developer.apple.com/documentation/foundation/nsstring/hasprefix(_:)
[hasSuffix]: https://developer.apple.com/documentation/foundation/nsstring/hassuffix(_:)
[lowercased]: https://developer.apple.com/documentation/foundation/nsstring/lowercased
[uppercased]: https://developer.apple.com/documentation/foundation/nsstring/uppercased
[capitalized]: https://developer.apple.com/documentation/foundation/nsstring/capitalized
[replacingOccurrences]: https://developer.apple.com/documentation/foundation/nsstring/replacingoccurrences(of:with:)
[joined]: https://developer.apple.com/documentation/swift/array/joined(separator:)-7uber
[string-docs]: https://developer.apple.com/documentation/swift/String
[nsstring-docs]: https://developer.apple.com/documentation/foundation/nsstring
[string-format-specifiers]: https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/Strings/Articles/formatSpecifiers.html
[string-indices]: https://docs.swift.org/swift-book/documentation/the-swift-programming-language/stringsandcharacters/#String-Indices
[unicode]: https://en.wikipedia.org/wiki/Unicode
[argument-parser]: https://apple.github.io/swift-argument-parser/documentation/argumentparser/
[network-framework]: https://developer.apple.com/documentation/network
[swifty-beaver]: https://github.com/SwiftyBeaver/SwiftyBeaver
[apple-foundation]: https://developer.apple.com/documentation/foundation
