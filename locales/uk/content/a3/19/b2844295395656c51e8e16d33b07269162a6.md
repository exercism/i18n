# Instructions

У цій вправі нам потрібно перекласти кілька правил із класичної гри Pac-Man у функції Unison.

У нас є чотири правила для перекладу, усі повʼязані зі станами гри.

> Не хвилюймося про те, як виводяться аргументи, а просто зосередьмося на поєднанні аргументів, щоб повернути потрібний результат.

## 1. Визначте, чи зʼїдає Pac-Man привида

Визначте функцію `pacmanRules.eatGhost`, яка приймає два аргументи (_чи має Pac-Man активну енергетичну таблетку_ та _чи торкається Pac-Man привида_) і повертає булеве значення (англ. Boolean), якщо Pac-Man може зʼїсти привида. Функція повинна повертати правду, лише якщо Pac-Man має активну енергетичну таблетку і торкається привида.

```unison
pacmanRules.eatGhost: Boolean -> Boolean -> Boolean
pacmanRules.eatGhost powerPelletActive touchingGhost =
  todo "implement eatGhost"
```

## 2. Визначте, чи набирає очки Pac-Man

Визначте функцію `pacmanRules.score`, яка приймає два аргументи (_чи торкається Pac-Man енергетичної таблетки_ та _чи торкається Pac-Man крапки_) і повертає булеве значення, якщо Pac-Man набрав очки. Функція повинна повертати правду, якщо Pac-Man торкається енергетичної таблетки або крапки.

```haskell
pacmanRules.score: Boolean -> Boolean -> Boolean
pacmanRules.score powerPelletActive touchingDot =
  todo "implement score"
```

## 3. Визначте, чи програє Pac-Man

Визначте функцію `pacmanRules.lose`, яка приймає два аргументи (_чи має Pac-Man активну енергетичну таблетку_ та _чи торкається Pac-Man привида_) і повертає булеве значення, якщо Pac-Man програє. Функція повинна повертати правду, якщо Pac-Man торкається привида і не має активної енергетичної таблетки.

```unison
pacmanRules.lose : Boolean -> Boolean -> Boolean
pacmanRules.lose powerPelletActive touchingGhost =
  todo "implement lose"
```

## 4. Визначте, чи перемагає Pac-Man

Визначте функцію `pacmanRules.win`, яка приймає три аргументи (_чи зʼїв Pac-Man усі крапки_, _чи має Pac-Man активну енергетичну таблетку_ та _чи торкається Pac-Man привида_) і повертає булеве значення, якщо Pac-Man перемагає. Функція повинна повертати правду, якщо Pac-Man зʼїв усі крапки і не програв на основі аргументів, визначених у частині 3.

```unison
pacmanRules.win : Boolean -> Boolean -> Boolean -> Boolean
pacmanRules.win eatenAll powerPelletActive touchingGhost
  todo "implement Rules.win"
```
