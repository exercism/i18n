# Вступ

Змінним, оголошеним із ключовим словом `final` замість `var`, можна присвоїти значення лише один раз.

```haxe
final a = "hello";
var b = "world";
trace(a, b); // hello, world
b = "haxe";
trace(a, b); // hello, haxe

// The following line would cause a compilation error:
// a = "bye";
```
