# Вступ

У F# булеві значення (англ. Boolean) представлені типом `bool`, значеннями якого можуть бути або `true`, або `false`.

F# підтримує три булеві оператори: `not` (заперечення), `&&` (логічне і) та `||` (логічне або).
