# Інструкції, специфічні для SQLite

У цій вправі потрібно встановити правильне значення стовпця `result` таблиці `color_code` на основі кольору, знайденого в стовпці `color`.

## Схема таблиці

```sql
CREATE TABLE "color_code" ("color" TEXT, "result" INT);
```
