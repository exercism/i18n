# Докладніше

Щоб перевірити, чи дві змінні простих типів (рядки тексту (англ. string), цілі числа тощо) рівні, ми використовуємо оператори рівності `==` і `!=`:

```haxe
var a = 5;
var b = 5;

a == b; // true
a != b; // false
```
