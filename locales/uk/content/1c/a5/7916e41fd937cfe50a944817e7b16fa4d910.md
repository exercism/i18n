# Вступ

У цій вправі нам також може знадобитися цикл. Існує кілька способів писати цикли в C#, але цикл `while` тут найдоречніший:

```csharp
int x = 23;

while (x > 10)
{
    // Execute logic if x > 10
    x = x - 2;
}
```
