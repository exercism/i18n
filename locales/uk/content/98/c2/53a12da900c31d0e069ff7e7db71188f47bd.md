# Вступ

Багато операторів можна також використовувати як складені присвоєння, тобто як скорочений запис, де `x = op y` можна записати як `x op= y`:

```csharp
var features = PhoneFeatures.Call;
features |= PhoneFeatures.Text;
features &= ~PhoneFeatures.Call;
```
