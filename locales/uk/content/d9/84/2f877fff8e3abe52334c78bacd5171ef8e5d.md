# Вступ

Стандартна бібліотека надає enum з назвою `Option`, який допомагає нам виразити, коли значення може існувати, а коли ні.
Його записують як `Option<T>`, де `T` позначає тип значення, з яким ми працюємо.
