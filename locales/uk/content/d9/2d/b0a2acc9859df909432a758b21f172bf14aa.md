# Вступ

У PHP змінну можна викликати як функцію, застосувавши до неї дужки [`()`]. Коли PHP намагається викликати функцію, він шукає функцію, назва якої збігається зі значенням змінної.

```php
function foo(): string
{
    return "foo() invoked";
}

$func = 'foo';
$func(); // This calls foo()
```
