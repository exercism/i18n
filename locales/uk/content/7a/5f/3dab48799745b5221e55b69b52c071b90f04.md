# Використайте `rand.uniform` замість `Enum.random`

Використайте `:rand.uniform` замість `Enum.random` у функції `random_stardate`. `:rand.uniform` дає змогу згенерувати випадкове число з плаваючою комою напряму, на відміну від `Enum.random`, який дає змогу генерувати лише випадкові цілі числа.
