# Використати один форматований рядок, а не кілька

Повертаються два дуже схожі примірники `string`. Як структурувати цей код так, щоб прибрати це дублювання? Для цього потрібно прибрати розгалуження та повертати лише один `string`.
