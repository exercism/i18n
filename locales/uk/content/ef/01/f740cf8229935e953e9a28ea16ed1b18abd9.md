# Віддаймо перевагу IO.puts у log_sent_email

Коли ми записуємо рядок у файл, краще використовувати `IO.puts/2`, а не `IO.write/2`, бо `IO.puts/2` автоматично додає символ нового рядка в кінці.

```elixir
# preferred
IO.puts(pid, email)

# not preferred
IO.write(pid, email)
IO.write(pid, "\n")
```
