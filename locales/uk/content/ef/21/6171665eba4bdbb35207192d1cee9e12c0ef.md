# виведення в else

Якщо операція `IO.write` завершується успішно, блок `else` зіставляється з результатом і повертає виведення для всього блоку. Використайте блок `else`, щоб повернути виведення функції `write/3`.
