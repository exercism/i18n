# Вступ

Часто буває корисно обʼєднати колекцію елементів у групу й працювати з такими групами як з єдиним цілим.

У Rust таку групу ми називаємо struct, а кожен елемент - одним із полів struct. Struct визначає загальний набір доступних полів, а конкретний приклад struct називається екземпляром.
