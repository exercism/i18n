# Докладніше

[Nil][nil-dictionary] - англійське слово, що означає «ніщо» або «нуль».
У Ruby `nil` - це обʼєкт, який використовують, щоб виразити _відсутність_ значення.
В інших мовах програмування подібну роль можуть відігравати значення `null` або `none`.

```ruby
# I do not have a favorite color
favorite_color = nil
```

Ruby надає будь-якій змінній екземпляра типове значення `nil`, коли вперше стикається з нею, доки не буде встановлено інше.

[nil-dictionary]: https://www.merriam-webster.com/dictionary/nil
