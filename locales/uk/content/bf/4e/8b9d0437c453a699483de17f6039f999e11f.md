# Вступ

У Python [`enum`][enum-docs] — це набір імен, привʼязаних до унікальних `literal` або `constant` значень.
`Enums` визначають через успадкування від класу `Enum` або створення його підкласу.
Вбудовані типи `enum` доступні в модулі `enum`, а клас `Enum` можна імпортувати за допомогою `from enum import Enum`.


```python
from enum import Enum

class Color(Enum):
    RED = 1
    GREEN = 2
```

Зауважмо, що значеннями членів `enum` можуть бути будь-які типи даних, як-от `str`, `tuple`, `float` тощо.

```python
class Color(Enum):
    RED = 'red'
    GREEN = 'green'
```

Якщо двом членам `enum` присвоїти однакове значення, то член, присвоєний пізніше, стане псевдонімом для першого. Не можна використовувати однакове імʼя для двох різних членів `enum`.

```python
class Color(Enum):
    RED = 1
    GREEN = 2
    ALIAS_OF_RED = 1

Color.ALIAS_OF_RED
#=> <Color.RED: 1>

Color.ALIAS_OF_RED.value
#=> 1
```

Перебрати члени `enum` можна стандартним синтаксисом `for member in`:

```python
for member in Color:
    print((member.name, member.value))
#=> (RED, 1)
#=> (GREEN, 2)
```

Члени `Enum` можна порівнювати за допомогою [`is` (_оператор ідентичності_)][identity-keyword] або `is not`. `==` або `!=` (_оператори рівності_) працюють так само:

```python
a = Color.RED

a is Color.RED
#=> True

a == Color.RED
#=> True
```

Щоб отримати член `enum` за заданим значенням, можна скористатися `<EnumName>(<value>)`:

```python
g = Color(2)

g is Color.GREEN
#=> True

g
#=> <Color.GREEN: 2>
```

[enum-docs]: https://docs.python.org/3/library/enum.html
[identity-keyword]: https://www.w3schools.com/python/ref_keyword_is.asp
