# Підказки

## Сортування рядків тексту

Щоб порівняти два рядки тексту (англ. string), можна використати метод `string.bytes` і перебирати байти, доки не дійдемо висновку, що один рядок тексту менший за інший.
