# Οδηγίες

Σε αυτή την άσκηση θα επεξεργάζεσαι γραμμές καταγραφής.

Κάθε γραμμή καταγραφής είναι μια συμβολοσειρά με την εξής μορφή: `"[<LEVEL>]: <MESSAGE>"`.

Υπάρχουν τρία διαφορετικά επίπεδα καταγραφής:

- `INFO`
- `WARNING`
- `ERROR`

Έχεις τρεις εργασίες, καθεμία από τις οποίες παίρνει μια γραμμή καταγραφής και σου ζητά να κάνεις κάτι με αυτήν.

## 1. Πάρε το μήνυμα από μια γραμμή καταγραφής

Υλοποίησε τη συνάρτηση `message()` ώστε να επιστρέφει το μήνυμα μιας γραμμής καταγραφής:

```haxe
message("[ERROR]: Invalid operation")
// => "Invalid operation"
```

Οποιαδήποτε κενά διαστήματα στην αρχή ή στο τέλος πρέπει να αφαιρούνται:

```haxe
message("[WARNING]:  Disk almost full\r\n")/ => "Disk almost full"
```

## 2. Πάρε το επίπεδο καταγραφής από μια γραμμή καταγραφής

Υλοποίησε τη συνάρτηση `logLevel()` ώστε να επιστρέφει το επίπεδο καταγραφής μιας γραμμής καταγραφής, το οποίο πρέπει να επιστρέφεται με πεζά γράμματα:

```haxe
logLevel("[ERROR]: Invalid operation")
// => "error"
```

## 3. Μορφοποίησε ξανά μια γραμμή καταγραφής

Υλοποίησε τη συνάρτηση `reformat()` που μορφοποιεί ξανά τη γραμμή καταγραφής, βάζοντας πρώτα το μήνυμα και μετά το επίπεδο καταγραφής μέσα σε παρενθέσεις:

```haxe
reformat("[INFO]: Operation completed")
// => "Operation completed (info)"
```
