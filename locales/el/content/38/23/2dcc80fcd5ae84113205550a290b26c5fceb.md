# Ειδικές οδηγίες για SQLite

Αυτή η άσκηση απαιτεί να ορίσεις τη στήλη `result` του πίνακα `roman-numerals` στη σωστή τιμή, με βάση τον αριθμό που βρίσκεται στη στήλη `number`.

## Σχήμα πίνακα

```sql
CREATE TABLE "roman-numerals" ("number" INT, "result" TEXT);
```
