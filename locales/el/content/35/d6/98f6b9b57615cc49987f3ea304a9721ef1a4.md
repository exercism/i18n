# Εισαγωγή

Τα Boolean στη Haxe αντιπροσωπεύονται από τον τύπο `Bool`, του οποίου οι τιμές μπορούν να είναι είτε `true` είτε `false`.

Η Haxe υποστηρίζει τρεις boolean τελεστές: `!` (NOT), `&&` (AND) και `||` (OR).
