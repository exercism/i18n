# Εισαγωγή

Σε αυτή την άσκηση μπορεί επίσης να θέλεις να χρησιμοποιήσεις έναν βρόχο. Υπάρχουν αρκετοί τρόποι για να γράψεις βρόχους στη C#, αλλά ο `while` βρόχος είναι ο πιο κατάλληλος εδώ:

```csharp
int x = 23;

while (x > 10)
{
    // Execute logic if x > 10
    x = x - 2;
}
```
