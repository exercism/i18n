# Υποδείξεις

## Γενικά

Η τεκμηρίωση που σχετίζεται με το `IDisposable` είναι [εδώ][idisposable]

## 2. Γράψε κάποια δεδομένα στη βάση δεδομένων

Το [`try`/`catch`][try-catch-finally] είναι απαραίτητο για να διασφαλιστεί ότι η βάση δεδομένων επανέρχεται στην κατάσταση `Closed` αν πεταχτεί μια εξαίρεση εξαιτίας λανθασμένων δεδομένων.

## 3. Κάνε commit τα δεδομένα που έγραψες προηγουμένως στη βάση δεδομένων

Το [`try`/`catch`][try-catch-finally] είναι απαραίτητο για να διασφαλιστεί ότι η βάση δεδομένων επανέρχεται στην κατάσταση `Closed` αν πεταχτεί μια εξαίρεση εξαιτίας λανθασμένων δεδομένων.

[idisposable]: https://docs.microsoft.com/en-us/dotnet/api/system.idisposable
[try-catch-finally]: https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/try-catch-finally
