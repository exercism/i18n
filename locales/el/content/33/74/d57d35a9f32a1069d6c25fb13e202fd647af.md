# Σχετικά

Στη Haxe, μπορείς να δημιουργήσεις έναν τυχαίο αριθμό χρησιμοποιώντας τη μέθοδο `Std.random(x)`. Δημιουργεί έναν τυχαίο αριθμό από το 0 (συμπεριλαμβανομένου) έως το `x` (μη συμπεριλαμβανομένου):

```haxe
public function main() {
  trace(Std.random(10)); // 0...9
}
```
