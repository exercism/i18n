# Εισαγωγή

## Boolean

Στη F#, οι Boolean τιμές αναπαρίστανται από τον τύπο `bool`, οι τιμές του οποίου μπορούν να είναι είτε `true` είτε `false`.

Η F# υποστηρίζει τρεις τελεστές Boolean: `not` (ΟΧΙ), `&&` (ΚΑΙ) και `||` (Ή).
