# Εισαγωγή

Στη Haxe, για να δημιουργήσεις έναν τυχαίο αριθμό, μπορείς να χρησιμοποιήσεις τη μέθοδο `Std.random(x)`. Δημιουργεί έναν τυχαίο αριθμό από το 0 (συμπεριλαμβανομένου) έως το `x` (μη συμπεριλαμβανομένου):

```haxe
public function main() {
  trace(Std.random(10)); // 0...9
}
```
