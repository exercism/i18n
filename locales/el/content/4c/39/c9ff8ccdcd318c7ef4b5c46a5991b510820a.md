# Σχετικά

Εκτός από τις δηλώσεις συναρτήσεων και τις εκφράσεις συναρτήσεων, η JavaScript έχει επίσης μια άλλη πολύ συνοπτική σύνταξη για τον ορισμό μιας συνάρτησης.
Αυτές οι συναρτήσεις ονομάζονται _συναρτήσεις βέλους_.

Σε αυτή την έννοια, θα επικεντρωθούμε στη σύνταξη που χρησιμοποιείται για να γράψουμε μια συνάρτηση βέλους.
Υπάρχουν διαφορές στον τρόπο που λειτουργεί μια συνάρτηση βέλους, όπως η δέσμευση του _this_, που θα καλυφθούν σε άλλες έννοιες.

Ακολουθεί μια σύγκριση μεταξύ μιας δήλωσης συνάρτησης και μιας συνάρτησης βέλους.

```javascript
function addUpTwoNumbers(num1, num2) {
  return num1 + num2;
}

// function keyword removed and => added
const addUpTwoNumbers = (num1, num2) => {
  return num1 + num2;
};
```

Παραπάνω, θα δεις ότι η σύνταξη της συνάρτησης βέλους:

1. αφαιρεί τη λέξη-κλειδί `function`
2. έχει δηλώσει το αναγνωριστικό `addUpTwoNumbers` ως `const`
3. προσθέτει ένα παχύ βέλος `=>`

Αν το σώμα της συνάρτησης περιέχει μόνο μια εντολή return, όπως στο παραπάνω παράδειγμα, τα `{}` και η λέξη-κλειδί `return` μπορούν να παραλειφθούν.

<!-- prettier-ignore-start -->
```javascript
const addUpTwoNumbers = (num1, num2) => { return num1 + num2 };

// can be shortened to
const addUpTwoNumbers = (num1, num2) => num1 + num2;
// braces {} and return removed
```
<!-- prettier-ignore-end -->

Στην ειδική περίπτωση που μια συνάρτηση βέλους επιστρέφει μόνο ένα αντικείμενο, χρειάζονται παρενθέσεις γύρω από το αντικείμενο για να μπορεί να παραλειφθεί η εντολή return.

```javascript
// explicit return of object
const addUpTwoNumbers = (num1, num2) => {
  return { num1, num2 };
};

// implicit return of object
const addUpTwoNumbers = (num1, num2) => ({ num1, num2 });
```

Η χρήση παρενθέσεων γύρω από τις παραμέτρους εξαρτάται από τον αριθμό των παραμέτρων.

<!-- prettier-ignore-start -->
```javascript
// one parameter does not need parenthesis
const square = num => num * num;

// two or more parameters need to be wrapped in parenthesis
const addUpTwoNumbers = (num1, num2) => num1 + num2;
```
<!-- prettier-ignore-end -->

Άλλες έννοιες, όπως οι [Παράμετροι rest][concept-rest] και η [Αποδόμηση][concept-destructure], μπορούν επίσης να χρησιμοποιηθούν με μια συνάρτηση βέλους.

[concept-rest]: /tracks/javascript/concepts/rest-and-spread
[concept-destructure]: /tracks/javascript/concepts/array-destructuring
