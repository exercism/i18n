# Εισαγωγή

## Η διεπαφή ως σύνολο μεθόδων

Στην πιο απλή της μορφή, ένας **τύπος διεπαφής** είναι ένα σύνολο υπογραφών μεθόδων.
Ακολουθεί ένα παράδειγμα ορισμού διεπαφής που περιλαμβάνει δύο μεθόδους, τις `Add` και `Value`:

```go
type Counter interface {
    Add(increment int)
    Value() int
}
```

Τα ονόματα των παραμέτρων, όπως το `increment`, μπορούν να παραλειφθούν από τον ορισμό της διεπαφής, αλλά συχνά αυξάνουν την αναγνωσιμότητα.

Τα ονόματα των διεπαφών στη Go δεν περιέχουν τη λέξη `Interface` ή το `I`.
Αντ' αυτού, συχνά τελειώνουν σε `er`, π.χ. `Reader`, `Stringer`.

## Η υλοποίηση μιας διεπαφής

Οποιοσδήποτε τύπος ορίζει τις μεθόδους της διεπαφής την "υλοποιεί" αυτόματα και έμμεσα.
Δεν υπάρχει λέξη-κλειδί `implements` στη Go.

Ο παρακάτω τύπος υλοποιεί τη διεπαφή `Counter` που είδαμε πιο πάνω.

```go
type Stats struct {
    value int
    // ...
}

func (s Stats) Add(v int) {
    s.value += v
}

func (s Stats) Value() int {
    return s.value
}

func (s Stats) SomeOtherMethod() {
    // The type can have additional methods not mentioned in the interface.
}
```

Για την υλοποίηση της διεπαφής, δεν έχει σημασία αν η μέθοδος έχει δέκτη τιμής ή δέκτη δείκτη.
(Ξαναδές τις [έννοιες των μεθόδων][concept-methods] αν έχεις αμφιβολίες γι' αυτά.)

> Μια τιμή τύπου διεπαφής μπορεί να περιέχει οποιαδήποτε τιμή που υλοποιεί αυτές τις μεθόδους. ((Go Tour: Interfaces)[go-tour-interfaces])

Αυτό σημαίνει ότι το `Stats` μπορεί τώρα να χρησιμοποιηθεί σε όλα τα σημεία που περιμένουν τη διεπαφή `Counter`.

```go
func SetUpAnalytics(counter Counter) {
    // ...
}

stats := Stats{}
SetUpAnalytics(stats)
// works because Stats implements Counter
```

Επειδή οι διεπαφές υλοποιούνται έμμεσα, ένας τύπος μπορεί εύκολα να υλοποιήσει πολλές διεπαφές.
Αρκεί να έχει ορίσει όλες τις απαραίτητες μεθόδους.

## Η κενή διεπαφή

Υπάρχει ένας πολύ ξεχωριστός τύπος διεπαφής στη Go, ο τύπος της **κενής διεπαφής**, που δεν περιέχει καμία μέθοδο.
Η κενή διεπαφή γράφεται έτσι: `interface{}`.
Στη Go 1.18 ή νεότερη, μπορεί να χρησιμοποιηθεί και το `any`.
Ορίστηκε ως ψευδώνυμο.

Επειδή η κενή διεπαφή δεν έχει μεθόδους, κάθε τύπος την υλοποιεί έμμεσα.
Αυτό είναι χρήσιμο για να ορίσεις μια συνάρτηση που μπορεί να δεχτεί γενικά οποιαδήποτε τιμή.
Σε αυτή την περίπτωση, η παράμετρος της συνάρτησης χρησιμοποιεί τον τύπο της κενής διεπαφής.

[go-tour-interfaces]: https://go.dev/tour/methods/9
[concept-methods]: /tracks/go/concepts/methods
