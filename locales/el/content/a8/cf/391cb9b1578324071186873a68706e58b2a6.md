# Εισαγωγή

Στη C#, τα Boolean (λογικές τιμές) αναπαρίστανται από τον τύπο `bool`, του οποίου οι τιμές μπορούν να είναι είτε `true` είτε `false`.

Η C# υποστηρίζει τρεις Boolean τελεστές: `!` (NOT), `&&` (AND) και `||` (OR).
