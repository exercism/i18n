# Εισαγωγή

Οι Boolean (λογική τιμή) στην F# αντιπροσωπεύονται από τον τύπο `bool`, του οποίου οι τιμές μπορούν να είναι είτε `true` είτε `false`.

Η F# υποστηρίζει τρεις τελεστές Boolean: `not` (ΟΧΙ), `&&` (ΚΑΙ), και `||` (Ή).
