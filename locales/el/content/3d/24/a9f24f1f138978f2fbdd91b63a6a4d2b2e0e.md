# Εισαγωγή

## Τα βασικά

Στην F#, η ανάθεση μιας τιμής σε ένα όνομα ονομάζεται _δέσμευση_. Οι δεσμεύσεις είναι αμετάβλητες, κάτι που τις κάνει παρόμοιες με τις σταθερές σε άλλες γλώσσες. Καθώς η F# είναι μια στατικά τυποποιημένη γλώσσα, κάθε δέσμευση έχει έναν τύπο κατά τη μεταγλώττιση.

Οι δεσμεύσεις ορίζονται χρησιμοποιώντας τη λέξη-κλειδί `let` (έστω). Ο καθορισμός του τύπου μιας δέσμευσης είναι προαιρετικός για τις περισσότερες δεσμεύσεις, καθώς η _εξαγωγή τύπων_ της F# μπορεί συνήθως να συμπεράνει τον τύπο με βάση την τιμή τους. Μια δέσμευση μοιάζει κάπως έτσι:

```fsharp
// Automatically inferred type
let fingers = 10
```

Οι συναρτήσεις είναι επίσης κανονικές δεσμεύσεις, αλλά με μία ή περισσότερες παραμέτρους. Μια συνάρτηση επιστρέφει αυτόματα την τελευταία της έκφραση. Η εξαγωγή τύπων λειτουργεί επίσης για τις περισσότερες συναρτήσεις, αναλύοντας με ποιες τιμές καλείται η συνάρτηση και ποια τιμή επιστρέφει.

```fsharp
// Automatically inferred types for parameters and return type
let add x y = x + y
```

Η κλήση μιας συνάρτησης γίνεται γράφοντας το όνομά της και περνώντας ορίσματα για κάθε παράμετρό της.

```fsharp
let five = add 2 3
```

Αν ο τύπος μιας δέσμευσης δεν μπορεί να εξαχθεί, ο μεταγλωττιστής θα αναφέρει ένα σφάλμα. Για να το διορθώσεις, πρόσθεσε έναν ρητό προσδιορισμό τύπου στη δέσμευση.

```fsharp
// Explicit type annotation
let fingers: int = 10

// Explicit type annotation (also for parameters)
let add (x: int) (y: int): int = x + y
```

Οι δεσμεύσεις στην F# μπορούν να χρησιμοποιηθούν μόνο _αφού_ έχουν οριστεί. Η χρήση μιας δέσμευσης πριν αυτή οριστεί οδηγεί σε σφάλμα μεταγλώττισης.

```fsharp
// Compile error as the add binding has not yet been defined
// let seven = add 3 4

let add x y = x + y
```

Η F# χρησιμοποιεί τα σημαντικά κενά για τον καθορισμό της εμβέλειας. Αυτό σημαίνει ότι η εμβέλεια καθορίζεται με εσοχή του κώδικα με κενά, σε σχέση με τη γραμμή που δηλώνει τη δέσμευση. Η προεπιλεγμένη σύμβαση είναι να χρησιμοποιούνται τέσσερα κενά για την εσοχή:

```fsharp
let toes =
    let left = 5
    let right = 5
    left + right

let multiplyPlusTwo x y =
    let product = x * y
    product + 2

// Trying to access the left, right or product bindings
// here would result in a compile error
```

Οι δεσμεύσεις της F# (στις οποίες περιλαμβάνονται και οι συναρτήσεις) οργανώνονται συνήθως σε αρθρώματα. Ένα άρθρωμα ομαδοποιεί σχετική λειτουργικότητα και ορίζεται χρησιμοποιώντας τη λέξη-κλειδί `module`. Ο ορισμός του αρθρώματος πρέπει να προηγείται των δεσμεύσεών του:

```fsharp
module Calculator

let pi = 3.14

let add x y = x + y
```

Η F# υποστηρίζει δύο τύπους σχολίων. Τα σχόλια μίας γραμμής προηγούνται από `//` και τα σχόλια πολλαπλών γραμμών τοποθετούνται ανάμεσα σε `(*` και `*)`.
