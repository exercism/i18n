# Εισαγωγή

Μια `string` στη Go είναι μια αμετάβλητη ακολουθία από bytes, τα οποία δεν χρειάζεται απαραίτητα να αναπαριστούν χαρακτήρες.

Τα κυριολεκτικά συμβολοσειρών ορίζονται μέσα σε διπλά εισαγωγικά:

```go
const name = "Jane"
```

Οι συμβολοσειρές μπορούν να συνενωθούν με τον τελεστή `+`:

```go
"Jane" + " " + "Austen"
// => "Jane Austen"
```

Κάποιοι ειδικοί χαρακτήρες χρειάζονται διαφυγή με μια ανάστροφη κάθετο μπροστά τους, όπως το `\t` για ένα tab και το `\n` για μια νέα γραμμή μέσα σε συμβολοσειρές.

```go
"How is the weather today?\nIt's sunny"  
// =>
// How is the weather today?
// It's sunny
```

Το πακέτο `strings` περιέχει πολλές χρήσιμες συναρτήσεις για την επεξεργασία συμβολοσειρών.
Για περισσότερες πληροφορίες σχετικά με τις συναρτήσεις συμβολοσειρών, δες την [τεκμηρίωση του πακέτου strings](https://pkg.go.dev/strings).
Ορίστε μερικά παραδείγματα:

```go
import "strings"

// strings.ToLower returns the string given as argument with all its characters lowercased
strings.ToLower("MaKEmeLoweRCase")
// => "makemelowercase"

// strings.Repeat returns a string with a substring given as argument repeated many times
strings.Repeat("Go", 3)
// => "GoGoGo"
```
