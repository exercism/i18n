# Οδηγίες

Η αποστολή σου είναι να αντιστρέψεις μια δεδομένη συμβολοσειρά.

Μερικά παραδείγματα:

- Μετέτρεψε το `"stressed"` σε `"desserts"`.
- Μετέτρεψε το `"strops"` σε `"sports"`.
- Μετέτρεψε το `"racecar"` σε `"racecar"`.
