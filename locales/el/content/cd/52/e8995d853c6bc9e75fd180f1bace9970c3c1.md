# Εισαγωγή

Ο τελεστής rest `...` χρησιμοποιείται για τον έλεγχο ορισμάτων με αόριστο αριθμό στοιχείων. Μπαίνει ως τελευταίο όρισμα σε μια συνάρτηση:

```haxe
function f(...nums:Int) {
  for (num in nums) {
    trace(num);
  }
}

f(1, 2, 3, 4, 5); // Output is 1 2 3 4 5
```
