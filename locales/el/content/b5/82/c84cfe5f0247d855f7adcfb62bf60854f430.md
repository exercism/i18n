# Σχετικά

Η δομή ελέγχου `foreach` παρέχει έναν βολικό τρόπο να διατρέχεις _πίνακες_ και _αντικείμενα_. Το `foreach` έχει δύο βασικές μορφές:

```php
foreach (iterable as $value) {
  // statements
}

foreach (iterable as $key => $value) {
  // statements
}
```
