# Εισαγωγή του IO με :only

Όταν εισάγεις συναρτήσεις από ένα module, είναι καλή πρακτική να εισάγεις μια ρητή λίστα, χρησιμοποιώντας την επιλογή `:only`, όπως εδώ:

```elixir
import IO, only: [inspect: 2, write: 1]
```
