# Σχετικά

Μπορείς να χρησιμοποιήσεις typedef για να συντομεύσεις ονόματα σύνθετων τύπων:

```haxe
typedef IA = Array<Int>;
```

Μπορείς επίσης να χρησιμοποιήσεις typedef για να δημιουργήσεις μια δομή τύπου:

```haxe
typedef User = {
  var name:String;
  var age:Int;
}
```
