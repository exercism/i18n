# Ειδικές οδηγίες για SQLite

Αυτή η άσκηση απαιτεί να ορίσεις τη στήλη `result` του πίνακα `color_code` στη σωστή τιμή, με βάση το χρώμα που βρίσκεται στη στήλη `color`.

## Σχήμα πίνακα

```sql
CREATE TABLE "color_code" ("color" TEXT, "result" INT);
```
