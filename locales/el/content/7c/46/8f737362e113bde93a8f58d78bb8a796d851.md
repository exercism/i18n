# Εισαγωγή

Οι μεταβλητές που δηλώνονται με τη λέξη-κλειδί `final` αντί για `var` μπορούν να πάρουν τιμή μόνο μία φορά.

```haxe
final a = "hello";
var b = "world";
trace(a, b); // hello, world
b = "haxe";
trace(a, b); // hello, haxe

// The following line would cause a compilation error:
// a = "bye";
```
