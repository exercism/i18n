# Εισαγωγή

Ο `do-while` βρόχος είναι μια εναλλακτική του `while` βρόχου, με τη μόνη διαφορά ότι ο `do-while` εκτελείται τουλάχιστον μία φορά:

```haxe
var i = 0;

do {
  trace("Hello");
} while (i < 10);
```
