# Εισαγωγή

Αυτή η άσκηση βασίζεται στη στοίβα δεδομένων και στον συμβολισμό επίδρασης στη στοίβα που συνάντησες στο [Cargo Shuffle][cargo-shuffle]. Εδώ θα γράψεις λέξεις που συνδυάζουν boolean (λογικές) τιμές.

## Λέξεις αναδιάταξης

Οι πιο συνηθισμένες λέξεις αναδιάταξης βρίσκονται στο [`kernel`][kernel]:

```
dup    ( x -- x x )
dupd   ( x y -- x x y )
drop   ( x -- )
swap   ( x y -- y x )
swapd  ( x y z -- y x z )
over   ( x y -- x y x )
pick   ( x y z -- x y z x )
rot    ( x y z -- y z x )
-rot   ( x y z -- z x y )
rotd   ( w x y z -- w y z x )
spin   ( x y z -- z y x )
nip    ( x y -- y )
tuck   ( x y -- y x y )

2dup   ( x y -- x y x y )
3dup   ( x y z -- x y z x y z )
4dup   ( w x y z -- w x y z w x y z )
2drop  ( x y -- )
3drop  ( x y z -- )
4drop  ( w x y z -- )
2nip   ( x y z -- z )
2swap  ( x y z w -- z w x y )
```

## Boolean

Οι δύο τιμές boolean είναι `t` και `f`. Το `f` είναι η μόνη ψευδής τιμή στη Factor. Όλα τα υπόλοιπα, ακόμα και το `0` και οι κενές συλλογές, είναι αληθή.

```factor
t .    ! => t
f .    ! => f
```

## Λέξεις boolean

Οι τρεις αυτές λέξεις boolean από το λεξιλόγιο `kernel`:

```
and ( x y -- ? )    ! true when both inputs are truthy
or  ( x y -- ? )    ! true when either input is truthy
not ( x   -- ? )    ! inverts its argument
```

```factor
t t and .    ! => t
t f and .    ! => f
f t or .     ! => t
t not .      ! => f
```

Το `?` σε κάθε έξοδο είναι η σύμβαση του συμβολισμού επίδρασης στη στοίβα για "μια τιμή boolean".

Θα χρειαστείς το `swap` για μία από τις παρακάτω εργασίες.

[cargo-shuffle]: https://exercism.org/tracks/factor/exercises/cargo-shuffle
[kernel]: https://docs.factorcode.org/content/vocab-kernel.html
