# Απόφυγε τη σύγκριση με τις κυριολεκτικές τιμές Boolean

Αντί να συγκρίνεις τιμές Boolean με τις κυριολεκτικές τους τιμές, μπορείς να απλοποιήσεις τον κώδικα χρησιμοποιώντας απλώς τη μεταβλητή:

```java
// instead of
return someBoolVariable == true;

// use
return someBoolVariable;
```
