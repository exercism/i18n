# Εισαγωγή

Οι συναρτήσεις έχουν εξωτερική σύνδεση από προεπιλογή και μπορούν να κληθούν από οποιοδήποτε άλλο αρχείο του έργου.
Για περισσότερες πληροφορίες σχετικά με τη σύνδεση, δες την [έννοια της σύνδεσης](../linkage/introduction.md#Introduction).
Μια συνάρτηση `static` μπορεί να κληθεί μόνο μέσα στο αρχείο πηγής όπου ορίζεται.

Για παράδειγμα

```c
// file scope, internal linkage
// can only be called from within this file
static void some_function()
{
    // code snipped
}

// file scope, external linkage by default
// can be called from any other file in the project
int main()
{
    // code snipped
}
```
