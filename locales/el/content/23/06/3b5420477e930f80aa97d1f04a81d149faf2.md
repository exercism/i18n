Στη Chapel, αν η συνάρτηση μπορεί να πετάξει ένα σφάλμα, αυτό θα πρέπει να δηλώνεται στον ορισμό της συνάρτησης με τη λέξη-κλειδί `throws`, όπως δείχνει το παρακάτω πρότυπο.

```chapel
proc canThrow(n: int) throws {
  // function body here 
}
```
