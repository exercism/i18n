# Εισαγωγή

Αν ο κώδικας μέσα σε έναν βρόχο πρέπει πάντα να εκτελείται τουλάχιστον μία φορά, μπορείς να χρησιμοποιήσεις έναν `do`/`while` βρόχο:

```csharp
int x = 23;

do
{
    // Always executes at least once, then repeats as long as x > 10
    x = x - 2;
} while (x > 10)
```
