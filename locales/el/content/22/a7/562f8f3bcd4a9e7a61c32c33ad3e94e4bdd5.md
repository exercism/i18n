# κλείσιμο μέσα στο after

Μπορείς να χρησιμοποιήσεις το μπλοκ `after` για να βγεις ομαλά, ανεξάρτητα από το αν ο κώδικας στο `try` εκτελέστηκε με επιτυχία ή όχι. Χρησιμοποίησε το μπλοκ `after` για να κλείσεις το `resource`.
