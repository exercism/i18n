# Pistas

## General

- Un valor entero se puede definir como uno o más dígitos consecutivos.

## 1. Define el tiempo esperado en el horno en minutos

- Necesitas definir un [enlace][bindings].

## 2. Calcula el tiempo restante en el horno en minutos

- Necesitas definir una [función][functions] con un solo parámetro.
- Puedes usar y referirte por su nombre al enlace que definiste previamente.
- La última expresión de una función se [devuelve automáticamente][return-values] desde la función; no tienes que indicar explícitamente qué valor devolver.
- Puedes usar el [operador matemático de resta][operators] para restar valores.

## 3. Calcula el tiempo de preparación en minutos

- Necesitas definir una [función][functions] con un solo parámetro.
- Puedes usar el [operador matemático de multiplicación][operators] para multiplicar valores.

## 4. Calcula el tiempo transcurrido en minutos

- Necesitas definir una [función][functions] con dos parámetros.
- Puedes [llamar][calling] a una de las otras funciones que definiste previamente.
- Puedes usar el [operador matemático de suma][operators] para sumar valores.

[bindings]: https://docs.microsoft.com/en-us/dotnet/fsharp/language-reference/values/#binding-a-value
[functions]: https://docs.microsoft.com/en-us/dotnet/fsharp/language-reference/functions/#remarks
[return-values]: https://docs.microsoft.com/en-us/dotnet/fsharp/language-reference/functions/#return-values
[operators]: https://docs.microsoft.com/en-us/dotnet/fsharp/language-reference/symbol-and-operator-reference/arithmetic-operators
[calling]: https://docs.microsoft.com/en-us/dotnet/fsharp/language-reference/functions/#calling-a-function
