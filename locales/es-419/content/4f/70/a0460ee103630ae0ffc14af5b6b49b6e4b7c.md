# Pistas

## General

- Lee la Introducción, que recorre un ejemplo similar.
- Hay otro ejemplo en la sección del manual de Julia sobre [interfaces][iteration-interface].

## 1. Define el tipo `Fiberator`

- `n` es cuántos números hay que generar.
- Mantenlo simple: no se necesita más información.

## 2. Implementa el o los métodos `iterate`

- Consulta la Introducción para ver detalles de implementación importantes.
- El argumento `state` no tiene que ser un entero. Puede ser de cualquier tipo.
- Piensa qué valores necesitas para calcular el siguiente elemento. Una tupla de esos valores debería ser el argumento `state`.
- El método `iterate` devuelve una `Tuple` con dos valores `(item, state)`. `item` es el número de Fibonacci, es decir, el `item` en `for item in Fiberator(10)`.
- Si tu `state` incluye varios valores, considera usar una tupla con nombre para aclarar el significado de cada valor.
  - Esto es útil para desempaquetar, pero no es obligatorio.

## 3. Haz que `collect` funcione

- Un método pequeño (consulta la Introducción para ver detalles de implementación importantes).
- Si falta esto, lee los mensajes de error para encontrar pistas.

## 4. Habilita la inferencia de tipos

- Un método pequeño (consulta la Introducción para ver detalles de implementación importantes).
- Ten cuidado con el argumento del método.
- Después de un `collect`, el resultado _no_ debería ser `Vector{Any}`.

[iteration-interface]: https://docs.julialang.org/en/v1/manual/interfaces/#man-interface-iteration
