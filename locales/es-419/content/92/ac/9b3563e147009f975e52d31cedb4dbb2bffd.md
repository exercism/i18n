# Acerca de

Al escribir funciones en Elixir, podemos hacer uso de un [estilo asertivo][assertive-style] con la [coincidencia de patrones][pattern-match-doc]:

```elixir
def read_file() do
  {:ok, contents} = File.read("hello.txt")
  contents
end
```

- La coincidencia de patrones se realiza de forma explícita con el operador de coincidencia, [`=/2`][match-op].

  - La coincidencia tiene éxito cuando la _forma_ de los datos del lado izquierdo del operador coincide con el lado derecho.
  - Cuando la coincidencia tiene éxito, las variables del lado izquierdo se vinculan con los valores del lado derecho.
  - Usar un guion bajo, `_`, nos permite ignorar los valores en esos lugares.

    ```elixir
    {:ok, number, _} = {:ok, 5, [4.5, 6.3]}
    number
    # => 5 is bound to this variable
    ```

  - El [operador pin `^`][getting-started-pin-operator] se puede usar para evitar volver a vincular una variable y, en su lugar, hacer coincidir el patrón con su valor actual.

    ```elixir
    number = 10
    {:ok, ^number, _} = {:ok, 5, [4.5, 6.3]}
    # => ** (MatchError) no match of right hand side value: {:ok, 5, [4.5, 6.3]}
    ```

- La coincidencia de patrones también puede aparecer en el encabezado de una cláusula de función, de modo que solo los argumentos que coincidan con el patrón invocarán la función.
- Las variables se pueden vincular en la coincidencia de patrones de una cláusula de función.

  ```elixir
  defmodule Example do
    def named_function(:a = atom_variable) do
      {atom_variable, 1}
    end
  end

  Example.named_function(:a)
  # => {:a, 1}
  # The first function clause matches, so it is invoked

  Example.named_function(:b)
  # => ** (FunctionClauseError) no function clause matching in Example.named_function/1
  ```

[assertive-style]: https://blog.plataformatec.com.br/2014/09/writing-assertive-code-with-elixir/
[pattern-match-doc]: https://hexdocs.pm/elixir/pattern-matching.html
[match-op]: https://hexdocs.pm/elixir/Kernel.SpecialForms.html#=/2
[getting-started-pin-operator]: https://hexdocs.pm/elixir/pattern-matching.html#the-pin-operator
