# Pistas

- La función `format` será necesaria para este ejercicio.

## 1. Formatear valores trimestrales

- `format` siempre devuelve `nil` a menos que el parámetro stream sea `nil`.
- `~A` se puede usar para formatear un valor.

## 2. El informe de dos trimestres

- Tu función anterior es perfecta para crear las dos líneas que se necesitan.
- `~%` y `~&` se pueden usar para crear saltos de línea.

## 3. Legible para humanos frente a Lisp Alien

- Tu función anterior de formateo de trimestres es perfecta para crear los strings que se necesitan.
- `~S` se puede usar para formatear valores de una forma legible.
