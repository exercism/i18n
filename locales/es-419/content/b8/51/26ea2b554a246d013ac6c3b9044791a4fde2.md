# Anexo de instrucciones

## Notas específicas del track

En la versión de Scheme, los argumentos son la `capacity` de la mochila, una lista de los `weights` y una lista de los `values`.
No será necesario validar los argumentos: los que usan las pruebas tienen valores válidos y listas de la misma longitud.
