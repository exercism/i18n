# Introducción

## Boolean en Julia

Los valores de verdadero y falso se representan con el tipo `Bool`.
Contiene solo dos valores: `true` y `false`.

```julia-repl
julia> true
true

julia> false
false
```

## Operadores Boolean

En Julia hay tres operadores Boolean.

`&&` es el «y» Boolean.
Se evalúa a `true` si las expresiones a *ambos* lados de `&&` son `true`.

```julia-repl
julia> true && true
true

julia> true && false
false
```

`||` es el «o» Boolean.
Se evalúa a `true` si una expresión a *cualquiera* de los lados de `||` es `true`.

```julia-repl
julia> true || true
true

julia> false || true
true
```

`!` es el «no» Boolean.
Intercambia los valores `true` y `false`.

```julia-repl
julia> !true
false

julia> !false
true
```

Para expresiones más largas y complicadas, es mejor usar paréntesis para dejar clara tu intención.

```julia-repl
julia> (true || false) && (false && true)
false
```
