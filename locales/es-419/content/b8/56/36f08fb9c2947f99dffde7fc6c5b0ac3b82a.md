# Introducción

C#, como muchos lenguajes de tipado estático, ofrece varios tipos que representan números enteros, cada uno con su propio rango de valores. En el extremo inferior, el tipo `sbyte` tiene un valor mínimo de -128 y un valor máximo de 127. Como en todos los tipos de enteros, estos valores están disponibles como `<type>.MinValue` y `<type>.MaxValue`. En el extremo superior, el tipo `long` tiene un valor mínimo de -9,223,372,036,854,775,808 y un valor máximo de 9,223,372,036,854,775,807. En medio están los tipos `short` e `int`.

Los rangos están determinados por el ancho de almacenamiento del tipo tal como lo asigna el sistema. Por ejemplo, un `byte` usa 8 bits y un `long` usa 64 bits.

Cada uno de los tipos anteriores tiene un equivalente sin signo: `sbyte`/`byte`, `short`/`ushort`, `int`/`uint` y `long`/`ulong`. En todos los casos, el rango de valores va de 0 al máximo negativo con signo multiplicado por 2 más 1.

| Tipo   | Ancho  | Mínimo                     | Máximo                      |
| ------ | ------ | -------------------------- | --------------------------- |
| sbyte  | 8 bits | -128                       | +127                        |
| short  | 16 bits| -32_768                    | +32_767                     |
| int    | 32 bits| -2_147_483_648             | +2_147_483_647              |
| long   | 64 bits| -9_223_372_036_854_775_808 | +9_223_372_036_854_775_807  |
| byte   | 8 bits | 0                          | +255                        |
| ushort | 16 bits| 0                          | +65_535                     |
| uint   | 32 bits| 0                          | +4_294_967_295              |
| ulong  | 64 bits| 0                          | +18_446_744_073_709_551_615 |

Una variable (o expresión) de un tipo se puede convertir fácilmente a otro. Por ejemplo, en una operación de asignación, si el tipo del valor que se asigna (lhs) garantiza que el valor estará dentro del rango del tipo al que se asigna (rhs), entonces se trata de una asignación simple:

```csharp
uint ui = uint.MaxValue;
ulong ul = ui;    // no problem
```

Por otro lado, si el rango del tipo desde el que se asigna no es un subconjunto del rango de valores del tipo destino, entonces se requiere una conversión de tipo, la operación `()`, incluso si el valor concreto está dentro del rango del tipo destino:

```csharp
short s = 42;
uint ui = (uint)s;
```

## Conversión de bits

La clase `BitConverter` ofrece una forma cómoda de convertir tipos enteros desde y hacia arrays de bytes.
