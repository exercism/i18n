# Introducción

En Haxe, para generar un número aleatorio puedes usar el método `Std.random(x)`. Genera un número aleatorio desde 0 (incluido) hasta `x` (sin incluirlo):

```haxe
public function main() {
  trace(Std.random(10)); // 0...9
}
```
