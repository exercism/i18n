# Mezcla de runes y bytes

Usar una mezcla de `runes` y `bytes` es seguro aquí, porque la entrada solo usa caracteres ASCII. 
Para ver qué pasa cuando no es seguro, intenta agregar un test que use caracteres no ASCII.
