# Anexo a las instrucciones

## Implementación

En el paquete grep, define una sola función de Go, Search, que acepta un string de patrón,
un slice de flags que son strings y un slice de strings con nombres de archivo.
Search debe devolver un slice de strings con la salida para
los flags y los nombres de archivo dados.

Usa la siguiente firma para la función Search:

```go
func Search(pattern string, flags, files []string) []string {
```
