# Introducción

Un conjunto es una colección de valores únicos. La implementación predeterminada de .NET para un conjunto es la clase `HashSet<T>`, que es una colección que solo contiene valores únicos.
