# Introducción

Los behaviours nos permiten definir interfaces (conjuntos de funciones y macros) en un _módulo de behaviour_ que después pueden implementar distintos _módulos de callback_. Gracias a la interfaz compartida, esos módulos de callback se pueden usar de forma intercambiable.

~~~~exercism/note
Ten en cuenta la ortografía británica de «behaviours».
~~~~

## Definir behaviours

Para definir un behaviour, necesitamos crear un módulo nuevo y especificar una lista de funciones que forman parte de la interfaz deseada. Cada función se debe definir con el atributo de módulo `@callback`. La sintaxis es idéntica a la de un [typespec de función][concept-typespecs] (`@spec`). Hay que especificar el nombre de la función, una lista de tipos de argumento y todos los tipos de retorno posibles.

```elixir
defmodule Countable do
  @callback count(collection :: any) :: pos_integer
end
```

## Implementar behaviours

Para agregar un behaviour existente a nuestro módulo (es decir, crear un módulo de callback), usamos el atributo de módulo `@behaviour`. Su valor debe ser el nombre del módulo de behaviour que estamos agregando.

Después tenemos que definir todas las funciones (callbacks) que requiere ese módulo de behaviour. Si estamos implementando el behaviour de otra persona, como los behaviours integrados de Elixir `Access` o `GenServer`, encontraremos la lista de todos los callbacks del behaviour en la documentación de [hexdocs.pm][hexdocs].

Un módulo de callback no se limita a implementar solo las funciones que forman parte de su behaviour. También es posible que un mismo módulo implemente varios behaviours.

Para indicar qué función proviene de qué behaviour, debemos usar el atributo de módulo `@impl` antes de cada función. Su valor debe ser el nombre del módulo de behaviour que define este callback.

```elixir
defmodule BookCollection do
  @behaviour Countable

  defstruct [:list, :owner]

  @impl Countable
  def count(collection) do
    Enum.count(collection.list)
  end

  def mark_as_read(collection, book) do
    # other function unrelated to the Countable behaviour
  end
end
```

## Implementaciones de callback por defecto

Al definir un behaviour, es posible proporcionar una implementación por defecto de un callback. Esta implementación se debe definir en la expresión citada de la macro `__using__/1`. Para que quienes usen el módulo de behaviour puedan sobrescribir la implementación por defecto, llama a la macro `defoverridable/1` después de la implementación de la función. Esta acepta una lista de palabras clave cuyas claves son nombres de funciones y cuyos valores son sus aridades.

```elixir
defmodule Countable do
  @callback count(collection :: any) :: pos_integer

  defmacro __using__(_) do
    quote do
      @behaviour Countable
      def count(collection), do: Enum.count(collection)
      defoverridable count: 1
    end
  end
end
```

Ten en cuenta que no se recomienda definir funciones dentro de `__using__/1` para ningún propósito distinto de definir implementaciones de callback por defecto, pero siempre puedes definir funciones en otro módulo e importarlas en la macro `__using__/1`.

[concept-typespecs]: https://exercism.org/tracks/elixir/concepts/typespecs
[hexdocs]: https://hexdocs.pm
