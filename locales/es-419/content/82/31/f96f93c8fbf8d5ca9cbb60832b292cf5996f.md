# Ejecutar las pruebas

Escribe tu código en `<exercise_name>.zig`.

Para ejecutar las pruebas de un ejercicio, ejecuta:

```bash
zig test test_exercise_name.zig
```

en el directorio raíz del ejercicio (reemplazando `exercise_name` con el nombre del ejercicio).
