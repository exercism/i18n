# Acerca de

## Más métodos de enumeración

En Enumeración, se te presentaron los métodos de enumeración `count`, `any?`, `select`, `all` y `map`.
Aquí tienes un repaso de ellos, con algunos métodos adicionales:

```ruby
fibonacci = [0, 1, 1, 2, 3, 5, 8, 13]

fibonacci.count  { |number| number == 1 }   #=> 2
fibonacci.any?   { |number| number > 20 }   #=> false
fibonacci.none?  { |number| number > 20 }   #=> true
fibonacci.select { |number| number.odd? }   #=> [1, 1, 3, 5, 13]
fibonacci.all?   { |number| number < 20 }   #=> true
fibonacci.map    { |number| number * 2  }   #=> [0, 2, 2, 4, 6, 10, 16, 26]
fibonacci.select { |number| number >= 5 }   #=> [5, 8, 13]
fibonacci.find   { |number| number >= 5 }   #=> 5

# Some methods work with or without a block
fibonacci.sum  #=> 33
fibonacci.sum { |number| number * number }  #=> 273

# There are also methods to help with nested arrays:
animals = [ ['cat', 'bob'], ['horse', 'caris'], ['mouse', 'arya'] ]
animals.flatten  #=> ["cat", "bob", "horse", "caris", "mouse", "arya"]
```

## Enumerar hashes

Enumerar objetos de tipo `Hash` es exactamente lo mismo que enumerar objetos de tipo `Array`, salvo que el bloque recibe dos argumentos: la clave y el valor:

```ruby
pet_names = {cat: "bob", horse: "caris", mouse: "arya"}
pet_names.each { |animal, name| ... }
```

Si solo necesitas uno de los valores, puedes usar el símbolo especial `_` para indicar que ese valor no es necesario.
Esto ayuda tanto a la claridad como al rendimiento, ya que es una optimización.

```ruby
pet_names = {cat: "bob", horse: "caris", mouse: "arya"}
pet_names.map { |_, name| name }  #=> ["bob, "caris", "arya"]
```

## Enumeraciones anidadas

También puedes enumerar en bloques anidados y encadenar métodos uno tras otro.
Por ejemplo, si tenemos un array de hashes de animales y queremos extraer los animales con nombres cortos, podríamos hacer algo así:

```ruby
pets = [
  { animal: "cats", names: ["bob", "fred", "sandra"] },
  { animal: "horses", names: ["caris", "black beard", "speedy"] },
  { animal: "mice", names: ["arya", "jerry"] }
]

pets.map { |pet|
  pet[:names].select { |name| name.length <= 5 }
}.flatten.sort
#=> ["arya", "bob", "caris", "fred", "jerry"]
```
