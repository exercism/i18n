# Acerca de

En Clojure, los valores Boolean se representan con `true` o `false`.

Las funciones predicado (las que devuelven un valor Boolean) suelen terminar con un signo de interrogación (`?`), pero esto es solo una convención.

La biblioteca principal incluye funciones para operadores lógicos como `not`, `and` y `or`.
