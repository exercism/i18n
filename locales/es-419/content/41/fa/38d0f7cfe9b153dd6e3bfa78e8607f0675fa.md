# Reutiliza el código

El método `%{callingMethod}` debería reutilizar la lógica implementada en `%{methodToCall}`.
Reutilizar los métodos existentes puede ayudar a que el código sea más fácil de mantener.
