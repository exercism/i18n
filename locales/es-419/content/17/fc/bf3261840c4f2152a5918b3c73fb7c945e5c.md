# reutilización de funciones

Para evitar redundancia en el código, asegúrate de que `ranks/0` llame al `rank_range/0` que definiste antes y de que `files/0` llame al `file_range/0` que definiste antes.
