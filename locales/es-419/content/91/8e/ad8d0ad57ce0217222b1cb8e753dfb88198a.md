# fmt errorf sin parámetro

Prefiere `errors.New` en lugar de `fmt.Errorf` para los mensajes de error que no necesitan interpolación, ya que evita analizar el mensaje en busca de marcadores de posición.
