# Firma incorrecta de uniqueTreasures

La firma de `uniqueTreasures` no es la esperada.
Las instrucciones dicen:

> `uniqueTreasures` debe recibir un `List Chest` sin condiciones específicas y devolver un `List Chest`, con la condición adicional `uniqueTreasure : ()` agregada al registro fantasma.
