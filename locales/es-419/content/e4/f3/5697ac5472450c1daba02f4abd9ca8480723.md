# Introducción

Cuando necesitas una fila de `n` valores donde cada uno depende de su posición, `<iota>` y `map` de Factor colaboran para hacer el trabajo sin escribir un contador de bucle.

## `<iota>` y `map`

`<iota>` (en [`sequences`][sequences]) toma un entero no negativo `n` y devuelve una secuencia *virtual* de los enteros `0, 1, …, n-1`. Luego `map` la recorre, aplicando tu quotation a cada elemento:

```factor
USING: math sequences ;

5 <iota> [ 2 * ] map .   ! => { 0 2 4 6 8 }
```

La quotation recibe un entero, el índice, y deja en la pila el valor para esa posición.

## Iterar y repetir por cantidad

Cuando solo necesitas los *efectos secundarios* de visitar cada índice (y no una nueva secuencia), `each-integer` (en [`math`][math]) ejecuta una quotation sobre `0, 1, …, n-1`:

```
each-integer ( n quot: ( i -- ) -- )
```

`replicate` (en [`sequences`][sequences]) llama a una quotation `n` veces y recoge los resultados, ignorando el índice; viene bien para construir `n` valores independientes:

```
replicate ( n quot: ( -- elt ) -- seq )
```

```factor
USING: sequences ;

3 [ { 0 0 } ] replicate .   ! => { { 0 0 } { 0 0 } { 0 0 } }
```

## Rangos acotados

Cuando el rango de índices no empieza en `0`, o quieres que incluya ambos extremos, el vocabulario [`ranges`][ranges] ofrece literales con notación de corchetes. Los corchetes (`[]`) incluyen el extremo; los paréntesis (`()`) lo excluyen.

```
[a..b]    ! a, a+1, …, b      (inclusive)
[a..b)    ! a, a+1, …, b-1    (half-open at the top)
```

```factor
USING: math ranges sequences ;

3 7 [a..b] [ 2 * ] map .   ! => { 6 8 10 12 14 }
3 7 [a..b) [ 2 * ] map .   ! => { 6 8 10 12 }
```

Algunas abreviaturas comunes de un solo argumento:

```
[1..b]    ! 1, 2, …, b        (same as 1 b [a..b])
[1..b)    ! 1, 2, …, b-1      (same as 1 b [a..b))
[0..b)    ! 0, 1, …, b-1      (same as 0 b [a..b), and same as `b <iota>`)
```

```factor
6 [1..b] [ sq ] map .      ! => { 1 4 9 16 25 36 }
6 [1..b) [ sq ] map .      ! => { 1 4 9 16 25 }
4 [0..b) [ 1 + ] map .     ! => { 1 2 3 4 }
```

`<iota>` es la forma más común; `[a..b]` y sus parientes son la herramienta correcta cuando el rango empieza en un punto distinto de `0` o necesita un límite superior inclusivo.

## Límites inferiores abiertos y rangos posiblemente vacíos

Los corchetes (`[]`) *incluyen* un extremo; los paréntesis (`()`) lo *excluyen*. El mismo truco a la izquierda da `(a..b]` y `(a..b)`:

```
(a..b]    ! a+1, …, b              (lower open, upper inclusive)
(a..b)    ! a+1, …, b-1            (both open)
```

`0 n (a..b]` da `{ 1 … n }` para `n > 0` y `{ }` para `n = 0`, la forma natural cuando se itera «cada entero positivo hasta `n` inclusive».

## Rangos descendentes

Las cuatro formas de corchetes, `[a..b]`, `[a..b)`, `(a..b]` y `(a..b)`, cuentan *hacia abajo* cuando `a > b`, no hacia un resultado vacío:

```factor
USING: ranges sequences ;

5 1 [a..b] >array .   ! => { 5 4 3 2 1 }
1 0 [a..b] >array .   ! => { 1 0 }   (not empty!)
```

## Construir un string por posición

`map` produce la forma que devuelva su quotation. Si la quotation deja un código de carácter en la pila, `>string` (en [`strings`][strings]) convierte el resultado en un string:

```factor
USING: math sequences strings ;

4 <iota> [ 2 * CHAR: A + ] map >string .   ! => "ACEG"
```

`CHAR: A` se analiza como el entero `65`. La quotation aquí duplica el índice y le suma esa base, con lo que cae en cada segunda letra mayúscula. Cualquier aritmética que apiles encima del índice decide cuál carácter termina en esa posición.

## Elegir por posición

Cuando el carácter depende de una *condición* sobre el índice, haz la elección dentro de la quotation:

```factor
USING: kernel math sequences strings ;

5 <iota> [ 3 < [ CHAR: a ] [ CHAR: b ] if ] map >string .
! => "aaabb"
```

Las primeras tres posiciones evalúan `i 3 <` como `t`, así que reciben `a`; el resto recibe `b`. La misma forma, `[ <test> [ <yes> ] [ <no> ] if ]`, cubre las pruebas de paridad con `even?` / `odd?` (en [`math`][math]) y las pruebas de «cada k-ésima posición» con `mod` y `zero?`.

[sequences]: https://docs.factorcode.org/content/article-sequences.html
[strings]: https://docs.factorcode.org/content/article-strings.html
[math]: https://docs.factorcode.org/content/vocab-math.html
[ranges]: https://docs.factorcode.org/content/vocab-ranges.html
