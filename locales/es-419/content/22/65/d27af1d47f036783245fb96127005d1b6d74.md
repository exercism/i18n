# Anexo de instrucciones

## Instrucciones específicas del track

Tu tarea es implementar dos funciones

- `colors`: que devuelve un array de strings con los colores en orden creciente de valor.
- `colorCode`: que recibe como argumento un string, que representa un solo color, y devuelve el valor correspondiente.
