# Argumentos splat

Define de forma explícita una variable `name` en lugar de usar la palabra clave del argumento splat (`*`). Esto hace que el código se lea con más claridad y que se necesite menos manipulación para acceder al valor del argumento.
