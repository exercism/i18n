# Cerrar en after

Puedes usar el bloque `after` para salir de forma controlada, sin importar si el código en `try` se ejecutó correctamente o no. Por favor, usa el bloque `after` para cerrar `resource`.
