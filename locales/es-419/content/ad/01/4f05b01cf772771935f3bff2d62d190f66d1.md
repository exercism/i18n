# Desestructurar expresiones de assignedToDevTeam

Como se menciona en las instrucciones, la sentencia `case` de `assignedToDevTeam` debería aplicar coincidencia de patrones sobre `assignedTo` hasta llegar a su contenido de string.
