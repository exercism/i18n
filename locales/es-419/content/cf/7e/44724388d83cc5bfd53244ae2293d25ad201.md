# Introducción

Una función te permite agrupar código en una unidad reutilizable.
Consta de la palabra clave `func`, el nombre de la función y una lista separada por comas de cero o más parámetros y tipos entre paréntesis.

## Parámetros de la función

Todos los parámetros deben tener un tipo explícito; no hay inferencia de tipos para los parámetros.
No hay valores predeterminados para los parámetros, así que todos los parámetros de la función son obligatorios.

```go
import "fmt"

// No parameters
func PrintHello() {
    fmt.Println("Hello")
}

// One parameter
func PrintHelloName(name string) {
  fmt.Println("Hello " + name)
}
```

Los parámetros del mismo tipo se pueden declarar juntos, seguidos de una sola declaración de tipo.

```go
import "fmt"

func PrintGreetingName(greeting, name string) {
  fmt.Println(greeting + " " + name)
}
```

## Parámetros vs. argumentos

Veamos rápidamente dos términos que suelen confundirse: `parameters` y `arguments`.
Los parámetros de una función son los nombres definidos en la firma de la función, como `greeting` y `name` en la función `PrintGreetingName` de arriba.
Los argumentos de una función son los valores concretos que se pasan a los parámetros de la función cuando llamamos a la función.
Por ejemplo, en el ejemplo de abajo, `"Hello"` y `"Katrina"` son los argumentos que se pasan a los parámetros `greeting` y `name`:

```go
PrintGreetingName("Hello", "Katrina")
```

## Valores de retorno

A los parámetros de la función les siguen cero o más valores de retorno, que también deben tener un tipo explícito.
Los valores de retorno únicos se dejan sin paréntesis; los múltiples se envuelven entre paréntesis.
Los valores se devuelven al código que llama desde las funciones usando la palabra clave `return`.
Puede haber varias sentencias `return` en una función.
La ejecución de la función termina en cuanto alcanza una de esas sentencias `return`.
Si se van a devolver varios valores desde una función, se separan por comas.

```go
func Hello(name string) string {
  return "Hello " + name
}

func HelloAndGoodbye(name string) (string, string) {
  return "Hello " + name, "Goodbye " + name
}
```

## Llamar a funciones

Para llamar a una función, se especifica el nombre de la función y se pasan argumentos para cada uno de sus parámetros entre paréntesis.

```go
import "fmt"

// No parameters, no return value
func PrintHello() {
    fmt.Println("Hello")
}
// Called like this:
PrintHello()

// One parameter, one return value
func Hello(name string) string {
  return "Hello " + name
}
// Called like this:
greeting := Hello("Dave")

// Multiple parameters, multiple return values
func SumAndMultiply(a, b int) (int, int) {
    return a+b, a*b
}
// Called like this:
aplusb, atimesb := SumAndMultiply(a, b)
```

## Valores de retorno con nombre y retorno desnudo

Además de los parámetros, los valores de retorno pueden tener un nombre de forma opcional.
Si se usan valores de retorno con nombre, una sentencia `return` sin argumentos devolverá esos valores.
Esto se conoce como un retorno «desnudo».

```go
func SumAndMultiplyThenMinus(a, b, c int) (sum, mult int) {
    sum, mult = a+b, a*b
    sum -= c
    mult -= c
    return
}
```
