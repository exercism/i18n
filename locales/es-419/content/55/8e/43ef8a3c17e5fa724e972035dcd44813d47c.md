# Se usa strings join para concatenar

No uses `strings.Join` si los datos no son de tipo `[]string`.
Usa `fmt.Sprintf` en su lugar. Es una forma muy potente de construir strings a partir de varias piezas.
