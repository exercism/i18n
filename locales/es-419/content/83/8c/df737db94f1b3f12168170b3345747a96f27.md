# Introducción

Una cadena representa datos textuales.
En Swift, un [`String`][strings-and-characters] es una colección ordenada de valores `Character`, donde cada carácter representa un carácter Unicode.

Creas un string con un literal de string, que es una secuencia de caracteres rodeada de comillas dobles (`"`).
Para strings de varias líneas, usa tres comillas dobles (`"""`):

```swift
let hello: String = "Hello, World!"

let poem: String = """
Roses are red,
Violets are blue.
Sugar is sweet,
And so are you.
"""
```

Los strings de Swift se integran a la perfección con la clase [`NSString`][nsstring-docs] del framework Foundation, lo que da acceso a amplias capacidades de procesamiento de texto.

## Caracteres

Un `Character` representa un único grupo de grafemas extendido, es decir, un carácter legible por humanos compuesto por uno o más [valores escalares de Unicode][unicode-scalar-values].
Un valor escalar de Unicode es un punto de código de 21 bits en el rango de `U+0000` a `U+D7FF` inclusive, o de `U+E000` a `U+10FFFF` inclusive.

```swift
let aChar: Character = "A"
```

## Inferencia de tipos

Cuando escribes un literal de string, Swift infiere automáticamente que su tipo es `String`.
Para guardar un solo carácter como `Character`, debes indicar una anotación de tipo explícita:

```swift
let aString = "A"        // Inferred as String
let aChar: Character = "A" // Explicitly typed as Character
print(type(of: aString)) // Prints String
print(type(of: aChar))   // Prints Character

// The following code will not compile:
let badChar: Character = "Too many characters"
// Error: Cannot convert value of type 'String' to specified type 'Character'
```

## Concatenación

Puedes unir strings con el operador de suma (`+`):

```swift
print("honey" + "comb")
// Prints "honeycomb"
```

No puedes concatenar un `Character` directamente a un `String` con `+` sin antes convertir el carácter a un string.
Sin embargo, puedes usar el método [`append(_:)`][append] para agregar un string o un carácter directamente a un string mutable:

```swift
var greeting = "Hello"
let world = ", world"
let period: Character = "."

greeting.append(world)
print(greeting) // Prints "Hello, world"

greeting.append(period)
print(greeting) // Prints "Hello, world."
```

## Interpolación de strings

La [interpolación de strings][string-interpolation] te permite construir un string insertando constantes, variables, literales y expresiones dentro de literales de string.
Encierra cada expresión entre paréntesis precedidos por una barra invertida (`\(` ... `)`):

```swift
let radius = 5.0
let circle = "The area of a circle with radius \(radius) is \(Double.pi * radius * radius)"
print(circle) // Prints "The area of a circle with radius 5.0 is 78.53981633974483"
```

## Propiedades de strings y caracteres

Los strings y los caracteres ofrecen varias propiedades para inspeccionar su contenido.

### Propiedades de los strings

Algunas propiedades comunes de los strings son:

| Propiedad | Descripción                                    | Ejemplo                          |
| --------- | ---------------------------------------------- | -------------------------------- |
| `isEmpty` | Devuelve `true` si el string no tiene caracteres | `"Hello".isEmpty` se evalúa como `false` |
| `count`   | Devuelve el número de caracteres del string | `"Hello".count` se evalúa como `5`       |

Para ver una lista completa de propiedades, consulta la [documentación de String de Apple][string-docs].

### Propiedades de los caracteres

Algunas propiedades comunes de los caracteres son:

| Propiedad      | Descripción                                   | Ejemplo                              |
| -------------- | --------------------------------------------- | ------------------------------------ |
| `isLowercase`  | Devuelve `true` si el carácter está en minúscula  | `"a".isLowercase` se evalúa como `true` |
| `isUppercase`  | Devuelve `true` si el carácter está en mayúscula  | `"A".isUppercase` se evalúa como `true` |
| `isNumber`     | Devuelve `true` si el carácter es un dígito    | `"1".isNumber` se evalúa como `true`    |
| `isWhitespace` | Devuelve `true` si el carácter es un espacio en blanco | `" ".isWhitespace` se evalúa como `true`|

Para ver una lista completa de propiedades, consulta la [documentación de Character de Apple][character-docs].

## Conversión de tipos

Puedes convertir otros tipos a strings usando el inicializador `String(_:)`, que admite tipos estándar como `Int`, `Double` y `Character`:

```swift
let charX: Character = "x"
print(String(charX))     // Prints "x"
print(String(110))       // Prints "110"
print(String(Double.pi)) // Prints "3.141592653589793"
```

## Caracteres especiales

Los literales de string de Swift admiten varias [secuencias de escape][special-characters] para caracteres especiales:

| Secuencia  | Descripción                   |
| ---------- | ----------------------------- |
| `\0`       | Carácter nulo                |
| `\\`       | Barra invertida                     |
| `\t`       | Tabulación horizontal                |
| `\n`       | Salto de línea (nueva línea)           |
| `\r`       | Retorno de carro               |
| `\'`       | Comilla simple         |
| `\"`       | Comilla doble         |
| `\u{n}`    | Escalar Unicode arbitrario (hexadecimal) |

```swift
let quote: String = "\"Hello\", he said."
print(quote) // Prints "Hello", he said.

let unicode: Character = "\u{1F496}"
print(unicode) // Prints 💖
```

[strings-and-characters]: https://docs.swift.org/swift-book/documentation/the-swift-programming-language/stringsandcharacters/
[string-docs]: https://developer.apple.com/documentation/swift/String
[nsstring-docs]: https://developer.apple.com/documentation/foundation/nsstring
[character-docs]: https://developer.apple.com/documentation/swift/character
[string-interpolation]: https://docs.swift.org/swift-book/documentation/the-swift-programming-language/stringsandcharacters/#String-Interpolation
[unicode-scalar-values]: https://docs.swift.org/swift-book/documentation/the-swift-programming-language/stringsandcharacters/
[concatenated]: https://docs.swift.org/swift-book/documentation/the-swift-programming-language/stringsandcharacters/#Concatenating-Strings-and-Characters
[append]: https://developer.apple.com/documentation/swift/string/append(_:)-4xa8f
[special-characters]: https://docs.swift.org/swift-book/documentation/the-swift-programming-language/stringsandcharacters/#Special-Characters-in-String-Literals
