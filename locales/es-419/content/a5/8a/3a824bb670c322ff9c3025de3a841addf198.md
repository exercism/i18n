# Evita comparar con literales booleanos

En lugar de comparar booleanos con valores literales, se puede simplificar y usar solo las variables:

```java
// instead of
return someBoolVariable == true;

// use
return someBoolVariable;
```
