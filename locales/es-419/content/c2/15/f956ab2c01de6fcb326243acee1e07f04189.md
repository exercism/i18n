# Anexo de instrucciones

## Implementación

Puedes usar `collect(1:10)` como una forma rápida de crear una colección, pero recuerda que, internamente, `collect` devuelve un array, así que también puedes crear un array explícitamente haciendo `my_array = [1, 2, 3, 4, 5]`.
