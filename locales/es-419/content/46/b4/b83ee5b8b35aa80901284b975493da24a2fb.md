# Pistas

## General

- Los enteros de 8 bits se devuelven en `al`.
- Los enteros de 16 bits se devuelven en `ax` y se pasan en `di`.
- Si una función llamada modifica un registro, necesitas guardar cualquier valor de ese registro que vayas a usar más adelante.

## 1. Extrae la máscara

- Los enteros de 8 bits se devuelven en `al`.
- Los enteros de 16 bits se devuelven en `ax` y se pasan en `di`.
- Si una función llamada modifica un registro, necesitas guardar cualquier valor de ese registro que vayas a usar más adelante.
- Hay una instrucción que puedes usar para desplazar bits a la izquierda una cantidad de posiciones.

## 2. Extrae el mensaje

- Los enteros de 8 bits se devuelven en `al`.
- Los enteros de 16 bits se devuelven en `ax` y se pasan en `di`.
- Si una función llamada modifica un registro, necesitas guardar cualquier valor de ese registro que vayas a usar más adelante.
- Los enteros de 8 bits se devuelven en `al`.

## 3. Extrae los bits redundantes

- Los enteros de 8 bits se devuelven en `al`.
- Los enteros de 16 bits se devuelven en `ax` y se pasan en `di`.
- Si una función llamada modifica un registro, necesitas guardar cualquier valor de ese registro que vayas a usar más adelante.
- Puedes usar `extract_higher_bits` y `extract_lower_bits`, definidas anteriormente.
- Hay una instrucción que activa un bit si está activo en ambos valores.

## 4. Activa todos los bits del mensaje

- Los enteros de 8 bits se devuelven en `al`.
- Los enteros de 16 bits se devuelven en `ax` y se pasan en `di`.
- Si una función llamada modifica un registro, necesitas guardar cualquier valor de ese registro que vayas a usar más adelante.
- Puedes usar `extract_higher_bits` y `extract_lower_bits`, definidas anteriormente.
- Hay una instrucción que activa un bit si está activo en cualquiera de los dos valores, incluidos ambos.

## 5. Rota la clave privada

- Los enteros de 8 bits se devuelven en `al`.
- Los enteros de 16 bits se devuelven en `ax` y se pasan en `di`.
- Si una función llamada modifica un registro, necesitas guardar cualquier valor de ese registro que vayas a usar más adelante.
- Puedes usar `extract_higher_bits` y `extract_lower_bits`, definidas anteriormente.
- Puedes definir una constante entera al principio del archivo usando `equ`.
- Hay una instrucción que puedes usar para obtener la cantidad de bits activos en un número de 16 bits.
- La instrucción [movzx][movzx] puede usarse para convertir un número de 8 bits en un número de 16 bits, borrando todos los bits adicionales.
- Hay una instrucción que puedes usar para rotar bits a la izquierda una cantidad de posiciones dada por un valor de 8 bits en `cl`.

## 6. Formatea la clave privada

- Los enteros de 8 bits se devuelven en `al`.
- Los enteros de 16 bits se devuelven en `ax` y se pasan en `di`.
- Si una función llamada modifica un registro, necesitas guardar cualquier valor de ese registro que vayas a usar más adelante.
- Puedes usar `rotate_private_key`, `extract_higher_bits` y `extract_lower_bits`, definidas anteriormente.
- Hay una instrucción que activa un bit si está activo en solo uno de los dos valores, no en ambos.
- Hay una instrucción para invertir todos los bits de un número.

## 7. Termina el descifrado

- Los enteros de 8 bits se devuelven en `al`.
- Los enteros de 16 bits se devuelven en `ax` y se pasan en `di`.
- Si una función llamada modifica un registro, necesitas guardar cualquier valor de ese registro que vayas a usar más adelante.
- Puedes usar `set_bits` y `format_private_key`, definidas anteriormente.
- La instrucción [movzx][movzx] puede usarse para convertir un número de 8 bits en un número de 16 bits, borrando todos los bits adicionales.
- Hay una instrucción que puedes usar para desplazar bits a la izquierda una cantidad de posiciones.
- Hay una instrucción que activa un bit si está activo en cualquiera de los dos valores, incluidos ambos.

[movzx]: https://www.felixcloutier.com/x86/movzx
