# Anexo

## Tu programa en awk

Las pruebas invocan tu programa con `gawk`, pasando un valor a una variable de awk, pero **sin nombres de archivo ni entrada redirigida**.
Deberás implementar la lógica dentro del bloque `BEGIN`.
