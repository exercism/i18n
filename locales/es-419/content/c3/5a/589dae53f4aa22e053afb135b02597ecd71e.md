# Acerca de

Un `DateTime` en F# es un objeto inmutable que contiene información tanto de fecha _como_ de hora. Se puede acceder a la información de fecha y hora a través de sus [propiedades][properties] integradas.

Se puede manipular un `DateTime` llamando a uno de sus [métodos][methods]. Como los valores de `DateTime` nunca pueden cambiar después de haberse definido, todos los métodos que parecen modificar un `DateTime` en realidad devuelven un nuevo `DateTime`.

Se pueden comparar instancias de `DateTime` con los operadores de comparación predeterminados (`<`, `>`, etc.). La fecha (y la hora) actual se puede obtener a través de la propiedad `DateTime.Now`.

Un aspecto importante de las fechas en F# es que dependen de la cultura. Por eso, cualquier método de `DateTime` que trabaje con `string`s dependerá de la cultura actual. Esto incluye el [método `DateTime.Parse()`][parse], que convierte un `string` en un `DateTime`, así como el [método `ToString()`][to-string] de la clase `DateTime`, que convierte un `DateTime` en un `string`.

Cuando usas un `DateTime` en una llamada a `sprintf`, en lugar de convertir manualmente el `DateTime` a un `string` y usar el marcador de posición de string `%s`, puedes usar el [marcador de posición `%O`][object-placeholder], que convierte automáticamente el `DateTime` a su valor de `string`.

[parse]: https://docs.microsoft.com/en-us/dotnet/api/system.datetime.parse?view=netcore-3.1#System_DateTime_Parse_System_String_
[operators]: https://docs.microsoft.com/en-us/dotnet/api/system.datetime?view=netcore-3.1#operators
[properties]: https://docs.microsoft.com/en-us/dotnet/api/system.datetime?view=netcore-3.1#properties
[to-string]: https://docs.microsoft.com/en-us/dotnet/api/system.datetime.tostring?view=netcore-3.1
[object-placeholder]: https://fsharpforfunandprofit.com/posts/printf/#formatting-for-dummies
[methods]: https://learn.microsoft.com/en-us/dotnet/api/system.datetime?view=netcore-3.1#methods
