# Números enteros

Los números enteros son números sin parte decimal que pertenecen al conjunto `{..., -2, -1, 0, 1, 2, ...}`.
Los valores máximo y mínimo de un número entero dependen del sistema en el que se ejecuta PHP.
Por lo general, en la mayoría de los sistemas modernos, el número entero es un valor de 64 bits, lo que significa que puede representar desde `-9_223_372_036_854_775_808` hasta `9_223_372_036_854_775_807`, ambos inclusive.

```php
<?php

$a = 1234;
$a = 1_234_567;
```

Puedes escribir valores literales enteros en una base distinta de la base 10 usando un prefijo de base:

```php
<?php

$a = 0123; // octal number (equivalent to 83 decimal)
$a = 0o123; // octal number (as of PHP 8.1.0)
$a = 0x1A; // hexadecimal number (equivalent to 26 decimal)
$a = 0b11111111; // binary number (equivalent to 255 decimal)
```

> Tomado de la [documentación][syntax] de PHP

[syntax]: https://www.php.net/manual/en/language.types.integer.php#language.types.integer.syntax
