# Apéndice de instrucciones

## Implementación

Si se encuentra un carácter o un codón no válido _durante_ la traducción, debe `throw` un error con el mensaje `Invalid codon`.

```javascript
translate('AAA');
// => Error: Invalid codon
```
