# usa operadores booleanos

Las funciones `canFastAttack`, `canSpy`, `canSignalPrisoner` y `canFreePrisoner` se pueden resolver usando únicamente `not`, `&&` y `||`.
Para aprender a manipular booleanos, asegúrate de resolver el ejercicio de esa manera.
