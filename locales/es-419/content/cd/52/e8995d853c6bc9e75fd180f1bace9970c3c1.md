# Introducción

El operador rest `...` sirve para manejar un número indefinido de argumentos. Se coloca como el último argumento de una función:

```haxe
function f(...nums:Int) {
  for (num in nums) {
    trace(num);
  }
}

f(1, 2, 3, 4, 5); // Output is 1 2 3 4 5
```
