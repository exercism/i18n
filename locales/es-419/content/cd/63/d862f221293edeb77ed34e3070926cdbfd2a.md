# Prefiere extraer la constante a nivel superior

En lugar de definir la constante _dentro_ de la función, considera extraerla al nivel superior. Las constantes, las funciones y las clases que no se marcan con `export` no son accesibles desde fuera del archivo.

```javascript
const %{name} = %{value}

export const gigasecond = (...)
```
