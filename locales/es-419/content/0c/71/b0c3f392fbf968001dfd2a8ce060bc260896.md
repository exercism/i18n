# Introducción

Cuando pasas una variable a una función, su propiedad se transfiere y la variable original deja de ser utilizable. Esto puede resultar un poco tedioso cuando necesitas acceder a un estado más de una vez, pero por suerte Cairo tiene dos funcionalidades para pasar valores sin destruirlos ni moverlos, llamadas `references` y `snapshots`.
