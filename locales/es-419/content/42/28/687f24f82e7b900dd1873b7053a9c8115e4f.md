# no uses funciones integradas de raíz cuadrada

El propósito de este ejercicio es construir un algoritmo para calcular la raíz cuadrada entera. No uses funciones integradas de raíz cuadrada como `:math.sqrt`, `:math.pow`, `Float.pow` o `Kernel.**`.
