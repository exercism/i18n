# Acerca de

Puedes usar typedefs para acortar los nombres de tipos complejos:

```haxe
typedef IA = Array<Int>;
```

También puedes usar typedefs para crear la estructura de un tipo:

```haxe
typedef User = {
  var name:String;
  var age:Int;
}
```
