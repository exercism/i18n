# parámetros

[comment]: # (Este error se genera cuando un parámetro no se usa)
[comment]: # (Algunos parámetros, como `_ as x`, no se pueden usar)
[comment]: # (requiere 1 parámetro:)
[comment]: # (- definition: extracto de código con un valor sin usar)

El parámetro que se indica abajo no se usa en ningún lado.
Puedes eliminarlo o usarlo en algún lugar, si se puede usar.

```
%{definition}
```
