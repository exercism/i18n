# usa un atributo de módulo

Usa un [atributo de módulo](https://elixir-lang.org/getting-started/module-attributes.html#as-constants) en `add_player/3` y `reset_score/2` como constante para la puntuación inicial.
