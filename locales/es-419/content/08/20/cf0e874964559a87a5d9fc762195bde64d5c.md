# Instrucciones específicas de SQLite

Este ejercicio requiere que establezcas la columna `result` de la tabla `color_code` con el valor correcto según el color que se encuentre en la columna `color`.

## Esquema de la tabla

```sql
CREATE TABLE "color_code" ("color" TEXT, "result" INT);
```
