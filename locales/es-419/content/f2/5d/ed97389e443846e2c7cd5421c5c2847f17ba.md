# Introducción

Si el código de un bucle debe ejecutarse siempre al menos una vez, puedes usar un bucle `do`/`while`:

```csharp
int x = 23;

do
{
    // Always executes at least once, then repeats as long as x > 10
    x = x - 2;
} while (x > 10)
```
