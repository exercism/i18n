# Usa `MapSet.member?` y `MapSet.put`

Usa `MapSet.member?` y `MapSet.put` en `add_card`. Hay otras formas idiomáticas de definir la función, pero estas son las que mejor se ajustan para aprender del todo el concepto de este ejercicio.