# export en línea

Las funciones, como `twoFer`, las clases y las constantes se pueden `export`ar en línea.

```javascript
// Not inlined
export { twoFer }

// Inlined
export function twoFer() {}
```
