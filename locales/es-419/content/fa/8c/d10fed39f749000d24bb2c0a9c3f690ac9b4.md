# Usa `MapSet.member?` y `MapSet.delete`

Usa `MapSet.member?` y `MapSet.delete` en `trade_card`. Hay otras formas idiomáticas de definir la función, pero son las que mejor encajan para aprender por completo el concepto de este ejercicio.