# Introducción

## Recursión

Las funciones recursivas son funciones que se llaman a sí mismas.

Una función recursiva necesita tener al menos un _caso base_ y al menos un _caso recursivo_.

Un _caso base_ devuelve un valor sin volver a llamar a la función.
Un _caso recursivo_ vuelve a llamar a la función, modificando el argumento para que en algún momento coincida con el caso base.

En Elm, las funciones recursivas normalmente se definen usando coincidencia de patrones con expresiones `case` o varias cláusulas de función.

```elm
count : List a -> Int
count list =
    case list of
        [] ->
            0  -- base case

        _ :: tail ->
            1 + count tail  -- recursive case
```

### Bucles mediante recursión

Debido a la inmutabilidad, los bucles en Elm se escriben de forma distinta que en los lenguajes imperativos.
Por ejemplo, los bucles suelen verse así:

```c
for(i = 0; i < array.length; i++) {
  // do something with array[i]
}
```

En un lenguaje funcional como Elm, no es posible mutar variables.
Por lo tanto, los bucles tienen que implementarse con recursión.

El equivalente de un bucle `for` en Elm se vería así:

```elm
loop : List a -> (a -> b) -> List b
loop list f =
    case list of
        [] ->
            []

        head :: tail ->
            f head :: loop tail f
```

En la práctica, iterar sobre listas y otras estructuras de datos en Elm casi siempre se hace con funciones del módulo `List`, como `List.map`, `List.filter` y `List.foldl`.
Internamente, estas funciones están implementadas usando recursión.
