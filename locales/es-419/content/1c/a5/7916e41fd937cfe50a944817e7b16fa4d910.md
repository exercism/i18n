# Introducción

En este ejercicio puede que también quieras usar un bucle. Hay varias maneras de escribir bucles en C#, pero el bucle `while` es el más adecuado aquí:

```csharp
int x = 23;

while (x > 10)
{
    // Execute logic if x > 10
    x = x - 2;
}
```
