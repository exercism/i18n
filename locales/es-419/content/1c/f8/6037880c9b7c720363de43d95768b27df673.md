# Acerca de

Para comprobar si dos variables de tipos simples (strings, enteros, etc.) son iguales, usamos los operadores de igualdad `==` y `!=`:

```haxe
var a = 5;
var b = 5;

a == b; // true
a != b; // false
```
