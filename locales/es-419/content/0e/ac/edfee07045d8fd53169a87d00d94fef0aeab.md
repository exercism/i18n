# Introducción

La clase `str` ofrece [muchos métodos útiles][str methods] para trabajar con strings y componerlos.
Entre ellos hay métodos para buscar, limpiar, dividir, transformar, traducir y muchas otras técnicas.

Los strings son [secuencias][text sequence] de [puntos de código Unicode][unicode code points]: cada «carácter» o punto de código individual (_strings de longitud 1_) se puede referenciar mediante un `0-based index` desde la izquierda, o un `-1-based index` desde la derecha.
Los strings implementan todas las [operaciones de secuencia comunes][common sequence operations].

Se pueden recorrer con la sintaxis `for item in <str>` o `for index, item in enumerate(<str>)`.
También se pueden concatenar con `<str> + <other str>` o `<str>.join(<iterable>)`.

Los strings son _inmutables_, lo que significa que el valor de un objeto `str` en memoria no puede cambiar.
Las funciones o métodos que operan sobre un `str` (_como los que estamos aprendiendo aquí_) devolverán una nueva `instance` de ese objeto `str` en lugar de modificar el `str` original.

A continuación hay una pequeña selección de métodos de string de Python.
Para ver la lista completa, consulta la [clase str][str methods] en la documentación de Python.


[`<str>.title()`][str-title] analiza un string y pone en mayúscula el primer «carácter» de cada «palabra» que encuentra.
En Python, esto depende mucho del [códec del idioma][codecs] que se use y de cómo ese idioma en particular representa las palabras y los caracteres.
También puede haber reglas de [locale][locale] para un idioma o un conjunto de caracteres.


```python
man_in_hat_th = 'ผู้ชายใส่หมวก'
man_in_hat_ru = 'мужчина в шляпе'
man_in_hat_ko = '모자를 쓴 남자'
man_in_hat_en = 'the man in the hat.'

>>> man_in_hat_th.title()
'ผู้ชายใส่หมวก'

>>> man_in_hat_ru.title()
'Мужчина В Шляпе'

>>> man_in_hat_ko.title()
'모자를 쓴 남자'

>> man_in_hat_en.title()
'The Man In The Hat.'
```

[`<str>.endswith(<suffix>)`][str-endswith] devuelve `True` si el string termina con `<suffix>`, y `False` en caso contrario.


```python
>>> 'My heart breaks. 💔'.endswith('💔')
True

>>> 'cheerfulness'.endswith('ness')
True

# Punctuation is part of the string, so needs to be included in any endswith match.
>>> 'Do you want to 💃?'.endswith('💃')
False

>> 'The quick brown fox jumped over the lazy dog.'.endswith('dog')
False
```

[`<str>.strip(<chars>)`][str-strip] devuelve una copia del `str` con los `<chars>` del principio y del final eliminados.
Los puntos de código indicados en `<chars>` no son un prefijo ni un sufijo: se eliminarán **todas las combinaciones** de esos puntos de código empezando por **ambos extremos** del string.
 Si no se indica nada para `<chars>`, se eliminarán todas las combinaciones de puntos de código de espacio en blanco.


 ```python
# This will remove "https://", because it can be formed from "/stph:". 
>>> 'https://unicode.org/emoji/'.strip('/stph:')
'unicode.org/emoji'

# Removal of all whitespace from both ends of the str.
>>> '   🐪🐪🐪🌟🐪🐪🐪   '.strip()
'🐪🐪🐪🌟🐪🐪🐪'

>>> justification = 'оправдание'
>>> justification.strip('еина')
'оправд'

# Prefix and suffix in one step.
>>> 'unaddressed'.strip('dnue')
'address'

>>> '  unaddressed  '.strip('dnue ')
'address'
```


[`<str>.replace(<substring>, <replacement substring>)`][str-replace] devuelve una copia del string con todas las apariciones de `<substring>` reemplazadas por `<replacement substring>`.

La cita que aparece abajo es de [The Hunting of the Snark][The Hunting of the Snark], de [Lewis Carroll][Lewis Carroll]

```python
# The Hunting of the Snark, by Lewis Carroll
>>> quote = '''
"Just the place for a Snark!" the Bellman cried,
   As he landed his crew with care;
Supporting each man on the top of the tide
   By a finger entwined in his hair.

"Just the place for a Snark! I have said it twice:
   That alone should encourage the crew.
Just the place for a Snark! I have said it thrice:
   What I tell you three times is true."
'''

>>> quote.replace('Snark', '🐲')
...
'\n"Just the place for a 🐲!" the Bellman cried,\n   As he landed his crew with care;\nSupporting each man on the top of the tide\n   By a finger entwined in his hair.\n\n"Just the place for a 🐲! I have said it twice:\n   That alone should encourage the crew.\nJust the place for a 🐲! I have said it thrice:\n   What I tell you three times is true."\n'

>>> 'bookkeeper'.replace('kk', 'k k')
'book keeper'
```

[Lewis Carroll]: https://www.poetryfoundation.org/poets/lewis-carroll
[The Hunting of the Snark]: https://www.poetryfoundation.org/poems/43909/the-hunting-of-the-snark
[codecs]: https://docs.python.org/3/library/codecs.html
[common sequence operations]: https://docs.python.org/3/library/stdtypes.html#text-sequence-type-str
[locale]: https://docs.python.org/3/library/locale.html#module-locale
[str methods]: https://docs.python.org/3/library/stdtypes.html#string-methods
[str-endswith]: https://docs.python.org/3/library/stdtypes.html#str.endswith
[str-replace]: https://docs.python.org/3/library/stdtypes.html#str.replace
[str-strip]: https://docs.python.org/3/library/stdtypes.html#str.strip
[str-title]: https://docs.python.org/3/library/stdtypes.html#str.title
[text sequence]: https://docs.python.org/3/library/stdtypes.html#text-sequence-type-str
[unicode code points]: https://stackoverflow.com/questions/27331819/whats-the-difference-between-a-character-a-code-point-a-glyph-and-a-grapheme
