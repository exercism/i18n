# evita los bucles

En lugar de usar un bucle, considera usar una sola expresión basada en uno de los operadores de comparación (es decir, `<=`, `>=`, `<`, `>`, etc.).
Puedes reutilizar los otros métodos ya implementados en la clase o agregar nuevos según sea necesario.
