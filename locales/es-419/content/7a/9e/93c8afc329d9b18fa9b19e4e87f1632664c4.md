# Binario, octal y hexadecimal

Binario, octal y hexadecimal (_también conocido como hex_) son distintos [sistemas de numeración][numeral-systems] con distintas bases.
El binario es base 2, el octal es base 8 y el hexadecimal es base 16.
Los enteros normales usan base 10 en Crystal.
Binario, octal y hexadecimal son representaciones de números enteros.
Esto significa que representan números positivos y negativos (_incluido el cero_) sin fracciones ni decimales, y admiten todas las operaciones que podemos hacer con números enteros.

## Binario

El [binario][binary] es un sistema de numeración de base 2 que usa solo los dígitos 0 y 1.
Suele representar los estados 0 («apagado») y 1 («encendido») del flujo eléctrico a través de transistores e interruptores en las computadoras, así como las cargas positivas y negativas en los medios de almacenamiento magnético.
El binario puede representar todos los números enteros que se usan en base 10.

Un fragmento del sistema de base 2 se ve así, aunque continúa infinitamente y no se detiene en 128:

| 128      | 64       | 32       | 16       | 8        | 4        | 2        | 1        |
| -------- | -------- | -------- | -------- | -------- | -------- | -------- | -------- |
| 2 \*\* 7 | 2 \*\* 6 | 2 \*\* 5 | 2 \*\* 4 | 2 \*\* 3 | 2 \*\* 2 | 2 \*\* 1 | 2 \*\* 0 |

Entonces, si queremos representar el número 6, en binario sería: 110

| Valor posicional | 4   | 2   | 1   |
| ---------------- | --- | --- | --- |
| Número binario   | 1   | 1   | 0   |

Y la operación sería: `4 + 2 + 0 = 6`

Otro ejemplo: 19

| Valor posicional | 16  | 8   | 4   | 2   | 1   |
| ---------------- | --- | --- | --- | --- | --- |
| Número binario   | 1   | 0   | 0   | 1   | 1   |

El número binario sería: 10011
Y la operación sería: `16 + 0 + 0 + 2 + 1 = 19`

## Binario en Crystal

En Crystal, podemos representar literales binarios usando el prefijo `0b`.
Si escribimos `0b10011`, Crystal lo interpretará como un número binario y lo convertirá a base 10.

```crystal
0b10011
# => 19

typeof(0b10011)
# => Int32
```

Si escribes un número con el prefijo `0b` que no esté en el sistema binario, lanzará un error de compilación.

```crystal
0b10211
# => Error: unexpected token: "211"
```

### Operaciones con números binarios

Como los números binarios son enteros, podemos hacer con ellos todas las operaciones que hacemos con números enteros.

```crystal
0b10011 + 0b10011
# => 38

0b10011 * 0b10011
# => 361

0b10011 / 3
# => 6.333333333333333
```

### Convertir hacia y desde la representación binaria

Crystal convertirá automáticamente un literal binario en un `Int32`.
Para convertir un `Int32` en una representación binaria, usa el método [`<int>.to_s(base)`][to_s] con el argumento base establecido en 2.
El método devolverá una representación en string del número binario:

```crystal
19.to_s(2)
# => "10011"
```

Para convertir una representación en string de un binario a un entero, podemos usar el método [`<int>.to_i(base)`][to_i], pasando la base (2) como argumento:

```crystal
"10011".to_i(2)
# => 19
```

Si das una base equivocada (_o una representación binaria inválida_), lanzará un `ArgumentError`:

```crystal
"011021".to_i(2)
# => Invalid Int32: "011021" (ArgumentError)
```

### Métodos binarios

Hay ciertos métodos que se pueden usar en los enteros para obtener información sobre la representación binaria del número.

[`<int>.bit_length`][bit_length] devolverá la cantidad de bits que se necesitan para representar el número:

```crystal
19.bit_length
# => 5
```

[`<int>.bit(index)`][bit] devolverá el bit en el índice indicado, empezando por la derecha (_bit menos significativo_):

```crystal
19.bit(0)
# => 1

0b10011.bit(1)
# => 1
```

## Octal

El [octal][octal] es un sistema de numeración de base 8.
Usa los dígitos 0, 1, 2, 3, 4, 5, 6 y 7.

En Crystal, podemos representar números octales usando el prefijo `0o`.
Igual que con el binario, Crystal convierte automáticamente una representación octal en un `Int32`.

```crystal
0o123
# => 83
```

Si antepones `0o` a un número que no esté en el sistema octal, lanzará un error de compilación.

### Convertir hacia y desde la representación octal 

Igual que con el binario, podemos usar el método [`<int>.to_s(base)`][to_s] para convertir un `Int32` en una representación octal.
Y podemos usar el método [`<int>.to_i(base)`][to_s] para convertir una representación octal en un `Int32`.

```crystal
83.to_s(8)
# => "123"

"123".to_i(8)
# => 83
```

Igual que con el binario, si das una base equivocada se lanzará un `ArgumentError`.

## Hexadecimal

El [hexadecimal][hexadecimal] es un sistema de numeración de base 16.
Usa los dígitos 0 - 9 y las letras A, B, C, D, E y F.
A es 10, B es 11, C es 12, D es 13, E es 14 y F es 15.

Podemos representar números hexadecimales en Crystal usando el prefijo `0x`.
Igual que con el binario y el octal, Crystal convertirá automáticamente los literales hexadecimales en `Int32`.

```crystal
#=> 0x123
291
```
 
Si antepones `0x` a un número que no sea hexadecimal, lanzará un error de compilación.

###  Convertir hacia y desde la representación hexadecimal

Igual que con el binario, podemos usar el método [`<int>.to_s(base)`][to_i] para convertir un `Int32` en una representación hexadecimal.
Y podemos usar el método [`<int>.to_i(base)`][to_s] para convertir una representación hexadecimal en un `Int32`.

```crystal
291.to_s(16)
# => "123"

"123".to_i(16)
# => 291
```

Igual que con el binario y el octal, si das una base equivocada se lanzará un `ArgumentError`.

## Precisión

Cuando convertimos un número a una representación en string, podemos especificar la precisión del número.
Es decir, la cantidad **mínima** de dígitos que se usan para representar el número.
Si el número tiene menos dígitos que la precisión, se agregarán ceros a la izquierda a la representación en string.
Esto se hace pasando el argumento `precision` al método [`<int>.to_s(<base>, precision: <value>)`][to_s].

```crystal
291.to_s(16, precision: 5)
# => "00123"

543.to_s(precision: 2)
# => "543"
```

## Mayúsculas

Cuando convertimos un número a una representación en string, podemos especificar si queremos que las letras del string estén en mayúsculas.
Esto se hace pasando el argumento `upcase` al método [`<int>.to_s(<base>, upcase: <value>)`][to_s].

```crystal
255.to_s(16, upcase: true)
# => "FF"

255.to_s(16, upcase: false)
# => "ff"
```

[binary]: https://en.wikipedia.org/wiki/Binary_number
[bit]: https://crystal-lang.org/api/Int.html#bit%28bit%29-instance-method
[bit_length]: https://crystal-lang.org/api/Int.html#bit_length%3AInt32-instance-method
[hexadecimal]: https://en.wikipedia.org/wiki/Hexadecimal
[numeral-systems]: https://en.wikipedia.org/wiki/Numeral_system
[octal]: https://en.wikipedia.org/w
[to_s]: https://crystal-lang.org/api/Int.html#to_s%28base%3AInt%3D10%2C%2A%2Cprecision%3AInt%3D1%2Cupcase%3ABool%3Dfalse%29%3AString-instance-method
[to_i]: https://crystal-lang.org/api/String.html#to_i%28base%3AInt%3D10%2Cwhitespace%3ABool%3Dtrue%2Cunderscore%3ABool%3Dfalse%2Cprefix%3ABool%3Dfalse%2Cstrict%3ABool%3Dtrue%2Cleading_zero_is_octal%3ABool%3Dfalse%29-instance-method
