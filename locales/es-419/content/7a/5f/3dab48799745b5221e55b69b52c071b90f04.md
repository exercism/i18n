# Usa `rand.uniform` en lugar de `Enum.random`

Usa `:rand.uniform` en lugar de `Enum.random` en la función `random_stardate`. `:rand.uniform` te permite generar un número de punto flotante aleatorio directamente, a diferencia de `Enum.random`, que solo permite generar números enteros aleatorios.
