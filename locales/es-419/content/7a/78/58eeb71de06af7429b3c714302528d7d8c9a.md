# Introducción

Las **expresiones** `throw` son una alternativa a las _sentencias_ `throw` y, en particular, pueden potenciar las expresiones ternarias y otras expresiones compuestas.

```haxe
trace(true ? throw "This is an error" : "Everything is fine"); // ERROR: This is an error
```
