# Optimiza el valor predeterminado

En lugar de usar un condicional para ramificar cuando no se pasa ningún valor a `twoFer`, establece el [valor predeterminado](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Default_parameters) de `%{parameter}` en `'you'` para evitar este condicional.
