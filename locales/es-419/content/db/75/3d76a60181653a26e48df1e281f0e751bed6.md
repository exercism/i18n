# Anexo de instrucciones

## Validación de la entrada

Este ejercicio no requiere que valides la cadena de ADN de entrada.
Cada entrada que se prueba contiene únicamente los caracteres `A`, `C`, `G` o `T`.
