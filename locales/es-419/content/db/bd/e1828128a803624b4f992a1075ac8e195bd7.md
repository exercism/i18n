# send_newsletter devuelve implícitamente

La función `send_newsletter/3` no debe devolver `:ok` de forma explícita, sino devolver implícitamente lo que devuelve `close_log/1` (que resulta ser `:ok` cuando todo sale bien).
