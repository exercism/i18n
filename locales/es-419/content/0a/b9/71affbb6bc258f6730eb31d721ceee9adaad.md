# Introducción

El valor `null` en JavaScript representa la ausencia intencional de un valor de objeto. Es uno de los tipos primitivos de JavaScript.

```javascript
// I do not have an apple.
var apple = null;
apple; // => null

// null is treated as falsy for boolean operations, therefore
!apple; // => true
!!apple; // => false
```
