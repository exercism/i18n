# usa el literal null

Cuando trabajes con `Strings`, prefiere comparar directamente con `null` en lugar de usar `Optional`.

Usar `Optional` en este contexto agrega complejidad innecesaria y evita la práctica que se busca: manejar los valores `null` directamente.
