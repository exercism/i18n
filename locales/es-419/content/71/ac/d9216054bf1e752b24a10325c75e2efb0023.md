# Introducción

La mayoría de las palabras de secuencia de Factor devuelven secuencias *nuevas* y dejan intactas las secuencias que reciben, pero tanto los arrays como los vectores también admiten **mutación en el lugar** cuando quieres registrar un estado que va cambiando con el tiempo.

`set-nth` y `change-nth` cambian una posición en el lugar, y funcionan tanto con un array como con un vector. `<array>` construye un array mutable de longitud fija; los literales `V{ }`, `<vector>`, `push` y `pop` son el conjunto exclusivo de los vectores, ya que los arrays no pueden cambiar de longitud. `clone` duplica la secuencia para que las mutaciones posteriores no se filtren en la original.

```factor
USING: arrays kernel sequences ;

5 0 <array>                 ! a fresh array { 0 0 0 0 0 }
dup 7 2 swap set-nth        ! { 0 0 7 0 0 } — set position 2 to 7
.

V{ } clone                  ! a fresh, empty vector
"alice" over push           ! V{ "alice" }
"bob" over push             ! V{ "alice" "bob" }
dup pop .                   ! "bob"
.                           ! V{ "alice" }
```

[arrays]: https://docs.factorcode.org/content/vocab-arrays.html
[sequences]: https://docs.factorcode.org/content/vocab-sequences.html
