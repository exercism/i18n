# Introducción

A menudo resulta útil agrupar una colección de elementos y tratar esos grupos como unidades.

En Rust, a ese grupo lo llamamos struct, y a cada elemento, uno de los campos del struct. Un struct define el conjunto general de campos disponibles, pero a un ejemplo concreto de un struct se le llama instancia.
