# concatenación con fmt

La forma más fácil de agregar una string nueva a una existente es usar el operador `+=`.
En casos con pocas concatenaciones, como en este ejercicio, también es más rápido que `%{function}`.
