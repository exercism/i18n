# Introducción

La mayoría de las palabras de orden superior que trabajan con
secuencias le pasan a la quotation un elemento a la vez. La familia
`*-index` le pasa el elemento *y* su posición. `times` ejecuta un
bucle contado en el que el cuerpo no tiene ninguna secuencia; es
útil cuando solo necesitas llevar la cuenta.
