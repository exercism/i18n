# `vec![]`

Puedes usar macros de Rust para generar vectores.

## Sintaxis

- `vec![]` usa una macro de la biblioteca estándar de Rust para inicializar un `Vec<T>` vacío
- `vec![1; 3]` inicializa un vector con tres unos
- `vec![1, 2, 3]` inicializa un vector con los valores literales 1, 2, 3
