# Introducción

Casi todos los programas en Factor son una secuencia de palabras que consumen y producen valores en la *pila de datos*. Cuando las entradas y las salidas se alinean bien, el código se lee de izquierda a derecha sin complicaciones. Cuando no, recurres a las *shuffle words*, y el código se vuelve ruidoso enseguida.

Este ejercicio te guía por la solución estándar de Factor: los combinadores.

## Shuffle words

Las shuffle words más comunes viven en [`kernel`][kernel]:

```
dup    ( x -- x x )
dupd   ( x y -- x x y )
drop   ( x -- )
swap   ( x y -- y x )
swapd  ( x y z -- y x z )
over   ( x y -- x y x )
pick   ( x y z -- x y z x )
rot    ( x y z -- y z x )
-rot   ( x y z -- z x y )
rotd   ( w x y z -- w y z x )
spin   ( x y z -- z y x )
nip    ( x y -- y )
tuck   ( x y -- y x y )

2dup   ( x y -- x y x y )
3dup   ( x y z -- x y z x y z )
4dup   ( w x y z -- w x y z w x y z )
2drop  ( x y -- )
3drop  ( x y z -- )
4drop  ( w x y z -- )
2nip   ( x y z -- z )
2swap  ( x y z w -- z w x y )
```

Ya habrás usado `dup` y `swap`. `over` hace una copia del *segundo* elemento desde arriba, lo cual resulta útil cuando la misma entrada debe alimentar dos cálculos:

```factor
! Sum a number with twice itself: x + 2*x
: triple ( x -- 3x ) dup 2 * + ;
```

Eso está bien para un solo uso. Pero encadenadas, las secuencias de `dup`, `swap`, `over`, `rot` se vuelven ilegibles enseguida.

## `keep`: conserva la entrada

`keep` (en [`kernel`][kernel]) llama a una quotation en la cima de la pila, pero deja el valor original encima del resultado:

```
keep ( x quot: ( x -- y ) -- y x )
```

```factor
5 [ 2 * ] keep .s
! => 10
! => 5
```

Eso reemplaza el patrón de un `dup` seguido de algo y luego un shuffle: donde escribirías `dup something swap`, puedes simplemente decir `[ something ] keep`.

## `bi`: dos funciones de una entrada

`bi` (en [`kernel`][kernel]) aplica *dos* quotations al mismo valor y deja ambos resultados en la pila:

```
bi ( x q1 q2 -- r1 r2 )
```

```factor
4 [ sq ] [ neg ] bi .s
! => 16
! => -4
```

Sin `bi` escribirías `dup sq swap neg`, que se lee bien para dos operaciones, pero se complica para tres.

## `tri` y `cleave`: muchas funciones de una entrada

`tri` extiende `bi` a tres quotations:

```
tri ( x q1 q2 q3 -- r1 r2 r3 )
```

`cleave` (en [`combinators`][combinators]) es la misma idea para un *array* de cualquier número de quotations:

```factor
4 { [ ] [ sq ] [ neg ] } cleave .s
! => 4
! => 16
! => -4
```

La quotation vacía `[ ]` actúa como la identidad, algo útil cuando uno de los huecos de la carta cortada debe ser la propia entrada.

## `dip` y `2dip`: operan por debajo

`dip` (en [`kernel`][kernel]) llama a una quotation sobre los valores que están *debajo* de la cima de la pila y deja la cima intacta. `2dip` hace lo mismo, pero protege los *dos* valores de arriba:

```
dip  ( x   quot -- x   )
2dip ( x y quot -- x y )
```

```factor
9 10 11 [ + ] dip .s
! => 19      (9 + 10)
! => 11      (the protected top, restored)

9 10 11 12 [ + ] 2dip .s
! => 19      (9 + 10)
! => 11      (the protected y, originally 11)
! => 12      (the protected x, originally 12)
```

Aquí `dip` escondió el `11`, ejecutó `9 10 +` y volvió a poner el `11` en la cima. `2dip` escondió el `11 12`, ejecutó `9 10 +` por debajo y luego restauró el `11 12`.

`dip` es la herramienta correcta cuando el orden natural de los argumentos deja el valor «de paso» en la cima de la pila. Recurre a él en lugar del sándwich `swap … swap`.

## `bi@`: la misma operación sobre dos valores

`bi@` (en [`kernel`][kernel]) aplica una quotation a dos valores de la pila:

```
bi@ ( x y quot -- r1 r2 )
```

```factor
3 4 [ sq ] bi@ .s
! => 9
! => 16
```

## `bi*`: operaciones diferentes sobre dos valores

`bi*` (en [`kernel`][kernel]) aplica quotations *diferentes* a los dos valores de la pila: la primera quotation al valor de abajo, la segunda al de arriba:

```
bi* ( x y q1 q2 -- r1 r2 )
```

```factor
3 4 [ sq ] [ neg ] bi* .s
! => 9
! => -4
```

Recurre a `bi@` cuando ambos valores reciben el mismo tratamiento, y a `bi*` cuando cada uno necesita el suyo.

## `tri*`: operaciones diferentes sobre tres valores

`tri*` (en [`combinators`][combinators]) extiende `bi*` a tres valores y tres quotations, y aplica cada quotation a un valor en orden:

```
tri* ( x y z q1 q2 q3 -- r1 r2 r3 )
```

```factor
1 2 3 [ sq ] [ neg ] [ 10 * ] tri* .s
! => 1
! => -2
! => 30
```

[kernel]: https://docs.factorcode.org/content/vocab-kernel.html
[combinators]: https://docs.factorcode.org/content/vocab-combinators.html
