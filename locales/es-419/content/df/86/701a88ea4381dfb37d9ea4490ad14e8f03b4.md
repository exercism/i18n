# Apéndice de instrucciones

## Implementación

Aunque estamos tratando con bytes de datos, los argumentos y los valores devueltos son `List<Integer>` (en lugar de `byte[]`) para evitar tener que hacer un cast o convertir los valores negativos de los bytes de 128 a 255.
