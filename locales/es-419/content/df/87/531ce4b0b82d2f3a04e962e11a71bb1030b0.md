# Apéndice de instrucciones

## Implementación

Si `n` no es un número entero positivo, detén la ejecución del programa y devuelve un mensaje de error.

En JavaScript, esto se puede hacer con la sentencia `throw`.

```javascript
throw new Error('Only positive integers are allowed');
```
