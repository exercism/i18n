# Anexo de instrucciones

## Implementación

En este ejercicio debes definir una clase `DndCharacter` que tenga:

- Un método estático para `modifier(constitution)`.
- Métodos para `strength`, `dexterity`, `constitution`, `intelligence`, `wisdom`, `charisma` y `hitpoints`
