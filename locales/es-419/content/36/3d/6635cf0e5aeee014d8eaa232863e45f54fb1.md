# Instrucciones

En este ejercicio vas a escribir algo de código para ayudarte a cocinar una lasaña espectacular de tu libro de cocina favorito.

Tienes cuatro tareas, todas relacionadas con el tiempo que se tarda en cocinar la lasaña.

## 1. Define el tiempo esperado en el horno en minutos

Define la constante `OvenTime` con cuántos minutos debe estar la lasaña en el horno.
Según el libro de cocina, el tiempo esperado en el horno es de 40 minutos:

```go
OvenTime
// => 40
```

## 2. Calcula el tiempo restante en el horno en minutos

Define la función `RemainingOvenTime()` que toma como parámetro los minutos que la lasaña lleva realmente en el horno y devuelve cuántos minutos le quedan aún por permanecer en el horno, según el tiempo esperado en el horno de la tarea anterior.

```go
func RemainingOvenTime(actual int) int {
    // TODO
}

RemainingOvenTime(30)
// => 10
```

## 3. Calcula el tiempo de preparación en minutos

Define la función `PreparationTime` que toma como parámetro la cantidad de capas que le agregaste a la lasaña y devuelve cuántos minutos dedicaste a prepararla, suponiendo que cada capa te toma 2 minutos.

```go
func PreparationTime(numberOfLayers int) int {
    // TODO
}

PreparationTime(2)
// => 4
```

## 4. Calcula el tiempo de trabajo transcurrido en minutos

Define la función `ElapsedTime` que toma dos parámetros: el primer parámetro es la cantidad de capas que le agregaste a la lasaña, y el segundo parámetro es la cantidad de minutos que la lasaña lleva en el horno.
La función debe devolver cuántos minutos en total dedicaste a cocinar la lasaña, que es la suma del tiempo de preparación en minutos y el tiempo en minutos que la lasaña lleva en el horno en ese momento.

```go
func ElapsedTime(numberOfLayers, actualMinutesInOven int) int {
    // TODO
}

ElapsedTime(3, 20)
// => 26
```
