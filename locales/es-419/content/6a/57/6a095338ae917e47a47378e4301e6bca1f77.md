# Pruebas

Ejecuta las pruebas con:

```bash
$ crystal spec
```

## Pruebas omitidas

En cada conjunto de pruebas, todas las pruebas menos la primera están omitidas.

Cuando logres que una prueba pase, puedes habilitar la siguiente cambiando `pending` por `it`.
