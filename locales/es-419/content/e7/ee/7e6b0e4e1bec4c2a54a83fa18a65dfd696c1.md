# Introducción

En Julia se usan tres tipos principales de condicionales:

- Expresiones `if`
- El operador ternario
- La evaluación de cortocircuito

## Expresiones `if`

Las expresiones `if` en Julia son similares a las de otros lenguajes:

```julia-repl
julia> function say_if_positive(n)
           if n > 0
               println("n is positive!")
           else
               println("n is not positive!")
           end
       end
say_if_positive (generic function with 1 method)

julia> say_if_positive(10)
n is positive!

julia> say_if_positive(-10)
n is not positive!
```

<!-- TODO: Agregar ese elegante recurso de resaltado de concepto a la expresión booleana -->

Si la expresión booleana que sigue al `if` se evalúa como `true`, se ejecuta el primer bloque de código y se omite el segundo.
Si la expresión booleana que sigue al `if` se evalúa como `false`, se omite el primer bloque de código y se ejecuta el segundo.
El programa continúa ejecutándose en la primera línea de código después de la palabra clave `end`.

~~~~exercism/note
En Julia, la palabra clave `end` marca el final de todas las expresiones de bloque.
Esta sintaxis no es específica de las expresiones `if` ni de las definiciones de funciones.
~~~~

Cuando el segundo bloque de código sería simplemente otra expresión `if`, `elseif` nos permite evitar anidar expresiones `if` dentro del bloque:

```julia-repl
julia> function dessert(fruit)
           if fruit == "apple"
               return "Apple Crumble"
           elseif fruit == "lemon"
               return "Lemon Meringue Pie"
           else
               return "Fruit Salad"
           end
       end
dessert (generic function with 1 method)

julia> dessert("apple")
"Apple Crumble"

julia> dessert("lemon")
"Lemon Meringue Pie"

julia> dessert("peach")
"Fruit Salad"
```

Si una expresión `if` solo necesita ejecutar código para uno de los casos, no hace falta escribir la rama `else`.

## Operador ternario

Las sentencias `if-else` simples también se pueden escribir con el operador ternario `a ? b : c`.
Si `a` es verdadero, se evaluará `b`.
De lo contrario, se evalúa `c`.

Por ejemplo, la siguiente función

```julia-repl
julia> function diagnose(heartrate)
           if heartrate > 100
               println("Elevated heart rate.")
           else
               println("Nominal heart rate or dead.")
           end
       end
diagnose (generic function with 1 method)
```

también se puede escribir como

```julia-repl
julia> diagnose(heartrate) = heartrate > 100 ? println("Elevated heart rate.") : println("Nominal heart rate or dead.")
diagnose (generic function with 1 method)
```

Esto es especialmente útil si quieres asignar un valor distinto a una variable según una condición.
`x = a ? b : c` asignará `b` a `x` si `a` es `true` y, de lo contrario, asignará `c` a `x`.
Por ejemplo:

```julia-repl
julia> function patient_status(heartrate)
           status = heartrate > 0 ? "alive" : "dead"

           println("The patient is ", status)
       end

julia> patient_status(100)
The patient is alive

julia> patient_status(0)
The patient is dead
```

## Evaluación de cortocircuito

Si se encadenan varias expresiones booleanas con los operadores `&&` y `||`, Julia solo evaluará la menor cantidad de expresiones necesaria para determinar el valor de toda la cadena.

Por ejemplo, considera la cadena de expresiones `n > 0 && k > 10`.
Si `n` es igual a `5`, la primera parte de la cadena, `n > 0`, es `true` y es necesario evaluar la segunda expresión `k > 10` para determinar si toda la expresión es `true`.
Sin embargo, si `n` es igual a `-5`, sabemos que la cadena en su conjunto **no puede** ser `true` porque su primera expresión es `false`.

Si en cambio consideramos `n > 0 || k > 10`, ocurre lo contrario.
Si `n` es igual a `5`, la primera parte de la cadena, `n > 0`, es `true` y sabemos que toda la cadena será `true` sin importar la segunda expresión.
Sin embargo, si `n` es igual a `-5`, es necesario evaluar la segunda expresión.

Este comportamiento se puede usar para escribir sentencias `if` muy cortas:

```julia
if a
    b
end
```

es equivalente a

```julia
a && b
```

La última expresión de una cadena de `&&` y `||` no necesita ser una expresión booleana.
A menudo verás que esta funcionalidad se usa para lanzar errores o hacer devoluciones anticipadas.
Por ejemplo, usando la función `dessert(fruit)` de arriba:

```julia-repl
julia> function whats_for_dessert(fruit, allergic_to)
           fruit == allergic_to && return "I can't eat that, guess I'll not have dessert tonight!"

           return dessert(fruit)
       end
whats_for_dinner (generic function with 1 method)

julia> whats_for_dinner("apple", "peach")
"Apple Crumble"

julia> whats_for_dinner("apple", "apple")
"I can't eat that, guess I'll not have dessert tonight!"
```

<!-- TODO: Mencionar ifelse(cond, x, y) en algún lugar como infobox -->
