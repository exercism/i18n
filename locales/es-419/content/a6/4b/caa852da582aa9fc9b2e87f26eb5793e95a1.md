# Mejor elección de función para `increase_quantity`

Usa la función `Map.new/2` o la función `Enum.into/3` en la función `increase_quantity`. Son las funciones que mejor se adaptan a esta tarea y no requieren el uso de la función `Enum.map/2`.
