# minimiza la cantidad de conversiones a rune

Es mejor convertir `%{name}` a `[]rune` una sola vez, guardar el resultado en una variable y reutilizarlo. Esta conversión es una operación relativamente costosa.
