# Acerca de

[concept:csharp/resource-cleanup]() usa la interfaz `IDispose` para indicar a quienes llaman a una clase que hay recursos o estado del programa que deben liberarse o restablecerse de manera oportuna cuando el objeto en cuestión ya no es necesario. En este ejercicio hemos introducido algo de azúcar sintáctico con la palabra clave `using`, que hace que el código sea menos verboso y que sea menos probable que se omitan llamadas importantes.

`using` puede verse como un reemplazo de [`try/finally`][try-finally] en algunos casos de uso.

```csharp
File file = null;
try
{
    file = new File("myStuff.txt");
    file.Write("more stuff");
}
finally
{
    file.Dispose();
}
```

Esta sintaxis se reemplaza con la versión más compacta y a prueba de errores:

```csharp
using (var file = new File("myStuff.txt")
{
    file.Write("more stuff");
}
```

Ten en cuenta que varios recursos se pueden combinar cómodamente siguiendo las reglas estándar para bloques de una sola línea:

```csharp
using (var file = new File("mystuff.txt")
using (var fileWriter = new StreamWriter(file)
{
     ...
}
```

En C# 8 se introdujo la siguiente variante, en la que la sentencia `using` aparece al inicio de un bloque:

```csharp
using var file = new File("myStuff.txt");
file.Write("more stuff");
```

Esto te permite tener varios objetos desechables en el mismo bloque y manejar de forma más natural [`try/catch`][try-catch]:

```csharp
using var fileIn = new File("myStuff.txt");
using var fileOut = new File("yourStuff.txt");
try
{
    var stuff = fileIn.Read();
    fileOut.Write(stuff);
}
catch (Exception)
{
    LogStuff();
}
```

Las reglas sobre cómo se puede usar la palabra clave `using` y con instancias de qué tipos se detallan [aquí][using-statement].

## Nota para desarrolladores de Java

Quienes programan en Java quizá reconozcan esto como un análogo del mecanismo de [_gestión automática de recursos_][automatic-resource-management] introducido en Java 7. Son muy similares. La sintaxis de Java, que reutiliza `try`, tiene la ventaja de incorporar los bloques `catch` de forma más natural que el `using` de C#.

## Versiones

Ten en cuenta que la versión más flexible de `using`, en la que no tiene su propio bloque sintáctico, se introdujo en C# 8, así que puede que necesites comprobar si el código base en el que trabajas usa C# 8 o posterior.

## Referencia

La documentación de la [sentencia `using`][using-statement] describe cómo y cuándo usar la palabra clave `using`.

[using-statement]: https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/using-statement
[automatic-resource-management]: https://docs.oracle.com/javase/tutorial/essential/exceptions/tryResourceClose.html
[try-finally]: https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/try-finally
[try-catch]: https://docs.microsoft.com/en-us/dotnet/standard/exceptions/how-to-use-the-try-catch-block-to-catch-exceptions
