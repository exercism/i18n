# Apéndice de instrucciones

## Funciones específicas del track de Emacs Lisp

* `list-empty-p` (*dado un array, devuelve si está vacío*)
* `list-sum` (*dado un array de números, devuelve la suma de todos los elementos*)
