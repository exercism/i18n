# Salida en el `else`

Si la operación `IO.write` tiene éxito, el bloque `else` hace coincidir el resultado y devuelve una salida para todo el bloque. Usa el bloque `else` para devolver la salida de la función `write/3`.
