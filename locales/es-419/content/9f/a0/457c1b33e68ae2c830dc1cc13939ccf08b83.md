# Introducción

JavaScript tiene un operador de incremento y uno de decremento.
Modifican una variable en su lugar.
`++` suma uno a un número, `--` resta uno a un número.

```javascript
let i = 3;
i++;
// i is now 4

let j = 0;
j--;
// j is now -1
```
