# Apéndice de instrucciones

## Notas específicas del track

Una forma de obtener argumentos opcionales en Scheme es especificar los argumentos como una lista.
Dos formas de hacerlo son: `(define (two-fer . args) ...)` o `(define two-fer (lambda args ...))`.
