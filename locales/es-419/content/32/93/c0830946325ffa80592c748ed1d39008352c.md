# Usa el nombre correcto del método

El nombre del método debe coincidir exactamente con el que se usó en los tests.

Renombra el método a `%{methodName}`.

**NOTA**: No modifiques los tests para que coincidan con un nombre de método distinto.
En general, intenta solo agregar tests adicionales (si quieres).
