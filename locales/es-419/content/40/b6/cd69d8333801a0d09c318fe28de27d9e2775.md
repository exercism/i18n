# Pruebas

Se incluye un `Makefile` con un objetivo predeterminado para compilar tu solución y ejecutar las pruebas. En la línea de comandos, dentro del directorio de tu ejercicio, escribe:

```bash
make
```
