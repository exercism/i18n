# condicional innecesaria

En lugar de crear un bloque `if (...) {}`, aprovecha directamente el valor Boolean que puedes devolver al comprobar el valor del parámetro.

Por ejemplo, lo siguiente:

```javascript
if (param === true) {
  return true;
}
```

se puede convertir en:

```javascript
return param === true;
```
