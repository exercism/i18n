# log_sent_email devuelve implícitamente

La función `log_sent_email/1` no debería devolver `:ok` explícitamente, sino devolver implícitamente lo que devuelve `IO.puts` (que resulta ser `:ok` cuando todo sale bien).
