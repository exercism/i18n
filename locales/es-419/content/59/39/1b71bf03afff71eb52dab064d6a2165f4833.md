# Acerca de

[Nil][nil-dictionary] es una palabra en inglés que significa «nada» o «cero».
En Ruby, `nil` es un objeto que se usa para expresar la _ausencia_ de un valor.
En otros lenguajes de programación, los valores `null` o `none` pueden desempeñar un papel similar.

```ruby
# I do not have a favorite color
favorite_color = nil
```

Ruby le asigna a cualquier variable de instancia el valor predeterminado de `nil` cuando aparece por primera vez, hasta que se le asigne otro valor.

[nil-dictionary]: https://www.merriam-webster.com/dictionary/nil
