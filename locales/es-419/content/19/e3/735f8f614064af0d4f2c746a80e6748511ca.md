# interpolación de string

Además de la interpolación de string, otra forma común de crear strings en Ruby es usar [String#%%](https://www.rubyguides.com/2012/01/ruby-string-formatting/) (que quizás se lee como «String format»).
Por ejemplo:

```ruby
"One for %%s, one for me." %% %{name_variable}
```
