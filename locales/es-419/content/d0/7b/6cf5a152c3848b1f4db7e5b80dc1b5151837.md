# Desestructura el argumento de assignTicketTo

Como se menciona en las instrucciones, por favor desestructura el argumento de `assignTicketTo` para extraer el campo `status` del tipo `Ticket` y usa la sintaxis `as` para nombrar el registro.
