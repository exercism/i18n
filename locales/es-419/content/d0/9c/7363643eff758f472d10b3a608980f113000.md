# Instrucciones

Tu conocida Erin necesita imprimir miles de volantes para varios eventos, ¡y necesita tu ayuda! Te pidió que crearas el diseño de un folleto que contenga un encabezado, una fecha opcional y una lista de artistas, cada uno asociado con un _ícono de unicode_. La meta es `print` y distribuir tu genial diseño de folleto, y para lograrlo vas a necesitar todas tus habilidades de formateo de cadenas de Python.

## 1. Crea la clase con un encabezado capitalizado

La función `capitalize_header` debe recibir un nombre de evento y devolver la versión capitalizada de ese nombre.

```python
>>> capitalize_header("fan meetup")
"Fan meetup"
```

## 2. Formatea la fecha

Crea un nuevo método `format_date` que reciba una lista con un día, un mes y un año. Muestra la fecha formateada con el formato `<month_name> <date>, <year>`

```python
>>> convert_date([9, 4, 2020])
>>> leaflet.date
"April 9, 2020"
>>> leaflet.set_date([13, 5, 2030])
>>> leaflet.date
"May 13, 2030"
```

## 3. Representa los puntos de código unicode como íconos

Cuando se llama al método `display_icons`, la lista de puntos de código unicode que se pasa se debe representar y devolver como una `list` de _íconos_.

```python
>>> leaflet.display_icons(['\U00000040', '\U0001D70B'])
['@', '𝜋']
```

## 4. Muestra el folleto terminado

Ahora vas a usar todas las funciones anteriores y las vas a combinar en la función `print_leaflet`.
Debe recibir un `event_name`, una lista de `icons`, una lista de `authors` y una lista `event_date`.

El póster debe seguir el diseño que se muestra a continuación.

El folleto sigue un diseño específico con forma de rectángulo:

- La primera y la última fila contienen 20 asteriscos. `"*"`
- Cada sección está separada por una fila vacía arriba y abajo.
- Una fila vacía contiene solo un asterisco al principio y otro al final.
- La primera sección es el encabezado del folleto, este título va capitalizado.
- La segunda sección es la fecha opcional; si no se pasa la fecha, esta sección debe ser una fila vacía.
- La tercera y última sección es la lista de los artistas, cada uno asociado con el ícono correspondiente.

```python
********************
*                  *
*    'Concert'     *
*                  *
*  June 22, 2020   *
*                  *
* John         🎸  *
* Benjamin     🎤  *
* Max          🎹  *
*                  *
********************
```
