En Chapel, si la función puede lanzar un error, esto se debe indicar en la definición de la función con la palabra clave `throws`, como muestra la siguiente plantilla.

```chapel
proc canThrow(n: int) throws {
  // function body here 
}
```
