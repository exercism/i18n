# Acerca de

Un [`Attribute` de C#][attribute-concept] ofrece una forma de decorar una declaración para asociarle metadatos: una clase, un método, una enumeración, un campo, una propiedad o cualquier otra [declaración compatible][attribute-targets].

Puedes aplicar [un atributo][attribute] a una declaración agregándolo entre corchetes `[]` antes de la declaración; el siguiente ejemplo usa tanto un `ClassAttribute` como un `FieldAttribute`:

```csharp
[Class]
public class MyClass
{
    [Field] public int myField;
}
```

Los metadatos declarativos solo asocian información estructurada adicional al código y no modifican su comportamiento, pero otras partes del código usan esos metadatos para cambiar el comportamiento de su destino, o para agregar, cambiar, quitar o restringir algunas de sus funcionalidades.

Existen varios atributos predefinidos, como `Flags`, `Obsolete` y `Conditional`. Ten en cuenta que el nombre completo del [atributo `Flags`][flags-attribute] es `FlagsAttribute`, pero el sufijo «Attribute» se puede omitir al usar el atributo.

Los siguientes cuatro atributos predefinidos se usan con frecuencia:

- `[Flags]`: Predefinido en el espacio de nombres System. Indica que la enumeración admite tanto operaciones bit a bit como el método `Enum.HasFlag()`; además, `ToString` mostraría todas las marcas: [ver ejemplo][flags-example].
- `[Obsolete]`: Predefinido en el espacio de nombres System. Permite agregar un mensaje sobre por qué el código está obsoleto; se puede usar para mostrar advertencias o errores del compilador.
- `[Conditional]`: Predefinido en el espacio de nombres System.Diagnostics. Permite quitar algunas llamadas a métodos en tiempo de compilación con fines de depuración (diagnóstico).
- `[CallerMemberName]`: Predefinido en el espacio de nombres System.Runtime.CompilerServices. Permite que un método obtenga información sobre quien lo llama.

[attribute-concept]: https://docs.microsoft.com/en-us/dotnet/csharp/programming-guide/concepts/attributes/
[attribute]: https://docs.microsoft.com/en-us/dotnet/csharp/tutorials/attributes
[attribute-targets]: https://docs.microsoft.com/en-us/dotnet/csharp/programming-guide/concepts/attributes/#attribute-targets
[flags-attribute]: https://docs.microsoft.com/en-us/dotnet/api/system.flagsattribute
[flags-example]: https://docs.microsoft.com/en-us/dotnet/api/system.flagsattribute
