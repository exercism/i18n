# Instrucciones

En este ejercicio, necesitas traducir algunas reglas del clásico juego Pac-Man a funciones de Unison.

Tienes cuatro reglas que traducir, todas relacionadas con los estados del juego.

> No te preocupes por cómo se derivan los argumentos, solo concéntrate en combinar los argumentos para devolver el resultado esperado.

## 1. Define si Pac-Man se come un fantasma

Define la función `pacmanRules.eatGhost` que toma dos argumentos (_si Pac-Man tiene una píldora de poder activa_ y _si Pac-Man está tocando un fantasma_) y devuelve un valor Boolean si Pac-Man puede comerse al fantasma. La función debe devolver true solo si Pac-Man tiene una píldora de poder activa y está tocando un fantasma.

```unison
pacmanRules.eatGhost: Boolean -> Boolean -> Boolean
pacmanRules.eatGhost powerPelletActive touchingGhost =
  todo "implement eatGhost"
```

## 2. Define si Pac-Man anota puntos

Define la función `pacmanRules.score` que toma dos argumentos (_si Pac-Man está tocando una píldora de poder_ y _si Pac-Man está tocando un punto_) y devuelve un valor Boolean si Pac-Man anotó puntos. La función debe devolver true si Pac-Man está tocando una píldora de poder o un punto.

```haskell
pacmanRules.score: Boolean -> Boolean -> Boolean
pacmanRules.score powerPelletActive touchingDot =
  todo "implement score"
```

## 3. Define si Pac-Man pierde

Define la función `pacmanRules.lose` que toma dos argumentos (_si Pac-Man tiene una píldora de poder activa_ y _si Pac-Man está tocando un fantasma_) y devuelve un valor Boolean si Pac-Man pierde. La función debe devolver true si Pac-Man está tocando un fantasma y no tiene una píldora de poder activa.

```unison
pacmanRules.lose : Boolean -> Boolean -> Boolean
pacmanRules.lose powerPelletActive touchingGhost =
  todo "implement lose"
```

## 4. Define si Pac-Man gana

Define la función `pacmanRules.win` que toma tres argumentos (_si Pac-Man se ha comido todos los puntos_, _si Pac-Man tiene una píldora de poder activa_ y _si Pac-Man está tocando un fantasma_) y devuelve un valor Boolean si Pac-Man gana. La función debe devolver true si Pac-Man se ha comido todos los puntos y no ha perdido según los argumentos definidos en la parte 3.

```unison
pacmanRules.win : Boolean -> Boolean -> Boolean -> Boolean
pacmanRules.win eatenAll powerPelletActive touchingGhost
  todo "implement Rules.win"
```
