# Introducción

## Importación

Si bien Swift ofrece una gran funcionalidad en su biblioteca estándar, puedes acceder a muchas funcionalidades adicionales importando bibliotecas y frameworks externos.
Estos módulos pueden venir directamente del proyecto Swift (como [Swift Argument Parser][argument-parser]), de frameworks de las plataformas de Apple (como [Foundation][apple-foundation] y [Network Framework][network-framework]), o de bibliotecas de código abierto de terceros (como [SwiftyBeaver][swifty-beaver]).

Los módulos principales como Foundation, una de las bibliotecas más usadas en Swift, vienen incluidos con la cadena de herramientas de Swift.
Las bibliotecas de terceros deben agregarse a la configuración de tu proyecto (por ejemplo, con Swift Package Manager) antes de poder importarlas.

Para importar módulos, se escribe la palabra clave `import` seguida del nombre del módulo.
Así, puedes importar Foundation agregando lo siguiente a tu programa.

```swift
import Foundation
```

Importar un módulo hace que sus tipos, métodos y funciones estén disponibles en tu código.
Por ejemplo, al importar Foundation se extiende `String` con métodos de utilidad útiles como `components(separatedBy:)`.

Por convención, las sentencias `import` se colocan al principio de un archivo de Swift para que las dependencias sean visibles de inmediato para cualquiera que lea el código.

## Indexación de strings

Se puede acceder a los caracteres individuales de un `String` usando el método de subíndices que se usa con los arrays.
Sin embargo, los [índices][string-indices] que se usan con los strings _no_ son enteros y no se puede trabajar con ellos directamente.
En su lugar, hay que calcularlos a partir de algún otro índice conocido, como `startIndex`, que apunta a la posición del primer carácter de un string no vacío, usando los métodos disponibles en las bibliotecas [`String`][string-docs] y [`NSString`][nsstring-docs] (cuando se importa el módulo `Foundation`, los strings de Swift tienen acceso a todas las propiedades y métodos de NSString).

Por ejemplo, dado el siguiente string:

```swift
let csv = "apple,pear,peach,orange,cherry,lime,goosberry"
```

No puedes escribir `csv[21]` para obtener la «g» de «orange»; en su lugar, debes calcular un valor de tipo `String.Index` y pasar ese índice.
Ten en cuenta que estos índices no están pensados para que los lea una persona directamente.
Son lo que se conoce como _índices opacos_, ya que las personas no necesitan saber qué hay dentro de ellos.

```swift
let index = csv.index(csv.startIndex, offsetBy: 21)
csv[index]
// "g"
print(index)
// prints Index(_rawBits: 1376513)
```

Sin embargo, ten en cuenta que si el desplazamiento no es un índice válido, es decir, si devuelve el índice anterior a `startIndex` o posterior a `endIndex`, la operación generará un error y hará que el programa se bloquee.
Para evitar este problema, puedes especificar un índice límite.
Esto devuelve un índice opcional y devolverá nil para los índices que de otro modo serían inválidos.

```swift
let tooFar = csv.index(csv.startIndex, offsetBy: 200)
// error: Execution was interrupted, reason: EXC_BAD_INSTRUCTION (code=EXC_I386_INVOP, subcode=0x0).
let tooFarButSafe = csv.index(csv.startIndex, offsetBy: 200, limitedBy: csv.endIndex)
// nil
```

Además, los índices no se pueden compartir entre strings.
Por ejemplo, usar el `index` de la «g» de «orange» calculado arriba para indexar el string `fam = "This is my family: 👨‍👩‍👦‍👦, this is our dog: 🐶, this is our cat: 🐱!"`, es decir, `fam[index]`, hará que tu programa se bloquee.

Hay muchas razones para todo esto, pero básicamente se reducen a que «Unicode es complicado».

En general, si necesitas acceso aleatorio a caracteres individuales, probablemente te convenga usar otra estructura de datos, como `Array<Character>`.

### Algunos métodos útiles

Swift tiene los métodos `first` y `last` para obtener el primer y el último carácter de un string.
Estos métodos devuelven un carácter opcional, así que si el string está vacío, devolverán nil.

```swift
let str = "Hello, world!"
print(str.first) // Prints Optional("H")
print(str.last)  // Prints Optional("!")
```

También puedes usar los métodos `prefix` y `suffix` para obtener una subcadena de un string.
Estos métodos reciben un entero como argumento y devuelven una subcadena con los primeros o los últimos n caracteres del string.

```swift
let str = "Hello, world!"
print(str.prefix(5)) // Prints "Hello"
print(str.suffix(6)) // Prints "world!"
```

## Métodos de string

Trabajar con texto es una tarea común en programación.
Swift, junto con el framework Foundation, ofrece muchos métodos y propiedades prácticos para inspeccionar, transformar y manipular strings.

## Dividir strings con `components(separatedBy:)`

Para dividir un string en un array de subcadenas según un delimitador, usa `components(separatedBy:)` (requiere importar Foundation):

```swift
import Foundation

let sentence = "Hello, world!"
let words = sentence.components(separatedBy: " ")
print(words) // Prints ["Hello,", "world!"]
```

## Comprobar prefijos y sufijos con `hasPrefix(_:)` y `hasSuffix(_:)`

Para comprobar si un string empieza o termina con una secuencia de caracteres específica, usa `hasPrefix(_:)` y `hasSuffix(_:)`:

```swift
let greeting = "Hello, world!"
print(greeting.hasPrefix("Hello"))  // Prints true
print(greeting.hasSuffix("world!")) // Prints true
```

## Cambiar mayúsculas y minúsculas

Puedes transformar las mayúsculas y minúsculas de un string con estos métodos y propiedades estándar:

- `lowercased()`: devuelve una copia del string en minúsculas.
- `uppercased()`: devuelve una copia del string en mayúsculas.
- `capitalized`: devuelve una copia en la que la primera letra de cada palabra está en mayúscula (de Foundation).

```swift
let greeting = "hello, world!"
print(greeting.lowercased())  // Prints "hello, world!"
print(greeting.uppercased())  // Prints "HELLO, WORLD!"
print(greeting.capitalized)   // Prints "Hello, World!"
```

## Reemplazar subcadenas con `replacingOccurrences(of:with:)`

Para reemplazar todas las apariciones de una subcadena objetivo por otro string, usa `replacingOccurrences(of:with:)` (de Foundation):

```swift
let greeting = "Hello, world!"
let updated = greeting.replacingOccurrences(of: "world", with: "Swift")
print(updated) // Prints "Hello, Swift!"
```

## Unir strings con `joined(separator:)`

El método `joined(separator:)` pertenece a las colecciones de strings, como `[String]`.
Une los elementos en un solo string usando un separador:

```swift
let words = ["Hello", "world"]
let sentence = words.joined(separator: ", ")
print(sentence) // Prints "Hello, world"
```

[components]: https://developer.apple.com/documentation/foundation/nsstring/components(separatedby:)-238fy
[hasPrefix]: https://developer.apple.com/documentation/foundation/nsstring/hasprefix(_:)
[hasSuffix]: https://developer.apple.com/documentation/foundation/nsstring/hassuffix(_:)
[lowercased]: https://developer.apple.com/documentation/foundation/nsstring/lowercased
[uppercased]: https://developer.apple.com/documentation/foundation/nsstring/uppercased
[capitalized]: https://developer.apple.com/documentation/foundation/nsstring/capitalized
[replacingOccurrences]: https://developer.apple.com/documentation/foundation/nsstring/replacingoccurrences(of:with:)
[joined]: https://developer.apple.com/documentation/swift/array/joined(separator:)-7uber
[string-docs]: https://developer.apple.com/documentation/swift/String
[nsstring-docs]: https://developer.apple.com/documentation/foundation/nsstring
[string-format-specifiers]: https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/Strings/Articles/formatSpecifiers.html
[string-indices]: https://docs.swift.org/swift-book/documentation/the-swift-programming-language/stringsandcharacters/#String-Indices
[unicode]: https://en.wikipedia.org/wiki/Unicode
[argument-parser]: https://apple.github.io/swift-argument-parser/documentation/argumentparser/
[network-framework]: https://developer.apple.com/documentation/network
[swifty-beaver]: https://github.com/SwiftyBeaver/SwiftyBeaver
[apple-foundation]: https://developer.apple.com/documentation/foundation
