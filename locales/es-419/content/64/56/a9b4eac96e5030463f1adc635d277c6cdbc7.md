# Pistas

## General

- Una dirección efectiva se puede calcular con la instrucción [lea][lea].
- Debes usar `rel` para formar direcciones relativas válidas en memoria.
- El prefijo para acceder a valores de 8 bytes en memoria es `qword`.

## 1. Revisa cuáles fueron los conteos la semana pasada

- Una dirección efectiva se puede calcular con la instrucción [lea][lea].
- Debes usar `rel` para formar direcciones relativas válidas en memoria.
- El prefijo para acceder a valores de 8 bytes en memoria es `qword`.
- Para acceder al contenido almacenado en una dirección de memoria, necesitas desreferenciarla usando `[]`.

## 2. Revisa cuáles son los conteos actuales de esta semana

- Una dirección efectiva se puede calcular con la instrucción [lea][lea].
- Debes usar `rel` para formar direcciones relativas válidas en memoria.
- El prefijo para acceder a valores de 8 bytes en memoria es `qword`.
- Las variables sin inicializar se definen en la _sección .bss_.
- Debes usar tanto `rax` como `rdx` para devolver dos enteros desde una función.

## 3. Guarda el conteo de cada día

- Una dirección efectiva se puede calcular con la instrucción [lea][lea].
- Debes usar `rel` para formar direcciones relativas válidas en memoria.
- El prefijo para acceder a valores de 8 bytes en memoria es `qword`.
- El prefijo para acceder a valores de 1 byte en memoria es `byte`.
- Puedes insertar o recuperar bytes en bloque usando el prefijo para un tamaño de datos mayor al acceder a la memoria.
- Puedes verificar condiciones usando [cmp][cmp].
- Puedes hacer un salto condicional usando [jcc][jcc].
- Puedes usar [inc][inc] para sumar 1 al valor de un operando destino.

## 4. Revisa cuántos pájaros visitaron hoy

- Una dirección efectiva se puede calcular con la instrucción [lea][lea].
- Debes usar `rel` para formar direcciones relativas válidas en memoria.
- El prefijo para acceder a valores de 8 bytes en memoria es `qword`.
- Los valores enteros de 1 byte se devuelven en `al`.
- Puedes usar [dec][dec] para restar 1 al valor de un operando destino.

## 5. Actualiza el conteo de hoy

- Una dirección efectiva se puede calcular con la instrucción [lea][lea].
- Debes usar `rel` para formar direcciones relativas válidas en memoria.
- El prefijo para acceder a valores de 8 bytes en memoria es `qword`.
- Puedes usar [add][add] con un operando de memoria destino.

## 6. Inserta entradas nuevas en bloque

- Una dirección efectiva se puede calcular con la instrucción [lea][lea].
- Debes usar `rel` para formar direcciones relativas válidas en memoria.
- El prefijo para acceder a valores de 8 bytes en memoria es `qword`.
- Puedes insertar o recuperar bytes en bloque usando el prefijo para un tamaño de datos mayor al acceder a la memoria.

[lea]: https://www.felixcloutier.com/x86/lea
[cmp]: https://www.felixcloutier.com/x86/cmp
[jcc]: https://www.felixcloutier.com/x86/jcc
[inc]: https://www.felixcloutier.com/x86/inc
[dec]: https://www.felixcloutier.com/x86/dec
[add]: https://www.felixcloutier.com/x86/add
