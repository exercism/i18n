# usa la interpolación de string

Usa la [interpolación de string](https://docs.scala-lang.org/overviews/core/string-interpolation.html) para construir un string dinámicamente, en lugar de usar la concatenación de string o `StringBuilder`.

La ventaja principal es que el código con interpolación de string suele ser más fácil de leer que la concatenación de string o `StringBuilder`.
