# Usa `MapSet.union` y `MapSet.size`

Usa `MapSet.union` y `MapSet.size` en `total_cards`. Existen otras maneras idiomáticas de definir la función, pero estas son las que mejor se ajustan para aprender por completo el concepto de este ejercicio.