# Anexo de instrucciones

## Palabras

`classify ( n -- str )` devuelve uno de los strings `"perfect"`,
`"abundant"` o `"deficient"`.

## Errores

Cuando el argumento sea cero o negativo, lanza el string

```
"Classification is only possible for positive integers."
```

(textualmente, incluido el punto final).
