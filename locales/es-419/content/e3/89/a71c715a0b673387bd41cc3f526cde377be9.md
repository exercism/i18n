# Introducción

Un `String` en Ruby es un objeto que almacena y manipula una secuencia arbitraria de bytes, que por lo general representa caracteres.
Los strings se manipulan llamando a los métodos del string.
