# Elimina el throw de la excepción no implementada

Intenta eliminar el código `throw new NotImplementedException`, que o bien hace que uno o más tests fallen, o está en una parte del código que nunca se ejecuta.
