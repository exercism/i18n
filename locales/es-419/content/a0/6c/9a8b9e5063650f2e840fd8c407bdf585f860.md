# Acerca de

**Closures** son un patrón de programación [en Clojure][clojure-guide-closures] que permite usar variables de un [ámbito léxico][wiki-lexical-scope] externo dentro de una función. Clojure admite closures de forma transparente, y muchas veces se usan sin saber qué son.

```clojure
;; Top-level definitions are global-scope
(def dozen 12)

;; Functions create a new scope.
;; Referencing the outer variable here is a closure.
(fn [n] (* dozen n))
```

## Closures para guardar estado y pasar valores

Usar un átomo permite conservar algo de estado:

```clojure
;; This function closure increments the counter's state
;; in the outer lexical context.
;; This way the counter can be shared between many calling contexts.

(def increment
  (let [counter (atom 0)]
    (fn [] (swap! counter inc))))
```

Cada llamada sucesiva a `increment` incrementa su contador:

``` clojure
(increment)
;;=> 1
(increment)
;;=> 2
```

[wiki-lexical-scope]: https://en.wikipedia.org/wiki/Scope_(computer_science)#Lexical_scoping
[clojure-guide-closures]: https://clojure.org/guides/higher_order_functions#_functions_returning_functions_and_closures