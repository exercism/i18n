# Usa `MapSet.difference` y `MapSet.size`

Usa `MapSet.difference` y `MapSet.size` en `extra_cards`. Hay otras formas idiomáticas de definir la función, pero estas son las que mejor se ajustan para aprender por completo el concepto de este ejercicio.