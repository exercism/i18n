# variables

[comment]: # (Este error se muestra cuando una variable no se usa)
[comment]: # (Algunas variables sin usar, como `_`, no se pueden usar en ningún lugar)
[comment]: # (requiere 1 parámetro:)
[comment]: # (- definición: fragmento de código con el valor sin usar)

El valor que se indica a continuación no se usa en ningún lugar o no se puede usar.
Puedes eliminarlo o usarlo en alguna parte.

```
%{definition}
```
