# unless con un bloque else

Evita usar expresiones `unless` con un bloque `else`. Usa una expresión `if` en su lugar.

La razón no es técnica, sino humana. En este caso, el `else` se ejecutará cuando se cumpla la condición, que es justo lo contrario de lo que sugiere la redacción.
