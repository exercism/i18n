# Introducción

Muchos programas necesitan valores (pseudo)aleatorios para simular eventos del mundo real.

Algunos ejemplos comunes y conocidos son:

- El lanzamiento de una moneda: un valor aleatorio de entre ('H', 'T').
- La tirada de un dado: un número entero aleatorio del 1 al 6.
- Barajar una baraja de cartas: un orden aleatorio de una lista de cartas.
- La creación de árboles y arbustos en una simulación gráfica en 3D.

Generar valores verdaderamente aleatorios con una computadora es un [desafío técnico sorprendentemente difícil][why-randomness-is-hard], y por eso también existen los generadores «pseudoaleatorios».

<!-- prettier-ignore -->
~~~exercism/caution
¡La función `Math.random()` NO se debe usar para aplicaciones de seguridad y criptografía!
Termina el(los) ejercicio(s) de aprendizaje sobre este concepto para aprender más
~~~

## Generar números aleatorios

En JavaScript, puedes generar números pseudoaleatorios con la función [`Math.random()`][Math.random].
Devuelve un número de punto flotante pseudoaleatorio entre 0 (incluido) y 1 (excluido).

Para obtener un número aleatorio entre _min_ (incluido) y _max_ (excluido), puedes usar una función parecida a esta:

```javascript
function getRandomInRange(min, max) {
  return min + Math.random() * (max - min);
}
getRandomInRange(4, 10);
// => 5.72
```

## Generar números enteros aleatorios

Para generar un número entero aleatorio, puedes usar `Math.floor()` o `Math.ceil()` para convertir un número generado aleatoriamente en un entero.

[why-randomness-is-hard]: https://www.malwarebytes.com/blog/news/2013/09/in-computers-are-random-numbers-really-random
[Math.random]: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math/random
