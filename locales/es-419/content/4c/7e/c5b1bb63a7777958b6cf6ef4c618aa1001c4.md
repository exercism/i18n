Usa operadores lógicos en lugar de operadores bit a bit.

Aunque no hay nada de malo en usar operadores bit a bit (como `|` y `&`), este ejercicio quiere que explores los operadores lógicos booleanos (como `||` y `&&`). Hay otros ejercicios dedicados a los operadores bit a bit.
