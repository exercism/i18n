# Introducción

Los `char` a veces se usan junto con un objeto `StringBuilder`. Este objeto tiene métodos que permiten construir una cadena carácter por carácter y manipularla. Al final del proceso, puedes llamar a `ToString` sobre él para producir una cadena completa.
