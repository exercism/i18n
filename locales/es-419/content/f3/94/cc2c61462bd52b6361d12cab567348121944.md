# firma no óptima

Si te fijas en las pruebas, la función `gigasecond(...)` solo recibe un parámetro. Elimina los parámetros adicionales de la definición de `gigasecond`, ya que el valor de esos parámetros extra siempre será `undefined`, o será un valor predeterminado tal como se define actualmente en la firma.
