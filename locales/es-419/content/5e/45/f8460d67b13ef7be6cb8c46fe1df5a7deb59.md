# Pruebas

Para compilar y ejecutar las pruebas, solo ejecuta lo siguiente en el directorio de tu ejercicio:

```shell
nim r test_exercise_name.nim
```

Reemplaza `exercise_name` con el nombre del ejercicio (por ejemplo, `all_your_base`).
