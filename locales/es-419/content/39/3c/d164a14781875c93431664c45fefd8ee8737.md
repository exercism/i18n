# Tuplas

Las tuplas en Elm pueden contener dos o tres valores, y cada valor puede tener cualquier tipo.
En Elm, las tuplas se usan sobre todo para tipos transitorios simples.
Por esta razón, las tuplas tienen un máximo de 3 valores y, como incentivo adicional, solo se proporcionan funciones de conveniencia para 2 valores.
Para datos más complejos, es mejor pasar a los registros.
Un uso común es cuando necesitas devolver más de un valor desde una función.
Los campos de una tupla no tienen nombres; se accede a ellos mediante desestructuración o por posición.

Puedes leer una [introducción concisa a las tuplas][tuples-concise].
También hay una [introducción más a fondo][tuples-in-depth], y la [documentación de la API del módulo Tuple][tuple-core] contiene ejemplos de código y documentación detallada.

## Declaración

Los tipos de las tuplas se crean con paréntesis y sus tipos de elementos se separan con comas.

```elm
type alias IntStringTuple = (Int, String)
```

### Creación

Las tuplas se crean con paréntesis y sus elementos se separan con comas.

```elm
aTuple : IntStringTuple
aTuple = (123, "elements can be of differing types")
```

También puedes usar `Tuple.pair` para crear una tupla con dos valores.

```elm
anotherTuple : (String, String)
anotherTuple = Tuple.pair "this time it's" "two strings"
    --> ("this time it's", "two strings")
```

### Acceso

También hay funciones para acceder a los valores de una tupla por posición: `Tuple.first` y `Tuple.second`.

```elm
Tuple.first aTuple
    --> 123

Tuple.second aTuple
    --> "elements can be of differing types"
```

Las tuplas se pueden _desestructurar_ en vinculaciones mediante la coincidencia de patrones de tuplas.
La desestructuración se puede hacer en expresiones `let`, en declaraciones de funciones y en sentencias `case`.

```elm
repeatString : (Int, String) -> Bool
repeatString aTuple =
let
    (aNumber, aString) = aTuple
in
    String.repeat aNumber aString

repeatStringAgain : (Int, String) -> Bool
repeatStringAgain (aNumber, aString) = String.repeat aNumber aString
```

La coincidencia de patrones en una sentencia `case` también te permite hacer coincidir el contenido de los valores:

```elm
has123 : (Int, String, Bool) -> Bool
has123 aTuple =
    case aTuple of
        (123, _, _) ->
            True
        (_, "123", _) ->
            True
        (_, _, True) ->
            True
        _ ->
            False
```

[tuples-concise]: https://www.bekk.christmas/post/2020/1/once-twice-three-times-a-value
[tuples-in-depth]: https://elmprogramming.com/tuple.html
[tuple-core]: https://package.elm-lang.org/packages/elm/core/latest/Tuple
