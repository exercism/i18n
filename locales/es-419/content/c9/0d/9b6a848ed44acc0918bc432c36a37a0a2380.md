# usa includes implícitos

Quita la comprobación explícita de existencia `.includes`. Cuando el color (el string que se pasa) se convierte en un código (el número `colorCode`), ya existe un valor especial que se devuelve si el color no está presente. Busca ese valor y úsalo en su lugar.