# Pistas

## 1. Define la función `with_retry`

- Puedes usar una expresión `case` para hacer coincidencia de patrones sobre un resultado.

## 2. Define la función `record_timing`

- Se debe llamar a la función `time_logger` incluso si la función `experiment` devuelve un valor `Error`.

## 3. Define la función `run_experiment`

- Puedes usar la [función `result.try`][result-try] en una expresión `use` para detenerte si un resultado es un valor `Error`.

[result-try]: https://hexdocs.pm/gleam_stdlib/gleam/result.html#try
