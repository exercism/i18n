# Introducción

En Swift, no puedes indexar un string usando un entero (como `string[3]`).
Como los strings de Swift están compuestos por caracteres Unicode de longitud variable, encontrar un carácter requiere recorrer los caracteres anteriores.

Para acceder a un carácter en una posición específica, debes calcular un índice de tipo `String.Index`.
Estos índices se llaman *índices opacos* porque su estructura interna la administra Swift en lugar de manipularse directamente como enteros sin procesar:

```swift
let csv = "apple,pear,peach,orange,cherry,lime,gooseberry"
let index = csv.index(csv.startIndex, offsetBy: 21)
print(csv[index])
// Prints "g"

print(index)
// Prints internal representation: Index(_rawBits: ...)
```
