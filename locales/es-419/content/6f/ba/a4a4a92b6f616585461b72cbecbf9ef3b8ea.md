# Introducción

En Clojure, los valores Boolean se representan con `true` o `false`.

Las funciones predicado (las funciones que devuelven un Boolean) normalmente terminan con un signo de interrogación (`?`), pero esto es solo por convención.

La biblioteca principal incluye funciones para operadores lógicos como `not`, `and` y `or`.
