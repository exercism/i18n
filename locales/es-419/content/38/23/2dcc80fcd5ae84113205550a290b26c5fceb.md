# Instrucciones específicas para SQLite

Este ejercicio requiere que establezcas la columna `result` de la tabla `roman-numerals` con el valor correcto según el número que se encuentra en la columna `number`.

## Esquema de la tabla

```sql
CREATE TABLE "roman-numerals" ("number" INT, "result" TEXT);
```
