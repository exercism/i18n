# Apéndice de instrucciones

## Instrucciones específicas del track

Existe una función integrada que casi hace esto por ti, y no hay nada de qué avergonzarse por usarla.
(Y, por supuesto, tampoco hay nada de qué avergonzarse por resolverlo con código largo y detallado.)
Consejo: la función integrada devuelve `{1}` en lugar del `{}` que exige este ejercicio, y tendrías que sobrescribir los dos parámetros predeterminados para que devuelva lo correcto.
