# Acerca de

PowerShell es un lenguaje de scripting dinámico y de tipado implícito (_aunque también admite [variables de tipado fuerte][strong typed]_), de Microsoft, enfocado principalmente en la automatización de tareas y la administración de la configuración.

PowerShell admite la programación imperativa, funcional, orientada a objetos e incluso la basada en canalizaciones.

PowerShell se construyó sobre la plataforma .NET y se lanzó por primera vez en 2006.
Se incluye de forma predeterminada en todos los sistemas operativos Windows modernos y también se puede instalar en Mac y Linux.


## Variables

Hay dos formas comunes de definir una variable en PowerShell.

Los nombres de las variables en PowerShell deben empezar con `$` y usar el operador de asignación `=` para vincular el nombre de la variable a un valor.

```powershell
> $intVar = 1
> $doubleVar = 2.0
> $stringVar = "Hello"

# Checking a variable type using the GetType() method
> Write-Host $doubleVar.GetType()
System.Double
```

También puedes especificar el tipo de una variable de forma explícita, si quieres forzar o exigir que el valor sea de cierto tipo.
PowerShell hará todo lo posible por convertir el valor de un tipo a otro cuando sea posible.

```powershell
# String represents a floating number can be converted to integer
> [int]$staticOne = "1.234"
> $staticOne
1

# String of letters can not be converted to double type
> [double]$staticTwo = "hello"
MetadataError: Cannot convert value "hello" to type "System.Double"
```

### Constantes

Las constantes en PowerShell son variables que no se pueden **cambiar** ni **eliminar** después de definirlas.
Esto nos lleva a la segunda forma de definir variables: usar el cmdlet `New-Variable`.

```powershell
> New-Variable -Name "MyConstant" -Value 100 -Option Constant
> $MyConstant
100
```


## Funciones

Las funciones en PowerShell se definen con la palabra clave `function`.
Cada función puede tener cero o más parámetros formales entre `()` paréntesis, seguidos del cuerpo de la función, que empieza con `{` y termina con `}`.

```powershell
function Greeting {
    return "Hello!"
}
```

Las funciones en PowerShell pueden devolver valores de forma explícita mediante la palabra clave `return`, después de llamarlas con la sintaxis `FuncName $Arg`.

```powershell
function AddTwoNumbers($NumOne, $NumTwo):
    $result = $NumOne + $NumTwo
    return $result

> AddTwoNumbers 10 20
30
```

## Comentarios

Los comentarios de una sola línea en PowerShell empiezan con `#` y terminan al final de la línea.
Los bloques de comentarios en PowerShell empiezan con `<#` y terminan con `#>`.

```powershell
function Main() {
    #This is a comment
    <#
    And this is a comment block
    that can span multiple lines
    #>
}
```

[strong typed]: https://pscustomobject.github.io/powershell/coding%20habits/powershell%20best%20practice/PowerShell-Strongly-Typed-Variables/
[assignment operator]: https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_assignment_operators
[New-Variable]: https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.utility/new-variable