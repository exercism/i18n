# Usa la búsqueda del índice

Reemplaza `%{current}` con otra función integrada, una función que hace exactamente lo que ahora está programado de forma explícita: encontrar el índice de un valor dado en un `Array`.