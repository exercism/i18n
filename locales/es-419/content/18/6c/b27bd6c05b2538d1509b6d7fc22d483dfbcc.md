# Reutiliza el código de describe con tres parámetros

El método `describe(Character, Destination, TravelMethod)` debe reutilizar la lógica que ya está implementada en `describe(Character)`, `describe(Destination)` y `describe(TravelMethod)`. Reutilizar los métodos existentes puede ayudar a que el código sea más fácil de mantener.
