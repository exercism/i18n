# Acerca de

Las **expresiones** `throw` son una alternativa a las _sentencias_ `throw` y, en particular, pueden aumentar la potencia del operador ternario y de otras expresiones compuestas.

```haxe
trace(true ? throw "This is an error" : "Everything is fine"); // ERROR: This is an error
```
