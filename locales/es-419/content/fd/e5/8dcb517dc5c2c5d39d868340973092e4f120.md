# Introducción

En Go, una constante es un valor simple e inmutable que se asigna a un nombre con la palabra clave `const`:

```go
const myWebsite = "Exercism"
```

Estas constantes no tienen tipo, pero se les asigna un tipo predeterminado según su sintaxis cuando se requiere un tipo, por ejemplo, cuando se pasan a un método.
Las constantes con tipo se pueden crear agregando un tipo de forma explícita:

```go
const myWebsite string = "Exercism"
```

Go no tiene enumeraciones como otros lenguajes, pero sí tiene un identificador predeclarado llamado `iota` para crear constantes enumeradas.
Las constantes dentro de un bloque se repiten de forma implícita:

```go
const (
    a = 9
    b
    c
    d = iota
    e
    f
    g
)
fmt.Print(a, b, c, d, e, f, g)
// Output: 9 9 9 3 4 5 6
```
