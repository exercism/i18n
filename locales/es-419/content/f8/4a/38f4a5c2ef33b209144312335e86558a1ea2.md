# Introducción

## Boolean

Los valores Boolean en C# se representan con el tipo `bool`, que puede tomar los valores `true` o `false`.

C# admite tres operadores Boolean: `!` (NOT), `&&` (AND) y `||` (OR).
