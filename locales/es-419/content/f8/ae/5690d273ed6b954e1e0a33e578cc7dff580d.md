# Introducción

La estructura de control `foreach` proporciona una forma cómoda de iterar sobre _arrays_ y _objetos_. `foreach` tiene dos formas básicas:

```php
foreach (iterable as $value) {
  // statements
}

foreach (iterable as $key => value) {
  // statements
}
```
