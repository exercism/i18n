# Introducción

Las funciones pueden declarar valores por defecto para uno o más argumentos. Veamos esta función:

```elixir
def number(n \\ 13), do: "That's not my favorite"
```

Al compilar, Elixir crea una definición de función para `number/0` (sin argumentos) y `number/1` (un argumento).

Si más de un argumento tiene valores por defecto, estos se aplicarán a la función de izquierda a derecha para completar los argumentos que falten.

Si la función tiene más de una cláusula, los argumentos por defecto deben definirse en un encabezado de función (una función sin cuerpo) antes de las cláusulas de la función:

```elixir
def number(n \\ 13)
def number(n) when n < 10, do: "Dream bigger!"
def number(n) when n > 100, do: "Not that big..."
```

[kernel-guards]: https://hexdocs.pm/elixir/Kernel.html#guards
