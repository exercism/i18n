# Desestructura el argumento de emptyComment

Como se menciona en las instrucciones, desestructura el argumento de `emptyComment` para extraer el contenido de la tupla.
Ignora el elemento `User` con el comodín `_` y conserva el `String`.
