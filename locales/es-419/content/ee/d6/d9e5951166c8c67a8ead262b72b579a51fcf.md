# Usa AddSeconds

Prueba a usar el método `AddSeconds` que `DateTime` trae integrado para simplificar el código. Ten en cuenta que `AddSeconds` devuelve una nueva instancia de `DateTime` y no modifica la instancia sobre la que se llama.
