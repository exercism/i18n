# Pistas

## 1. Da la razón entre éxitos e intentos

- Crea números `Rational` usando los éxitos como numerador y los intentos como denominador.
- Puedes usar bucles, comprensiones u operaciones vectoriales para crear el nuevo array.

## 2. Encuentra las probabilidades reales asociadas a la razón entre éxitos e intentos

- Crea números `Float` usando los éxitos como numerador y los intentos como denominador.
- Puedes usar bucles, comprensiones u operaciones vectoriales para crear el nuevo array.

## 3. Verifica la media de las probabilidades

- La media se obtiene sumando los datos y dividiendo entre el número de eventos.
- Usa tus funciones `rationalize` y `probabilities`.
- Si encuentras una discrepancia entre usar números de punto flotante y racionales, devuelve el número racional.
- Puede que necesites la función `float()` en la comparación (consulta la introducción).
- Puedes usar bucles u operaciones vectoriales.

## 4. Verifica la probabilidad independiente

- Las probabilidades de eventos independientes se pueden multiplicar entre sí para hallar la probabilidad total de que esos eventos ocurran juntos.
- Usa tus funciones `rationalize` y `probabilities`.
- Si encuentras una discrepancia entre usar números de punto flotante y racionales, devuelve el número racional.
- Puede que necesites la función `float()` en la comparación (consulta la introducción).
- Puedes usar bucles u operaciones vectoriales.
