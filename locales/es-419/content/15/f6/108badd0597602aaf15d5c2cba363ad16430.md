# no se encontró la función de la solución

El conjunto de pruebas de este ejercicio espera que se defina la siguiente función en el archivo `src/lib.rs`:
```rust
pub fn reverse(input: &str) -> String {
}
```
No se encontró la función requerida.
