# usa una propiedad de solo lectura

La propiedad `%{name}` se puede definir como una propiedad _de solo lectura_, en la que se omite el setter. Aun así podrás asignarle un valor a la propiedad, pero solo desde el constructor.
