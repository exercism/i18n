# Depuración

```pyret
fun print-forty-two():
  print(42) # 42
end
```

```pyret
fun return-forty-two() block:
  print(42) # 42
  42
end
```

[print][print] es una expresión que devuelve un valor.
Pyret solo permite una única expresión dentro de un bloque de código dado, a menos que el bloque exterior esté marcado con `block`.
En el segundo ejemplo, agregar `block` a la declaración de la función nos permite imprimir 42 antes de devolverlo.

## Espías

```pyret
fun do-something():
    x = "foo"
    y = "bar"
    spy "strings": x, y end
    a = 1
    b = 2
    spy "numbers": a, b end
end
do-something();

# Spying "strings" (at <clipped>/example.arr:4:4-4:27)
#  x: "foo"
#  y: "bar"
# Spying "numbers" (at <clipped>/example.arr:7:4-7:27)
#  a: 1
#  b: 2
```

Una sentencia `spy` recibe una etiqueta opcional y una o más variables, y reporta cada variable junto con su valor.
Como no es una expresión, se puede usar una sentencia `spy` dentro de un bloque sin que el bloque exterior tenga que estar marcado con `block`.

[print]: https://pyret.org/docs/latest/_global_.html#%28idx._%28gentag._57%29%29
[spy]: https://pyret.org/docs/latest/s_spies.html
