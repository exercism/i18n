# Acerca de

## Tipos de función

Ya hemos visto cómo se definen las firmas de función: un nombre de función seguido de una lista de etiquetas de parámetros separadas por comas junto con sus tipos, luego `->` y el tipo de los valores que devuelve la función.

Si se quitaran todos los nombres y etiquetas de esta firma, quedaría el [tipo de la función][function-types].

```swift
func shoppingList(item: String, quantity: Int) -> String {
  "You need to buy \(quantity) \(item)."
}
// shoppingList: (String, Int) -> String

func repeater(_ phrase: String, times: Int) -> String {
  var result = ""
  for _ in 1...times {
    result += phrase
  }
  return result
}
// repeater: (String, Int) -> String

func printString(line: String) {
  print(line)
}
// printString: (String) -> ()
```

Fíjate que los tipos de las dos primeras funciones son iguales, `(String, Int) -> String`, aunque las dos funciones trabajan de manera muy distinta. Ambas reciben un `String` y un `Int` como argumentos y devuelven un `String`. Y, como se puede ver en el tipo de la tercera función, aunque la función no parece devolver nada, en realidad devuelve `()`, el único valor de tipo `Void`. Ten en cuenta que `()` y `Void` son intercambiables como nombres de tipo, así que el tipo de `printString` también se podría escribir como `String) -> Void`.

Estos tipos se pueden usar de la misma manera que cualquier otro tipo en Swift; por ejemplo, se pueden usar en la declaración de variables o constantes. Y como las funciones son valores en Swift, se pueden asignar funciones ya existentes a estas constantes o variables:

```swift
var stringAndIntToString: (String, Int) -> String

stringAndIntToString = shoppingList
stringAndIntToString("carrots", 3)
// => "You need to buy 3 carrots."

stringAndIntToString = repeater
stringAndIntToString("carrots", 3)
// => "carrotscarrotscarrots"
```

Al igual que con otras variables o constantes, el valor que le asignes debe ser del tipo correcto:

```swift
stringAndIntToString = printShoppingList
// Error: Cannot assign value of type '(String) -> ()' to type '(String, Int) -> String'
```

## Tipos de función como tipos de parámetro

Como los tipos de función se pueden usar en cualquier lugar donde se puedan usar otros tipos, se deduce que [se pueden usar como tipos de parámetro][functions-as-parameters] y pasarse a otras funciones, y llamarse desde dentro de esas funciones.

```swift
func apply3(to str: String, function f: (String, Int) -> String) -> String {
  f(str,3)
}
// apply3: ([String], (String, Int) -> String) -> [String]

apply3(to: "eggs", function: shoppingList)
// => "You need to buy 3 eggs."

apply3(to: "eggs", function: repeater)
// => "eggseggseggs"

func apply(functions fs: [(String, Int) -> String],
           to string: String,
          ) -> [String] {
  var result = [String]()
  for f in fs {
    result.append(f(string, 3))
  }
  return result

}
// apply: ([(String, Int) -> String], String) -> [String]


apply(functions: [shoppingList, repeater], to: "carrots")
// => ["You need to buy 3 carrots.", "carrotscarrotscarrots"]
```

## Tipos de función como tipos de retorno

De manera similar, [los tipos de función se pueden usar como tipos de retorno][functions-as-returns] de las funciones. En otras palabras, se pueden escribir funciones que crean y devuelven otras funciones. Al crear estas funciones, la función que se devuelve puede usar los otros parámetros que se pasan a la función o las variables locales creadas dentro de la función contenedora.

```swift
func makeAdder(base: Int) -> (Int) -> Int {
  func adder(_ i: Int) -> Int {
    base + i
  }
  return adder
}
// makeAdder: (Int) -> (Int) -> Int

let add10 = makeAdder(base: 10)
// add10: (Int) -> Int

let subtract20 = makeAdder(base: -20)
// subtract10: (Int) -> Int

add10(5)
// => 15

subtract20(5)
// => -15
```

## Capturar valores

Como se vio con la función `makeAdder(base:)` de arriba, las funciones anidadas pueden acceder a los parámetros y variables de las funciones que las rodean. Además, pueden mantener el acceso a estos valores después de que la función contenedora termina. Esta acción de obtener y mantener el acceso se conoce como [_captura_][capturing-values].

## Funciones de orden superior

Las funciones que reciben funciones como parámetros o que devuelven funciones se conocen como funciones de orden superior. Swift tiene muchas funciones de orden superior en sus distintas bibliotecas. Por ejemplo, el método `sorted(by:)` disponible en las colecciones de Swift como `Array`, que para un array con elementos de tipo `T` recibe una función de tipo `(T, T) -> Bool` que determina cuál de dos valores debe ir antes que el otro en un array ordenado, y devuelve `true` si el valor de la izquierda debe aparecer primero y `false` si el valor de la derecha debe aparecer primero.

Otro ejemplo es el método `contains(where:)` de las colecciones, que para una colección con elementos de tipo `T` recibe una función de tipo `(T) -> Bool` que devuelve `true` si el elemento cumple alguna propiedad y `false` en caso contrario. Luego, `contains(where:)` devuelve `true` si algún elemento de la colección cumple esa propiedad.

```swift
func longer(_ strA: String, _ strB: String) -> Bool {
  strA.count > strB.count
}
// longer: (String, String) -> Bool

["apple", "ball", "carrot"].sorted(by: longer)
// => ["carrot", "apple", "ball"]

["apple", "ball", "carrot"].sorted(by: >)
// => ["carrot", "ball", "apple"]


func isBig(_ i: Int) -> Bool {
  i > 100
}
// isBig: (Int) -> Bool

func isVowel(_ char: Character) -> Bool {
  switch char.lowercased() {
  case "a", "e", "i", "o", "u": return true
  default: return false
  }
}
// isVowel: (Character) -> Bool

[1, 75, 3, 99].contains(where: isBig)
// => false

"Owl".contains(where: isVowel)
// => true

```

[function-types]: https://docs.swift.org/swift-book/LanguageGuide/Functions.html#ID174
[functions-as-parameters]: https://docs.swift.org/swift-book/LanguageGuide/Functions.html#ID176
[functions-as-returns]: https://docs.swift.org/swift-book/LanguageGuide/Functions.html#ID177
[capturing-values]: https://docs.swift.org/swift-book/LanguageGuide/Closures.html#ID103
