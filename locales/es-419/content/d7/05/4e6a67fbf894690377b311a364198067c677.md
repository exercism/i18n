# Introducción

Un **objeto** JSON es, usando terminología de otros lenguajes, un «hash», un «map» o un «dictionary».

JSON define un _objeto_ así:

> Un objeto es un conjunto no ordenado de pares **nombre**/**valor**.
> Un objeto comienza con `{` llave de apertura y termina con `}` llave de cierre.
> Cada _nombre_ va seguido de `:` dos puntos y los pares _nombre/valor_ se separan con `,` coma.

El _nombre_ **debe ser un string**.
Otra palabra para _nombre_ es _clave_.

El _valor_ puede ser de cualquier tipo de JSON.
Distintos _valores_ dentro del mismo objeto pueden ser de tipos distintos, como en este ejemplo.

<!-- prettier-ignore -->
```json
{
  "name": "Jane",
  "age": 42,
  "pets": ["cat", "fish"],
  "address": {"street": "123 Main St", "city": "Springfield"}
}
```

<!-- prettier-ignore-end -->

Ten en cuenta que **no debe** haber una coma después del _último_ par clave-valor.

## Crear objetos

Usa llaves para reunir los pares nombre/valor.
Aunque los nombres deben ser strings, no es necesario entrecomillarlos si los nombres son **similares a un identificador** (compuestos por caracteres alfanuméricos y guion bajo, y que no empiecen con un dígito).

```jq
{name: "Jane", age: 42}
```

Es válido usar claves que no sean _similares a un identificador_.
Solo entrecomíllalas.

```jq
{"first name": "Jane", "last name": "Lopez", age: 42}
```

Si el nombre es el resultado de una expresión, la expresión **debe** ir entre paréntesis.

```sh
$ echo "Jane" | jq -Rc '{.: 42}'
# verbose error message ...

$ echo "Jane" | jq -Rc '{(.): 42}'
{"Jane":42}
```

## Indexación

Los valores se obtienen de un objeto con **notación de punto**.

```jq
{name: "Jane", age: 42} | .age    # => 42
```

Si no puedes referirte a la clave como un identificador, usa **notación de corchetes**.

```jq
"name" as $key | {name: "Jane", age: 42} | .$key    # => error
"name" as $key | {name: "Jane", age: 42} | .[$key]  # => "Jane"
```

## Agregar o cambiar pares clave-valor

Para agregar un nuevo par clave-valor a un array, o para actualizar el valor de una clave existente, usa el operador de asignación `=`, con una expresión de índice del lado izquierdo.

```jq
{name: "Jane", age: 42} | .sport = "tennis" | .age = 21
# => {
#      "name": "Jane",
#      "age": 21,
#      "sport": "tennis"
#    }
```

El operador `+` _fusiona_ objetos.

```jq
{Richard: 54} + {Jane: 42}
# => {
#      "Richard": 54,
#      "Jane": 42
#    }
```

## Eliminar una clave

Usa la función `del` para eliminar una clave.
Devuelve el objeto actualizado.

```jq
{name: "Jane", age: 42} | del(.age)   # => {"name": "Jane"}
```

El parámetro de `del` es una **expresión de índice** (que usa notación de punto o de corchetes) que se resuelve a una clave del objeto.
`jq` la llama **expresión de ruta**.
No basta con dar solo un string.

```jq
{name: "Jane", age: 42} | del(name)        # error: name/0 is not defined
{name: "Jane", age: 42} | del("name")      # error: Invalid path expression with result "name"
{name: "Jane", age: 42} | del(.name)       # OK
{name: "Jane", age: 42} | del(.["name"])   # OK
```

## Pertenencia

- Para comprobar si el objeto tiene una clave, usa la función `has`.

  ```jq
  {name: "Jane", age: 42} as $example
  |
    ($example | has("name")),    # => true
    ($example | has("sport"))    # => false
  ```

- Comprueba si una clave está en un objeto con `in`.

  ```jq
  {name: "Jane", age: 42} as $example
  |
    ("name"  | in($example)),    # => true
    ("sport" | in($example))     # => false
  ```

## Listar todas las claves

Usa la función `keys` para obtener una lista de todas las claves.

```jq
{name: "Jane", age: 42} | keys   # => ["age", "name"]
```

Ten en cuenta que `keys` _ordena_ las claves.
Para obtener las claves en el orden original, usa `keys_unsorted`.

No hay una función equivalente para listar todos los _valores_.
Sin embargo, el filtro `.[]` genera los valores del objeto como un _stream_, y ese stream se puede capturar con el constructor de array `[...]`.

```jq
[{first: "Jane", last: "Lopez", status: "awesome!"} | .[]]
# => ["Jane", "Lopez", "awesome!"]
```

## Iteración

- La función `map_values(filter)` aplica el filtro a cada _valor_ del objeto.

  ```jq
  {first: "Jane", last: "Lopez", status: "awesome!"}
  | map_values(ascii_upcase)
  # => {"first": "JANE", "last": "LOPEZ", "status": "AWESOME!"}
  ```

- Para iterar sobre un objeto, primero debemos convertirlo en un array de objetos clave-valor.
  La función `to_entries` hace eso.

  ```jq
  {name: "Jane", age: 42} | to_entries'
  # => [
  #      {
  #        "key": "name",
  #        "value": "Jane"
  #      },
  #      {
  #        "key": "age",
  #        "value": 42
  #      }
  #    ]
  ```

  En este punto, podemos usar funciones de iteración de arrays, como `map`.

- La función `from_entries` es la inversa: convierte un array de objetos clave-valor en un objeto.

  ```jq
  [
    {"key":"name", "value":"Jane"},
    {"key":"age", "value":42}
  ] | from_entries                  # =>{"name": "Jane", "age": 42}
  ```

- Para aplicar un filtro a _cada_ par clave-valor de un objeto, usa la función `with_entries(filter)`.

  Por ejemplo, dado un objeto que asigna un nombre a una edad, las claves y los valores se pueden intercambiar así.

  ```jq
  {"Jane": 42, "Richard": 54}
  | with_entries({key: (.value | tostring), value: .key})
  ```

  produce

  ```json
  {
    "42": "Jane",
    "54": "Richard"
  }
  ```

  `with_entries(filter)` es lo mismo que

  ```jq
  to_entries | map(filter) | from_entries
  ```
