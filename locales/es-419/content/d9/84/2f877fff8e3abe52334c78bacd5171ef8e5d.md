# Introducción

La biblioteca estándar incluye un enum llamado `Option` que te ayuda a expresar cuándo un valor puede existir o no.
Lo verás escrito como `Option<T>`, donde `T` es el tipo del valor con el que estás trabajando.
