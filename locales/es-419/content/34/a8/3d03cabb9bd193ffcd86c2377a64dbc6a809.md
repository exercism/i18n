# usa if else

Muy bien.
Eso es un _retorno temprano_ que usaste en la función `%{function}`.
Para el propósito del concepto que este ejercicio busca enseñar, intenta resolver esto
usando una sentencia `else`.
