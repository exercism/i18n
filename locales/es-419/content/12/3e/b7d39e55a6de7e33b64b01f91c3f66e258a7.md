# Usa `MapSet.intersection` y `Enum.sort`

Usa `MapSet.intersection` en `boring_cards`. Hay otras formas idiomáticas de definir la función, pero es la que mejor se ajusta para aprender por completo el concepto de este ejercicio.