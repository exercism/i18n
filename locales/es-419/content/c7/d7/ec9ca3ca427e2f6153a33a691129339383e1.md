# error capturado sin archivo fuente

Se esperaba el archivo fuente `%{expected}`, pero se encontró: `%{available}`.

El archivo de prueba hace `import` desde el archivo fuente esperado. Asegúrate de que el archivo exista y tenga el nombre correcto.
