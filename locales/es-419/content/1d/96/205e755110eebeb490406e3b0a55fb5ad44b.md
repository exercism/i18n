# Sin tipo de retorno

Especifica un tipo de `return` para todos los miembros públicos. Incluir el tipo de retorno permite que el compilador valide el tipo de `return` esperado. También asegura que cambiar la inferencia de tipos de Scala mantenga la compatibilidad binaria.
