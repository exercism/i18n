# Usa recursión

Como mencionan las instrucciones, debes implementar `keep/2` y `discard/2` sin usar `Enum.filter/2` ni `Enum.reject/2`. También evita sus equivalentes en `Stream` y las comprensiones de listas.
