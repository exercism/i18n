# usa `with` con `else` en `get_new_passport`

Usa `with` con un bloque `else` en tu implementación de `get_new_passport/3` para manejar el caso en el que algo falla. El propósito de este ejercicio es que aprendas a usar la forma especial `with`, incluyendo cómo usar sus cláusulas `else`.
