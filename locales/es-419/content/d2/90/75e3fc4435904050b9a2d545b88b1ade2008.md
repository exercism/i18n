# close_log devuelve de forma implícita

La función `close_log/1` no debería devolver `:ok` de forma explícita, sino devolver de forma implícita lo que devuelve `File.close` (que resulta ser `:ok` cuando todo sale bien).
