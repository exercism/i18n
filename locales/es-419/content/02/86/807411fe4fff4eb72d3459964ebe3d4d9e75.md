# Introducción

Con un bucle `while` puedes ejecutar código de forma repetida siempre que se cumpla una condición determinada.
Se escribe con la palabra clave `while`, seguida de una condición entre paréntesis y de un bloque de código entre llaves que contiene el _cuerpo_ del bucle.

```javascript
while (condition) {
  // code that is executed repeatedly as long as the condition is true
}
```

JavaScript también tiene un bucle `do-while`.
Aquí la condición se comprueba después de que se haya ejecutado el cuerpo del bucle.
Esto es útil cuando la condición depende del resultado del código que está en el cuerpo.

```javascript
do {
  // The code here will always be executed once and then
  // repeatedly while the condition is true.
} while (condition);
```

Dentro del cuerpo de un bucle puedes usar la palabra clave `break` para detener por completo la ejecución del bucle.
En cambio, la palabra clave `continue` solo detiene la ejecución de la iteración actual y continúa con la siguiente.
Con `continue` a menudo puedes evitar envolver grandes partes del cuerpo del bucle en un condicional.

```javascript
let i = 0;

while (i < 100) {
  i = i + 2;

  if (i % 3 === 0) {
    continue;
  }

  // The code here will only be executed when i was not divisible
  // by 3 in the check above.
}
```
