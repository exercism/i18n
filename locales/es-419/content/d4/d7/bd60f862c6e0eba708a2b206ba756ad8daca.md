# Introducción

Las funciones **definidas por el usuario** se crean con la palabra clave `function`. Después de esta se escribe el nombre de la función y una lista de argumentos.

```php
function alarm($hour, $minute, $second)
{
    return 'Alarm!';
}
```
