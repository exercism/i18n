# Pistas

## 1. Determina cuánto tiempo toma mezclar un jugo

- Julia no tiene una sentencia `switch`, a diferencia de muchos otros lenguajes.
- La sintaxis `if` ... `elsefif` ... `else` se vio en el concepto de condicionales.

## 2. Reabastece el suministro de gajos de lima

- Un bucle `while` puede ejecutarse hasta que tengas suficientes gajos de lima.
- La función `popfirst!()` se vio en el concepto de vectores.

## 3. Enumera los tiempos para mezclar cada pedido en la cola

- Recorrer un vector es fácil con `for` ... `in`.
- Para construir un vector nuevo puedes usar `push!()`, que se vio en el concepto de vectores.
- Más adelante en el temario exploraremos otras formas de hacer esto en Julia.

## 4. Termina el turno

- Consulta la tarea 2.
