# Evita devolver literales Boolean

En lugar de devolver literales Boolean después de una condición, puedes devolver la condición directamente.

Ejemplo:

```java
// instead of
if (someExpression) {
  return true;
} else {
  return false;
}

// ... return the expression directly
return someExpression;
```
