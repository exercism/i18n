# Apéndice de instrucciones

## Implementación

El grafo se representa en el DSL mediante la clase `Graph`.
La implementación de los nodos y las aristas (representados por las clases `Node` y `Edge`, respectivamente) se proporciona.

Para más detalles sobre el diseño esperado del DSL, revisa los casos de prueba en `GraphTest.java`.
