# Acerca de

En Haxe, para generar un número aleatorio, puedes usar el método `Std.random(x)`. Genera un número aleatorio entre 0 inclusive y `x` exclusive:

```haxe
public function main() {
  trace(Std.random(10)); // 0...9
}
```
