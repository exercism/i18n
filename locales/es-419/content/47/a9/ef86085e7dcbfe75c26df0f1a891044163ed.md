# Introducción

El operador `|>` se llama operador de tubería. Se puede usar para encadenar llamadas a funciones de tal forma que el valor devuelto por la llamada anterior se pase como primer argumento a la siguiente llamada.

```elixir
"hello"
|> String.upcase()
|> Kernel.<>("?!")

# => "HELLO?!"
```
