# Pistas

## 1. Define el tipo opaco `TreasureChest`.

- Los tipos opacos se definen con la palabra clave `opaque`.

## 2. Define la función `create`.

- Puedes usar la [función `string.length`][length] para obtener la longitud de un string.

## 3. Define la función `open`.

- Puedes usar el operador `==` para comparar dos strings y saber si son iguales.

[length]: https://hexdocs.pm/gleam_stdlib/gleam/string.html#length
