# Introducción

La definición de una colección normalmente incluye un marcador de posición entre paréntesis angulares, a menudo `T` por convención. A una colección así se le llama tipo genérico. Esto permite que quien usa la colección especifique qué tipo de elementos almacenar en ella. En el código de ejemplo anterior estamos instanciando un array de strings.
