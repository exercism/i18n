# Desestructura el argumento de assignedToDevTeam

Como se menciona en las instrucciones, desestructura el argumento de `assignedToDevTeam` para extraer el campo del registro `assignedTo` del tipo `Ticket`.
