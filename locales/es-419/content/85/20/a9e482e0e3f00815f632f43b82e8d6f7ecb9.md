# Hamming distingue entre mayúsculas y minúsculas

La distancia de Hamming distingue entre mayúsculas y minúsculas. Para verificar que esta solución cumple con ese requisito, agrega una prueba que compruebe que la diferencia entre `"a"` y `"A"` es `1`.
