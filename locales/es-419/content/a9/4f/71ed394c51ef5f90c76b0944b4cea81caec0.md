# Introducción

La función `switch()` puede ser un reemplazo conciso para una larga serie de pruebas `if` ... `else if`.
La variable sobre la que se hace switch suele ser un string y, en ese caso, se pueden omitir las comillas del selector.

```R
star <- function(type) {
  switch(type,
    M = , # will "fall through" if no value given
    K = "red dwarf",
    G = "Earth-like",
    "bigger star" # only correct for O,B,A,F
  )
}

> star("M")
[1] "red dwarf"
```

Fíjate que las opciones solo avanzan a la siguiente si se deja el valor en blanco, como con `M` en el ejemplo anterior.
No hace falta incluir sentencias `break`, como sí ocurre en otros lenguajes.

Cuando se hace switch sobre tipos `character`, el último valor es un valor predeterminado, como aquí.
Con tipos enteros, no se puede dar un valor predeterminado.

## Funciones en `dplyr`

El `switch` de R base es bastante limitado: solo hace coincidir de forma exacta un único valor.
Esto parecía razonable cuando R se lanzó por primera vez en 1993, pero necesita mejoras para el uso moderno.

~~~~exercism/note
Como se menciona en el concepto [`Conditionals`][concept-conditionals], la colección de paquetes `tidyverse` está diseñada para complementar (y a veces reemplazar) la funcionalidad de R base sin afectar la compatibilidad con versiones anteriores.

Puedes llevar el paquete `dplyr` al ámbito agregando `library(dplyr)` (para el paquete individual) o `library(tidyverse)` (para toda la colección) al inicio de tu código.

[concept-conditionals]: https://exercism.org/tracks/r/concepts/conditionals
~~~~

La biblioteca `dplyr` ofrece dos funciones adicionales relacionadas con `switch`.

### Las funciones `recode_values` y `replace_values`

Estas dos funciones relacionadas permiten un mapeo vectorizado, similar a `switch`, de valores antiguos a valores nuevos.

La principal diferencia entre ellas es que `recode_values()` crea un vector completamente nuevo, mientras que `replace_values()` permite actualizaciones parciales de un vector existente.

La coincidencia sigue siendo exacta, pero:

- Las opciones de la izquierda pueden ser vectores y hay coincidencia si coincide cualquiera de sus elementos.
- Las opciones de la izquierda y los resultados de la derecha se enlazan con una tilde `~` en lugar de `=`.
- Se puede especificar un valor predeterminado en todos los casos.

```R
library(dplyr)

x <- c("a", "b", "a", "d", "b", NA, "c", "e", "z")
recode_values(
  x,
  "a" ~ 1,
  "b" ~ 2,
  "c" ~ 3,
  c("d", "e") ~ 4, # either "d' or "e" will match
  NA ~ 0,          # matches missing values
  default = 100   # note the different syntax for the default
)
#> [1]   1   2   1   4   2   0   3   4 100
```

### La función `case_when`

`case_when` lleva la sintaxis de `case_match` un paso más allá, ya que permite cualquier expresión lógica a la izquierda de la `~`.

El vector que se evalúa (`x` en el ejemplo siguiente) no se pasa como argumento; solo tiene que estar ya definido.

```R
x <- 1:10
case_when(
  x < 3 ~ "low",
  between(x, 3, 5) ~ "mid",
  between(x, 6, 8) ~ "high",
  .default = "what?"
)
#>  [1] "low"   "low"   "mid"   "mid"   "mid"   "high"  "high"  "high"  "what?" "what?"
```

La función `between()` también forma parte de `dplyr`.

[concept-nothingness]: https://exercism.org/tracks/r/concepts/nothingness
