# Pistas

## General

- Los primeros seis argumentos enteros se pasan a una función en `rdi`, `rsi`, `rdx`, `rcx`, `r8` y `r9`.
- Para acceder a menos de 64 bits de un registro, puedes consultar el [manual de AMD][manual] y buscar el nombre adecuado.

## 1. Define `get_box_weight`

- Los primeros seis argumentos enteros se pasan a una función en `rdi`, `rsi`, `rdx`, `rcx`, `r8` y `r9`.
- Para acceder a menos de 64 bits de un registro, puedes consultar el [manual de AMD][manual] y buscar el nombre adecuado.
- Puedes retornar antes de tiempo para comprobar cada paso.
- Hay una [instrucción][movzx] para extender valores con ceros.

## 2. Define `max_number_of_boxes`

- Los primeros seis argumentos enteros se pasan a una función en `rdi`, `rsi`, `rdx`, `rcx`, `r8` y `r9`.
- Para acceder a menos de 64 bits de un registro, puedes consultar el [manual de AMD][manual] y buscar el nombre adecuado.
- Puedes retornar antes de tiempo para comprobar cada paso.
- Hay una [instrucción][div] para la división sin signo.

## 3. Define `items_to_be_moved`

- Los primeros seis argumentos enteros se pasan a una función en `rdi`, `rsi`, `rdx`, `rcx`, `r8` y `r9`.
- Para acceder a menos de 64 bits de un registro, puedes consultar el [manual de AMD][manual] y buscar el nombre adecuado.
- Puedes retornar antes de tiempo para comprobar cada paso.
- La resta funciona igual para números sin signo y con signo.

## 4. Define `calculate_payment`

- Los primeros seis argumentos enteros se pasan a una función en `rdi`, `rsi`, `rdx`, `rcx`, `r8` y `r9`.
- Para acceder a menos de 64 bits de un registro, puedes consultar el [manual de AMD][manual] y buscar el nombre adecuado.
- Puedes retornar antes de tiempo para comprobar cada paso.
- Si obtienes respuestas muy grandes cuando se espera un número negativo, es probable que el signo de uno de los números sea incorrecto.
- Hay una [instrucción][cqo] para extender el signo de los valores de `rax` a `rdx`.
- Hay una [instrucción][idiv] para la división con signo.
- El pago se reparte entre tú y cierta cantidad de trabajadores, y además te quedas con el residuo.
- El residuo de una división se almacena en `rdx`.

[manual]: https://docs.amd.com/v/u/en-US/24592_3.24#page=62
[movzx]: https://www.felixcloutier.com/x86/movzx
[cqo]: https://www.felixcloutier.com/x86/cwd:cdq:cqo
[div]: https://www.felixcloutier.com/x86/div
[idiv]: https://www.felixcloutier.com/x86/idiv
