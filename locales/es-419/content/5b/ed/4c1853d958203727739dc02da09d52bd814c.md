# Usa `with` en `get_new_passport`

Este ejercicio trata de aprender a «seguir el camino feliz» con la forma especial `with`, que te permite lidiar con los errores al final. Usa `with` en tu implementación de `get_new_passport/3`.
