# Introducción

A veces necesitas ver una secuencia como una serie de *piezas* en lugar de elemento por elemento. Los vocabularios [`grouping`][grouping], [`splitting`][splitting] y [`splitting.monotonic`][splitting.monotonic] te dan cuatro formas de hacerlo.

## `group`: fragmentos disjuntos de tamaño *n*

```
group ( seq n -- groups )
```

```factor
USING: grouping ;

{ 1 2 3 4 5 6 7 } 3 group .
! => { { 1 2 3 } { 4 5 6 } { 7 } }
```

Si la longitud de la secuencia no es múltiplo de *n*, el grupo final es más corto que *n*.

## `clump`: ventana deslizante superpuesta de tamaño *n*

```
clump ( seq n -- clumps )
```

```factor
USING: grouping ;

{ 1 2 3 4 5 } 2 clump .
! => { { 1 2 } { 2 3 } { 3 4 } { 4 5 } }
```

Si la secuencia es más corta que *n*, el resultado queda vacío.

## `split-when`: corta donde se dispare un predicado

```
split-when ( seq quot: ( elt -- ? ) -- pieces )
```

```factor
USING: splitting ;

{ 1 2 0 3 4 0 5 } [ zero? ] split-when .
! => { { 1 2 } { 3 4 } { 5 } }
```

Cada elemento recibe su propia llamada al predicado; los elementos *truthy* son los *puntos de corte* y no se conservan en el resultado.

## `monotonic-split`: corta las rachas donde falla una comprobación de pares adyacentes

```
monotonic-split ( seq quot: ( a b -- ? ) -- pieces )
```

```factor
USING: splitting.monotonic ;

{ 1 1 2 2 2 3 } [ = ] monotonic-split .
! => { { 1 1 } { 2 2 2 } { 3 } }
```

El predicado se ejecuta sobre *pares adyacentes*. Mientras devuelva un valor verdadero, la racha continúa; cuando devuelve `f`, empieza una pieza nueva. Con `[ = ]` obtienes rachas de elementos iguales; con `[ <= ]` obtienes rachas no decrecientes; y así sucesivamente.

## Cuándo usar cada uno

- **`group`/`clump`**: conoces el *tamaño* que quieres.
- **`split-when`**: sabes cómo se ve un *elemento de corte*.
- **`monotonic-split`**: sabes qué hace que dos elementos adyacentes *sigan perteneciendo juntos*.

[grouping]: https://docs.factorcode.org/content/vocab-grouping.html
[splitting]: https://docs.factorcode.org/content/vocab-splitting.html
[splitting.monotonic]: https://docs.factorcode.org/content/vocab-splitting.monotonic.html
