# Introducción

Una forma común de ejecutar lógica de manera condicional en JavaScript es el condicional `if`.
Se compone de la palabra clave `if`, una condición entre paréntesis y un bloque de código entre llaves.
El bloque de código solo se ejecutará si la condición se evalúa como `true`.

```javascript
if (condition) {
  // code that is executed if "condition" is true
}
```

Se puede usar por sí solo o combinado con la palabra clave `else`.

```javascript
if (condition) {
  // code that is executed if "condition" is true
} else {
  // code that is executed otherwise
}
```

Para anidar otra condición en la sentencia `else`, puedes usar `else if`.

```javascript
if (condition1) {
  // code that is executed if "condition1" is true
} else if (condition2) {
  // code that is executed if "condition2" is true
  // but "condition1" was false
} else {
  // code that is executed otherwise
}
```
