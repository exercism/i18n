# Instalación

En la mayoría de los sistemas, GNU Sed ya viene instalado por defecto o se puede instalar directamente desde el gestor de paquetes.

## Versión

Este track usa GNU Sed versión 4.9, de 2022. Para comprobar la versión de `sed`, ejecuta este comando:

```sh
sed --version
```
