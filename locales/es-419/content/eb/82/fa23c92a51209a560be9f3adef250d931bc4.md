# Anexo de instrucciones

## Restricciones

Puedes asumir que el argumento que recibe `isBalanced` nunca contiene más de 10 niveles de anidamiento.

Puedes resolver este ejercicio sin usar el `allocator`, pero ten en cuenta que la función `isBalanced` no puede devolver solo `bool`, porque las pruebas usan `try`.
