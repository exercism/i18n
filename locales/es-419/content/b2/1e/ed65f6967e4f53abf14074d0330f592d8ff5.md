# Pistas

## General

- Aunque recibes una `reference` a un `ElectionResult`, puedes acceder a sus miembros con la notación de punto `.`, ¡como si no fuera una `reference`!
- Puedes obtener una `reference` a una variable usando el operador `&`, por ejemplo `int&`.
- Si tienes dudas sobre cómo funcionan las `references`, intenta leer [Learn C++: References][learncpp-references].

## 1. Obtén el número de votos de un `ElectionResult`

- Necesitas crear una función con una `reference` a un `ElectionResult`, en otras palabras, un `ElectionResult&`.
- - Aunque recibes una `reference` a un `ElectionResult`, puedes acceder a sus miembros con la notación de punto `.`, ¡como si no fuera una `reference`!

## 2. Incrementa los votos de un `ElectionResult`

- Necesitas crear una función con una `reference` a un `ElectionResult`, en otras palabras, un `ElectionResult&`.
- - Aunque recibes una `reference` a un `ElectionResult`, puedes cambiar sus variables miembro con la notación de punto `.`, ¡como si no fuera una `reference`!

## 3. Conteo de votos y presidencia

- Puedes iterar una `reference` a un `vector` como un vector normal con un bucle.
- Puedes obtener el tamaño de un vector con `myvector.size()`.


[learncpp-references]: https://www.learncpp.com/cpp-tutorial/lvalue-references/
