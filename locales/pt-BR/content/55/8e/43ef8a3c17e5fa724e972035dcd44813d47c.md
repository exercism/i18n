# junção de strings usada para concatenação

Não use `strings.Join` se os dados não forem do tipo `[]string`.
Use `fmt.Sprintf` em vez disso. É uma forma muito poderosa de montar strings a partir de várias partes.
