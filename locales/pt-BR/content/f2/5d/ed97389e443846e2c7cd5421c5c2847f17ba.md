# Introdução

Se o código de um laço precisar ser executado pelo menos uma vez, você pode usar um laço `do`/`while`:

```csharp
int x = 23;

do
{
    // Always executes at least once, then repeats as long as x > 10
    x = x - 2;
} while (x > 10)
```
