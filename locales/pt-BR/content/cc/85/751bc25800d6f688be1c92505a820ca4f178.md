# reutilização de funções

Reutilizar uma função já escrita pode ajudar quando você quer escrever um código de fácil manutenção.

Reutilize funções que já existem:
- `initial/1` pode reutilizar `first_letter/1`
- `initials/1` pode reutilizar `initial/1`
- `pair/2` pode reutilizar `initials/1`
