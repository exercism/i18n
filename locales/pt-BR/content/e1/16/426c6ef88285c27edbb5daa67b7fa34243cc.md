# Introdução

Os Boolean em Clojure são representados por `true` ou `false`.

As funções de predicado (funções que retornam um boolean) costumam terminar com um ponto de interrogação (`?`), mas isso é apenas uma convenção.

A biblioteca principal inclui funções para operadores lógicos como `not`, `and` e `or`.
