# o setter da propriedade é acessível fora da classe

O setter da propriedade `%{name}` deve ser `private`.
Assim, a definição do valor da propriedade fica restrita à classe em que ela é declarada, e qualquer pessoa continua podendo chamar o getter.
