# Sobre

Uma condicional `if` pode ser usada para executar código condicionalmente. A condição de uma condicional `if` deve ser do tipo `bool`. O C# não tem o conceito de valores _truthy_.

A forma mais comum de fazer isso no C# é usar uma [condicional `if/else`][if-else]:

```csharp
int x = 6;

if (x == 5)
{
    // Execute logic if x equals 5
}
else if (x > 7)
{
    // Execute logic if x greater than 7
}
else
{
    // Execute logic in all other cases
}
```

[if-else]: https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/if-else
