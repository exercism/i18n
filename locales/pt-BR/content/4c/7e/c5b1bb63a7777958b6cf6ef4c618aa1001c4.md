Use operadores lógicos em vez de operadores bit a bit.

Embora não haja nada de errado em usar operadores bit a bit (como `|` e `&`), este exercício quer que você explore operadores lógicos booleanos (como `||` e `&&`).
Existem exercícios dedicados a operadores bit a bit.
