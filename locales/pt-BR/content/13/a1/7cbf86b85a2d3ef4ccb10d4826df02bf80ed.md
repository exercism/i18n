# unless com bloco else

Evite usar expressões `unless` com um bloco `else`. Use uma expressão `if` no lugar.

O motivo disso não é técnico, mas humano. Neste caso, o `else` será executado quando a condição for satisfeita, o que é o oposto do que a palavra sugere.
