# use o `AddSeconds`

Experimente usar o método `AddSeconds` que já vem no `DateTime` para simplificar o código. Repare que `AddSeconds` retorna uma nova instância de `DateTime` e não modifica a instância sobre a qual foi chamado.
