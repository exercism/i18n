# Introdução

O valor `null` em JavaScript representa a ausência intencional de um valor de objeto. É um dos tipos primitivos do JavaScript.

```javascript
// I do not have an apple.
var apple = null;
apple; // => null

// null is treated as falsy for boolean operations, therefore
!apple; // => true
!!apple; // => false
```
