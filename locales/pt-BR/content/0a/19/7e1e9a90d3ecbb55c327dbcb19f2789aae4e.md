# use o literal null

Ao trabalhar com `Strings`, prefira comparar diretamente com `null` em vez de usar `Optional`.

Usar `Optional` nesse contexto adiciona complexidade desnecessária e foge da prática pretendida de lidar com valores `null` diretamente.
