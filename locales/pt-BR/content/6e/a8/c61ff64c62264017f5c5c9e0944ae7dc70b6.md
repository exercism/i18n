# variáveis

[comment]: # (Este erro é gerado quando uma variável não é usada)
[comment]: # (Algumas variáveis não usadas, como `_`, não podem ser usadas em nenhum lugar)
[comment]: # (requer 1 parâmetro:)
[comment]: # (- definition: trecho de código com o valor não usado)

O valor indicado abaixo não é usado em nenhum lugar ou não pode ser usado.
Você pode removê-lo ou usá-lo em algum lugar.

```
%{definition}
```
