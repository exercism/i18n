# Sobre

Em Clojure, os valores Boolean são representados por `true` ou `false`.

Funções predicado (funções que retornam um Boolean) geralmente terminam com um ponto de interrogação (`?`), mas isso é apenas uma convenção.

A biblioteca principal inclui funções para operadores lógicos como `not`, `and` e `or`.
