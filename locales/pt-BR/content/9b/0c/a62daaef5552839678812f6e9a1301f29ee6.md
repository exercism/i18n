# Introdução

Go tem tipos numéricos básicos que conseguem representar conjuntos de valores inteiros ou de ponto flutuante.

Este conceito vai se concentrar em dois tipos de número:

- `int`: por exemplo, `0`, `255`, `2147483647`.
   Um inteiro com sinal que ocupa pelo menos 32 bits (faixa de valores: -2147483648 a 2147483647).
   Mas isso depende da arquitetura do sistema.
   A maioria dos computadores modernos é de 64 bits, então `int` vai ocupar 64 bits (faixa de valores: -9223372036854775808 a 9223372036854775807).

- `float64`: por exemplo, `0.0`, `3.14`.
   Contém o conjunto de todos os números de ponto flutuante de 64 bits.

Go dá suporte ao conjunto padrão de operadores aritméticos `+`, `-`, `*`, `/` e `%` (resto, não módulo).

Em Go, atribuir um valor entre tipos diferentes exige conversão explícita.
Por exemplo, para converter um `int` em um `float64`, você precisaria fazer o seguinte:

```go
var x int = 42
f := float64(x)

fmt.Printf("x is of type: %s\n", reflect.TypeOf(x))
// Output: x is of type: int

fmt.Printf("f is of type: %s\n", reflect.TypeOf(f))
// Output: f is of type: float64
```
