# Melhor escolha de função para `increase_quantity`

Use a função `Map.new/2` ou a função `Enum.into/3` na função `increase_quantity`. Elas são as funções que melhor se encaixam nessa tarefa e não exigem o uso da função `Enum.map/2`.
