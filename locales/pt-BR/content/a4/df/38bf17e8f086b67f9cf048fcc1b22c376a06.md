# Introdução

A forma especial `import` permite que você use funções de um módulo externo sem usar o nome do módulo.

Importar um módulo externo inteiro pode criar conflitos com funções locais já existentes. Para evitar isso, há duas opções: `:except` e `:only`. As duas esperam uma lista de palavras-chave, em que a chave é o nome da função e o valor é a aridade da função.

Normalmente, as importações são adicionadas no início da definição do módulo.

```elixir
defmodule Square do
  import Integer, only: [pow: 2]

  def area(a), do: pow(a, 2)
end
```
