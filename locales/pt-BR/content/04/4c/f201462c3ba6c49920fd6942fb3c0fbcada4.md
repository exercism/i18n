# importar ANSI com :except

As instruções dizem "Você planeja adicionar suporte a outras cores de carro, então quer importar o módulo inteiro", menos a função `color`, que entra em conflito. Para fazer isso, use a opção `:except` assim:

```elixir
import IO.ANSI, except: [color: 1]
```
