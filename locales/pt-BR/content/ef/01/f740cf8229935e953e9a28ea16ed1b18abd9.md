# prefira `IO.puts` em log_sent_email

Ao escrever uma linha em um arquivo, prefira `IO.puts/2` a `IO.write/2`, porque `IO.puts/2` adiciona automaticamente um caractere de nova linha no final.

```elixir
# preferred
IO.puts(pid, email)

# not preferred
IO.write(pid, email)
IO.write(pid, "\n")
```
