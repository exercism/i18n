# Introdução

A API entry do Rust fornece uma visão de uma única entrada em um mapa, que pode ser um `HashMap` ou um `BTreeMap`. A entrada pode estar ocupada ou vazia, e a API fornece métodos para modificar o conteúdo da entrada.
