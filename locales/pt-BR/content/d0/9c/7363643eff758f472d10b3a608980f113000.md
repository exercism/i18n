# Instruções

Erin, uma conhecida sua, precisa imprimir milhares de panfletos para vários eventos e precisa da sua ajuda! Erin pediu que você criasse o layout de um folheto com um cabeçalho, uma data opcional e uma lista de artistas, cada um associado a um _ícone unicode_. O objetivo é usar `print` e distribuir o seu novo design de folheto, e você vai precisar de todas as suas habilidades de formatação de strings em Python para conseguir.

## 1. Crie a classe com um cabeçalho com a inicial maiúscula

A função `capitalize_header` deve receber o nome de um evento e retornar a versão do nome do evento com a inicial maiúscula.

```python
>>> capitalize_header("fan meetup")
"Fan meetup"
```

## 2. Formate a data

Crie um novo método `format_date` que recebe uma lista contendo um dia, um mês e um ano. Ele exibe a data formatada no formato `<month_name> <date>, <year>`

```python
>>> convert_date([9, 4, 2020])
>>> leaflet.date
"April 9, 2020"
>>> leaflet.set_date([13, 5, 2030])
>>> leaflet.date
"May 13, 2030"
```

## 3. Renderize os pontos de código unicode como ícones

Quando o método `display_icons` for chamado, a lista de pontos de código unicode passada deve ser renderizada e retornada como uma `list` de _ícones_.

```python
>>> leaflet.display_icons(['\U00000040', '\U0001D70B'])
['@', '𝜋']
```

## 4. Exiba o folheto finalizado

Agora você vai usar todas as funções acima e combiná-las na função `print_leaflet`.
Ela deve receber um `event_name`, uma lista de `icons`, uma lista de `authors` e uma lista `event_date`.

O folheto deve seguir o layout abaixo.

O folheto segue um layout específico em forma de retângulo:

- A primeira e a última linha contêm 20 asteriscos. `"*"`
- Cada seção é separada por uma linha vazia acima e abaixo dela.
- Uma linha vazia contém apenas um asterisco no início e um no fim.
- A primeira seção é o cabeçalho do folheto, e esse título tem a inicial maiúscula.
- A segunda seção é a data opcional; se a data não for passada, essa seção deve ser uma linha vazia.
- A terceira e última seção é a lista dos artistas, cada artista associado ao ícone correspondente.

```python
********************
*                  *
*    'Concert'     *
*                  *
*  June 22, 2020   *
*                  *
* John         🎸  *
* Benjamin     🎤  *
* Max          🎹  *
*                  *
********************
```
