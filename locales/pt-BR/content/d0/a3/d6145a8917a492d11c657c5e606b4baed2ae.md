# Instruções específicas do SQLite

Este exercício exige que você defina a coluna `result` da tabela `etl` com o valor correto com base no objeto JSON encontrado na coluna `input`. As chaves do objeto de resultado devem estar em ordem alfabética.

## Esquema da tabela

```sql
CREATE TABLE "etl" ("input" TEXT, "result" TEXT);
```
