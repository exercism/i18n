# Desestruturar o argumento de assignTicketTo

Como as instruções mencionam, desestruture o argumento de `assignTicketTo` para extrair o campo `status` do tipo `Ticket` e use a sintaxe `as` para nomear o record.
