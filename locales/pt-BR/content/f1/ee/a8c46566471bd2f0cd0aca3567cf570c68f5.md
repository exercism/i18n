# Complemento das instruções

## Notas específicas da trilha

Uma forma de ter argumentos opcionais em Scheme é especificar os argumentos como uma lista. Duas formas de fazer isso são: `(define (two-fer . args) ...)` ou `(define two-fer (lambda args ...))`.
