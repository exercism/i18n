# send_newsletter retorna implicitamente

A função `send_newsletter/3` não deve retornar `:ok` explicitamente: ela deve retornar implicitamente o que `close_log/1` retorna (que, quando tudo dá certo, acaba sendo `:ok`).
