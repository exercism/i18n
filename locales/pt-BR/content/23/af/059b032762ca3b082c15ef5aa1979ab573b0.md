# Introdução

A herança é um conceito central na POO (Programação Orientada a Objetos).
Ela representa uma relação É-UM.
Na programação, significa literalmente o mesmo que significa em inglês: herdar características do pai (na programação, as características normalmente são funções e variáveis).

Considere uma classe, `Animal`, como mostrado a seguir:

```java
//Creating an Animal class with bark() as a member function.
public class Animal {

    public void bark() {
        System.out.println("This is an animal");
    }

}
```

`Animal` é uma classe pai, porque as propriedades que essa classe tem podem ser estendidas a todos os animais em geral.

Considere um animal chamado `Lion`, com uma classe como esta:

```java
//Lion class is a child class of Animal.
public class Lion extends Animal {

    @Override
    public void bark() {
        System.out.println("Lion here!!");
    }

}
```

~~~~exercism/note
A anotação `Override` é usada para indicar que um método em uma subclasse está sobrescrevendo um método da sua superclasse.
Ela não é estritamente necessária, mas usá-la é uma boa prática.
~~~~

Agora, sempre que fazemos

```java
Animal animal = new Lion(); //creating instance of Animal, of type Lion
animal.bark();
```

Observação: estamos inicializando a classe `Animal` com `Lion`.
A saída vai ficar assim:

```java
Lion here!!
```

De acordo com a POO, existem muitos tipos de herança, mas o Java dá suporte apenas a alguns deles (multinível e hierárquica).
Para saber mais sobre isso, leia [este material][java-inheritance].

[java-inheritance]: https://www.javatpoint.com/inheritance-in-java#:~:text=On%20the%20basis%20of%20class,will%20learn%20about%20interfaces%20later.
