Em Chapel, se a função puder lançar um erro, isso deve ser indicado na definição da função com a palavra-chave `throws`, como mostra o modelo a seguir.

```chapel
proc canThrow(n: int) throws {
  // function body here 
}
```
