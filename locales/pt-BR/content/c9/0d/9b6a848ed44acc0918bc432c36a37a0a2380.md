# use o includes implícito

Remova a verificação explícita de existência `.includes`. Quando a cor (a string de entrada) é convertida em um código (o número `colorCode`), já existe um valor especial que é retornado se a cor não estiver presente. Em vez disso, procure esse valor e use-o.