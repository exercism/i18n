# não use filter

Como as instruções mencionam, você deve implementar `keep` e `discard` sem usar `List.filter` ou `List.filterMap`. Em vez disso, você pode usar recursão ou outras funções de `List`.
