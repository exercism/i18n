# função desnecessária

Em vez de usar sobrecarga de funções, experimente usar um (valor padrão de parâmetro)[https://docs.scala-lang.org/tour/default-parameter-values.html] para definir um valor padrão para um parâmetro.
