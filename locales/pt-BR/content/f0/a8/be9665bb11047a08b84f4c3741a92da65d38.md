# use uma propriedade getter-only

A propriedade `%{name}` pode ser definida como uma propriedade _getter-only_, em que o setter é omitido.
Você ainda consegue atribuir um valor à propriedade, mas só a partir do construtor.
