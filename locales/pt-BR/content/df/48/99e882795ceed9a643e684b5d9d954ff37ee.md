# Dicas

## 1. Verifique se um ataque rápido pode ser feito

Precisamos verificar se o cavaleiro está acordado e _inverter_ o valor boolean que representa o estado dele. Isso pode ser feito com a função [`not`][not].

## 2. Verifique se o grupo pode ser espionado

Queremos retornar `true` se _qualquer_ um dos predicados fornecidos for `true`. Isso pode ser feito com a função [`or`][or].

## 3. Verifique se o prisioneiro pode ser sinalizado

Queremos retornar `true` se e somente se _todos_ os predicados fornecidos forem `true`. Isso pode ser feito com a função [`and`][and].

## 4. Verifique se o prisioneiro pode ser libertado

Precisamos combinar os três operadores lógicos básicos: [`and`][and], [`or`][or] e [`not`][not].

[not]: https://clojuredocs.org/clojure.core/not
[or]: https://clojuredocs.org/clojure.core/or
[and]: https://clojuredocs.org/clojure.core/and
