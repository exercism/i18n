# Instruções adicionais

## Implementação

Se `n` não for um número inteiro positivo, interrompa a execução do programa e retorne uma mensagem de erro.

No JavaScript, isso pode ser feito usando a instrução `throw`.

```javascript
throw new Error('Only positive integers are allowed');
```
