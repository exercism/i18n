# Introdução

Mapeamento é a aplicação de uma função a cada elemento de uma sequência para produzir uma nova sequência.

Isso pode ser feito facilmente em uma lista com a função `mapcar`.
Vamos supor que temos uma função `double` que multiplica seu argumento por `2`:

```lisp
(mapcar #'double '(1 2 3 4)) ; => (2 4 6 8)
```

Repare que o primeiro argumento é uma função a ser chamada. O `#'` é uma sintaxe especial de citação para especificar uma função.

`double` foi chamada primeiro com o primeiro elemento, depois com o segundo, e assim por diante.
Os resultados foram reunidos em uma lista, que é o valor resultante da expressão.
