# Introdução

## Exceções

As exceções em C# oferecem uma maneira estruturada, uniforme e segura em relação a tipos de lidar com condições de erro que ocorrem durante a execução. O tratamento adequado de exceções e erros é importante quando você quer evitar que a aplicação quebre.

Em C#, todas as exceções têm a classe `System.Exception` como tipo base. Ela contém propriedades importantes, como `Message`, que traz uma descrição legível por humanos do motivo pelo qual a exceção foi lançada.

Para sinalizar que deve haver um erro em uma certa parte do código, é preciso criar um novo objeto de exceção e lançá-lo, usando a palavra-chave `throw`:

```csharp
using System;
static int Square(int number)
{
    if (number >= 46341)
    {
        throw new ArgumentException($"Argument {number} cannot be higher than 46340 as its' square doesn't fit into int type.");
    }
    return number * number;
}
```

Quando uma exceção é lançada, o runtime tem a tarefa de encontrar um trecho de código responsável por tratar essa exceção. Se nenhum tratador adequado for encontrado, o runtime exibe a mensagem de exceção não tratada e ainda interrompe a execução do programa.

Para criar um tratador para uma exceção, o C# usa a instrução try-catch, que consiste em um bloco `try` e uma ou mais cláusulas `catch`. O bloco `try` deve conter e proteger o código que pode fazer com que a exceção seja lançada. As cláusulas `catch` devem conter o código que trata o comportamento do programa depois que o erro aconteceu. É importante observar que a ordem das exceções importa depois do bloco `try`, pois, quando várias exceções são listadas, a primeira cláusula `catch` que corresponder é executada.

```csharp
try
{
   if (number == 42)
   {
       throw new ArgumentException("The number cannot be equal to 42.", "number");
   }

   if (number < 0)
   {
      throw new ArgumentOutOfRangeException("number", "The number cannot be negative.");
   }

    // Process number ...
}
catch (ArgumentOutOfRangeException e)
{
    Console.WriteLine($"Number is out of range: {e.Message}.");
}
catch (ArgumentException)
{
    Console.WriteLine("Invalid number.");
}
```
