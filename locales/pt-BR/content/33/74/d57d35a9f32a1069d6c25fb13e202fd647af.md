# Sobre

No Haxe, para gerar um número aleatório, você pode usar o método `Std.random(x)`. Ele gera um número aleatório entre 0 (inclusive) e `x` (exclusive):

```haxe
public function main() {
  trace(Std.random(10)); // 0...9
}
```
