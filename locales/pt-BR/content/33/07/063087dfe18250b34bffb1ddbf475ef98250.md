# Complemento das instruções

## Implementação

O grafo é representado na DSL pela classe `Graph`.
A implementação dos nós e das arestas (representados pelas classes `Node` e `Edge`, respectivamente) é fornecida.

Para mais detalhes sobre o design esperado da DSL, dê uma olhada nos casos de teste em `GraphTest.java`.
