# use uma única string formatada em vez de várias

Há duas instâncias de `string` muito parecidas sendo retornadas. Como este código poderia ser estruturado para que essa duplicação seja eliminada? Isso vai envolver remover qualquer ramificação e retornar apenas uma única `string`.
