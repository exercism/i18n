# usar interpolação de string

Use [interpolação de string](https://docs.scala-lang.org/overviews/core/string-interpolation.html) para construir uma string dinamicamente, em vez de usar concatenação de strings ou `StringBuilder`.

A principal vantagem é que o código com interpolação de string costuma ser mais fácil de ler do que a concatenação de strings ou o `StringBuilder`.
