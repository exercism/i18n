# Sobre

Como todos os tipos de dados padrão do Clojure são imutáveis, ele oferece *tipos de referência*, que permitem mutação controlada. Os mais simples e mais usados deles são chamados de Atoms.

Atoms são criados com a função `atom`, que recebe um valor inicial:

```clojure
(def players (atom ()))
```

O valor atual do atom é desreferenciado com `deref` ou com o atalho `@`:

```clojure
@players
;;=> ()
```

O valor atual do atom `players` pode ser modificado com `swap!`, que atualiza o valor aplicando uma função:

```clojure
(swap! players conj :player1)
;;=> (:player1)
```

A função `reset!` substitui o valor de um atom sem levar em conta o valor atual:

```clojure
(reset! players ())
```
