# Evite comparar com literais booleanos

Em vez de comparar valores booleanos com valores literais, dá para simplificar e usar só as variáveis:

```java
// instead of
return someBoolVariable == true;

// use
return someBoolVariable;
```
