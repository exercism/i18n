# Sobre

As **expressões** `throw` são uma alternativa às _instruções_ `throw` e, em particular, podem aumentar o poder das expressões ternárias e de outras expressões compostas.

```haxe
trace(true ? throw "This is an error" : "Everything is fine"); // ERROR: This is an error
```
