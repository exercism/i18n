# hamming diferencia maiúsculas de minúsculas

A distância de Hamming diferencia maiúsculas de minúsculas. Para verificar esta solução em relação a esse requisito, adicione um teste que afirme que a diferença entre `"a"` e `"A"` é `1`.
