# Dicas

## Geral

A documentação relacionada a `IDisposable` está [aqui][idisposable]

## 2. Escreva alguns dados no banco de dados

O `try`/`catch` é necessário para garantir que o banco de dados volte ao estado `Closed` se uma exceção for lançada por causa de dados inválidos.

## 3. Faça commit dos dados escritos anteriormente no banco de dados

O `try`/`catch` é necessário para garantir que o banco de dados volte ao estado `Closed` se uma exceção for lançada por causa de dados inválidos.

[idisposable]: https://docs.microsoft.com/en-us/dotnet/api/system.idisposable
[try-catch-finally]: https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/try-catch-finally
