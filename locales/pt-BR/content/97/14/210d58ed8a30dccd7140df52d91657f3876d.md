# Debug

Quando um teste falha, uma mensagem é exibida descrevendo o que deu errado e para qual entrada. Você também pode aproveitar o fato de que qualquer saída no `console` também será mostrada. Para escrever no console, use:

```javascript
console.log('Debug message');
```
