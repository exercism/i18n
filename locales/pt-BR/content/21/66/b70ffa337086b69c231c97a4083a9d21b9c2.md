# Sobre

Você pode usar typedefs para encurtar os nomes de tipos complexos:

```haxe
typedef IA = Array<Int>;
```

Você também pode usar typedefs para criar uma estrutura de tipo:

```haxe
typedef User = {
  var name:String;
  var age:Int;
}
```
