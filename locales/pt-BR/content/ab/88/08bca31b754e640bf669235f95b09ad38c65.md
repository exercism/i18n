# Use operadores bool

As funções `canFastAttack`, `canSpy`, `canSignalPrisoner` e `canFreePrisoner` podem ser resolvidas todas apenas com o uso de `not`, `&&` e `||`.
Para aprender a manipular Boolean, certifique-se de resolver o exercício dessa forma.
