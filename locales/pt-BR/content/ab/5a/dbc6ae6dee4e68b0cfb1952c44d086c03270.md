# Apêndice das instruções

## Implementação

Se um caractere ou códon inválido for encontrado _durante_ a tradução, o código deve `throw` um erro com a mensagem `Invalid codon`.

```javascript
translate('AAA');
// => Error: Invalid codon
```
