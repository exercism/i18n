# use a anotação override

Embora não seja estritamente necessário, usar a anotação `@Override` ao sobrescrever métodos de uma superclasse é uma boa prática.

Isso pode ajudar a evitar erros: se um método marcado com `@Override` não sobrescrever corretamente um método de uma das suas superclasses, o compilador vai gerar um erro.
