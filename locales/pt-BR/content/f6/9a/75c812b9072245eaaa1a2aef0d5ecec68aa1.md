# Complemento das instruções

## Funções específicas da trilha de Emacs Lisp

* `list-empty-p` (*dada uma lista, retorna se a lista está vazia*)
* `list-sum` (*dada uma lista de números, retorna a soma de todos os elementos*)
