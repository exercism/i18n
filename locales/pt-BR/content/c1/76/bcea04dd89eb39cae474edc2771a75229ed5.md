# Anexo das instruções

## Implementação

Neste exercício, você vai implementar um procedimento que deve alocar a string que retorna. Liberar a string é responsabilidade de quem chama o procedimento.

```odin
// The procedure will be called like that:
converted := convert(3)
// ...
// and eventually the caller will delete the string
delete(converted)
```
