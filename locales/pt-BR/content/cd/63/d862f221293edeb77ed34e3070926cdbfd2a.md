# prefira constante extraída no nível superior

Em vez de definir a constante _dentro_ da função, considere extraí-la para o
nível superior. Constantes, funções e classes sem `export` não são acessíveis
fora do arquivo.

```javascript
const %{name} = %{value}

export const gigasecond = (...)
```
