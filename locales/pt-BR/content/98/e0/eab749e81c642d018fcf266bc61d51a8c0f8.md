# Anexo às instruções

## Palavras

`classify ( n -- str )` retorna uma das strings `"perfect"`,
`"abundant"` ou `"deficient"`.

## Erros

Quando a entrada for zero ou negativa, lance a string

```
"Classification is only possible for positive integers."
```

(exatamente como está, incluindo o ponto final).
