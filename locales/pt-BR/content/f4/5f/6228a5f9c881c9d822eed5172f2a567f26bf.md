# Introdução

Os arrays associativos do Factor, isto é, as tabelas hash e afins, compartilham um [protocolo][assoc-protocol] comum de [`assocs`][assocs]. O literal de tabela hash é `H{ }`:

```factor
H{ { "coal" 1 } { "wood" 2 } } .

"coal" H{ { "coal" 1 } { "wood" 2 } } at .   ! => 1
"gold" H{ { "coal" 1 } { "wood" 2 } } at .   ! => f
```

Operações comuns:

```
at        ( key assoc -- value/f )
key?      ( key assoc -- ? )
set-at    ( value key assoc -- )
delete-at ( key assoc -- )
inc-at    ( key assoc -- )
change-at ( key assoc quot -- )
keys      ( assoc -- keys )
values    ( assoc -- values )
>alist    ( assoc -- alist )
```

As tabelas hash são *mutáveis*: faça `clone` de um literal `H{ }` antes de modificar, para que o literal não seja compartilhado entre chamadas.

[assocs]: https://docs.factorcode.org/content/vocab-assocs.html
[assoc-protocol]: https://docs.factorcode.org/content/article-assocs-protocol.html
