# Use um atributo de módulo

Use um [atributo de módulo](https://elixir-lang.org/getting-started/module-attributes.html#as-constants) em `add_player/3` e `reset_score/2` como constante para a pontuação inicial.
