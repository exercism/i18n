# Instruções específicas do SQLite

Este exercício exige que você defina a coluna `result` da tabela `color_code` com o valor correto, com base na cor encontrada na coluna `color`.

## Esquema da tabela

```sql
CREATE TABLE "color_code" ("color" TEXT, "result" INT);
```
