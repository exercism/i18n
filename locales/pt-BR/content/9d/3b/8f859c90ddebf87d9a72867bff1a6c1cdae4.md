# Limitar o número de cores

Limite o número de cores de entrada que são processadas. Se mais de duas cores forem passadas, apenas as duas primeiras devem ser usadas para calcular o valor total de `colorCode`.
