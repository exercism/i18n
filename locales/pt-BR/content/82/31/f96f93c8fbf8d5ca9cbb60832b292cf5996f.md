# Rodando os testes

Escreva seu código em `<exercise_name>.zig`.

Para rodar os testes de um exercício, rode:

```bash
zig test test_exercise_name.zig
```

no diretório raiz do exercício (substituindo `exercise_name` pelo nome do exercício).
