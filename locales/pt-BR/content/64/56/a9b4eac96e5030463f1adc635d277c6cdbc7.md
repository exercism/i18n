# Dicas

## Geral

- Um endereço efetivo pode ser calculado com a instrução [lea][lea].
- Você deve usar `rel` para formar endereços relativos válidos na memória.
- O prefixo para acessar valores de 8 bytes na memória é `qword`.

## 1. Verifique quais eram as contagens na semana passada

- Um endereço efetivo pode ser calculado com a instrução [lea][lea].
- Você deve usar `rel` para formar endereços relativos válidos na memória.
- O prefixo para acessar valores de 8 bytes na memória é `qword`.
- Para acessar o conteúdo armazenado em um endereço na memória, você precisa desreferenciá-lo usando `[]`.

## 2. Verifique quais são as contagens atuais desta semana

- Um endereço efetivo pode ser calculado com a instrução [lea][lea].
- Você deve usar `rel` para formar endereços relativos válidos na memória.
- O prefixo para acessar valores de 8 bytes na memória é `qword`.
- Variáveis não inicializadas são definidas na _seção .bss_.
- Você deve usar tanto `rax` quanto `rdx` para retornar dois inteiros de uma função.

## 3. Salve a contagem de cada dia

- Um endereço efetivo pode ser calculado com a instrução [lea][lea].
- Você deve usar `rel` para formar endereços relativos válidos na memória.
- O prefixo para acessar valores de 8 bytes na memória é `qword`.
- O prefixo para acessar valores de 1 byte na memória é `byte`.
- Você pode inserir ou recuperar bytes em massa usando o prefixo de um tamanho de dado maior ao acessar a memória.
- Você pode verificar condições usando [cmp][cmp].
- Você pode desviar condicionalmente usando [jcc][jcc].
- Você pode usar [inc][inc] para somar 1 ao valor de um operando de destino.

## 4. Verifique quantos pássaros visitaram hoje

- Um endereço efetivo pode ser calculado com a instrução [lea][lea].
- Você deve usar `rel` para formar endereços relativos válidos na memória.
- O prefixo para acessar valores de 8 bytes na memória é `qword`.
- Valores inteiros de 1 byte são retornados em `al`.
- Você pode usar [dec][dec] para subtrair 1 do valor de um operando de destino.

## 5. Atualize a contagem de hoje

- Um endereço efetivo pode ser calculado com a instrução [lea][lea].
- Você deve usar `rel` para formar endereços relativos válidos na memória.
- O prefixo para acessar valores de 8 bytes na memória é `qword`.
- Você pode usar [add][add] com um operando de destino na memória.

## 6. Insira novas entradas em massa

- Um endereço efetivo pode ser calculado com a instrução [lea][lea].
- Você deve usar `rel` para formar endereços relativos válidos na memória.
- O prefixo para acessar valores de 8 bytes na memória é `qword`.
- Você pode inserir ou recuperar bytes em massa usando o prefixo de um tamanho de dado maior ao acessar a memória.

[lea]: https://www.felixcloutier.com/x86/lea
[cmp]: https://www.felixcloutier.com/x86/cmp
[jcc]: https://www.felixcloutier.com/x86/jcc
[inc]: https://www.felixcloutier.com/x86/inc
[dec]: https://www.felixcloutier.com/x86/dec
[add]: https://www.felixcloutier.com/x86/add
