# condicional desnecessária

Em vez de criar um bloco `if (...) {}`, aproveite diretamente o valor Boolean que pode ser retornado ao verificar o valor do parâmetro.

Por exemplo, o seguinte:

```javascript
if (param === true) {
  return true;
}
```

pode ser transformado em:

```javascript
return param === true;
```
