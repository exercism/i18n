# Introdução

A Wikipédia descreve uma instrução `switch` como "um tipo de mecanismo de controle de seleção usado para permitir que o valor de uma variável ou de uma expressão altere o fluxo de controle do programa".

O mecanismo envolve as seguintes palavras-chave: `switch`, `case`, `break` e `default`.

Na forma mais simples, uma instrução `switch` testa uma expressão primitiva ou uma string e toma uma decisão com base no valor dela. Por exemplo:

```csharp
string direction = GetDirection();
switch (direction)
{
    case "left":
        GoLeft();
        break;
    case "right":
        GoRight();
        break;
    default:
        MarkTime();
        break;
}
```

Na forma mais sofisticada, ela introduz o downcasting com `case <Type> <variable>:` e as guardas (`when`).

```csharp
Animal animal = GetAnimal();

switch (animal)
{
    case Dog canine:
    case Coyote canine:
        canine.Bark();
        break;
    case Cat cat when cat.HasOnly8Lives():
        cat.IsCareful();
        cat.Meow();
        break;
    case Cat cat:
        cat.Meow();
        break;
}
```
