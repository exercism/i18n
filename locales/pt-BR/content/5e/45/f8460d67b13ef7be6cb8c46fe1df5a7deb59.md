# Testes

Para compilar e rodar os testes, basta rodar o seguinte no diretório do seu exercício:

```shell
nim r test_exercise_name.nim
```

Substitua `exercise_name` pelo nome do exercício (por exemplo, `all_your_base`).
