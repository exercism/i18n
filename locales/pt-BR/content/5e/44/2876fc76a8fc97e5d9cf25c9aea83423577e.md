# Desestruture o argumento de `numberOfCreatorComments`

Como as instruções mencionam, desestruture o argumento de `numberOfCreatorComments` para extrair os campos do record de interesse do tipo `Ticket`.
