# Use `MapSet.union` e `MapSet.size`

Use `MapSet.union` e `MapSet.size` em `total_cards`. Existem outras formas idiomáticas de definir a função, mas essas são as mais adequadas para aprender o conceito deste exercício por completo.