# Introdução

## Enums

O tipo `enum` do C# representa um conjunto fixo de constantes nomeadas (uma enumeração). Seu principal objetivo é oferecer uma forma segura em termos de tipos para interagir com constantes numéricas, limitando os valores disponíveis a um conjunto predefinido. Um enum simples pode ser definido da seguinte forma:

```csharp
enum Season
{
    Spring,
    Summer,
    Autumn,
    Winter
}
```

Se não forem definidos explicitamente, os membros do enum recebem automaticamente valores inteiros incrementais, sendo o primeiro deles zero. Também é possível atribuir valores explicitamente:

```csharp
enum Answer
{
    Maybe = 1,
    Yes = 3,
    No = 5
}
```
