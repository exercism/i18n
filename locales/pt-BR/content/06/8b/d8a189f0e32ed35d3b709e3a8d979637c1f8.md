# Complemento das instruções

## Implementação

No pacote grep, defina uma única func Go, Search, que recebe uma string de padrão, um slice de flags (que são strings) e um slice de strings com nomes de arquivos. Search deve retornar um slice de strings com a saída para as flags e os nomes de arquivos informados.

Use a seguinte assinatura para a func Search:

```go
func Search(pattern string, flags, files []string) []string {
```
