# Dicas

## 1. Defina a função `with_retry`

- Uma expressão `case` pode ser usada para fazer pattern matching em um resultado.

## 2. Defina a função `record_timing`

- A função `time_logger` deve ser chamada mesmo se a função `experiment` retornar um valor `Error`.

## 3. Defina a função `run_experiment`

- A [função `result.try`][result-try] pode ser usada em uma expressão `use` para parar se um resultado for um valor `Error`.

[result-try]: https://hexdocs.pm/gleam_stdlib/gleam/result.html#try
