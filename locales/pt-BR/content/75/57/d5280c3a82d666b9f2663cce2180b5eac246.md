# Introdução

Um `Option` é um enum usado para verificar a validade do retorno de funções. Ele é definido assim:

```haxe
enum Option<T> {
  Some(v:T);
  None;
}
```

Ele faz parte do pacote `haxe.ds`:

```haxe
import haxe.ds.Option;

class Main {
  static public function main() {
    var result = trySomething();

    switch (result) {
      case None:
        trace("Nothing");
      case Some(s):
        trace("Got: " + s);
    }
  }

  static public function trySomething():Option<String> {
    if (Math.random() > 0.5) {
      return None;
    } else {
      return Some("Success");
    }
  }
}
```
