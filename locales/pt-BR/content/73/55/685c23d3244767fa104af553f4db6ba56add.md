# minimize as conversões de slice para rune

Prefira converter `%{name}` em `[]rune` apenas uma vez, guardando o resultado em uma variável e reutilizando-o.
Essa conversão é uma operação relativamente custosa.
