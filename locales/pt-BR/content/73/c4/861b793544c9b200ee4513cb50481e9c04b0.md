# Introdução

Quando os tipos não podem ser convertidos implicitamente, você precisa usar um operador de conversão explícita.
O operador de conversão nada mais é do que o tipo de destino entre parênteses, antes do valor que você quer converter:

```csharp

int i = 7;
byte b = (byte)i; // Explicit cast from int to byte
```
