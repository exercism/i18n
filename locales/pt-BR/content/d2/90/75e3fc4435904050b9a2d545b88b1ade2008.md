# close_log retorna implicitamente

A função `close_log/1` não deve retornar `:ok` de forma explícita, mas sim retornar implicitamente o que `File.close` retorna (que, quando tudo dá certo, acaba sendo `:ok`).
