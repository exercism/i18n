# Introdução

As expressões `throw` são uma alternativa às instruções `throw` e, em particular, podem ampliar o poder das expressões ternárias e de outras expressões compostas.

```csharp
string trimmed = str == null ? throw new ArgumentException() : str.Trim();
```
