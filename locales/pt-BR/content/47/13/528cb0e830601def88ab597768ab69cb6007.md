## Geral

A solução não diferencia maiúsculas de minúsculas, o que significa que `"WOrd"` é o mesmo que `"word"` ou `"woRd"`. Talvez ajude dar uma olhada na [biblioteca padrão](https://doc.rust-lang.org/std/primitive.char.html) em busca de funções que façam essa conversão.
