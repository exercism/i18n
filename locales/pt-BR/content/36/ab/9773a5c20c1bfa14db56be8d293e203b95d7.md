# Apêndice das instruções

## Implementação

Neste exercício, você deve definir uma classe `DndCharacter` com:

- Um método estático para `modifier(constitution)`.
- Métodos para `strength`, `dexterity`, `constitution`, `intelligence`, `wisdom`, `charisma` e `hitpoints`
