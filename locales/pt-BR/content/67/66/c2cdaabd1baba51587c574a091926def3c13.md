# `vec![]`

Você pode usar macros do Rust para gerar vetores.

## Sintaxe

- `vec![]` usa uma macro da biblioteca padrão do Rust para inicializar um `Vec<T>` vazio
- `vec![1; 3]` inicializa um vetor com três elementos de valor 1
- `vec![1, 2, 3]` inicializa um vetor com os valores literais 1, 2, 3
