## 1. Extraia a mensagem

- Implemente `log-message` com `subseq`
- As strings são indexadas a partir do zero

## 2. Determine a gravidade

- Implemente `log-severity` com uma expressão condicional usando `string=`

## 3. Lidando com os caprichos da realidade

- `string-equal` é uma versão de `string=` que não diferencia maiúsculas de minúsculas

## 4. Reformatando a mensagem de log de acordo com a gravidade do log

- Há três funções mencionadas na introdução para fazer as transformações necessárias.
