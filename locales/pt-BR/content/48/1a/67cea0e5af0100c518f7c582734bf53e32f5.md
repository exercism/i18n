# otimize o valor padrão explícito

Defina o [valor padrão](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Default_parameters)
de `%{parameter}` como `'you'`, em vez de contar com `%{maybe_undefined_expression}`
sendo `undefined` quando nenhum valor é passado.
