# `log_sent_email` retorna implicitamente

A função `log_sent_email/1` não deve retornar `:ok` explicitamente, mas sim retornar implicitamente o que `IO.puts` retorna (que, quando tudo dá certo, acaba sendo `:ok`).
