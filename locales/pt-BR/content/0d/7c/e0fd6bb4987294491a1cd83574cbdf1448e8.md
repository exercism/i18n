# Desestruture o argumento de `emptyComment`

Como as instruções mencionam, desestruture o argumento de `emptyComment` para extrair o conteúdo da tupla.
Ignore o elemento `User` com o coringa `_` e mantenha a `String`.
