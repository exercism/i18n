# Introdução

Funções podem declarar valores padrão para um ou mais argumentos. Vamos considerar esta função:

```elixir
def number(n \\ 13), do: "That's not my favorite"
```

Ao ser compilada, o Elixir cria uma definição de função para `number/0` (sem argumentos) e `number/1` (um argumento).

Se mais de um argumento tiver valores padrão, os valores padrão serão aplicados à função da esquerda para a direita para preencher os argumentos que faltam.

Se a função tiver mais de uma cláusula, os argumentos padrão devem ser definidos em um cabeçalho de função (uma função sem corpo) antes das cláusulas da função:

```elixir
def number(n \\ 13)
def number(n) when n < 10, do: "Dream bigger!"
def number(n) when n > 100, do: "Not that big..."
```

[kernel-guards]: https://hexdocs.pm/elixir/Kernel.html#guards
