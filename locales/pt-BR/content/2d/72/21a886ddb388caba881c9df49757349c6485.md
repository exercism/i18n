# Dicas

## 1. Divida a comida igualmente

- Comece escrevendo a assinatura da função `DivideFood`.
  Ela deve aceitar 2 parâmetros do tipo `FodderCalculator` e `int` e retornar dois valores dos tipos `float64` e `error`.
  Volte ao [conceito de funções][concept-functions] se precisar de mais informações sobre como definir funções.
- No corpo da função, chame o [método][concept-methods] `FodderAmount` em `FodderCalculator` para obter a quantidade total padrão de ração para as vacas.
  Ele vai retornar o resultado real e um erro.
  Trate o erro com uma condicional, como foi explicado na introdução.
- Depois disso, chame o método `FatteningFactor` e trate o valor de retorno de erro como antes.
- Agora que você tem a quantidade de ração e o fator, pode calcular o resultado final.
  Você precisa dividir a ração pelo número de vacas (volte a [concept-numbers] para dicas sobre conversão de tipos) e multiplicar pelo fator.
  Confira na introdução o que retornar como valor de erro em caso de sucesso.

## 2. Verifique o número de vacas

- `ValidateInputAndDivideFood` tem a mesma assinatura de função que `DivideFood`.
- Como você quer retornar cedo em caso de erro em Go, primeiro verifique se o número de vacas é menor ou igual a 0 com uma condicional.
- Se for, você retorna um erro que criou com `errors.New`.
  Certifique-se de que a mensagem corresponde às instruções.
- Se o número de vacas for válido, você pode seguir e chamar a função `DivideFood` existente da tarefa 1.

## 3. Melhore o tratamento de erros

- Comece criando a [struct][concept-structs] `InvalidCowsError` com dois campos não exportados que guardam o número de vacas e a mensagem.
- Em seguida, defina o método `Error` nessa struct (com um receptor de ponteiro).
  Volte à introdução do exercício para ajuda sobre como fazer isso.
- Agora você pode trabalhar na função `ValidateNumberOfCows`.
  Dependendo do número de vacas ([condicional][concept-conditionals]), ela deve criar e retornar uma nova instância de `InvalidCowsError` e definir a mensagem correta ao fazer isso.
  Se o número de vacas for válido, `nil` deve ser retornado.

[concept-methods]: /tracks/go/concepts/methods
[concept-functions]: /tracks/go/concepts/functions
[concept-numbers]: /tracks/go/concepts/numbers
[concept-structs]: /tracks/go/concepts/structs
[concept-conditionals]:  /tracks/go/concepts/conditionals-if
