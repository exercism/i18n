# Evite retornar literais Boolean

Em vez de retornar literais Boolean depois de uma condição, você pode retornar a condição diretamente.

Exemplo:

```java
// instead of
if (someExpression) {
  return true;
} else {
  return false;
}

// ... return the expression directly
return someExpression;
```
