# Introdução

O módulo [`fractions`][fractions] permite criar e trabalhar com [`rational numbers`][rational]: frações com um numerador inteiro dividido por um denominador inteiro.
Por exemplo, podemos armazenar `2/3` como uma fração exata em vez do valor aproximado de `float`, `0.6666...`.

Ao contrário dos números `int`, `float` e `complex`, as frações não têm uma forma literal.
No entanto, o construtor de frações é bastante flexível.

O mais óbvio é que ele pode receber dois inteiros como argumentos.
Fatores comuns são removidos automaticamente, convertendo a fração para a sua "forma irredutível" (_os menores inteiros que representam a fração com precisão_):

```python
>>> from fractions import Fraction

>>> f1 = Fraction(2, 3) # 2/3
>>> f1
Fraction(2, 3)

>>> f2 = Fraction(6, 9)
>>> f2
Fraction(2, 3)  # automatically simplified

>>> f1 == f2
True
```

O construtor de frações também pode interpretar uma representação em string:

```python
>>> f3 = Fraction('2/3')
>>> f3
Fraction(2, 3)
```

As frações também podem trabalhar com parâmetros `float`, mas isso pode dar problemas por causa da natureza aproximada de representar o valor decimal internamente em binário.
Para saber mais sobre esse problema de representação, veja o site [0.30000000000000004][0.30000000000000004] e [Floating Point Arithmetic: Issues and Limitations][fp-issues] na documentação do Python.

Para um resultado mais confiável ao usar números de ponto flutuante com frações, existe o método `<fraction>.limit_denominator()`.


## Aritmética com frações

Os [`arithmetic operators`][operators] de sempre, `+ - * / **`, funcionam com frações, assim como com outros tipos numéricos.

Inteiros e outros `Fraction`s podem ser incluídos na equação e dar como resultado uma `Fraction`.
Incluir um `float` na expressão resulta em uma saída `float`, com uma consequente (possível) perda de precisão:

```python
>>> Fraction(2, 3) + Fraction(1, 4) # addition
Fraction(11, 12)

>>> Fraction(2, 3) * Fraction(6, 5) # multiply fractions
Fraction(4, 5)

>>> Fraction(2, 3) * 6 / 5 # fraction with integers
Fraction(4, 5)

>>> Fraction(2, 3) * 1.2  # fraction with float -> float
0.7999999999999999

>>> Fraction(2, 3) ** 2  # exponentiation with integer
Fraction(4, 9)
```

Várias funções numéricas padrão do Python também dão o resultado que você esperaria ao trabalhar com os tipos `int` e `float`:

```python
>>> round(Fraction(11, 3))
4

>>> from math import floor, ceil
>>> floor(Fraction(11, 3))
3
>>> ceil(Fraction(11, 3))
4

>>> float(Fraction(11, 3))
3.6666666666666665
```

[0.30000000000000004]: https://0.30000000000000004.com/
[fp-issues]: https://docs.python.org/3/tutorial/floatingpoint.html#tut-fp-issues
[fractions]: https://docs.python.org/3/library/fractions.html
[operators]: https://docs.python.org/3/library/stdtypes.html#numeric-types-int-float-complex
[rational]: https://en.wikipedia.org/wiki/Rational_number
