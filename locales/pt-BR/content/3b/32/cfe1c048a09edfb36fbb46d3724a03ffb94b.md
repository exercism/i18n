# Introdução

## Strings

Lembre-se de que o `jq` dá suporte aos mesmos tipos de dados que o JSON.
O JSON descreve as **strings** assim:

> Uma string é uma cadeia de zero ou mais caracteres Unicode, envolta em aspas duplas, usando escapes com barra invertida.

### Sequências de escape

Dentro de uma string, use o caractere de barra invertida para inserir caracteres "especiais":

- `\"` uma aspa dupla literal,
- `\\` uma barra invertida literal,
- caracteres de espaço em branco `\n`, `\t`, `\r`, `\f`, `\b`
- caracteres Unicode `\uAAAA`, em que `A` é um dígito hexadecimal

### Comprimento de string

Para saber o número de caracteres, use a função `length`.

```sh
$ jq -cn --args  '$ARGS.positional[] | length' "Hello world!" "❄🌡🤧🤒🏥🕰😀"
12
7
```

### Substrings

A notação de **slice** pode ser usada para extrair substrings.
A sintaxe é `.[i:j]`.
A substring retornada tem comprimento `j - i`, retornando caracteres do índice `i` (inclusive) até o índice `j` (exclusive).
Qualquer um dos índices pode ser negativo; nesse caso, ele conta de trás para frente a partir do fim da string.
Qualquer um dos índices pode ser omitido; nesse caso, ele se refere ao início ou ao fim da string.
Os índices começam em zero.

```jq
"abcdefghij"[3:6]   # => "def"
"abcdefghij"[3:]    # => "defghij"
"abcdefghij"[:-2]   # => "abcdefgh"
```

### Concatenação

Use `+` para juntar strings:

```jq
"Hello" + " " + "world!"
# => "Hello world!"
```

Use `add` quando tiver um array de strings:

```jq
["Hello", " ", "world!"] | add
# => "Hello world!"
```

### Dividir e juntar

Para dividir, use a função `split/1` ou o operador `/`:

```jq
"Hello beautiful world!" | split(" ")   # => ["Hello", "beautiful", "world!"]
```

```jq
"Hello beautiful world!" / " "          # => ["Hello", "beautiful", "world!"]
```

O inverso de `split/1` é `join/1`.

Muitas vezes é preciso converter uma string em um array de caracteres.
Há duas formas de fazer isso:

1. dividir em uma string vazia

   ```jq
   "Hi friend 😀" / ""   # => ["H","i"," ","f","r","i","e","n","d"," ","😀"]
   ```

1. `explode` em um array de _pontos de código_

   ```jq
   "Hi friend 😀" | explode   # => [72,105,32,102,114,105,101,110,100,32,128512]
   ```

   `implode` é o inverso de `explode`.

### Encontrar o índice de uma substring

Use a função `index/1`; o índice começa em zero.

```jq
"hello" | index("el")'   # => 1
```

Se a substring não estiver na string, o resultado é `null`

```jq
"hello" | index("elk")   # => null
```

### Interpolação de strings

Dentro de uma string, a sequência `\(expression)` insere o _resultado_ dessa expressão [na string][interpolate]:

```jq
"The current datetime is \(now | strflocaltime("%c"))"   # => "The current datetime is Wed Nov 16 17:06:33 2022"
```

### Conversão de maiúsculas e minúsculas

Use as funções `ascii_downcase` e `ascii_upcase`.

### Expressões regulares

O `jq` tem um suporte rico a expressões regulares: esse será o tema de uma lição mais adiante.

[interpolate]: https://jqlang.org/manual/#string-interpolation
