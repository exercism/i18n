# Introdução

No PHP, é possível chamar uma variável como função aplicando parênteses [`()`] a ela. Quando o PHP tenta invocar a função, ele procura uma função com o mesmo nome do valor da variável.

```php
function foo(): string
{
    return "foo() invoked";
}

$func = 'foo';
$func(); // This calls foo()
```
