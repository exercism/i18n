# Use `with` com `else` em `get_new_passport`

Use `with` com um bloco `else` na sua implementação de `get_new_passport/3` para lidar com o caminho infeliz. O objetivo deste exercício é aprender a usar a forma especial `with`, incluindo como usar suas cláusulas `else`.
