# Introdução

Uma forma comum de executar lógica condicionalmente em JavaScript é usar a condicional.
Ela é formada pela palavra-chave `if`, uma condição entre parênteses e um bloco de código entre chaves.
O bloco de código só será executado se a condição for avaliada como `true`.

```javascript
if (condition) {
  // code that is executed if "condition" is true
}
```

Ela pode ser usada sozinha ou combinada com a palavra-chave `else`.

```javascript
if (condition) {
  // code that is executed if "condition" is true
} else {
  // code that is executed otherwise
}
```

Para aninhar outra condição na cláusula `else`, você pode usar `else if`.

```javascript
if (condition1) {
  // code that is executed if "condition1" is true
} else if (condition2) {
  // code that is executed if "condition2" is true
  // but "condition1" was false
} else {
  // code that is executed otherwise
}
```
