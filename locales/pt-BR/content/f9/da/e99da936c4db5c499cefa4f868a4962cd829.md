# Introdução

Este exercício continua o trabalho com a pilha de dados e a notação de efeito de pilha que você conheceu em [Cargo Shuffle][cargo-shuffle]. Aqui você vai escrever palavras que combinam valores Boolean.

## Palavras de embaralhamento

As palavras de embaralhamento mais comuns ficam em [`kernel`][kernel]:

```
dup    ( x -- x x )
dupd   ( x y -- x x y )
drop   ( x -- )
swap   ( x y -- y x )
swapd  ( x y z -- y x z )
over   ( x y -- x y x )
pick   ( x y z -- x y z x )
rot    ( x y z -- y z x )
-rot   ( x y z -- z x y )
rotd   ( w x y z -- w y z x )
spin   ( x y z -- z y x )
nip    ( x y -- y )
tuck   ( x y -- y x y )

2dup   ( x y -- x y x y )
3dup   ( x y z -- x y z x y z )
4dup   ( w x y z -- w x y z w x y z )
2drop  ( x y -- )
3drop  ( x y z -- )
4drop  ( w x y z -- )
2nip   ( x y z -- z )
2swap  ( x y z w -- z w x y )
```

## Boolean

Os dois literais Boolean são `t` e `f`. `f` é o único valor falsy do Factor: tudo o mais, incluindo `0` e coleções vazias, é truthy.

```factor
t .    ! => t
f .    ! => f
```

## Palavras Boolean

Estas três palavras Boolean do vocabulário `kernel`:

```
and ( x y -- ? )    ! true when both inputs are truthy
or  ( x y -- ? )    ! true when either input is truthy
not ( x   -- ? )    ! inverts its argument
```

```factor
t t and .    ! => t
t f and .    ! => f
f t or .     ! => t
t not .      ! => f
```

O `?` em cada saída é a convenção da notação de efeito de pilha para "um Boolean".

Você vai precisar de `swap` para uma das tarefas abaixo.

[cargo-shuffle]: https://exercism.org/tracks/factor/exercises/cargo-shuffle
[kernel]: https://docs.factorcode.org/content/vocab-kernel.html
