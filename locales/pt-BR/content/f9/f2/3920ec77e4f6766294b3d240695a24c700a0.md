# defguard com ponto de interrogação

[comment]: # (requires 2 params:)
[comment]: # (- expected: the corrected name)
[comment]: # (- actual: the incorrect name that the student used)

Elixir tem convenções de nomenclatura específicas, que incluem como nomear [verificações que retornam Boolean e são válidas em cláusulas de guarda](https://hexdocs.pm/elixir/naming-conventions.html#is_-prefix-is_foo):

> Verificações de tipo e outras verificações Boolean permitidas em cláusulas de guarda são nomeadas com o prefixo `is_`.

Esse tipo de verificação inclui algumas funções embutidas, algumas macros e guards.

```elixir
# this is the expected name for checks that return booleans and are valid in guard clauses

%{expected}

# this is the current name

%{actual}
```
Repare que funções e macros que retornam Boolean e não são válidos em cláusulas de guarda seguem outra convenção e são nomeados com o sufixo `?` (`def foo?`, `defmacro foo?`). As duas convenções não podem se misturar.
