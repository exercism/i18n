# Debug

Quando um teste falha, uma mensagem é exibida descrevendo o que deu errado e para qual valor de entrada. Você pode inspecionar valores arbitrários do seu programa passando-os para o método `debug`. Isso vai capturar os valores e mostrar a saída.

```ruby
debug "The value is #{value}."
debug "The input is #{input.inspect}"
```
