# send_newsletter reutiliza funções

Para evitar redundância de código, verifique se `send_newsletter/3` chama as funções `open_log/1`, `close_log/1`, `read_emails/1` e `log_sent_email/2` definidas anteriormente.
