# Instruções específicas do SQLite

Este exercício exige que você defina a coluna `result` da tabela `roman-numerals` com o valor correto, com base no número encontrado na coluna `number`.

## Esquema da tabela

```sql
CREATE TABLE "roman-numerals" ("number" INT, "result" TEXT);
```
