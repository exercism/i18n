# Dicas

## 1. Defina o tipo opaco `TreasureChest`.

- Tipos opacos são definidos com a palavra-chave `opaque`.

## 2. Defina a função `create`.

- A função [`string.length`][length] pode ser usada para obter o comprimento de uma string.

## 3. Defina a função `open`.

- Você pode usar o operador `==` para comparar duas strings e verificar se elas são iguais.

[length]: https://hexdocs.pm/gleam_stdlib/gleam/string.html#length
