# Use `with` em `get_new_passport`

Este exercício ensina a "seguir o caminho feliz" com a forma especial `with`, que permite lidar com os erros no final. Use `with` na sua implementação de `get_new_passport/3`.
