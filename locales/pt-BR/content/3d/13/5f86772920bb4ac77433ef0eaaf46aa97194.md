# Introdução

Strings são úteis para guardar dados que podem ser representados na forma de texto. Algumas das operações mais usadas com strings são verificar o `length`, construir e concatenar strings com os operadores de string `+` e `+=`, verificar a existência ou a posição de substrings com o método `indexOf()` ou extrair substrings com o método `substring()`.
