# argumentos splat

Defina uma variável `name` explícita em vez de usar a palavra-chave de argumento splat (`*`). Isso deixa o código mais claro de ler e exige menos manipulação para acessar o valor do argumento.
