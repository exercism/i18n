# Use `rand.uniform` em vez de `Enum.random`

Use `:rand.uniform` em vez de `Enum.random` na função `random_stardate`. O `:rand.uniform` permite gerar um número de ponto flutuante aleatório diretamente, ao contrário do `Enum.random`, que só permite gerar números inteiros aleatórios.
