# Introdução

Um método é uma função com um argumento especial chamado _receptor_.
O receptor aparece na própria lista de argumentos, entre a palavra-chave `func` e o nome do método.

```go
func (receiver type) MethodName(parameters) (returnTypes) {

}
```

Você só pode definir um método com um receptor cujo tipo esteja definido no mesmo pacote do método.

```go
type Person struct {
    Name string
}

func (p Person) Greetings() string {
	return fmt.Sprintf("Welcome %s!", p.Name)
}

s := Person{Name: "Bronson"}
fmt.Println(s.Greetings())
// Output: Welcome Bronson!
```

Existem dois tipos de receptor: os receptores de valor e os receptores de ponteiro.

Métodos com receptor de valor operam sobre uma cópia do valor recebido. Isso significa que qualquer modificação feita ao receptor dentro do método não é visível para quem chama.

Você pode declarar métodos com receptores de ponteiro para modificar o valor para o qual o receptor aponta.
Isso é feito colocando um `*` antes do nome do tipo. Por exemplo, com o tipo `rect`, um receptor de ponteiro seria declarado como `*rect`.
Essas modificações também são visíveis para quem chama o método.

```go
type rect struct {
	width, height int
}
func (r *rect) squareIt() {
	r.height = r.width
}

r := rect{width: 10, height: 20}
fmt.Printf("Width: %d, Height: %d\n", r.width, r.height)
// Output: Width: 10, Height: 20

r.squareIt()
fmt.Printf("Width: %d, Height: %d\n", r.width, r.height)
// Output: Width: 10, Height: 10

```
