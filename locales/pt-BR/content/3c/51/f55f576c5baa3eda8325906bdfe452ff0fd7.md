# otimizar o valor padrão

Em vez de usar uma condicional para ramificar caso não seja passado nenhum valor para `twoFer`, defina o [valor padrão](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Default_parameters) de `%{parameter}` como `'you'` para evitar essa condicional.
