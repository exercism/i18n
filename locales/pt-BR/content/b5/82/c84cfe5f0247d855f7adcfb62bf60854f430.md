# Sobre

A estrutura de controle `foreach` oferece uma maneira prática de iterar sobre _arrays_ e _objetos_. O `foreach` tem duas formas básicas:

```php
foreach (iterable as $value) {
  // statements
}

foreach (iterable as $key => $value) {
  // statements
}
```
