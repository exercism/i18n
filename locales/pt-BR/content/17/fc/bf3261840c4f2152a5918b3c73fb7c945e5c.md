# reutilização de funções

Para evitar redundância de código, certifique-se de que `ranks/0` chame o `rank_range/0` definido anteriormente e que `files/0` chame o `file_range/0` definido anteriormente.
