# Reutilize o código de Describe com três parâmetros

O método `describe(Character, Destination, TravelMethod)` deve reutilizar a lógica implementada em `describe(Character)`, `describe(Destination)` e `describe(TravelMethod)`. Reutilizar métodos existentes pode ajudar a tornar o código mais fácil de manter.
