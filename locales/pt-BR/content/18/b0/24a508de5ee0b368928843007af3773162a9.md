# Sobre

A torre numérica do Factor faz a promoção automática entre valores inteiros, racionais e de ponto flutuante. `1 2 /` avalia para o racional *exato* `1/2`: sem truncamento, sem conversão forçada para ponto flutuante. Para forçar um resultado de ponto flutuante, use `1.0 2 /`.

```factor
3 4 + .         ! => 7
1 2 / .         ! => 1/2
1 2 / 4 * .     ! => 2
2.5 floor .     ! => 2.0
2.5 >integer .  ! => 2
```

A maior parte da aritmética fica em [`math`][math]: `+`, `-`, `*`, `/`, `mod`,
`/i`, `/mod`, `gcd`, `abs`, `neg`, `sq`, `^`. Predicados: `zero?`,
`even?`, `odd?`, `positive?`, `negative?`. Comparação: `<`, `<=`,
`>`, `>=`, `=`. De [`math.order`][math.order]: `min`, `max`,
`between?`. De [`math.functions`][math.functions]: `floor`,
`ceiling`, `round`, `divisor?`. De [`math.primes`][math.primes]:
`prime?`. Construtores e conversões como `>integer`, `>float`,
`>fraction` estão por perto.

[math]: https://docs.factorcode.org/content/vocab-math.html
[math.order]: https://docs.factorcode.org/content/vocab-math.order.html
[math.functions]: https://docs.factorcode.org/content/vocab-math.functions.html
[math.primes]: https://docs.factorcode.org/content/vocab-math.primes.html
