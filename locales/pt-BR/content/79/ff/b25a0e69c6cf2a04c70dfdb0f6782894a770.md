# não normalize as entradas

Remova a chamada a `.toLowerCase`. Os testes só fornecem as entradas em
minúsculas, e as cores devem ser definidas em minúsculas. Não há necessidade de
normalizar as entradas manualmente.