# Introdução

Um laço `do-while` é uma alternativa ao laço `while`, com a única diferença de que um laço `do-while` roda pelo menos uma vez:

```haxe
var i = 0;

do {
  trace("Hello");
} while (i < 10);
```
