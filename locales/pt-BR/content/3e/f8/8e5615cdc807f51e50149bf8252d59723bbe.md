# Dicas

## Geral

- Consulte a [referência de `cmath`][cmath-reference] para conhecer as operações e transformações matemáticas comuns que funcionam com `doubles`.

## 1. Calcule a diária a partir de um valor por hora

- Operações aritméticas básicas em que um argumento é um `int` e o outro é um `double` retornam um `double`.

## 2. Calcule um preço com desconto

- Operações aritméticas básicas em que um argumento é um `int` e o outro é um `double` retornam um `double`.

## 3. Calcule o valor mensal a partir de um valor por hora e um desconto

- Existe uma [função no cabeçalho `cmath`][cmath-ceil] para arredondar para cima.

## 4. Calcule o número de dias úteis a partir de um orçamento, um valor por hora e um desconto

- Converter um `double` em um `int` trunca o número na parte decimal.

[cmath-reference]: https://en.cppreference.com/w/cpp/header/cmath
[cmath-ceil]: https://en.cppreference.com/w/cpp/numeric/math/ceil
