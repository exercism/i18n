# prefira template strings

Use uma [template string](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Template_literals)
para interpolar valores dinâmicos, em vez de montar a string manualmente usando
concatenação de strings:

```javascript
"Hello there \${firstName}, I will give you \${calculateInventory()} apples."
```
