# Introdução

Quando você passa uma variável para uma função, a posse dela é transferida, o que deixa a variável original inutilizável.
Isso pode ser um pouco chato quando você acessa um estado mais de uma vez, mas, por sorte, o Cairo tem dois recursos para passar valores sem destruí-los nem movê-los, chamados `references` e `snapshots`.
