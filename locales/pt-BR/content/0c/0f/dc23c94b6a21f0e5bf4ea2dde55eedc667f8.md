# Instruções específicas do SQLite

Este exercício exige que você defina a coluna `result` da tabela `kindergarten-garden` com o valor correto, com base nas colunas `diagram` e `student`.

## Esquema da tabela

```sql
CREATE TABLE "kindergarten-garden" ("diagram" TEXT, "student" TEXT, "result" TEXT);
```
