# use o operador de coalescência nula em vez de verificar se é nulo

Use o [operador de coalescência nula](https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/operators/null-coalescing-operator) para simplificar seu código, em vez de verificar explicitamente se um valor é igual a `null` e depois fazer algo com ele.
