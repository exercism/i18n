# mistura de `runes` e `bytes`

Usar uma mistura de `runes` e `bytes` é seguro aqui, porque a entrada usa apenas caracteres ASCII. 
Para ver o que acontece quando isso não é seguro, tente adicionar um teste que use caracteres não ASCII.
