# Casos ignorados em assignTicketTo

Quando vários padrões de `case` levam ao mesmo valor, como acontece com os status dos tickets em `assignTicketTo`, é comum deixá-los por último e agrupá-los com o curinga `_`.
