# assinatura não é a ideal

Olhando os testes, a função `gigasecond(...)` recebe apenas um
parâmetro. Remova os parâmetros adicionais da definição de `gigasecond`,
já que o valor desses parâmetros extras sempre será `undefined`, ou
será um valor padrão como está definido atualmente na assinatura.
