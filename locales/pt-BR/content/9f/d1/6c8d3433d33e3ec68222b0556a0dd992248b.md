# Dicas

## Geral

- Fique atento a quaisquer enumeradores que encontrar.
- Os testes só podem ser executados quando você tiver implementado todas as tarefas.
  Para ver os resultados dos testes, você só precisa de um esqueleto básico das funções e das classes.
- Toda função, classe e enumeração usada pelos testes deve ser declarada no arquivo de cabeçalho.

## 1. Tente extrair as primeiras pistas

- A classe e o enum estão em namespaces diferentes.
- Use `class enum` para `System`
- Você pode ter dois construtores, ou um construtor com um argumento padrão.

## 2. Encontre mais detalhes

- A primeira linha `REQUIRE` verifica o `System` padrão.
- `generation` parece aumentar em um em uma nave clonada.
- O `System` parece não ser afetado pelo processo de clonagem.

## 3. Investigue o funcionamento interno

- A função membro `shoot_buster` parece depender de um contador interno.
