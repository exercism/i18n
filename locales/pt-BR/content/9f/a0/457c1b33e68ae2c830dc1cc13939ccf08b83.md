# Introdução

O JavaScript tem um operador de incremento e um de decremento.
Eles modificam diretamente o valor de uma variável.
`++` soma um a um número, `--` subtrai um de um número.

```javascript
let i = 3;
i++;
// i is now 4

let j = 0;
j--;
// j is now -1
```
