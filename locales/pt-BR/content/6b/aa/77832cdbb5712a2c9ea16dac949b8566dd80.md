# Introdução

Para testar se duas variáveis de tipos simples (strings, inteiros etc.) são iguais, usamos os operadores de igualdade `==` e `!=`:

```haxe
var a = 5;
var b = 5;

a == b; // true
a != b; // false
```
