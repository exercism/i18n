# Meilleur choix de fonction pour `increase_quantity`

Dans la fonction `increase_quantity`, utilise soit la fonction `Map.new/2`, soit la fonction `Enum.into/3`. Ce sont les fonctions les plus adaptées à cette tâche et elles ne nécessitent pas d'utiliser la fonction `Enum.map/2`.
