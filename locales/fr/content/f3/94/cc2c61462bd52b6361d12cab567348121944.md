# signature non optimale

Si l'on regarde les tests, la fonction `gigasecond(...)` ne prend qu'un seul paramètre. Supprime les paramètres supplémentaires de la définition de `gigasecond`, car la valeur de ces paramètres sera toujours `undefined`, ou correspondra à la valeur par défaut définie actuellement dans la signature.
