# Introduction

En C#, l'aléatoire s'obtient grâce à `System.Random`. En général, on crée une instance, puis on appelle l'une de ses méthodes `Next()` ou `NextDouble()`, éventuellement plusieurs fois selon le cas d'usage.
