# Utilisation de strings.Builder pour la concaténation

Utiliser `strings.Builder` est excessif pour un cas d'usage aussi simple.
Utilise plutôt `fmt.Sprintf` ou une simple concaténation avec l'opérateur `+`.
