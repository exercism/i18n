# Ajout aux instructions

## Implémentation

Si un caractère ou un codon invalide est rencontré _pendant_ la traduction, il faut `throw` une erreur avec le message `Invalid codon`.

```javascript
translate('AAA');
// => Error: Invalid codon
```
