# utilise les opérateurs booléens

Les fonctions `canFastAttack`, `canSpy`, `canSignalPrisoner` et `canFreePrisoner` peuvent toutes se résoudre en utilisant exclusivement `not`, `&&` et `||`.
Pour apprendre à manipuler les booléens, veille à résoudre l'exercice de cette façon.
