# Minimise les conversions en tranche de runes

Préfère ne convertir `%{name}` en `[]rune` qu'une seule fois, en stockant le résultat dans une variable et en le réutilisant. 
Cette conversion est une opération relativement coûteuse.
