# Introduction

En C#, les booléens sont représentés par le type `bool`, dont les valeurs peuvent être `true` ou `false`.

C# prend en charge trois opérateurs booléens : `!` (PAS), `&&` (ET) et `||` (OU).
