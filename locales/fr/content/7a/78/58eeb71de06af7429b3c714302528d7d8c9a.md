# Introduction

Les **expressions** `throw` sont une alternative aux _instructions_ `throw` et permettent notamment de renforcer la puissance des expressions ternaires et d'autres expressions composées.

```haxe
trace(true ? throw "This is an error" : "Everything is fine"); // ERROR: This is an error
```
