# Utilise `rand.uniform` plutôt que `Enum.random`

Utilise `:rand.uniform` plutôt que `Enum.random` dans la fonction `random_stardate`. `:rand.uniform` te permet de générer directement un nombre décimal aléatoire, contrairement à `Enum.random` qui ne permet que de générer des nombres entiers aléatoires.
