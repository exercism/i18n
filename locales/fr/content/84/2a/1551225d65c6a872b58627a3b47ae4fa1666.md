# Tests

On utilise npm pour exécuter les tests. Suis les instructions [ici](https://www.npmjs.com/get-npm) pour installer npm.

Une fois que tu as récupéré un exercice avec ```exercism fetch reasonml```