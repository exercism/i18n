# Ajout aux instructions

## Implémentation

Comme Uiua n'a pas de `null`, on a remplacé les valeurs `null` par des valeurs `∞` (`infinity`).
Tu dois donc aplatir le tableau _et_ supprimer les valeurs `∞`.
