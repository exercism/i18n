# Instructions supplémentaires

## Mise en œuvre

Si `n` n'est pas un entier strictement positif, empêche le programme de poursuivre son exécution et renvoie un message d'erreur.

En JavaScript, on peut le faire avec l'instruction `throw`.

```javascript
throw new Error('Only positive integers are allowed');
```
