# Ajout aux instructions

## Tests bonus

Active les tests avec les grappes de graphèmes en décommentant (ou en définissant) `enable_grapheme_clusters <- TRUE`.

Par exemple :

- Transforme `"Würstchenstand"` en `"dnatsnehctsrüW"`.

La bibliothèque `stringr` peut t'être utile.
