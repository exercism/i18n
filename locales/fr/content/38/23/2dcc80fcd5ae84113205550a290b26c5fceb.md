# Instructions spécifiques à SQLite

Cet exercice te demande de renseigner la colonne `result` de la table `roman-numerals` avec la valeur correcte, en te basant sur le nombre indiqué dans la colonne `number`.

## Schéma de la table

```sql
CREATE TABLE "roman-numerals" ("number" INT, "result" TEXT);
```
