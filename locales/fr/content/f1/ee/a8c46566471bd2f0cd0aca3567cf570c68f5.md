# Complément d'instructions

## Notes spécifiques au parcours

En Scheme, une façon d'obtenir des arguments optionnels consiste à passer les arguments sous forme de liste.
Deux façons de faire cela : `(define (two-fer . args) ...)` ou `(define two-fer (lambda args ...))`.
