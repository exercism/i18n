# À propos

La structure de contrôle `foreach` offre un moyen pratique de parcourir des _tableaux_ et des _objets_. `foreach` prend deux formes de base :

```php
foreach (iterable as $value) {
  // statements
}

foreach (iterable as $key => $value) {
  // statements
}
```
