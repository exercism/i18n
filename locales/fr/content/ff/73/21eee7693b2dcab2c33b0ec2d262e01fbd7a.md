# Ajout aux instructions

## Implémentation

Les tests ignorés vers la fin du fichier `anagram_test.php` sont des **objectifs bonus**, ils sont facultatifs.
Ils nécessitent l'utilisation des fonctions `mb_string`, qui ne sont pas installées par défaut avec toutes les versions de PHP.
