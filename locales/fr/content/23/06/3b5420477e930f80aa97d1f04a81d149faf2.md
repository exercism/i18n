En Chapel, si la fonction peut lever une erreur, il faut l'indiquer dans la définition de la fonction avec le mot-clé `throws`, comme le montre le modèle suivant.

```chapel
proc canThrow(n: int) throws {
  // function body here 
}
```
