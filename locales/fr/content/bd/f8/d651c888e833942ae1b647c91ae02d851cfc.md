# Indices

## Général

- Une [valeur entière][integers] peut être définie comme un ou plusieurs chiffres consécutifs.
- Si tu vois une erreur `panic` en lançant les tests, c'est que tu n'as pas implémenté une des fonctions (le message devrait indiquer laquelle) ou que tu as laissé le code d'amorçage en place.
  Tu dois supprimer la ligne `panic!(...)` du code fourni et la remplacer par une véritable implémentation.

## 1. Définis le temps de cuisson attendu en minutes

- Tu dois définir une [fonction][functions] sans aucun paramètre.
- Tu dois [renvoyer][return] le temps de cuisson attendu en minutes depuis la fonction.

## 2. Calcule le temps de cuisson restant en minutes

- Tu dois définir une [fonction][functions] avec un seul paramètre.
- Le paramètre de la fonction est un [entier][integers].
- Tu dois [renvoyer explicitement un entier][return] depuis une fonction.
- Tu peux [appeler][functions] une des autres fonctions que tu as définies précédemment.
- Tu peux utiliser l'[opérateur mathématique de soustraction][operators] pour soustraire des valeurs.

## 3. Calcule le temps de préparation en minutes

- Tu dois définir une [fonction][functions] avec un seul paramètre.
- Le paramètre de la fonction est un [entier][integers].
- Tu dois [renvoyer explicitement un entier][return] depuis une fonction.
- Tu peux utiliser l'[opérateur mathématique de multiplication][operators] pour multiplier des valeurs.

## 4. Calcule le temps de travail écoulé en minutes

- Tu dois définir une [fonction][functions] avec deux paramètres.
- Le paramètre de la fonction est un [entier][integers].
- Tu dois [renvoyer explicitement un entier][return] depuis une fonction.
- Tu peux [appeler][functions] une des autres fonctions que tu as définies précédemment.
- Tu peux utiliser l'[opérateur mathématique d'addition][operators] pour additionner des valeurs.

[functions]: https://book.cairo-lang.org/ch02-03-functions.html
[return]: https://book.cairo-lang.org/ch02-03-functions.html#functions-with-return-values
[operators]: https://book.cairo-lang.org/appendix-02-operators-and-symbols.html
[integers]: https://book.cairo-lang.org/ch02-02-data-types.html#integer-types
