# Introduction

En F#, les booléens sont représentés par le type `bool`, dont les valeurs peuvent être soit `true` soit `false`.

F# prend en charge trois opérateurs booléens : `not` (pas), `&&` (et) et `||` (ou).
