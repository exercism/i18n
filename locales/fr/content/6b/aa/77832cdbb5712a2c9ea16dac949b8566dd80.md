# Introduction

Pour tester l'égalité de deux variables de types simples (_strings_, nombres entiers, etc.), on utilise les opérateurs d'égalité `==` et `!=` :

```haxe
var a = 5;
var b = 5;

a == b; // true
a != b; // false
```
