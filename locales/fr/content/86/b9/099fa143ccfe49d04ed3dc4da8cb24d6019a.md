# utilise la récursion

Comme le mentionnent les instructions, tu dois implémenter `keep/2` et `discard/2` sans utiliser `Enum.filter/2` ni `Enum.reject/2`. Évite aussi leurs équivalents `Stream` et les compréhensions de listes.
