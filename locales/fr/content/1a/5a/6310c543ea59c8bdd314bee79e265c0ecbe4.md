# Introduction

Les _typedefs_ permettent de raccourcir les noms de types complexes :

```haxe
typedef IA = Array<Int>;
```

On peut aussi utiliser des _typedefs_ pour créer une structure de type :

```haxe
typedef User = {
  var name:String;
  var age:Int;
}
```
