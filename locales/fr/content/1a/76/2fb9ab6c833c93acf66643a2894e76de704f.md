# Indices

## Général

- [Le Cairo Book : le type tuple][book-tuples]
- [Starknet By Example : les tuples][sbe-tuples]

## 1. Écris une fonction `divmod` qui renvoie le quotient et le reste d'une division

- Pas besoin d'optimiser pour l'efficacité ; l'implémentation naïve suffit

## 2. Écris un adaptateur d'itérateur `evens` qui renvoie les éléments pairs d'un itérateur quelconque

- Il suffit d'enchaîner les méthodes suggérées et tout fonctionnera
- Un nombre `n` est pair si `n % 2 == 0`
- Une fermeture est une fonction à la syntaxe abrégée : le ou les noms d'arguments se placent entre une paire de symboles `|`, et l'expression suit.
  Contrairement aux fonctions normales, il n'est pas toujours nécessaire de préciser explicitement le type de chaque argument, seulement son nom.
  Par exemple, voici comment construire un itérateur de carrés impairs : `(0..).map(|n| 2 * n + 1).map(|n| n * n)`.

## 3. Implémente une méthode `manhattan` sur une structure tuple `Position`

- Pas besoin de te soucier de la syntaxe des méthodes ; remplacer la partie `todo` dans le bloc `impl Position` fera ce qu'il faut
- N'oublie pas que certaines valeurs d'une `Position` peuvent être négatives, mais qu'une distance ne l'est jamais
- Le calcul de la valeur absolue sera un jour pris en charge nativement en Cairo, et en attendant on a mis à disposition un trait d'assistance que tu peux utiliser

[book-tuples]: https://book.cairo-lang.org/ch02-02-data-types.html?highlight=tuple#the-tuple-type
[sbe-tuples]: https://starknet-by-example.voyager.online/getting-started/cairo_cheatsheet/tuples.html
