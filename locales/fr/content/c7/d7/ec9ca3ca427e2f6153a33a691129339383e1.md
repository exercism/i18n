# Erreur capturée sans source

Fichier source attendu `%{expected}`, trouvé : `%{available}`.

Le fichier de test fait un `import` depuis le fichier source attendu. 
Assure-toi que le fichier existe et qu'il est correctement nommé.
