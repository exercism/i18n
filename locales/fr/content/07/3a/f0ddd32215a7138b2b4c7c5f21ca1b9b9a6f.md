# À propos

Il existe essentiellement deux types de boucles :

1. Boucler jusqu'à ce qu'une condition soit remplie.
2. Parcourir les éléments d'une collection.

Les deux sont [possibles en R][web-loops], même si la première est sans doute la plus courante.

## `while` et `repeat`

Pour les problèmes ouverts, où le nombre de tours de boucle n'est pas connu à l'avance, R dispose de la boucle [`while`][ref-control] avec une condition, et de la boucle [`repeat`][ref-control] sans condition (équivalent à `while (TRUE)`).

La forme de base est assez simple :

```R
while (condition) {
  do_something()
}
```

Dans ce cas, le programme répète la boucle jusqu'à ce que `condition` ne soit plus `TRUE`.

Il existe deux façons de sortir prématurément de la boucle :

- Un `break` fait sortir de la boucle, l'exécution se poursuivant à la ligne suivante, après l'accolade fermante `}` de la boucle.
- Un `return(x)` arrête l'exécution de la fonction courante et renvoie la valeur de retour `x` à l'appelant.

Avec ces options à disposition, il peut parfois être pratique de créer une boucle « infinie » avec `repeat { ... }`, puis de compter sur une condition d'arrêt trouvée dans le corps de la boucle pour déclencher un `break` ou un `return`.

Ces trois variantes sont équivalentes et se terminent toutes avec `x == 0.4444...`

```R
x <- 12
while (x > 1) {
  x <- x / 3
}

x <- 12
while (TRUE) {
  x <- x/3
  if (x <= 1) break
}

x <- 12
# no boolean clause 
repeat { 
  x <- x/3
  if (x <= 1) break
}
```

## Parcourir un vecteur (ou une autre collection)

C'est souvent inutile en R.
Comme nous l'avons vu dans d'autres concepts, de nombreuses fonctions opèrent sur des vecteurs entiers.

Cependant, les boucles explicites sont parfois inévitables.
C'est particulièrement vrai lorsque :

- Le corps de la boucle a des effets de bord, comme l'affichage ou les entrées-sorties de fichiers.
- La boucle se poursuit jusqu'à ce qu'un critère soit atteint, après un nombre imprévisible d'itérations.

La condition de la boucle doit être entre parenthèses.
Comme pour le corps des fonctions, les accolades sont facultatives (mais souvent présentes) pour un corps de boucle sur une seule ligne.

```R
words <- c("This", "is", "a", "loop")
for (w in words) { print(w) } # the braces are optional here
#> [1] "This"
#> [1] "is"
#> [1] "a"
#> [1] "loop"
```

Si l'itération en cours ne satisfait pas une condition, il est possible de passer immédiatement à l'itération suivante avec un `next` :

```R
for (n in 1:5) {
  if (n %% 2 == 0) next
  print(n)
}
#> [1] 1
#> [1] 3
#> [1] 5
```

Si l'indice numérique est nécessaire, utilise [`seq_along()`][ref-seq_along].

```R
v <- LETTERS[1:3]
v
#> [1] "A" "B" "C"

for (i in seq_along(v)) { print(sprintf("%s%i", v[i], i)) }
#> [1] "A1"
#> [1] "B2"
#> [1] "C3"
```

Utiliser `i in 1:length(v)` n'est _pas_ recommandé, car cela peut poser problème quand `length(v)` vaut zéro.
L'intervalle `1:0` est équivalent à `c(1, 0)`, donc le corps de la boucle s'exécutera et échouera probablement.
La fonction `seq_along()` est conçue pour gérer correctement ce cas.

[web-loops]: https://intro2r.com/loops.html
[ref-control]: https://www.rdocumentation.org/packages/base/versions/3.3.0/topics/Control
[ref-seq_along]: https://www.rdocumentation.org/packages/base/versions/3.3.0/topics/seq
