# Tests

Exécute les tests avec :

```bash
$ crystal spec
```

## Tests ignorés

Dans chaque suite de tests, tous les tests sauf le premier sont ignorés.

Une fois qu'un test passe, tu peux réactiver le suivant en remplaçant `pending` par `it`.
