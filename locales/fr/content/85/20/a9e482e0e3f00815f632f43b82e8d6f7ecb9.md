# hamming est sensible à la casse

La distance de hamming est sensible à la casse. Pour vérifier que cette solution respecte cette exigence, ajoute un test qui vérifie que la différence entre `"a"` et `"A"` vaut `1`.
