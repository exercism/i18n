# Indices

## Général

- Les entiers de 8 bits sont renvoyés dans `al`.
- Les entiers de 16 bits sont renvoyés dans `ax` et passés dans `di`.
- Si une fonction appelée modifie un registre, il faut sauvegarder toute valeur de ce registre qui sera utilisée plus tard.

## 1. Extrais le masque

- Les entiers de 8 bits sont renvoyés dans `al`.
- Les entiers de 16 bits sont renvoyés dans `ax` et passés dans `di`.
- Si une fonction appelée modifie un registre, il faut sauvegarder toute valeur de ce registre qui sera utilisée plus tard.
- Il existe une instruction qui permet de décaler des bits vers la gauche d'un certain nombre de positions.

## 2. Extrais le message

- Les entiers de 8 bits sont renvoyés dans `al`.
- Les entiers de 16 bits sont renvoyés dans `ax` et passés dans `di`.
- Si une fonction appelée modifie un registre, il faut sauvegarder toute valeur de ce registre qui sera utilisée plus tard.
- Les entiers de 8 bits sont renvoyés dans `al`.

## 3. Extrais les bits redondants

- Les entiers de 8 bits sont renvoyés dans `al`.
- Les entiers de 16 bits sont renvoyés dans `ax` et passés dans `di`.
- Si une fonction appelée modifie un registre, il faut sauvegarder toute valeur de ce registre qui sera utilisée plus tard.
- Tu peux utiliser les fonctions `extract_higher_bits` et `extract_lower_bits` définies précédemment.
- Il existe une instruction qui positionne à 1 les bits qui valent 1 dans les deux valeurs.

## 4. Positionne tous les bits du message

- Les entiers de 8 bits sont renvoyés dans `al`.
- Les entiers de 16 bits sont renvoyés dans `ax` et passés dans `di`.
- Si une fonction appelée modifie un registre, il faut sauvegarder toute valeur de ce registre qui sera utilisée plus tard.
- Tu peux utiliser les fonctions `extract_higher_bits` et `extract_lower_bits` définies précédemment.
- Il existe une instruction qui positionne à 1 les bits qui valent 1 dans l'une ou l'autre des deux valeurs, ou dans les deux.

## 5. Fais tourner la clé privée

- Les entiers de 8 bits sont renvoyés dans `al`.
- Les entiers de 16 bits sont renvoyés dans `ax` et passés dans `di`.
- Si une fonction appelée modifie un registre, il faut sauvegarder toute valeur de ce registre qui sera utilisée plus tard.
- Tu peux utiliser les fonctions `extract_higher_bits` et `extract_lower_bits` définies précédemment.
- Un entier constant peut être défini en haut du fichier avec `equ`.
- Il existe une instruction qui permet d'obtenir le nombre de bits à 1 dans un nombre de 16 bits.
- L'instruction [movzx][movzx] permet de convertir un nombre de 8 bits en un nombre de 16 bits, en mettant à zéro tous les bits supplémentaires.
- Il existe une instruction qui permet de faire tourner des bits vers la gauche d'un nombre de positions donné par une valeur de 8 bits dans `cl`.

## 6. Mets en forme la clé privée

- Les entiers de 8 bits sont renvoyés dans `al`.
- Les entiers de 16 bits sont renvoyés dans `ax` et passés dans `di`.
- Si une fonction appelée modifie un registre, il faut sauvegarder toute valeur de ce registre qui sera utilisée plus tard.
- Tu peux utiliser les fonctions `rotate_private_key`, `extract_higher_bits` et `extract_lower_bits` définies précédemment.
- Il existe une instruction qui positionne un bit à 1 s'il vaut 1 dans une seule des deux valeurs, et non dans les deux.
- Il existe une instruction pour inverser tous les bits d'un nombre.

## 7. Termine le déchiffrement

- Les entiers de 8 bits sont renvoyés dans `al`.
- Les entiers de 16 bits sont renvoyés dans `ax` et passés dans `di`.
- Si une fonction appelée modifie un registre, il faut sauvegarder toute valeur de ce registre qui sera utilisée plus tard.
- Tu peux utiliser les fonctions `set_bits` et `format_private_key` définies précédemment.
- L'instruction [movzx][movzx] permet de convertir un nombre de 8 bits en un nombre de 16 bits, en mettant à zéro tous les bits supplémentaires.
- Il existe une instruction qui permet de décaler des bits vers la gauche d'un certain nombre de positions.
- Il existe une instruction qui positionne à 1 les bits qui valent 1 dans l'une ou l'autre des deux valeurs, ou dans les deux.

[movzx]: https://www.felixcloutier.com/x86/movzx
