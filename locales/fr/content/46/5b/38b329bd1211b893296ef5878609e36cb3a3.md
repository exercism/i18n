# Introduction

Une [`string`][ref-string] en Kotlin est une séquence immuable de caractères Unicode.

[`Immutable`][wiki-immutable] signifie que toute opération sur une _string_ doit renvoyer une nouvelle _string_ : la _string_ d'origine ne peut jamais changer.

[`Unicode`][wiki-unicode] signifie que la plupart des systèmes d'écriture du monde peuvent être représentés, mais (contrairement à des langages plus anciens comme le C) il n'existe pas de correspondance 1:1 entre les caractères et les octets.

Une _string_ est entourée de guillemets doubles `" "`.

Certains caractères doivent être échappés : `\\`, ainsi que les caractères non imprimables habituels comme `\t` (tabulation) et `\n` (nouvelle ligne).

```kotlin
val s = "Escape backslash \\."
// Escape backslash \.
```

Les _strings_ multilignes sont entourées de 3 guillemets doubles, et peuvent contenir n'importe quel texte (aucun échappement nécessaire).

```kotlin
val multi = """I'm a
    multi-line
    string with special characters \ \t """
//I'm a
//    multi-line
//    string with special characters  \ \t 
```

Utilise [trimIndent][trimIndent-doc] pour retirer l'indentation commune des lignes.
C'est utile pour mettre en forme la _string_ :

```kotlin
val multi = """
    I'm a
      multi-line
    string""".trimIndent()

//I'm a
//  multi-line
//string
```

Autrement, [trimMargin][trimMargin-doc] permet de préciser un délimiteur.
Chaque ligne de la `String` commence alors après le délimiteur.
Par défaut, le délimiteur est `|`, mais on peut préciser un autre délimiteur en paramètre.
Par exemple :

```kotlin
val multi = """
    |I'm a
    |  multi-line
    |string""".trimMargin()

//I'm a
//  multi-line
//string

val multi2 = """
    start>I'm a
    start>  multi-line
    start>string""".trimMargin("start>")

//I'm a
//  multi-line
//string
```

Les _strings_ peuvent être concaténées avec `+`, mais mieux vaut s'en tenir aux cas courts et simples.
Il existe d'autres options, souvent meilleures.

## String templates

Cela correspond à ce que d'autres langages appellent « interpolation ».

Si une _string_ contient un signe dollar `$` suivi d'un identifiant, ou bien des accolades (`{expression}`) autour d'une expression, ceux-ci sont remplacés respectivement par la valeur ou par le résultat de l'expression.

```kotlin
val x = 42
val st = "x is $x, x squared is {x * x}"
// x is 42, x squared is 1764
```

Les accolades `{ }` sont nécessaires autour des expressions lorsque l'analyse serait sinon ambiguë.

En général, l'utilisation de _string templates_ est une façon plus efficace et plus idiomatique de combiner des _strings_ que d'utiliser `+`.

## Fonctions de chaînes

Kotlin fournit de _nombreuses_ [`functions`][ref-string-functions] pour manipuler les chaînes de caractères.

La plupart du temps, ce sont des [`extensions functions`][ref-extensions] plutôt que des membres de la classe `String`, même si cela change peu la façon dont on les utilise.

~~~~exercism/note
Les pages de [documentation][ref-string-functions] de Kotlin, plutôt complexes, masquent les fonctions d'extension dans la vue par défaut.
Pense à cliquer sur `Members & Extensions` pour déplier cette section.

[ref-string-functions]: https://kotlinlang.org/api/core/kotlin-stdlib/kotlin/-string/
~~~~

Les exemples suivants ne montrent qu'un petit échantillon de ce qui existe :

```kotlin
val str = "Hello World!"

str.length              // => 12 (a property, not a function)
str.elementAt(6)        // => W
str.elementAtOrNull(20) // => null (index out of range)
str.substring(6, 11)    // => "World"
str.substringAfter(" ") // => "World!"

str.lowercase()         // => "hello world!"
str.uppercase()         // => "HELLO WORLD!"

str.startsWith("Hel")   // => true
str.endsWith("xyz")     // => false
str.indexOf("0")        // => 4

str.toCharArray()       // => [H, e, l, l, o,  , W, o, r, l, d, !]
"42".toInt() + 1        // => 43  (parsing; see also toFloat)

"Howdy!  ".trim()       // => "Howdy"
```


[ref-string]: https://kotlinlang.org/docs/strings.html
[wiki-immutable]: https://en.wikipedia.org/wiki/Immutable_object
[wiki-unicode]: https://en.wikipedia.org/wiki/Unicode
[ref-stringbuilder]: https://kotlinlang.org/api/core/kotlin-stdlib/kotlin.text/-string-builder/
[ref-extensions]: https://kotlinlang.org/docs/extensions.html#extensions.md
[ref-string-functions]: https://kotlinlang.org/api/core/kotlin-stdlib/kotlin/-string/
[trimIndent-doc]: https://kotlinlang.org/api/core/kotlin-stdlib/kotlin.text/trim-indent.html
[trimMargin-doc]: https://kotlinlang.org/api/core/kotlin-stdlib/kotlin.text/trim-margin.html
