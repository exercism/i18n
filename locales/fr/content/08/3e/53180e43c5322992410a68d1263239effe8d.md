# À propos

Manipuler du texte est une tâche courante en programmation.
Swift, avec le _framework_ Foundation, propose de nombreuses méthodes et propriétés pratiques pour inspecter, transformer et manipuler les _strings_.

## Découpe des _strings_ avec `components(separatedBy:)`

Pour découper une _string_ en un tableau de sous-chaînes à partir d'un délimiteur, utilise `components(separatedBy:)` (nécessite d'importer Foundation) :

```swift
import Foundation

let sentence = "Hello, world!"
let words = sentence.components(separatedBy: " ")
print(words) // Prints ["Hello,", "world!"]
```

## Vérifie les préfixes et suffixes avec `hasPrefix(_:)` et `hasSuffix(_:)`

Pour vérifier si une _string_ commence ou se termine par une séquence de caractères donnée, utilise `hasPrefix(_:)` et `hasSuffix(_:)` :

```swift
let greeting = "Hello, world!"
print(greeting.hasPrefix("Hello"))  // Prints true
print(greeting.hasSuffix("world!")) // Prints true
```

## Change la casse

Tu peux transformer la casse d'une _string_ à l'aide de ces méthodes et propriétés standard :

- `lowercased()` : renvoie une copie de la _string_ en minuscules.
- `uppercased()` : renvoie une copie de la _string_ en majuscules.
- `capitalized` : renvoie une copie dans laquelle la première lettre de chaque mot est en majuscule (vient de Foundation).

```swift
let greeting = "hello, world!"
print(greeting.lowercased())  // Prints "hello, world!"
print(greeting.uppercased())  // Prints "HELLO, WORLD!"
print(greeting.capitalized)   // Prints "Hello, World!"
```

## Remplace des sous-chaînes avec `replacingOccurrences(of:with:)`

Pour remplacer toutes les occurrences d'une sous-chaîne cible par une autre _string_, utilise `replacingOccurrences(of:with:)` (vient de Foundation) :

```swift
let greeting = "Hello, world!"
let updated = greeting.replacingOccurrences(of: "world", with: "Swift")
print(updated) // Prints "Hello, Swift!"
```

## Assemble des _strings_ avec `joined(separator:)`

La méthode `joined(separator:)` appartient aux collections de _strings_, comme `[String]`.
Elle assemble les éléments en une seule _string_ à l'aide d'un séparateur :

```swift
let words = ["Hello", "world"]
let sentence = words.joined(separator: ", ")
print(sentence) // Prints "Hello, world"
```

[components]: https://developer.apple.com/documentation/foundation/nsstring/components(separatedby:)-238fy
[hasPrefix]: https://developer.apple.com/documentation/foundation/nsstring/hasprefix(_:)
[hasSuffix]: https://developer.apple.com/documentation/foundation/nsstring/hassuffix(_:)
[lowercased]: https://developer.apple.com/documentation/foundation/nsstring/lowercased
[uppercased]: https://developer.apple.com/documentation/foundation/nsstring/uppercased
[capitalized]: https://developer.apple.com/documentation/foundation/nsstring/capitalized
[replacingOccurrences]: https://developer.apple.com/documentation/foundation/nsstring/replacingoccurrences(of:with:)
[joined]: https://developer.apple.com/documentation/swift/array/joined(separator:)-7uber
