# Instructions spécifiques à SQLite

Pour cet exercice, tu dois définir la colonne `result` de la table `color_code` avec la valeur correcte, en fonction de la couleur indiquée dans la colonne `color`.

## Schéma de la table

```sql
CREATE TABLE "color_code" ("color" TEXT, "result" INT);
```
