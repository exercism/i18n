# Utilise un attribut de module

Utilise un [attribut de module](https://elixir-lang.org/getting-started/module-attributes.html#as-constants) dans `add_player/3` et `reset_score/2` comme constante pour le score initial.
