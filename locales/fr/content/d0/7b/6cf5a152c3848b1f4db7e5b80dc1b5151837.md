# Déstructure l'argument de assignTicketTo

Comme les instructions le mentionnent, déstructure l'argument de `assignTicketTo` pour extraire le champ `status` de l'enregistrement du type `Ticket` et utilise la syntaxe `as` pour nommer l'enregistrement.
