# Instructions

Une de tes connaissances, Erin, doit imprimer des milliers de tracts pour plusieurs événements et a besoin de ton aide ! On t'a demandé de créer la mise en page d'un dépliant contenant un en-tête, une date facultative et une liste d'artistes, chacun associé à une icône _unicode_. L'objectif est de `print` ton nouveau dépliant au design génial, puis de le distribuer : pour y arriver, tu auras besoin de toutes tes compétences de formatage de _strings_ en Python.

## 1. Crée la classe avec un en-tête dont la première lettre est en majuscule

La fonction `capitalize_header` doit prendre en entrée un nom d'événement et renvoyer la version de ce nom avec une majuscule initiale.

```python
>>> capitalize_header("fan meetup")
"Fan meetup"
```

## 2. Formate la date

Crée une nouvelle méthode `format_date` qui prend en entrée une liste contenant une date, un mois et une année. Elle affiche la date mise en forme au format `<month_name> <date>, <year>`

```python
>>> convert_date([9, 4, 2020])
>>> leaflet.date
"April 9, 2020"
>>> leaflet.set_date([13, 5, 2030])
>>> leaflet.date
"May 13, 2030"
```

## 3. Affiche les points de code unicode sous forme d'icônes

Quand la méthode `display_icons` est appelée, la liste de points de code unicode qui lui est passée doit être rendue puis renvoyée sous la forme d'une `list` d'_icônes_.

```python
>>> leaflet.display_icons(['\U00000040', '\U0001D70B'])
['@', '𝜋']
```

## 4. Affiche le dépliant terminé

Tu vas maintenant utiliser toutes les fonctions ci-dessus et les combiner dans la fonction `print_leaflet`.
Elle doit prendre en entrée un `event_name`, une liste d'`icons`, une liste d'`authors` et une liste `event_date`.

L'affiche doit respecter la mise en page ci-dessous.

Le dépliant suit une mise en page précise, en forme de rectangle :

- Les première et dernière lignes contiennent 20 astérisques. `"*"`
- Chaque section est séparée par une ligne vide au-dessus et au-dessous d'elle.
- Une ligne vide ne contient qu'un seul astérisque au début et un seul à la fin.
- La première section est l'en-tête du dépliant ; ce titre commence par une majuscule.
- La deuxième section est la date facultative ; si la date n'est pas passée, cette section doit être une ligne vide.
- La troisième et dernière section est la liste des artistes, chaque artiste étant associé à l'icône correspondante.

```python
********************
*                  *
*    'Concert'     *
*                  *
*  June 22, 2020   *
*                  *
* John         🎸  *
* Benjamin     🎤  *
* Max          🎹  *
*                  *
********************
```
