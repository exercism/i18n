# À propos

Pour tester si deux variables de types simples (_strings_, entiers, etc.) sont égales, on utilise les opérateurs d'égalité `==` et `!=` :

```haxe
var a = 5;
var b = 5;

a == b; // true
a != b; // false
```
