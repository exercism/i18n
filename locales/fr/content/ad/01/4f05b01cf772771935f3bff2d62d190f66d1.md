# déstructurer les expressions assignedToDevTeam

Comme le mentionnent les instructions, l'instruction `case` dans `assignedToDevTeam` devrait faire correspondre `assignedTo` jusqu'à son contenu _string_.
