# Introduction

Les booléens en C++ sont représentés par le type `bool`.
Un `bool` vaut soit `true`, soit `false`.

## Opérateurs logiques

C++ prend en charge trois opérateurs booléens : `!` (NON), `&&` (ET) et `||` (OU).
Tu peux aussi utiliser les versions alternatives `not`, `and` et `or`.

```cpp
true || false // => true
true && false // => false
!true // => false
not false // => true
```

## Priorité

Les trois opérateurs booléens ont chacun une _priorité d'opérateur_ différente.
Par conséquent, ils sont évalués dans cet ordre : `!` d'abord, `&&` ensuite, et enfin `||`.
Si tu veux forcer un ordre différent, tu peux entourer une expression booléenne de parenthèses (c'est-à-dire `()`), car les parenthèses ont une priorité encore plus élevée.

```cpp
!true && false   // => false
!(true and false) // => true
```
~~~~exercism/advanced
## Conversion

Si tu utilises `true` ou `false` à un endroit où un nombre est attendu, ils seront convertis en `1` et `0` respectivement.
Si tu utilises un nombre dans une opération booléenne, tout sauf `0` est traité comme `true`, même les valeurs négatives.

```cpp
!true && 0.0   // => false
true + true + false // => 2
```
~~~~
