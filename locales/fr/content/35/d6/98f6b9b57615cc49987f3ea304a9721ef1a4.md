# Introduction

En Haxe, les booléens sont représentés par le type `Bool`, dont les valeurs peuvent être `true` ou `false`.

Haxe prend en charge trois opérateurs booléens : `!` (pas), `&&` (et) et `||` (ou)
