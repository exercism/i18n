# Introduction

## Récursion

Les fonctions récursives sont des fonctions qui s'appellent elles-mêmes.

Une fonction récursive doit avoir au moins un _cas de base_ et au moins un _cas récursif_.

Un _cas de base_ renvoie une valeur sans rappeler la fonction. Un _cas récursif_ rappelle la fonction, en modifiant l'entrée pour qu'elle finisse par correspondre au cas de base.

Très souvent, chaque cas est écrit dans sa propre clause de fonction.

```elixir
# base case
def count([]), do: 0

# recursive case
def count([_head | tail]), do: 1 + count(tail)
```
