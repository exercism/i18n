# Introduction

La bibliothèque standard fournit un enum appelé `Option` pour t'aider à exprimer le fait qu'une valeur peut exister ou non.
Tu le verras désigné sous la forme `Option<T>`, où `T` est le type de la valeur avec laquelle tu travailles.
