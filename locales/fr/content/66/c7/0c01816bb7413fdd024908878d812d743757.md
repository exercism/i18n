# Tests

Exécute les tests avec :

```bash
$ gradlew test
```

> Utilise `gradlew.bat` si tu es sous Windows

## Tests ignorés

Dans les suites de tests, tous les tests sauf le premier sont ignorés.

Une fois qu'un test passe, tu peux activer le suivant en supprimant l'annotation `@Ignore`.
