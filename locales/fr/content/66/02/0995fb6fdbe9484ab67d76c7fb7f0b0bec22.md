# Utilise les `includes` implicites

Utiliser `includes` parcourt le tableau _deux fois_. La performance n'est pas un problème dans cet exercice, mais cette logique est superflue.

**À noter** : lever une `Error` quand une couleur n'existe pas est tout à fait acceptable, et cette pratique ne doit pas être découragée.