# Introduction

De nombreux opérateurs peuvent aussi s'utiliser comme des affectations composées, une notation abrégée qui permet d'écrire `x op= y` à la place de `x = op y` :

```csharp
var features = PhoneFeatures.Call;
features |= PhoneFeatures.Text;
features &= ~PhoneFeatures.Call;
```
