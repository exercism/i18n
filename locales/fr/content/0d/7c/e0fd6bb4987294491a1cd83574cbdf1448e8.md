# Déstructure l'argument de emptyComment

Comme indiqué dans les instructions, déstructure l'argument de `emptyComment` pour extraire le contenu du tuple.
Ignore l'élément `User` avec le joker `_` et conserve la `String`.
