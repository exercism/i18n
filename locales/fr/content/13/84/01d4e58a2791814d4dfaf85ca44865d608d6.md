# absence d'instruction `return` (cas général)

Cette fonction n'utilise pas d'instruction `return` pour renvoyer un résultat.
 Ce code aurait dû échouer sur un ou plusieurs cas de test pendant les tests.

Pense à consulter les résultats des tests et à modifier le code en conséquence.
