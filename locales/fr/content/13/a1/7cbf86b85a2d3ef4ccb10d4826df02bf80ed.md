# `unless` avec un bloc `else`

Évite d'utiliser des expressions `unless` avec un bloc `else`. Utilise plutôt une expression `if`.

La raison n'est pas technique, mais humaine. Dans ce cas, le `else` sera exécuté quand la condition est remplie, soit l'inverse de ce que la formulation laisse penser.
