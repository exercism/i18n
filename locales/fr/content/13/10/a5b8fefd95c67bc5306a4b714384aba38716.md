# utiliser les méthodes `filter` et `count` sur un Stream

Quand tu utilises les Streams pour calculer la distance de Hamming, utilise les méthodes `filter` et `count` du Stream pour rendre le calcul plus clair.
