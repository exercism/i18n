# Utilise `Enum.random`

L'un des objectifs de cet exercice est de découvrir l'aléatoire. Pour t'entraîner, utilise la fonction `Enum.random` dans les fonctions `random_planet_class` et `random_ship_registry_number`.