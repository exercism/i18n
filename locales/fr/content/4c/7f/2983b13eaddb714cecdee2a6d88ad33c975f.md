# Utilise un seul `return`

Cette solution utilise plusieurs instructions `return`, mais il est possible de l'écrire avec un seul `return`.
Pense à utiliser un [opérateur ternaire](https://docs.oracle.com/javase/tutorial/java/nutsandbolts/op2.html) pour gérer le cas où l'entrée est `null`.
