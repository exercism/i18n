# Réutilise le code de Describe three params

La méthode `describe(Character, Destination, TravelMethod)` devrait réutiliser la logique implémentée dans `describe(Character)`, `describe(Destination)` et `describe(TravelMethod)`. Réutiliser des méthodes existantes peut aider à rendre le code plus facile à maintenir.
