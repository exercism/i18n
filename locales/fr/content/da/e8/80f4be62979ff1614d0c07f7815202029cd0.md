# Introduction

Les constantes sont des valeurs définies au moment de la compilation et qui ne peuvent pas être réaffectées.
On utilise le mot-clé `const` pour déclarer une seule constante :

```go
const greeting = "hello"
```

On peut déclarer plusieurs constantes dans un même bloc.
Dans un bloc, une constante dont la valeur n'est pas explicitement définie reprend l'expression précédente :

```go
const (
    greeting    = "hello"
    buffSize    = 4096
    chunkSize   // 4096
)
```

Les constantes sont généralement déclarées au niveau du paquet, même si on peut aussi les déclarer à l'intérieur d'une fonction.

## Constantes typées et non typées

Déclarer une constante sans préciser de type la rend non typée.
Une constante non typée n'a pas de type fixe ; c'est sa valeur qui détermine les types avec lesquels elle est compatible.
Tant que la valeur sous-jacente est compatible, le compilateur la convertit vers le type requis à chaque utilisation :

```go
const numA = 2

math.Sqrt(numA)  // numA is treated as float64 here
var numB int = numA  // numA is treated as int here
```

Une constante typée fixe le type dès la déclaration :

```go
const num int = 2

math.Sqrt(num)  // compile error because a float is expected
```

## Ce qui peut être une constante

Une constante ne peut contenir que des booléens, des nombres, des _strings_ et des _runes_.
Comme les valeurs sont définies au moment de la compilation, le résultat d'un appel de fonction ne peut pas servir à définir une constante.

```go
const s = math.Sqrt(4)  // compile error because the value isn't known at compile time
```

## Iota

Dans un bloc de constantes, `iota` représente la position de la constante dans le bloc :

```go
const (
    Sunday    = iota // 0
    Monday           // 1
    Tuesday          // 2
    Wednesday        // 3
)
```

Chaque constante après la première hérite de la même expression, `iota` étant incrémenté de un.

`iota` fonctionne aussi dans les expressions.

```go
const (
    _  = iota             // position 0: value discarded
    KB = 1 << (10 * iota) // position 1: 1 << 10 = 1024
    MB                    // position 2: 1 << 20 = 1048576
    GB                    // position 3: 1 << 30 = 1073741824
)
```

Pour aller plus loin sur les constantes en Go, voir [The Go Blog][const-blog] et [Effective Go][effective-go].

[const-blog]: https://go.dev/blog/constants
[effective-go]: https://go.dev/doc/effective_go#constants
