# N'utilise pas filter

Comme le mentionnent les instructions, tu dois implémenter `keep` et `discard` sans utiliser `List.filter` ni `List.filterMap`. Tu peux à la place avoir recours à la récursion ou à d'autres fonctions de `List`.
