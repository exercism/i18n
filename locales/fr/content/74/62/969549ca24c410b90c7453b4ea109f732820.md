# Concaténation via fmt

La façon la plus simple d'ajouter une nouvelle _string_ à une _string_ existante est d'utiliser l'opérateur `+=`.
Dans les cas où il y a peu de concaténations, comme dans cet exercice, c'est aussi plus rapide que `%{function}`.
