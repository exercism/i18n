# Pas de type de retour

Précise un type `return` pour tous les membres publics. Inclure le type de retour permet au compilateur de vérifier le type `return` attendu. Cela garantit aussi qu'une évolution de l'inférence de types de Scala préserve la compatibilité binaire.
