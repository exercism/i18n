# Introduction

## Le hasard

En C#, le hasard s'obtient grâce à `System.Random`. En général, on crée une instance, puis on appelle l'une de ses méthodes `Next()` ou `NextDouble()`, éventuellement plusieurs fois selon l'usage que l'on en fait.
