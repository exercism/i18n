# aucune valeur renvoyée

La fonction `${export.name}` ne semble pas renvoyer de valeur via `return` dans tous les cas.

Es-tu certain que les tests passent ?

Si c'est le cas, ouvre une issue ici (**Incorrect Analysis**) et fournis ta solution :

<https://github.com/exercism/javascript-analyzer/issues/new/choose>
