# strings join utilisé pour la concaténation

N'utilise pas `strings.Join` si les données ne sont pas de type `[]string`.
Utilise plutôt `fmt.Sprintf`. C'est un moyen très puissant de construire des _strings_ à partir de plusieurs éléments.
