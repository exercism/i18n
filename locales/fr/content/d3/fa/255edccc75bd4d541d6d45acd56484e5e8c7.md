# Installation

## Windows (recommandé)

Tu utilises un système d'exploitation Windows ?

Appuie sur `Windows Key`, tape `cmd`, puis appuie sur `enter`.

> Batch est déjà installé sur ton système si ton système d'exploitation est Windows.

Si tu utilises un autre système d'exploitation, tu peux installer Batch en suivant les instructions ci-dessous.

## Mac OS

1. Gatekeeper ne doit pas être configuré pour bloquer les paquets non signés.
2. Ouvre l'application Terminal.
3. Exécute la commande suivante pour installer Batch :

```bash
brew install --cask --no-quarantine wine-stable
```

## Ubuntu

1. Si ton système est 64 bits, active l'architecture 32 bits (si ce n'est pas déjà fait) :

```bash
sudo dpkg --add-architecture i386
```

2. Ajoute le dépôt WineHQ :

```bash
sudo mkdir -pm755 /etc/apt/keyrings
sudo wget -O /etc/apt/keyrings/winehq-archive.key https://dl.winehq.org/wine-builds/winehq.key
sudo apt update
```

3. Installe WineHQ :

```bash
sudo apt install --install-recommends winehq-stable
```

## Debian

1. Si ton système est 64 bits, active l'architecture 32 bits (si ce n'est pas déjà fait) :

```bash
sudo dpkg --add-architecture i386
```

2. Ajoute le dépôt WineHQ :

```bash
sudo mkdir -pm755 /etc/apt/keyrings
sudo wget -O /etc/apt/keyrings/winehq-archive.key https://dl.winehq.org/wine-builds/winehq.key
sudo apt update
```

3. Installe WineHQ :

```bash
sudo apt install --install-recommends winehq-stable
```

## Fedora

1. Active le dépôt Wine :

```bash
# Fedore 39
dnf config-manager --add-repo https://dl.winehq.org/wine-builds/fedora/39/winehq.repo

# Fedora 40
dnf config-manager --add-repo https://dl.winehq.org/wine-builds/fedora/40/winehq.repo
```

2. Installe WineHQ :

```bash
dnf install winehq-stable
```
