# avertissement Pylint

**La ligne %{lineno} [_%{code}_]** a été signalée par Pylint :

%{message}.

Le code contient un problème qui pourrait mener à un bug ou à une erreur dans le programme.
Bien que cette erreur ne soit peut-être pas _grave_, elle pourrait entraîner des problèmes plus graves à l'avenir.
Il est recommandé de régler le problème avant d'aller plus loin.

%{bad_code}
%{good_code}
%{related_info}
%{details}
