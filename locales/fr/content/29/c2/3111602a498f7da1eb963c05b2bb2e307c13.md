# Utilise withDefault

Le filtrage par motif est une façon tout à fait valable de résoudre l'exercice, mais pour en apprendre plus sur le module `Maybe`, essaie d'écrire `introduce` en utilisant `Maybe.withDefault`.
