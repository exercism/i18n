# log_sent_email renvoie implicitement

La fonction `log_sent_email/1` ne devrait pas renvoyer `:ok` explicitement, mais renvoyer implicitement ce que renvoie `IO.puts` (qui se trouve être `:ok` quand tout se passe bien).
