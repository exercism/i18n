# Ajout aux instructions

## Implémentation

Tu peux utiliser `collect(1:10)` comme moyen rapide de créer une collection, mais garde en tête que sous le capot, `collect` renvoie un tableau, donc tu peux aussi simplement créer un tableau explicitement avec `my_array = [1, 2, 3, 4, 5]`.
