# Utilise `MapSet.union` et `MapSet.size`

Utilise `MapSet.union` et `MapSet.size` dans `total_cards`. Il existe d'autres façons idiomatiques de définir la fonction, mais ce sont les plus adaptées pour apprendre pleinement le concept de cet exercice.