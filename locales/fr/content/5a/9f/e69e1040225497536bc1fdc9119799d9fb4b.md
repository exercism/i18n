# Introduction

En Haxe, pour générer un nombre aléatoire, on peut utiliser la méthode `Std.random(x)`. Elle génère un nombre aléatoire compris entre 0 inclus et `x` exclu :

```haxe
public function main() {
  trace(Std.random(10)); // 0...9
}
```
