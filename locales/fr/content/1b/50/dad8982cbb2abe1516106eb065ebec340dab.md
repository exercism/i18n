# Utilise `Enum.filter` ou `Enum.reject`

Utilise la fonction `Enum.filter` ou `Enum.reject` dans la fonction `with_missing_price`. Ce sont les fonctions d'`Enum` qui conviennent le mieux pour cette tâche.