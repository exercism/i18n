# Utilise AddSeconds

Essaie d'utiliser la méthode `AddSeconds` intégrée de `DateTime` pour simplifier le code. Note que `AddSeconds` renvoie une nouvelle instance de `DateTime` et ne modifie pas l'instance sur laquelle elle est appelée.
