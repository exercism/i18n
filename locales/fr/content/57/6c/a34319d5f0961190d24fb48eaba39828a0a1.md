# signature non optimale

Si on regarde les tests, la fonction `value` ne reçoit qu'un seul paramètre.
Un second paramètre n'est pas nécessaire : sa valeur sera toujours `undefined` (ou la valeur par défaut que tu as définie).
