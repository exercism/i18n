# Introduction

Une fonction est [récursive terminale][recursion-tc] si la _dernière_ chose exécutée par la fonction est un appel à elle-même.

Chaque fois qu'une fonction est appelée, un _cadre de pile_ contenant ses variables locales et ses arguments est empilé au sommet de [la pile d'appels][call-stack].
Quand une fonction se termine, son cadre de pile est retiré de la pile.

Les fonctions récursives terminales permettent l'[optimisation des appels terminaux][tail-call-optimization] (ou l'élimination des appels terminaux).
C'est une optimisation qui permet de réutiliser le dernier cadre de pile pour l'appel de fonction suivant, quand on est certain que la fonction précédente n'en a plus besoin.
Cela limite le risque de saturation de la pile d'appels, une situation où il y a tellement de cadres sur la pile qu'il ne reste plus assez de mémoire pour en créer un nouveau.

## L'optimisation des appels terminaux en Elm

Sous certaines conditions, le compilateur Elm est capable d'effectuer automatiquement une optimisation des appels terminaux lorsqu'il compile vers JavaScript.

L'optimisation peut avoir lieu pour une fonction récursive lorsque la _dernière_ opération d'une branche consiste à appeler la fonction elle-même dans une simple application de fonction.
Regardons quelques exemples :

```elm
factorial : Int -> Int
factorial n =
  if n <= 1 then
    n
  else
    n * factorial (n-1)
```

L'implémentation ci-dessus n'est pas récursive terminale, car la dernière opération de la branche `else` est une multiplication `n *`.

```elm
factorial : Int -> Int
factorial n =
  factorialHelper n n

factorialHelper : Int -> Int -> Int
factorialHelper n resultSoFar =
  if n <= 1 then
    resultSoFar
  else
    factorialHelper (n-1) (n * resultSoFar)
```

L'implémentation ci-dessus est récursive terminale et sera optimisée, car la dernière opération de la branche `else` est `factorialHelper` qui s'appelle elle-même.
Ce ne serait pas possible pour une fonction dont la signature de type est `Int -> Int`, et en pratique, l'optimisation des appels terminaux est souvent obtenue en définissant des fonctions auxiliaires.

[recursion-tc]: https://en.wikipedia.org/wiki/Tail_call
[call-stack]: https://en.wikipedia.org/wiki/Call_stack
[tail-call-optimization]: https://jfmengels.net/tail-call-optimization/
