# éviter les boucles

Au lieu d'utiliser une boucle, pense à utiliser une seule expression basée sur l'un des opérateurs de comparaison (c'est-à-dire `<=`, `>=`, `<`, `>`, etc.).
Tu peux réutiliser les autres méthodes déjà implémentées dans la classe, ou en ajouter de nouvelles si nécessaire.
