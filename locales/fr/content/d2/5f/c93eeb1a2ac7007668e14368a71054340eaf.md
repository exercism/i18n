# Introduction

Les expressions `throw` sont une alternative aux instructions `throw` et peuvent, en particulier, renforcer la puissance des expressions ternaires et des autres expressions composées.

```csharp
string trimmed = str == null ? throw new ArgumentException() : str.Trim();
```
