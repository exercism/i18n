# close_log renvoie implicitement

La fonction `close_log/1` ne doit pas renvoyer explicitement `:ok`, mais renvoyer implicitement ce que renvoie `File.close` (qui se trouve être `:ok` quand tout se passe bien).
