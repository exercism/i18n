# Importe IO avec :only

Quand on importe des fonctions depuis un module, il est recommandé d'importer une liste explicite, avec l'option `:only`, comme ceci :

```elixir
import IO, only: [inspect: 2, write: 1]
```
