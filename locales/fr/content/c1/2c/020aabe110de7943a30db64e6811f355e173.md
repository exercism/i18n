# Introduction

Comme dans d'autres langages de programmation, les _enums_, abréviation de `enumerations`, servent en Cairo à définir des types de données personnalisés qui regroupent un ensemble fixe de valeurs nommées appelées _variants_.
Chaque _variant_ qui compose un _enum_ est distinct et a une signification précise.
