# send_newsletter renvoie implicitement

La fonction `send_newsletter/3` ne doit pas renvoyer explicitement `:ok`, mais renvoyer implicitement ce que renvoie `close_log/1` (qui se trouve être `:ok` quand tout se passe bien).
