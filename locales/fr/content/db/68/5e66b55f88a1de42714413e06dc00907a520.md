# export en ligne

Les fonctions, comme `value`, les classes et les constantes, comme `COLORS`, peuvent être `export`ées en ligne.

```javascript
export %{constant.signature}

export %{method.signature}
```
