# Introduction

Les nombres en Clojure comprennent :

- Les entiers : nombres sans chiffre après le séparateur décimal (nombres entiers). Par exemple `-6`, `0`, `1`, `25`, `976` et `500000`.
- Les nombres à virgule flottante : nombres avec zéro ou plusieurs chiffres après le séparateur décimal. Par exemple `-2.4`, `0.1`, `3.14`, `16.984025` et `1024.0`.

Deux types numériques courants sont `int` et `float`. Un `int` est un entier de 32 bits et un `float` est un nombre à virgule flottante de 64 bits.

L'arithmétique se fait à l'aide des opérateurs arithmétiques standard. On peut comparer les nombres à l'aide des opérateurs de comparaison numériques standard (`>`, `<`, `<=`, `>=`), de l'opérateur d'égalité (`=`) et de l'opérateur de non-égalité (`not=`).

Dans cet exercice, tu dois exécuter de la logique de manière conditionnelle. Une façon courante de faire cela en Clojure consiste à utiliser `cond` :

```clojure
(cond (= x 5) "Expression to evaluate when x equals 5"
      (> x 7) "Expression to evaluate when x is greater than 7"
      :else   "Expression to evaluate in all other cases")
```

## Note
L'opérateur `==` peut être préférable à `=` dans certains cas où l'on doit comparer des nombres indépendamment de leur type exact. Par exemple,
```clojure
(== 5.0 5) ;; true as both numbers are equal when type is ignored
(= 5.0 5) ;; false as the types of numbers are also taken into account here i.e. float is different from int
```
Le [guide de Clojure sur l'égalité][guide-equality], et plus particulièrement la section sur [Numbers][guide-numbers], entre dans plus de détails.

[guide-equality]: https://clojure.org/guides/equality
[guide-numbers]: https://clojure.org/guides/equality#numbers
