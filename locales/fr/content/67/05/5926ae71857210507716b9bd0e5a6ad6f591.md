## 1. Extrais le message

- Implémente `log-message` avec `subseq`
- Les _strings_ sont indexées à partir de zéro

## 2. Détermine la sévérité

- Implémente `log-severity` avec une expression conditionnelle qui utilise `string=`

## 3. Gère les aléas de la réalité

- `string-equal` est une version de `string=` insensible à la casse

## 4. Reformate le message de log selon la sévérité du log

- Trois fonctions sont mentionnées dans l'introduction pour effectuer les transformations nécessaires.
