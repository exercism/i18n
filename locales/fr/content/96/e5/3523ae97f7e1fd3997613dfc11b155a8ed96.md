# instruction `if` inutile

Au lieu de créer un bloc `if (...) {}`, exploite directement la valeur booléenne que l'on peut renvoyer en vérifiant la valeur du paramètre.

Par exemple, ceci :

```javascript
if (param === true) {
  return true;
}
```

peut devenir :

```javascript
return param === true;
```
