# Introduction

L'API _entry_ de Rust donne une vue sur une entrée unique d'une table associative, qui peut être soit un `HashMap`, soit un `BTreeMap`.
L'entrée peut être soit occupée, soit vacante, et cette API fournit des méthodes pour en modifier le contenu.
