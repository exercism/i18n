# Indices

## 1. Détermine le temps nécessaire pour préparer un jus

- Julia ne possède pas d'instruction `switch`, contrairement à beaucoup d'autres langages.
- La syntaxe `if` ... `elsefif` ... `else` a été abordée dans le concept Conditions.

## 2. Réapprovisionne la réserve de quartiers de citron vert

- Une boucle `while` peut s'exécuter jusqu'à ce que tu aies assez de quartiers de citron vert.
- La fonction `popfirst!()` a été abordée dans le concept Vecteurs.

## 3. Dresse la liste des temps de préparation de chaque commande de la file

- Parcourir un vecteur est simple avec `for` ... `in`.
- Pour construire un nouveau vecteur, on peut utiliser `push!()`, qui a été abordée dans le concept Vecteurs.
- Plus tard dans le programme, on explorera plusieurs autres façons de faire en Julia.

## 4. Termine le service

- Voir la tâche 2.
