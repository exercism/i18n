# erreur personnalisée créée

Une `error` personnalisée sert généralement à fournir un comportement spécifique, ou à pouvoir distinguer les erreurs selon leur type par la suite.
Pour les cas plus simples, on s'appuie le plus souvent sur `errors.New` ou `fmt.Errorf`.
