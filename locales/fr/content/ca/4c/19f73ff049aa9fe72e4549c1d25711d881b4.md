# Mélange de runes et de bytes

Utiliser un mélange de `runes` et de `bytes` est sans risque ici, parce que l'entrée n'utilise que des caractères ASCII.
Pour voir ce qui se passe quand ce n'est pas sans risque, essaie d'ajouter un test qui utilise des caractères non ASCII.
