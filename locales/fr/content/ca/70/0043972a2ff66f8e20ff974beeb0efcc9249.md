# Ajout aux instructions

## Travaille avec les dates en Prolog

La prise en charge des dates intégrée à Prolog est [assez rudimentaire][built-in-dates-support].
Tu peux certes résoudre cet exercice avec ce que Prolog propose de base, mais il est bien plus agréable de manipuler les dates grâce au [paquet date_time][date-time-package].

### Installe le paquet date_time

Si la version de Swipl que tu as installée est suffisamment à jour, tu peux installer le paquet en lançant :

```bash
swipl pack install date_time
```

Si ça ne fonctionne pas, tu peux lancer :

```shell
swipl -g 'pack_install(date_time)'
```

~~~~exercism/note
Si tu utilises l'éditeur en ligne, tu n'as rien besoin d'installer manuellement.
~~~~

### Utilise le paquet date_time

Ajoute le code suivant en haut de ton fichier de solution pour utiliser le paquet `date_time` :

```prolog
:- use_module(library(date_time)).
```

~~~~exercism/note
Astuce : la [source du paquet date_time][date-time-package-source] est bien documentée et pourra t'être d'une grande aide !

[date-time-package-source]: https://www.swi-prolog.org/pack/file_details/date_time/prolog/date_time.pl?show=src
~~~~

[built-in-dates-support]: https://www.swi-prolog.org/pldoc/man?section=timedate
[date-time-package]: https://www.swi-prolog.org/pack/list?p=date_time
