# Sortie dans le else

Si l'opération `IO.write` réussit, le bloc `else` correspond au résultat et renvoie une sortie pour tout le bloc. Utilise le bloc `else` pour renvoyer la sortie de la fonction `write/3`.
