# variables

[comment]: # (Cette erreur est déclenchée quand une variable n'est pas utilisée)
[comment]: # (Certaines variables inutilisées, comme `_`, ne peuvent être utilisées nulle part)
[comment]: # (nécessite 1 paramètre : )
[comment]: # (- definition : extrait de code contenant une valeur inutilisée)

La valeur indiquée ci-dessous n'est utilisée nulle part ou bien est inutilisable.
Tu peux la supprimer ou l'utiliser quelque part.

```
%{definition}
```
