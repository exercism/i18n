# Introduction

Un ensemble est une collection de valeurs uniques. Dans .NET, l'implémentation par défaut d'un ensemble est la classe `HashSet<T>`, une collection qui ne contient que des valeurs uniques.
