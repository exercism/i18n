# fonction de solution introuvable

La suite de tests de cet exercice attend la fonction suivante, définie dans le fichier `src/lib.rs` :
```rust
pub fn reverse(input: &str) -> String {
}
```
La fonction requise n'a pas été trouvée.
