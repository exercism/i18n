# Utilise `with` avec `else` dans `get_new_passport`

Utilise `with` avec un bloc `else` dans l'implémentation de `get_new_passport/3` pour gérer le cas d'échec. Le but de cet exercice est d'apprendre à utiliser la forme spéciale `with`, y compris comment utiliser ses clauses `else`.
