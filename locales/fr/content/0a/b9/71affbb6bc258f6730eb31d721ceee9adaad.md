# Introduction

La valeur `null` en JavaScript représente l'absence intentionnelle d'une valeur d'objet. C'est l'un des types primitifs de JavaScript.

```javascript
// I do not have an apple.
var apple = null;
apple; // => null

// null is treated as falsy for boolean operations, therefore
!apple; // => true
!!apple; // => false
```
