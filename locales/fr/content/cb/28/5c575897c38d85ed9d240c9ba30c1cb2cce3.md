# Instructions supplémentaires

## Les points de code Unicode face aux unités de code

Une _string_ encodée en UTF-16 « normale » peut être représentée comme une série de caractères, où chaque caractère peut faire jusqu'à 16 bits de long (d'où le nom UTF-16).
Cela signifie qu'il y a au maximum 2¹⁶ (deux à la puissance seize), soit 65536 caractères possibles représentables avec 16 bits, soit 1 **unité** de code.
Ces 65536 caractères forment ce qu'on appelle le [plan multilingue de base][basic-multilingual-set], ce qui est largement suffisant pour les caractères les plus courants de la plupart des langues.

Cependant, certains symboles ne tiennent pas dans une seule unité de code. La solution est de les représenter avec deux unités de code.
Ces deux unités de code UTF-16, souvent appelées aussi _surrogate pair_, forment un **point** de code.

Donc, en résumé, quand on parle d'encodage UTF-16 :

- Un `code unit` correspond à 16 bits (ou moins) représentant un seul caractère.
- Un `code point` est une ou deux unités de code qui représentent un seul caractère.

Pour ajouter à la confusion, il y a aussi les _grapheme clusters_,
qui sont en gros des séquences de caractères Unicode (points de code) qu'il faut traiter comme une seule unité visuelle.
Par exemple, certains émojis, comme celui-ci 👨‍👦.

## UTF-16 en JavaScript

La plupart des méthodes JavaScript intégrées fonctionnent avec des _strings_ encodées en UTF-16, mais elles se basent sur les unités de code UTF-16.
Par exemple, la méthode [`String.prototype.split("")`][split] découpe une _string_ en unités de code.

En revanche, les [`String iterators`][iterator] itèrent par points de code.

Tu peux en lire beaucoup plus, et trouver des exemples sur les _strings_ Unicode, sur [MDN][MDN].

[basic-multilingual-set]: https://en.wikipedia.org/wiki/Plane_(Unicode)#Basic_Multilingual_Plane
[split]: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/split
[iterator]: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/@@iterator
[MDN]: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String#utf-16_characters_unicode_code_points_and_grapheme_clusters
