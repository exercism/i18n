# Éviter de renvoyer des littéraux booléens

Au lieu de renvoyer des littéraux booléens après une condition, tu peux directement renvoyer la condition.

Exemple :

```java
// instead of
if (someExpression) {
  return true;
} else {
  return false;
}

// ... return the expression directly
return someExpression;
```
