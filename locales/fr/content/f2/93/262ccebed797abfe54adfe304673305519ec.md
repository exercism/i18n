# export en ligne

Les fonctions, comme `twoFer`, les classes et les constantes peuvent être `export`ées en ligne.

```javascript
// Not inlined
export { twoFer }

// Inlined
export function twoFer() {}
```
