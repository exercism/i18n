# Introduction

Si le code d'une boucle doit toujours être exécuté au moins une fois, on peut utiliser une boucle `do`/`while` :

```csharp
int x = 23;

do
{
    // Always executes at least once, then repeats as long as x > 10
    x = x - 2;
} while (x > 10)
```
