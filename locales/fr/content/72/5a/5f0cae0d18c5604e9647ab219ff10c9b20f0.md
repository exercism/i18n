# utiliser une seule _string_ formatée plutôt que plusieurs

Le code renvoie deux instances de `string` très similaires. Comment le structurer pour éliminer cette duplication ? Cela implique de supprimer tout branchement et de ne renvoyer qu'une seule `string`.
