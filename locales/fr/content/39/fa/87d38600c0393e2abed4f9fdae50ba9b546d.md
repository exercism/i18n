# Introduction

En Common Lisp, les tables de hachage sont des structures de données qui permettent de stocker des données associatives.
Elles associent des clés (qui peuvent être n'importe quel objet) à des valeurs (qui peuvent être n'importe quel objet).
D'autres langages désignent ce concept sous les noms de _maps_, _dictionaries_ ou _hashes_.

## Créer des tables de hachage

On peut créer des tables de hachage avec la fonction `make-hash-table`.

`(make-hash-table) ; => #<HASH-TABLE :TEST EQL :COUNT 0 {10024AD2B3}>`

(À noter que la présentation imprimée d'une table de hachage dépend de l'implémentation, elle peut donc s'afficher différemment dans le REPL.)

## Récupérer des valeurs par clé

Pour récupérer une valeur à partir d'une clé dans la table de hachage, on utilise la fonction `gethash` :

```lisp
*h* ; => #<HASH-TABLE :TEST EQL :COUNT 3 {10024AD2B3}>`
(gethash :foo *h*) ; => :bar
                   ;    T
```

`gethash` renvoie [plusieurs valeurs][concept-multiple-values], ce qui sera expliqué dans un autre concept.

## Insérer des valeurs

Pour insérer une valeur dans une table de hachage, on utilise `setf` avec `gethash`, comme ceci :

`(setf (gethash :foo *hash-table*) :bar) : => :bar`

Cela modifie la valeur associée à la clé `:foo` dans la table de hachage `*hash-table*` pour qu'elle devienne `:bar`.
Cela renvoie la valeur.

L'utilisation de `setf` sera expliquée dans un autre concept.

[concept-multiple-values]: /tracks/common-lisp/concepts/multiple-values
