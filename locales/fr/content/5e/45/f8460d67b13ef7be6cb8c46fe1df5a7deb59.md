# Tests

Pour compiler et exécuter les tests, il suffit de lancer la commande suivante dans le répertoire de l'exercice :

```shell
nim r test_exercise_name.nim
```

Remplace `exercise_name` par le nom de l'exercice (par exemple `all_your_base`).
