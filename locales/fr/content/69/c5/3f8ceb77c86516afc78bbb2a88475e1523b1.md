# Introduction

Un booléen représente l'une de deux valeurs : `true` ou `false`. Les opérateurs logiques (`not`, `&&`, `||`) s'utilisent généralement avec des valeurs booléennes et renvoient une valeur booléenne.
