# Ajout aux instructions

## Contraintes

Tu peux considérer que l'entrée de `isBalanced` ne contient jamais plus de 10 niveaux d'imbrication.

Tu peux tout à fait résoudre cet exercice sans utiliser `allocator`, mais note que la fonction `isBalanced` ne peut pas renvoyer seulement `bool`, car les tests utilisent `try`.
