# Introduction

## Tuples

Un tuple est un groupement _immuable_ de valeurs non nommées mais ordonnées.
Les tuples peuvent contenir n'importe quel type de données (ou plusieurs types de données), y compris d'autres tuples.

Les tuples sont définis comme des valeurs séparées par des virgules entre les caractères `(` et `)` : `(<element_1>, ... , <element_n>)`.

```fsharp
("one", 2) // Tuple pair (2 values)
("one", 2, true) // Tuple triplet (3 values)
```

Seuls les tuples ayant la même longueur et les mêmes types (dans le même ordre) peuvent être comparés.
Les tuples ont une égalité structurelle, ce qui signifie qu'ils sont égaux _seulement_ si toutes leurs valeurs sont égales.

```fsharp
(1, 2) = (1, 2) // Same length, same types, same values, same order
// => true

(1, 2) = (2, 1) // Same length, same types, same values, different order
// => false

(1, 2) = (1, "2") // Same length, different types
// compiler error

(1, 2) = (1, 2, 3) // Different length
// compiler error
```

Il existe trois façons d'extraire des valeurs d'un tuple :

- Les fonctions `fst` et `snd`
- La déconstruction de tuple
- Le filtrage par motif

```fsharp
let person = ("Jordan", 170)

// Option 1: fst/snd
let name1 = fst person
let length2 = snd person

// Option 2: deconstruction
let (name2, length2) = person
// => name2 = "Jordan"
// => length2 = 170

// Option 3: pattern matching
match person with
| name3, length3 -> printf "%s: %d" name3 length3
```
