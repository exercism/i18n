# Pas de debug

Les fonctions `Debug` (`Debug.todo`, `Debug.toString` et `Debug.log`) sont très utiles pendant le développement, mais elles ne doivent jamais être livrées en production, publiées dans un paquet, ni faire partie de ta solution finale.

Pense à les retirer avant de publier ta solution.
