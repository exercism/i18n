# Introduction

## Booléens

Les booléens en C# sont représentés par le type `bool`, dont les valeurs peuvent être soit `true`, soit `false`.

C# prend en charge trois opérateurs booléens : `!` (NON), `&&` (ET) et `||` (OU).
