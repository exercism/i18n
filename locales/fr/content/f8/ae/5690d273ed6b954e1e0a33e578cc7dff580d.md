# Introduction

La structure de contrôle `foreach` offre un moyen pratique de parcourir des tableaux et des objets. `foreach` prend deux formes de base :

```php
foreach (iterable as $value) {
  // statements
}

foreach (iterable as $key => value) {
  // statements
}
```
