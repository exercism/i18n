# pas de _string_

Le but de cet exercice est d'enseigner les _charlists_. Résous-le sans utiliser de _strings_ : n'utilise ni `to_string`, ni `to_charlist`, ni le module `String`, ni la forme spéciale de bitstring `<<>>`.
