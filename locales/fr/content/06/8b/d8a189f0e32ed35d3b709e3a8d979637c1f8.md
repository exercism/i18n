# Ajout aux instructions

## Implémentation

Dans le paquet grep, définis une seule fonction Go, `Search`, qui prend un motif (une _string_), un _slice_ de _flags_ (des _string_) et un _slice_ de noms de fichiers (des _string_). `Search` doit renvoyer un _slice_ de _string_ contenant la sortie correspondant aux _flags_ et aux noms de fichiers donnés.

Utilise la signature suivante pour la fonction `Search` :

```go
func Search(pattern string, flags, files []string) []string {
```
