# Ajout aux instructions

## Fonctions spécifiques au parcours Emacs Lisp

* `list-empty-p` (*étant donné une liste, renvoie si la liste est vide*)
* `list-sum` (*étant donné une liste de nombres, renvoie la somme de tous les éléments*)
