# Indices

## Général

La documentation relative à `IDisposable` se trouve [ici][idisposable]

## 2. Écris des données dans la base de données

[`try`/`catch`][try-catch-finally] est nécessaire pour garantir que la base de données soit rétablie dans son état `Closed` si une exception est levée à cause de données incorrectes.

## 3. Valide dans la base de données les données écrites précédemment

[`try`/`catch`][try-catch-finally] est nécessaire pour garantir que la base de données soit rétablie dans son état `Closed` si une exception est levée à cause de données incorrectes.

[idisposable]: https://docs.microsoft.com/en-us/dotnet/api/system.idisposable
[try-catch-finally]: https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/try-catch-finally
