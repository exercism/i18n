# Utilise l'opérateur OU (`||`)

Félicitations, ta solution utilise l'opérateur OU (`||`) !

Ta solution utilise une façon efficace de gérer `nil`, et ça mérite qu'on prenne un instant pour le célébrer.
