# Introduction

Les `char` sont parfois utilisés avec un objet `StringBuilder`. Cet objet possède des méthodes qui permettent de construire une _string_ caractère par caractère, puis de la manipuler. À la fin du processus, on peut appeler `ToString` dessus pour obtenir une _string_ complète.
