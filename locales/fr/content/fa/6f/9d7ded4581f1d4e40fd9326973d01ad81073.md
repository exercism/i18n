# Introduction

## Les _strings_

En Common Lisp, les _strings_ sont des [vecteurs][concept-vectors] de [caractères][concept-characters].
Une _string_ s'écrit comme une suite de caractères entourée de guillemets doubles (par exemple "Hello World!").

Le prédicat `stringp` renvoie vrai uniquement si la valeur donnée est une _string_.

On peut découper une _string_ en sous-_strings_ avec la fonction `subseq`, qui prend la _string_, un indice de début et éventuellement un indice de fin.
Les indices commencent à zéro.

```lisp
(subseq "Hello World" 1 5)   ; => "ello"
(subseq "skip to the end" 5) ; => "to the end"
```

On peut comparer l'égalité de deux _strings_ avec `string=`.
Si la casse des caractères doit être ignorée, on peut utiliser `string-equal`.

```lisp
(string= "macaroni" "macaroni")      ; => T
(string= "macaroni" "MACARONI")      ; => NIL
(string-equal "macaroni" "MACARONI") ; => T
```

On peut mettre une _string_ en majuscules, en minuscules ou capitaliser chacun de ses mots avec `string-upcase`, `string-downcase` et `string-capitalize`.

```lisp
(string-upcase "not too loud")                ; => "NOT TOO LOUD"
(string-downcase "YELL IT FROM THE ROOFTOPS") ; => "yell it from the rooftops"
(string-capitalize "practical COMMON LiSP")   ; => "Practical Common Lisp"
```

[concept-characters]: /tracks/common-lisp/concepts/characters
[concept-vectors]: /tracks/common-lisp/concepts/vectors
