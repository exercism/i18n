# Introduction

Un nombre à virgule flottante est un nombre comportant zéro ou plusieurs chiffres après le séparateur décimal. Par exemple : `-2.4`, `0.1`, `3.14`, `5.`, `16.984025` et `1024.0`.

Différents types de nombres à virgule flottante peuvent stocker un nombre différent de chiffres après le séparateur décimal, ce que l'on appelle leur précision.

Go propose deux types de nombres à virgule flottante :

- `float32` : 32 bits (environ 6 à 9 chiffres de précision).
- `float64` : 64 bits (environ 15 à 17 chiffres de précision).
   C'est le type à virgule flottante par défaut.

Comme on peut le voir, les deux types peuvent stocker un nombre différent de chiffres.
Ainsi, essayer de stocker PI dans un `float32` ne stockera que les 6 à 9 premiers chiffres (le dernier chiffre étant arrondi).

Par défaut, Go utilise `float64` pour les nombres à virgule flottante, sauf si ce nombre est :

1. affecté à une variable de type `float32`, ou
2. renvoyé par une fonction dont le type de retour est `float32`, ou
3. passé en argument à la fonction `float32()`.

Le [paquet `math`] contient de nombreuses fonctions mathématiques utiles.

[paquet `math`]: https://pkg.go.dev/math
