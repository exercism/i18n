# Cas ignorés dans assignTicketTo

Quand plusieurs motifs `case` mènent à la même valeur, comme c'est le cas pour les statuts de ticket dans `assignTicketTo`, il est d'usage de les placer en dernier et de les regrouper avec le joker `_`.
