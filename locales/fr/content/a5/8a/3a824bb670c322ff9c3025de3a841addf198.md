# Évite de comparer avec des littéraux booléens

Plutôt que de comparer des booléens aux valeurs littérales, on peut se contenter des variables :

```java
// instead of
return someBoolVariable == true;

// use
return someBoolVariable;
```
