# pas de paramètre par défaut

Indique une (valeur par défaut)[https://docs.scala-lang.org/tour/default-parameter-values.html] pour le paramètre `name`. Fournir un paramètre par défaut évite d'avoir recours à une logique conditionnelle.
